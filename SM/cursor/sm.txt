<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Management Dashboard</title>
   <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/ag-grid/32.0.2/ag-grid-community.min.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/ag-grid-community@32.0.2/styles/ag-grid.css">
    <link rel="stylesheet" href="https://unpkg.com/ag-grid-community@32.0.2/styles/ag-theme-alpine.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

        /* ═══════ TOKENS ═══════════════════════════════════════════════════════ */
        /* MAGENTA LIGHT — default */
        :root {
            --bg-body: linear-gradient(145deg, #f9f0ff, #fce7ff, #f5eeff);
            --bg-card: #ffffff;
            --bg-header: rgba(255, 255, 255, 0.97);
            --bg-sidebar: linear-gradient(180deg, #fdf4ff, #f8ecff);
            --bg-content: #f9f3ff;
            --bg-input: rgba(168, 85, 247, 0.06);
            --bg-secondary: rgba(168, 85, 247, 0.05);
            --bg-hover: rgba(168, 85, 247, 0.07);
            --border: rgba(168, 85, 247, 0.2);
            --border-s: rgba(168, 85, 247, 0.45);
            --t1: #1e1133;
            --t2: #4b3f6b;
            --t3: #8b7aad;
            --acc: #a855f7;
            --acc2: #c724b1;
            --glow: rgba(168, 85, 247, 0.28);
            --grad: linear-gradient(135deg, #9248b9, #c724b1);
            --nab: rgba(168, 85, 247, 0.1);
            --nab2: rgba(168, 85, 247, 0.35);
            --sc: #a855f7;
            --cs: 0 1px 8px rgba(168, 85, 247, 0.12);
            --ch: 0 6px 20px rgba(168, 85, 247, 0.2);
            --chip: rgba(168, 85, 247, 0.08);
        }

        /* MAGENTA DARK */
        body[data-theme="dark"] {
            --bg-body: linear-gradient(145deg, #120820, #1e0d33, #160a28);
            --bg-card: #1f0d38;
            --bg-header: rgba(18, 8, 32, 0.98);
            --bg-sidebar: linear-gradient(180deg, #230f3d, #1a0a2e);
            --bg-content: #160a28;
            --bg-input: rgba(168, 85, 247, 0.12);
            --bg-secondary: rgba(168, 85, 247, 0.08);
            --bg-hover: rgba(168, 85, 247, 0.12);
            --border: rgba(168, 85, 247, 0.2);
            --border-s: rgba(168, 85, 247, 0.5);
            --t1: #f3e8ff;
            --t2: #c4b5fd;
            --t3: #8b5cf6;
            --acc: #c084fc;
            --acc2: #e879f9;
            --glow: rgba(192, 132, 252, 0.35);
            --grad: linear-gradient(135deg, #9248b9, #c724b1);
            --nab: rgba(168, 85, 247, 0.16);
            --nab2: rgba(168, 85, 247, 0.45);
            --sc: #e879f9;
            --cs: 0 2px 16px rgba(0, 0, 0, 0.5);
            --ch: 0 6px 24px rgba(168, 85, 247, 0.25);
            --chip: rgba(168, 85, 247, 0.15);
        }

        /* DURALUX LIGHT */
        body[data-theme="duralux"] {
            --bg-body: linear-gradient(145deg, #f0f4ff, #e8edff, #f4f6ff);
            --bg-card: #ffffff;
            --bg-header: rgba(255, 255, 255, 0.97);
            --bg-sidebar: linear-gradient(180deg, #f0f4ff, #e8edff);
            --bg-content: #f0f4ff;
            --bg-input: rgba(76, 111, 255, 0.06);
            --bg-secondary: rgba(76, 111, 255, 0.05);
            --bg-hover: rgba(76, 111, 255, 0.07);
            --border: rgba(76, 111, 255, 0.2);
            --border-s: rgba(76, 111, 255, 0.45);
            --t1: #0f1a3e;
            --t2: #3d4f8a;
            --t3: #7a8fc4;
            --acc: #4c6fff;
            --acc2: #818cf8;
            --glow: rgba(76, 111, 255, 0.28);
            --grad: linear-gradient(135deg, #4c6fff, #818cf8);
            --nab: rgba(76, 111, 255, 0.1);
            --nab2: rgba(76, 111, 255, 0.35);
            --sc: #4c6fff;
            --cs: 0 1px 8px rgba(76, 111, 255, 0.1);
            --ch: 0 6px 20px rgba(76, 111, 255, 0.18);
            --chip: rgba(76, 111, 255, 0.08);
        }

        /* DURALUX DARK */
        body[data-theme="duralux-dark"] {
            --bg-body: linear-gradient(145deg, #060c1f, #0c1535, #080e25);
            --bg-card: #0e1630;
            --bg-header: rgba(6, 12, 31, 0.98);
            --bg-sidebar: linear-gradient(180deg, #101a38, #0c1430);
            --bg-content: #090f28;
            --bg-input: rgba(76, 111, 255, 0.12);
            --bg-secondary: rgba(76, 111, 255, 0.08);
            --bg-hover: rgba(76, 111, 255, 0.12);
            --border: rgba(76, 111, 255, 0.2);
            --border-s: rgba(76, 111, 255, 0.5);
            --t1: #e2e8f0;
            --t2: #a5b4c8;
            --t3: #637899;
            --acc: #818cf8;
            --acc2: #a5b4fc;
            --glow: rgba(129, 140, 248, 0.35);
            --grad: linear-gradient(135deg, #4c6fff, #818cf8);
            --nab: rgba(76, 111, 255, 0.14);
            --nab2: rgba(76, 111, 255, 0.45);
            --sc: #818cf8;
            --cs: 0 2px 16px rgba(0, 0, 0, 0.55);
            --ch: 0 6px 24px rgba(76, 111, 255, 0.2);
            --chip: rgba(76, 111, 255, 0.15);
        }

        /* ═══════ RESET ════════════════════════════════════════════════════════ */
        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box
        }

        h1,
        h2,
        h3,
        h4,
        h5,
        h6 {
            margin: 0 !important;
            padding: 0 !important;
            font-weight: inherit
        }

        html {
            scroll-behavior: smooth
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--bg-body);
            color: var(--t1);
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
            transition: background .3s, color .3s;
            line-height: 1.5;
        }

        select,
        input,
        textarea,
        button {
            font-family: inherit
        }

        /* ═══════ PORTAL SHELL ══════════════════════════════════════════════════ */
        .portal-shell {
            max-width: 1800px;
            margin: 0 auto;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background: var(--bg-body);
        }

        /* ═══════ HEADER ════════════════════════════════════════════════════════ */
        .header {
            background: var(--bg-header);
            border-bottom: 1px solid var(--border);
            backdrop-filter: blur(16px);
            padding: 0 1.25rem;
            height: 56px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 1rem;
            position: sticky;
            top: 0;
            z-index: 200;
            flex-shrink: 0;
        }

        .logo-section {
            display: flex;
            align-items: center;
            gap: .65rem;
            min-width: 0
        }

        .logo {
            width: 36px;
            height: 36px;
            border-radius: 10px;
            background: var(--grad);
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 14px var(--glow);
            flex-shrink: 0;
            overflow: hidden;
        }

        .logo img {
            width: 24px;
            height: 24px;
            object-fit: contain
        }

        .header-title {
            font-size: 1.05rem;
            font-weight: 800;
            white-space: nowrap;
            background: var(--grad);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .header-center {
            display: flex;
            align-items: center;
            gap: .75rem;
            flex: 1;
            justify-content: flex-start;
            padding-left: .75rem
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: .5rem;
            flex-shrink: 0
        }

        .live-badge-pill {
            display: inline-flex;
            align-items: center;
            gap: .3rem;
            padding: .22rem .65rem;
            border-radius: 20px;
            background: rgba(22, 163, 74, .12);
            border: 1px solid rgba(22, 163, 74, .28);
            font-size: .73rem;
            font-weight: 700;
            color: #16a34a;
            white-space: nowrap;
        }

        body[data-theme="dark"] .live-badge-pill,
        body[data-theme="duralux-dark"] .live-badge-pill {
            background: rgba(34, 197, 94, .1);
            border-color: rgba(34, 197, 94, .25);
            color: #22c55e;
        }

        .status-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: #16a34a;
            animation: pulseDot 2s infinite;
        }

        body[data-theme="dark"] .status-dot,
        body[data-theme="duralux-dark"] .status-dot {
            background: #22c55e
        }

        @keyframes pulseDot {

            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(22, 163, 74, .4)
            }

            50% {
                box-shadow: 0 0 0 5px rgba(22, 163, 74, 0)
            }
        }

        .date-badge {
            font-size: .75rem;
            color: var(--t3);
            display: flex;
            align-items: center;
            gap: .3rem;
            white-space: nowrap
        }

        .date-badge [data-lucide],
        [data-lucide] {
            vertical-align: middle
        }

        .color-scheme-selector {
            padding: .3rem .65rem;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: 18px;
            color: var(--t1);
            font-size: .75rem;
            cursor: pointer;
            transition: border-color .2s;
        }

        .color-scheme-selector:hover {
            border-color: var(--border-s)
        }

        .color-scheme-selector option {
            background: #fff;
            color: #000
        }

        body[data-theme="dark"] .color-scheme-selector option,
        body[data-theme="duralux-dark"] .color-scheme-selector option {
            background: #1a0a2e;
            color: #e8d5ff
        }

        .theme-toggle {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            background: var(--bg-input);
            border: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            color: var(--t1);
            transition: all .2s;
            flex-shrink: 0;
        }

        .theme-toggle:hover {
            border-color: var(--border-s)
        }

        /* old .header-left used by sub-views */
        .header-left {
            display: flex;
            flex-direction: column;
            gap: .1rem
        }

        .header-left h1 {
            font-size: 1.05rem;
            font-weight: 800;
            color: var(--t1);
            display: flex;
            align-items: center;
            gap: .5rem;
        }

        .header-left p {
            font-size: .76rem;
            color: var(--t3)
        }

        /* ═══════ SIDEBAR TOGGLE — sits on right edge of sidebar ═══════════════ */
        .sidebar-toggle-btn {
            position: absolute;
            right: -14px;
            top: 20px;
            z-index: 300;
            width: 28px;
            height: 28px;
            border-radius: 50%;
            background: var(--bg-card);
            border: 1px solid var(--border);
            box-shadow: var(--cs);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            color: var(--t1);
            transition: all .2s;
        }

        .sidebar-toggle-btn:hover {
            border-color: var(--border-s);
            background: var(--bg-hover)
        }

        /* When collapsed: show a small tab on left edge of content to re-open */



        /* ═══════ MAIN CONTAINER ════════════════════════════════════════════════ */
        .main-container {
            display: flex;
            flex: 1;
            min-height: 0;
            position: relative
        }

        /* When showing pending/transfer, hide dashboard-view completely */
        .content.section-mode #dashboard-view {
            display: none !important
        }

        .content.section-mode {
            background: transparent !important;
            padding: 0 !important;
        }

        .content.section-mode .expand-tab {
            display: none !important
        }

        /* Edit account — full-bleed beside sidebar (same as PSD) */
        .portal-shell.edit-full-shell {
            max-width: none;
            width: 100%;
        }

        .content.edit-full-mode {
            padding: 1.1rem 1.25rem !important;
            width: 100%;
            flex: 1;
            min-width: 0;
        }

        #editRequestView {
            width: 100%;
            max-width: none;
            padding: 0;
            box-sizing: border-box;
            margin: 0;
        }

        #editRequestView .table-section {
            width: 100%;
            max-width: none;
            margin-bottom: 1.25rem;
        }

        #editRequestView #editCurrentInfo {
            display: flex;
            flex-direction: column;
            gap: 1.75rem;
        }

        #editRequestView .edit-section-block {
            padding: 1.25rem 1.35rem;
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            box-shadow: var(--cs);
        }

        #editRequestView .edit-info-grid {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 1rem;
        }

        #editRequestView .edit-revenue-grid {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 1rem;
            margin-top: .75rem;
        }

        #editRequestView .edit-section-block .table-title {
            margin: 0 0 1rem !important;
        }

        #editRequestView .edit-section-block h4 {
            margin: 0 0 .75rem !important;
            font-size: .82rem;
            color: var(--t2);
            font-weight: 600;
        }

        #editRequestView .edit-field-card {
            padding: .85rem 1rem;
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 10px;
        }

        #editRequestView .edit-field-label {
            font-size: .68rem;
            color: var(--t3);
            margin-bottom: .35rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .04em;
        }

        #editRequestView .edit-field-value {
            font-size: .9rem;
            font-weight: 600;
            color: var(--t1);
            line-height: 1.4;
        }

        #editRequestView .edit-revenue-card {
            padding: 1rem;
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 10px;
        }

        #editRequestView .filter-group {
            gap: .45rem;
        }

        @media (max-width: 900px) {
            #editRequestView .edit-info-grid,
            #editRequestView .edit-revenue-grid {
                grid-template-columns: 1fr;
            }
        }

        /* ═══════ SIDEBAR ═══════════════════════════════════════════════════════ */
        .sidebar {
            width: 210px;
            min-width: 210px;
            background: var(--bg-sidebar);
            border-right: 1px solid var(--border);
            padding: 1rem 0;
            overflow: visible;
            flex-shrink: 0;
            transition: width .26s ease, min-width .26s ease, opacity .22s, padding .25s;
        }

        .sidebar.collapsed {
            width: 52px;
            min-width: 52px;
            padding: 1rem 0;
            border-right: 1px solid var(--border);
            overflow: hidden !important
        }

        .sidebar.collapsed .nav-items {
            opacity: 1;
            pointer-events: all;
        }

        .sidebar.collapsed .nav-label,
        .sidebar.collapsed .nav-section-label {
            display: none;
        }

        .sidebar.collapsed .nav-item {
            justify-content: center;
            padding: .55rem 0;
        }

        .sidebar.collapsed .sidebar-toggle-btn {
            right: -14px;
        }

        .sidebar.collapsed .sidebar-toggle-btn {
            right: -14px;
            opacity: 1 !important;
            pointer-events: all !important;
            background: var(--bg-card);
            box-shadow: var(--cs);
        }

        .nav-items {
            display: flex;
            flex-direction: column;
            gap: 2px;
            padding: 0 .65rem
        }

        .nav-section-label {
            font-size: .67rem;
            text-transform: uppercase;
            letter-spacing: .1em;
            color: var(--t3);
            padding: .75rem .6rem .3rem;
            white-space: nowrap;
            overflow: hidden;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: .65rem;
            padding: .55rem .7rem;
            border-radius: 9px;
            cursor: pointer;
            color: var(--t2);
            border: 1px solid transparent;
            transition: all .17s;
            white-space: nowrap;
            overflow: hidden;
            font-size: .84rem;
            font-weight: 500;
            user-select: none;
        }

        .nav-item .nav-icon {
            display: flex;
            align-items: center;
            flex-shrink: 0;
            width: 17px;
            height: 17px
        }

        .nav-item .nav-label {
            font-size: .84rem;
            font-weight: 500
        }

        .nav-item:hover {
            background: var(--bg-hover);
            color: var(--t1)
        }

        .nav-item.active {
            background: var(--nab);
            border-color: var(--nab2);
            color: var(--t1);
            font-weight: 600
        }

        /* ═══════ CONTENT ═══════════════════════════════════════════════════════ */
        .content {
            flex: 1;
            overflow-y: auto;
            min-width: 0;
            background: var(--bg-content);
            padding: 1.1rem 1.25rem
        }

        .dashboard-section {
            animation: fadeSection .25s ease;
            padding: 1rem
        }

        /* PSD Assignment — full-bleed layout (edge-to-edge beside sidebar) */
        .portal-shell.psd-full-shell {
            max-width: none;
            width: 100%;
        }

        .content.psd-full-mode {
            padding: 0 !important;
            width: 100%;
            flex: 1;
            min-width: 0;
        }

        #psdView {
            width: 100%;
            max-width: none;
            padding: 0.75rem 1rem 1.25rem;
            box-sizing: border-box;
        }

        #psdView #psdContainer,
        #psdView .psd-root {
            width: 100%;
            max-width: none;
        }

        #psdView .top-stats {
            grid-template-columns: repeat(6, minmax(0, 1fr));
        }

        @media (max-width: 1400px) {
            #psdView .top-stats {
                grid-template-columns: repeat(3, minmax(0, 1fr));
            }
        }

        @media (max-width: 900px) {
            #psdView .top-stats {
                grid-template-columns: repeat(2, minmax(0, 1fr));
            }
        }

        @keyframes fadeSection {
            from {
                opacity: 0;
                transform: translateY(4px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        /* ═══════ SUB-VIEW SHELLS (.dashboard class) ════════════════════════════ */
        /* These are the full-page views: amAdDashboard, accountSearchView, etc */
        .dashboard {
            max-width: 1800px;
            margin: 0 auto;
            min-height: 100vh;
            background: var(--bg-body);
            display: flex;
            flex-direction: column;
        }

        .dashboard>.header {
            position: sticky;
            top: 0;
            z-index: 200
        }

        .dashboard-body {
            padding: 1.25rem;
            background: var(--bg-content);
            flex: 1;
            min-height: 0;
        }

        /* Make all direct children after header act as dashboard-body */
        .dashboard>div:not(.header) {
            background: var(--bg-content);
            padding: 1.25rem;
            margin: 0.5rem;
            border-radius: 8px;
        }

        /* ═══════ SECTION TITLE ══════════════════════════════════════════════════ */
        .section-title {
            font-size: 1rem;
            font-weight: 800;
            margin: .85rem 0 !important;
            display: flex;
            align-items: center;
            gap: .45rem;
            color: var(--acc);
        }

        /* h2 with inline style used in sub-views */
        h2[style*="font-size: 24px"],
        h2[style*="font-size:24px"] {
            font-size: 1.15rem !important;
            font-weight: 800 !important;
            color: var(--t1) !important;
            margin-bottom: .9rem !important;
            display: flex !important;
            align-items: center !important;
            gap: .45rem !important;
        }

        /* ═══════ STAT CARDS ════════════════════════════════════════════════════ */
        .top-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: .85rem;
            margin-bottom: 1rem;
        }

        .stat-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 1rem 1.1rem;
            box-shadow: var(--cs);
            position: relative;
            overflow: hidden;
            transition: transform .18s, box-shadow .18s;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--grad);
        }

        .stat-card:hover {
            transform: translateY(-2px);
            box-shadow: var(--ch)
        }

        .stat-label {
            font-size: .68rem;
            text-transform: uppercase;
            letter-spacing: .06em;
            color: var(--t3);
            font-weight: 700;
            margin-bottom: .35rem;
        }

        .stat-value {
            font-size: 1.7rem;
            font-weight: 900;
            line-height: 1;
            color: var(--sc);
            margin-bottom: .25rem
        }

        .stat-subtitle {
            font-size: .7rem;
            color: var(--t3)
        }

        .stat-breakdown {
            margin-top: .45rem;
            display: flex;
            flex-wrap: wrap;
            gap: .4rem
        }

        .breakdown-item {
            display: inline-flex;
            align-items: center;
            gap: .2rem;
            font-size: .7rem
        }

        .breakdown-label {
            color: var(--t3)
        }

        .breakdown-value {
            font-weight: 700;
            color: var(--t2)
        }

        .stat-col {
            display: flex;
            flex-direction: column;
            min-width: 70px
        }

        .stat-col .stat-label {
            font-size: .65rem;
            margin-bottom: .15rem
        }

        .stat-col .stat-value {
            font-size: 1rem;
            font-weight: 800
        }

        /* ═══════ FILTERS ════════════════════════════════════════════════════════ */
        .filters-section {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: .9rem 1rem;
            margin-bottom: 1rem;
            box-shadow: var(--cs);
        }

        .filters-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: .65rem;
            margin-bottom: .65rem;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
            gap: .25rem
        }

        .filter-label {
            font-size: .68rem;
            font-weight: 700;
            color: var(--t2);
            text-transform: uppercase;
            letter-spacing: .05em
        }

        .filter-select,
        input.filter-select,
        select.filter-select,
        textarea.filter-select {
            padding: .48rem .7rem;
            border-radius: 8px;
            border: 1px solid var(--border);
            background: var(--bg-input);
            color: var(--t1);
            font-size: .83rem;
            transition: border-color .18s, box-shadow .18s;
            width: 100%;
        }

        .filter-select:focus,
        input.filter-select:focus {
            outline: none;
            border-color: var(--border-s);
            box-shadow: 0 0 0 3px var(--glow);
        }

        select.filter-select option {
            background: #fff;
            color: #111
        }

        body[data-theme="dark"] select.filter-select option,
        body[data-theme="duralux-dark"] select.filter-select option {
            background: #1a0a2e;
            color: #e8d5ff
        }

        .filter-actions {
            display: flex;
            gap: .5rem;
            justify-content: flex-end
        }

        /* ═══════ BUTTONS ════════════════════════════════════════════════════════ */
        .export-btn,
        button.export-btn {
            padding: .5rem 1rem;
            border-radius: 8px;
            font-size: .82rem;
            font-weight: 700;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: .35rem;
            transition: all .2s;
            border: none;
            background: var(--grad);
            color: #fff;
            box-shadow: 0 2px 10px var(--glow);
            white-space: nowrap;
        }

        .export-btn:hover {
            box-shadow: 0 4px 16px var(--glow);
            transform: translateY(-1px)
        }

        .export-btn:active {
            transform: translateY(0)
        }

        /* Action column buttons — always white text, compact */
        .ag-pinned-right-cols-container .export-btn,
        .ag-pinned-right-floating-top .export-btn {
            color: #fff !important;
        }

        .reset-btn,
        button.reset-btn {
            padding: .5rem 1rem;
            border-radius: 8px;
            font-size: .82rem;
            font-weight: 600;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: .35rem;
            transition: all .2s;
            background: var(--bg-input);
            color: var(--t1);
            border: 1px solid var(--border);
            white-space: nowrap;
        }

        .reset-btn:hover {
            border-color: var(--border-s)
        }

        .analytics-role-tab.active {
            background: var(--grad) !important;
            color: #fff !important;
            border-color: transparent !important;
        }

        .analytics-role-tab:hover {
            opacity: .85;
        }

        .theme-btn,
        button.theme-btn {
            padding: .5rem 1rem;
            border-radius: 8px;
            font-size: .82rem;
            font-weight: 600;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: .35rem;
            transition: all .2s;
            background: var(--bg-input);
            color: var(--t1);
            border: 1px solid var(--border);
            white-space: nowrap;
        }

        .theme-btn:hover {
            border-color: var(--border-s)
        }

        /* back button (red tint) */
        button.theme-btn[style*="rgba(239, 68, 68"] {
            background: rgba(239, 68, 68, .12) !important;
            color: #ef4444 !important;
            border-color: rgba(239, 68, 68, .3) !important;
        }

        #toggleChartsBtn {
            background: var(--grad);
            color: #fff;
            border: none;
            border-radius: 9px;
            padding: .55rem 1.25rem;
            font-size: .84rem;
            font-weight: 700;
            cursor: pointer;
            box-shadow: 0 2px 12px var(--glow);
            transition: all .2s;
            display: inline-flex;
            align-items: center;
            gap: .4rem;
        }

        #toggleChartsBtn:hover {
            box-shadow: 0 4px 18px var(--glow);
            transform: translateY(-1px)
        }

        /* ═══════ LM TILES ══════════════════════════════════════════════════════ */
        .line-managers-section {
            margin-bottom: 1rem
        }

        .line-managers-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            gap: .85rem;
        }

        .lm-tile {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 13px;
            padding: .9rem 1rem;
            cursor: pointer;
            transition: all .2s;
            box-shadow: var(--cs);
            position: relative;
            overflow: hidden;
        }

        .lm-tile::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--grad)
        }

        .lm-tile:hover {
            transform: translateY(-2px);
            box-shadow: var(--ch);
            border-color: var(--border-s)
        }

        .lm-tile.selected {
            border-color: var(--nab2);
            background: var(--nab)
        }

        .lm-header {
            display: flex;
            flex-direction: column;
            gap: .15rem;
            margin-bottom: .6rem;
        }

        .lm-header h3 {
            font-size: .88rem;
            font-weight: 700;
            color: var(--t1);
            line-height: 1.2
        }

        .lm-meta {
            font-size: .68rem;
            color: var(--t3)
        }

        .lm-stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: .5rem;
            margin-bottom: .6rem
        }

        .lm-stat-box {
            background: var(--bg-secondary);
            border-radius: 7px;
            padding: .5rem .6rem;
            border: 1px solid var(--border)
        }

        .lm-stat-label {
            font-size: .65rem;
            text-transform: uppercase;
            color: var(--t3);
            margin-bottom: .12rem;
            font-weight: 600
        }

        .lm-stat-value {
            font-size: 1.05rem;
            font-weight: 800;
            color: var(--sc)
        }

        .lm-revenue {
            border-radius: 8px;
            padding: .55rem .7rem;
            background: var(--grad);
            color: #fff;
            margin-bottom: .55rem
        }

        .lm-revenue-label {
            font-size: .65rem;
            text-transform: uppercase;
            opacity: .82;
            font-weight: 700;
            margin-bottom: .15rem
        }

        .lm-revenue-count {
            font-size: 1.05rem;
            font-weight: 900;
            margin-bottom: .3rem
        }

        .lm-revenue-breakdown {
            display: flex;
            flex-direction: column;
            gap: .15rem
        }

        .lm-revenue-item {
            display: flex;
            justify-content: space-between;
            align-items: center
        }

        .lm-revenue-item-label {
            font-size: .66rem;
            opacity: .8
        }

        .lm-revenue-item-value {
            font-size: .72rem;
            font-weight: 700
        }

        .lm-progress-section {
            margin-top: .45rem
        }

        .lm-progress-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: .12rem
        }

        .lm-progress-label,
        .lm-progress-value {
            font-size: .68rem;
            color: var(--t3)
        }

        .lm-progress-value {
            font-weight: 700
        }

        .progress-bar {
            background: var(--bg-secondary);
            border-radius: 20px;
            height: 5px;
            overflow: hidden;
            margin-bottom: .4rem
        }

        .progress-fill {
            height: 100%;
            border-radius: 20px;
            background: var(--grad);
            transition: width .5s ease
        }

        /* ═══════ SM ROWS ════════════════════════════════════════════════════════ */
        .sm-performance-section {
            margin-bottom: 1rem
        }

        .manager-list {
            display: flex;
            flex-direction: column;
            gap: .55rem
        }

        .manager-row {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 11px;
            padding: .75rem 1rem;
            display: flex;
            align-items: center;
            flex-wrap: wrap;
            gap: .85rem;
            box-shadow: var(--cs);
            transition: all .18s;
            cursor: pointer;
        }

        .manager-row:hover {
            border-color: var(--border-s);
            transform: translateX(2px)
        }

        .manager-row.selected {
            border-color: var(--nab2);
            background: var(--nab)
        }

        .rank-badge {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            font-size: .76rem;
            color: #fff;
            flex-shrink: 0;
        }

        .rank-badge.rank-1 {
            background: var(--grad);
            box-shadow: 0 0 10px var(--glow)
        }

        .rank-badge.rank-2 {
            background: linear-gradient(135deg, #94a3b8, #64748b)
        }

        .rank-badge.rank-3 {
            background: linear-gradient(135deg, #c2853b, #d97706)
        }

        .rank-badge.rank-other,
        .rank-badge.rank-4,
        .rank-badge.rank-5 {
            background: var(--bg-secondary);
            color: var(--t3);
            border: 1px solid var(--border);
        }

        .manager-name-col {
            flex: 0 0 140px;
            min-width: 0
        }

        .sm-doj-col {
            display: flex;
            flex-direction: column;
            min-width: 80px;
            flex: 0 0 80px
        }

        .sm-doj-col .stat-label {
            font-size: .62rem;
            color: var(--t3);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: .04em;
            margin-bottom: .15rem
        }

        .sm-doj-col .stat-value {
            font-size: .82rem;
            font-weight: 700;
            color: var(--t1)
        }

        .sm-doj-col .stat-sub {
            font-size: .68rem;
            color: var(--t3);
            margin-top: .1rem
        }

        .manager-name {
            font-weight: 700;
            font-size: .86rem;
            color: var(--t1);
            margin-bottom: .15rem
        }

        .manager-tag {
            display: inline-block;
            padding: .15rem .5rem;
            border-radius: 5px;
            font-size: .65rem;
            font-weight: 700;
            background: var(--chip);
            color: var(--acc);
            border: 1px solid var(--nab2);
        }

        .progress-bar-col {
            flex: 1;
            min-width: 120px;
            max-width: 200px
        }

        .progress-label {
            font-size: .67rem;
            color: var(--t3);
            margin-bottom: .12rem
        }

        /* ═══════ CHARTS ════════════════════════════════════════════════════════ */
        .charts-section {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: .85rem;
            margin-bottom: 1rem
        }

        .chart-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 1rem;
            box-shadow: var(--cs)
        }

        .chart-title {
            font-size: .8rem;
            font-weight: 700;
            color: var(--t2);
            margin-bottom: .65rem;
            display: flex;
            align-items: center;
            gap: .35rem
        }

        .chart-container {
            position: relative;
            height: 260px
        }

        /* ═══════ TABLE SECTION ═════════════════════════════════════════════════ */
        .table-section {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 1rem 1.1rem;
            margin-bottom: 1rem;
            box-shadow: var(--cs);
        }

        .table-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: .65rem;
            margin-bottom: .85rem
        }

        .table-title {
            font-size: .9rem;
            font-weight: 800;
            color: var(--t1);
            display: flex;
            align-items: center;
            gap: .35rem;
            margin: 0 !important;
        }

        .table-actions {
            display: flex;
            align-items: center;
            gap: .5rem;
            flex-wrap: wrap
        }

        .table-wrapper {
            overflow-x: auto
        }

        .table-wrapper table {
            width: 100%;
            border-collapse: collapse;
            font-size: .84rem
        }

        .table-wrapper th {
            background: var(--bg-secondary);
            color: var(--t1);
            padding: .6rem .85rem;
            text-align: left;
            font-size: .72rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .04em;
            border-bottom: 2px solid var(--border-s);
            white-space: nowrap;
        }

        .table-wrapper td {
            padding: .58rem .85rem;
            color: var(--t2);
            border-bottom: 1px solid var(--border)
        }

        .table-wrapper tr:last-child td {
            border-bottom: none
        }

        .table-wrapper tr:hover td {
            background: var(--bg-hover)
        }

        .search-box {
            padding: .45rem .75rem;
            border-radius: 8px;
            border: 1px solid var(--border);
            background: var(--bg-input);
            color: var(--t1);
            font-size: .82rem;
            min-width: 180px;
            transition: border-color .18s;
        }

        .search-box:focus {
            outline: none;
            border-color: var(--border-s)
        }

        .search-box::placeholder {
            color: var(--t3)
        }

        /* ═══════ STATUS BADGES ═════════════════════════════════════════════════ */
        .status-badge {
            display: inline-block;
            padding: .18rem .55rem;
            border-radius: 20px;
            font-size: .68rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .04em;
        }

        .badge-success {
            background: rgba(22, 163, 74, .12);
            color: #16a34a;
            border: 1px solid rgba(22, 163, 74, .28)
        }

        .badge-warning {
            background: rgba(245, 158, 11, .12);
            color: #b45309;
            border: 1px solid rgba(245, 158, 11, .28)
        }

        .badge-danger {
            background: rgba(220, 38, 38, .12);
            color: #dc2626;
            border: 1px solid rgba(220, 38, 38, .28)
        }

        .badge-info {
            background: rgba(37, 99, 235, .12);
            color: #2563eb;
            border: 1px solid rgba(37, 99, 235, .28)
        }

        body[data-theme="dark"] .badge-success,
        body[data-theme="duralux-dark"] .badge-success {
            background: rgba(34, 197, 94, .12);
            color: #22c55e;
            border-color: rgba(34, 197, 94, .25)
        }

        body[data-theme="dark"] .badge-warning,
        body[data-theme="duralux-dark"] .badge-warning {
            background: rgba(251, 191, 36, .12);
            color: #fbbf24;
            border-color: rgba(251, 191, 36, .25)
        }

        /* ═══════ AG GRID ════════════════════════════════════════════════════════ */
        .ag-theme-alpine .ag-root-wrapper {
            background: var(--bg-card) !important;
            border-color: var(--border) !important;
            border-radius: 10px !important;
            overflow: hidden !important
        }

        .ag-theme-alpine .ag-header {
            background: var(--bg-secondary) !important;
            border-bottom: 2px solid var(--border-s) !important;
        }

        .ag-theme-alpine .ag-header-row,
        .ag-theme-alpine .ag-header-viewport,
        .ag-theme-alpine .ag-pinned-left-header,
        .ag-theme-alpine .ag-pinned-right-header {
            background: var(--bg-secondary) !important;
            border-bottom: none !important;
        }

        .ag-theme-alpine .ag-header-row.ag-header-row-column-group {
            border-bottom: 1px solid var(--border) !important;
        }

        .ag-theme-alpine .ag-header-cell.ag-header-span-height {
            border-bottom: none !important;
        }

        .ag-theme-alpine .ag-header-cell {
            background: var(--bg-secondary) !important;
            color: var(--t1) !important;
            font-weight: 700 !important;
            font-size: .71rem !important;
            text-transform: uppercase !important;
            letter-spacing: .04em !important
        }

        .ag-theme-alpine .ag-header-cell-text,
        .ag-theme-alpine .ag-header-cell-label {
            color: var(--t1) !important;
            font-weight: 700 !important
        }

        .ag-theme-alpine .ag-icon {
            color: var(--t3) !important
        }

        .ag-theme-alpine .ag-body-viewport,
        .ag-theme-alpine .ag-center-cols-viewport,
        .ag-theme-alpine .ag-pinned-left-cols-container,
        .ag-theme-alpine .ag-pinned-right-cols-container {
            background: var(--bg-card) !important
        }

        .ag-theme-alpine .ag-row {
            background: var(--bg-card) !important;
            color: var(--t1) !important;
            border-bottom-color: var(--border) !important
        }

        .ag-theme-alpine .ag-row-odd {
            background: var(--bg-secondary) !important
        }

        .ag-theme-alpine .ag-row-hover,
        .ag-theme-alpine .ag-row-hover.ag-row-even,
        .ag-theme-alpine .ag-row-hover.ag-row-odd {
            background: var(--bg-hover) !important
        }

        .ag-theme-alpine .ag-cell {
            color: var(--t1) !important
        }

        .ag-theme-alpine .ag-paging-panel {
            background: var(--bg-card) !important;
            color: var(--t1) !important;
            border-top-color: var(--border) !important
        }

        .ag-theme-alpine .ag-paging-button {
            color: var(--t1) !important
        }

        .ag-theme-alpine .ag-menu,
        .ag-theme-alpine .ag-filter,
        .ag-theme-alpine .ag-filter-body-wrapper {
            background: var(--bg-card) !important;
            color: var(--t1) !important;
            border-color: var(--border) !important
        }

        .ag-theme-alpine input.ag-input-field-input {
            background: var(--bg-input) !important;
            color: var(--t1) !important;
            border-color: var(--border) !important
        }

        /* ═══════ LANDING ════════════════════════════════════════════════════════ */
        .landing-page {
            min-height: 100vh;
            display: flex !important;
            flex-direction: row !important;
            background: var(--bg-body)
        }

        .landing-left,
        .landing-right {
            flex: 1 !important;
            display: flex !important;
            align-items: center !important;
            justify-content: center !important;
            padding: 2rem !important;
            min-width: 0 !important
        }

        .landing-container {
            width: 100%;
            max-width: 420px
        }

        .landing-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 1.4rem;
            box-shadow: var(--cs);
            margin-bottom: 1rem
        }

        .landing-header {
            text-align: center;
            margin-bottom: 1.4rem
        }

        .landing-logo {
            display: flex;
            justify-content: center;
            margin-bottom: .85rem
        }

        .logo-icon {
            width: 60px;
            height: 60px;
            border-radius: 15px;
            background: var(--grad);
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 24px var(--glow);
            overflow: hidden
        }

        .landing-title {
            font-size: 1.4rem;
            font-weight: 800;
            color: var(--t1);
            margin-bottom: .25rem
        }

        .landing-subtitle {
            font-size: .82rem;
            color: var(--t3)
        }

        .welcome-badge {
            display: inline-flex;
            align-items: center;
            gap: .35rem;
            padding: .25rem .7rem;
            border-radius: 20px;
            background: var(--nab);
            border: 1px solid var(--nab2);
            font-size: .75rem;
            font-weight: 600;
            color: var(--acc);
            margin-bottom: .75rem
        }

        .badge-pulse {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: var(--acc);
            animation: pulseDot 2s infinite
        }

        .landing-user-info {
            display: flex;
            align-items: center;
            gap: .8rem
        }

        .landing-avatar {
            position: relative;
            width: 48px;
            height: 48px;
            flex-shrink: 0
        }

        .avatar-ring {
            position: absolute;
            inset: 0;
            border-radius: 50%;
            border: 2px solid var(--acc);
            opacity: .5
        }

        .avatar-placeholder {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: var(--grad);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1rem;
            font-weight: 800;
            color: #fff
        }

        .landing-details h3 {
            font-size: .95rem;
            font-weight: 700;
            color: var(--t1)
        }

        .landing-details p {
            font-size: .77rem;
            color: var(--t3)
        }

        .landing-role {
            display: inline-block;
            margin-top: .25rem;
            padding: .17rem .55rem;
            border-radius: 5px;
            font-size: .7rem;
            font-weight: 600;
            background: var(--chip);
            color: var(--acc);
            border: 1px solid var(--border)
        }

        .landing-stats-container {
            margin: .9rem 0
        }

        .stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: .55rem
        }

        .landing-stat-card {
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 10px;
            padding: .7rem;
            display: flex;
            gap: .55rem;
            align-items: center
        }

        .stat-icon-wrapper {
            position: relative;
            width: 34px;
            height: 34px;
            flex-shrink: 0
        }

        .stat-icon-bg {
            position: absolute;
            inset: 0;
            border-radius: 7px;
            background: var(--grad);
            opacity: .15
        }

        .stat-icon {
            width: 18px;
            height: 18px;
            color: var(--acc);
            position: relative
        }

        .stat-content {
            flex: 1;
            min-width: 0
        }

        .stat-mini {
            display: flex;
            align-items: center;
            gap: .3rem;
            font-size: .72rem;
            color: var(--t3)
        }

        .stats-secondary {
            display: flex;
            justify-content: center;
            gap: 1.25rem;
            margin-top: .65rem
        }

        .landing-actions {
            margin-top: .9rem
        }

        .landing-btn {
            width: 100%;
            padding: .72rem;
            border-radius: 11px;
            font-size: .88rem;
            font-weight: 700;
            cursor: pointer;
            border: none;
            transition: all .2s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: .45rem;
            position: relative;
            overflow: hidden
        }

        .btn-primary-enhanced {
            background: var(--grad);
            color: #fff;
            box-shadow: 0 3px 14px var(--glow)
        }

        .btn-primary-enhanced:hover {
            box-shadow: 0 5px 20px var(--glow);
            transform: translateY(-1px)
        }

        .btn-primary-enhanced:disabled {
            opacity: .5;
            cursor: not-allowed;
            transform: none
        }

        .btn-shine {
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .15), transparent);
            transition: left .5s
        }

        .landing-btn:hover .btn-shine {
            left: 100%
        }

        .btn-icon,
        .btn-text {
            position: relative
        }

        .landing-note {
            display: flex;
            align-items: flex-start;
            gap: .35rem;
            margin-top: .6rem;
            font-size: .74rem;
            color: var(--t3)
        }

        .landing-footer {
            text-align: center;
            font-size: .72rem;
            color: var(--t3);
            margin-top: .4rem
        }

        .landing-branding {
            max-width: 420px
        }

        .landing-brand-title {
            font-size: 1.9rem;
            font-weight: 900;
            color: var(--t1)
        }

        .landing-brand-subtitle {
            font-size: .86rem;
            color: var(--t3);
            margin-top: .4rem
        }

        .landing-features {
            margin-top: 1.3rem;
            display: flex;
            flex-direction: column;
            gap: .7rem
        }

        .landing-feature {
            display: flex;
            align-items: flex-start;
            gap: .65rem
        }

        .landing-feature-icon {
            width: 34px;
            height: 34px;
            border-radius: 9px;
            flex-shrink: 0;
            background: var(--nab);
            border: 1px solid var(--nab2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--acc)
        }

        .landing-feature-title {
            font-weight: 700;
            font-size: .86rem;
            color: var(--t1)
        }

        .landing-feature-desc {
            font-size: .76rem;
            color: var(--t3);
            margin-top: .12rem
        }

        .landing-bg-gradient,
        .landing-bg-orbs,
        .orb {
            display: none
        }

        .live-badge {
            display: inline-flex;
            align-items: center;
            gap: .3rem;
            padding: .22rem .65rem;
            border-radius: 20px;
            background: rgba(22, 163, 74, .12);
            border: 1px solid rgba(22, 163, 74, .28);
            font-size: .73rem;
            font-weight: 700;
            color: #16a34a
        }

        body[data-theme="dark"] .live-badge,
        body[data-theme="duralux-dark"] .live-badge {
            background: rgba(34, 197, 94, .1);
            border-color: rgba(34, 197, 94, .25);
            color: #22c55e
        }

        .gradient-text {
            background: var(--grad);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text
        }

        .sidebar.collapsed #nlNewBadge {
            display: none !important;
        }

        /* ═══════ ANIM ═══════════════════════════════════════════════════════════ */
        @keyframes spin {
            to {
                transform: rotate(360deg)
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0
            }

            to {
                opacity: 1
            }
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(24px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        @keyframes scaleIn {
            from {
                opacity: 0;
                transform: scale(.94)
            }

            to {
                opacity: 1;
                transform: scale(1)
            }
        }

        .animate-fade-in {
            animation: fadeIn .55s ease forwards
        }

        .animate-slide-up {
            animation: slideUp .7s ease forwards
        }

        .animate-scale {
            animation: scaleIn .55s ease forwards
        }

        /* ═══════ RESPONSIVE ════════════════════════════════════════════════════ */
        @media(max-width:900px) {
            .charts-section {
                grid-template-columns: 1fr
            }
        }

        @media(max-width:768px) {
            .header-center {
                display: none
            }

            .sidebar {
                width: 180px;
                min-width: 180px
            }
        }

        @media(max-width:600px) {

            .sidebar,
            .sidebar.collapsed {
                width: 52px;
                /* was: 0 */
                min-width: 52px;
                /* was: 0 */
                padding: 1rem 0;
                /* was: 0 */
                border-right: 1px solid var(--border);
                /* add this back */
                overflow: hidden !important
            }

            .sidebar.collapsed .nav-label,
            .sidebar.collapsed .nav-section-label {
                display: none !important;
            }

            .sidebar.collapsed .nav-item {
                justify-content: center;
                padding: .55rem;
            }

            .top-stats {
                grid-template-columns: repeat(2, 1fr)
            }

            .filters-grid {
                grid-template-columns: 1fr 1fr
            }
        }
    </style>

</head>

<body>
    <!-- ======================================================================================== -->
    <!-- SHAREPOINT COMPATIBILITY: All CSS in body since SharePoint strips <head> section -->
    <!-- ======================================================================================== -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/lucide.min.js"></script>

    <!-- SharePoint Compatibility Wrapper -->

    <!-- ===== LANDING PAGE ===== -->
    <div id="landingPage" class="landing-page" style="display: none;">
        <!-- Animated background gradients -->
        <div class="landing-bg-gradient"></div>
        <div class="landing-bg-orbs">
            <div class="orb orb-1"></div>
            <div class="orb orb-2"></div>
            <div class="orb orb-3"></div>
        </div>

        <!-- LEFT SECTION - Branding & Features -->
        <div class="landing-left animate-fade-in">
            <div class="landing-branding">
                <div class="landing-brand-icon">
                    <img src="http://sharedspaces:8086/sites/SM/Shared%20Documents/DUlogo%202026.png" alt="du Logo" style="width: 64px; height: 64px; object-fit: contain;">
                </div>
                <h1 class="landing-brand-title gradient-text">Service Management</h1>
                <p class="landing-brand-subtitle">Enterprise-grade account performance and revenue analytics platform for du Telecom</p>

                <div class="landing-features">
                    <div class="landing-feature">
                        <div class="landing-feature-icon">
                            <i data-lucide="trending-up" style="width: 20px; height: 20px;"></i>
                        </div>
                        <div class="landing-feature-text">
                            <div class="landing-feature-title">Real-time Analytics</div>
                            <div class="landing-feature-desc">Track revenue trends and account performance live</div>
                        </div>
                    </div>
                    <div class="landing-feature">
                        <div class="landing-feature-icon">
                            <i data-lucide="shield-check" style="width: 20px; height: 20px;"></i>
                        </div>
                        <div class="landing-feature-text">
                            <div class="landing-feature-title">Secure Access</div>
                            <div class="landing-feature-desc">Role-based permissions and data protection</div>
                        </div>
                    </div>
                    <div class="landing-feature">
                        <div class="landing-feature-icon">
                            <i data-lucide="bar-chart-3" style="width: 20px; height: 20px;"></i>
                        </div>
                        <div class="landing-feature-text">
                            <div class="landing-feature-title">Comprehensive Reports</div>
                            <div class="landing-feature-desc">Export detailed account and revenue data</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- RIGHT SECTION - Login Form -->
        <div class="landing-right">
            <div class="landing-container">
                <!-- Header -->
                <div class="landing-header animate-slide-up">
                    <div class="landing-logo">
                        <div class="logo-icon gradient-overlay-purple-pink">
                            <img src="http://sharedspaces:8086/sites/SM/Shared%20Documents/DUlogo%202026.png" alt="du Logo" style="width: 48px; height: 48px; object-fit: contain;">
                        </div>
                    </div>
                    <h1 class="landing-title">Welcome Back</h1>
                    <p class="landing-subtitle">Sign in to access your dashboard</p>
                </div>

                <!-- Main card with stagger animation -->
                <div class="landing-card animate-slide-up" style="animation-delay: 0.2s;">
                    <!-- Welcome section -->
                    <div class="landing-welcome">
                        <div class="welcome-badge">
                            <span class="badge-pulse"></span>
                            <span>Welcome Back</span>
                        </div>
                        <div class="landing-user-info">
                            <div class="landing-avatar">
                                <div class="avatar-ring"></div>
                                <div class="avatar-placeholder" id="avatarPlaceholder">?</div>
                            </div>
                            <div class="landing-details">
                                <h3 id="landingUserName">Loading...</h3>
                                <p id="landingUserEmail">Verifying access...</p>
                                <p id="landingUserRole" class="landing-role"></p>
                            </div>
                        </div>
                    </div>

                    <!-- Enhanced stats grid -->
                    <div class="landing-stats-container">
                        <div class="stats-grid">
                            <div class="landing-stat-card landing-stat-card-primary animate-scale" style="animation-delay: 0.4s;">
                                <div class="stat-icon-wrapper">
                                    <div class="stat-icon-bg"></div>
                                    <i data-lucide="trending-up" class="stat-icon"></i>
                                </div>
                                <div class="stat-content">
                                    <div class="stat-label">Total Visits</div>
                                    <div class="stat-value" id="landingVisitCount">--</div>
                                </div>
                            </div>

                            <div class="landing-stat-card landing-stat-card-secondary animate-scale" style="animation-delay: 0.5s;">
                                <div class="stat-icon-wrapper">
                                    <div class="stat-icon-bg"></div>
                                    <i data-lucide="clock" class="stat-icon"></i>
                                </div>
                                <div class="stat-content">
                                    <div class="stat-label">Last Visit</div>
                                    <div class="stat-value" id="landingLastVisit">--</div>
                                </div>
                            </div>
                        </div>

                        <!-- System status -->
                        <div class="stats-secondary">
                            <div class="stat-mini">
                                <i data-lucide="shield-check" style="width: 16px; height: 16px;"></i>
                                <span>Secure Session</span>
                            </div>
                            <div class="stat-mini">
                                <i data-lucide="activity" style="width: 16px; height: 16px;"></i>
                                <span id="systemStatus">System Online</span>
                            </div>
                        </div>
                    </div>

                    <!-- Enhanced button -->
                    <div class="landing-actions">
                        <button type="button" id="goToDashboard" class="landing-btn btn-primary-enhanced" disabled>
                            <span class="btn-icon">
                                <i data-lucide="arrow-right" style="width: 20px; height: 20px;"></i>
                            </span>
                            <span class="btn-text">Verifying Access...</span>
                            <span class="btn-shine"></span>
                        </button>
                        <p class="landing-note" id="accessNote">
                            <i data-lucide="info" style="width: 14px; height: 14px;"></i>
                            <span>Please wait while we verify your permissions...</span>
                        </p>
                        <!-- OWNER VIEW AS PANEL -->
                        <div id="ownerViewAsPanel" style="display:none; margin-top:16px; padding:16px;
                          background:var(--bg-secondary); border:1px solid var(--border); border-radius:12px;">
                          <div style="font-size:12px;font-weight:700;text-transform:uppercase;letter-spacing:.05em;
                            color:var(--t3);margin-bottom:10px;">
                            <i data-lucide="user-cog" style="width:13px;height:13px;display:inline-block;
                              vertical-align:middle;margin-right:4px;"></i>View As Another User
                          </div>
                          <div style="display:flex;gap:8px;align-items:center;">
                            <input type="text" id="ownerViewAsEmail" class="filter-select"
                              placeholder="Enter user email..."
                              style="flex:1;font-size:13px;"
                              onkeydown="if(event.key==='Enter') ownerSwitchView()" />
                            <button type="button" onclick="ownerSwitchView()" class="export-btn"
                              style="padding:.45rem .9rem;font-size:12px;white-space:nowrap;">
                              <i data-lucide="log-in" style="width:13px;height:13px;display:inline-block;
                                vertical-align:middle;margin-right:4px;"></i>Go
                            </button>
                          </div>
                          <div id="ownerViewAsMsg" style="font-size:12px;margin-top:8px;min-height:16px;"></div>
                        </div>
                        <button type="button" id="goToAccountSearch" class="landing-btn btn-primary-enhanced" style="margin-top: 12px; background: linear-gradient(135deg, #10b981, #059669);">
                            <span class="btn-icon">
                                <i data-lucide="search" style="width: 20px; height: 20px;"></i>
                            </span>
                            <span class="btn-text">Go to Account Search</span>
                            <span class="btn-shine"></span>
                        </button>
                        <button type="button" id="goToOccSearch" class="landing-btn btn-primary-enhanced" style="display:none; margin-top: 12px; background: linear-gradient(135deg, #2563eb, #1d4ed8);">
                            <span class="btn-icon">
                                <i data-lucide="file-search" style="width: 20px; height: 20px;"></i>
                            </span>
                            <span class="btn-text">Go to OCC Search</span>
                            <span class="btn-shine"></span>
                        </button>
                    </div>
                </div>

                <!-- Footer -->
                <div class="landing-footer animate-fade-in" style="animation-delay: 0.6s;">
                    <p>Powered by du Telecom Service Management Platform</p>
                </div>
            </div>
        </div>
    </div>

    <!-- ===== MAIN DASHBOARD ===== -->
    <div class="portal-shell" style="display:none;" id="dashboardContent">
        <div class="header">
            <div class="logo-section">
                <div class="logo">
                    <img src="http://sharedspaces:8086/sites/SM/Shared%20Documents/DUlogo%202026.png" alt="du Logo" style="width:24px;height:24px;object-fit:contain;">
                </div>
                <span class="header-title">Service Management</span>
            </div>
            <div class="header-center">
                <div class="live-badge-pill"><span class="status-dot"></span><span>Live</span></div>
                <div class="date-badge"><i data-lucide="calendar"></i><span id="currentDate">Loading...</span></div>
            </div>
            <div class="header-actions">
                <button type="button" class="export-btn tab-refresh-btn" id="headerRefreshBtn" onclick="refreshCurrentTab()" title="Refresh current tab data" style="padding:.45rem .85rem;font-size:.82rem;">
                    <i data-lucide="refresh-cw" style="width:15px;height:15px;display:inline-block;vertical-align:middle;margin-right:5px;"></i>Refresh
                </button>
                <select class="color-scheme-selector" id="colorSchemeSelector" onchange="changeTheme(event)">
                    <option value="magenta">&#9670; Magenta</option>
                    <option value="duralux">&#9670; Duralux</option>
                </select>
                <div class="theme-toggle" onclick="toggleTheme()" title="Light/Dark">
                    <i data-lucide="moon" id="themeIcon"></i>
                </div>
            </div>
        </div>
        <div class="main-container">
            <div class="sidebar" id="sidebar" style="position:relative;">
                <div class="sidebar-toggle-btn" id="sidebarToggleBtn" onclick="toggleSidebar()" title="Toggle Sidebar">
                    <i data-lucide="chevrons-left" id="sidebarToggleIcon"></i>
                </div>
                <div class="nav-items">
                    <div class="nav-item" onclick="showNewsletterView()">
                        <div class="nav-icon"><i data-lucide="mail"></i></div>
                        <div class="nav-label">Newsletter</div>
                        <span id="nlNewBadge" style="display:none;background:#ef4444;color:#fff;font-size:9px;font-weight:800;padding:2px 6px;border-radius:20px;margin-left:auto;line-height:1.4;">NEW</span>
                    </div>
                    <div class="nav-section-label">Overview</div>
                    <div class="nav-item active" data-section="dashboard-view" onclick="switchDashboardSection('dashboard-view')">
                        <div class="nav-icon"><i data-lucide="layout-dashboard"></i></div>
                        <div class="nav-label">Dashboard</div>
                    </div>
                    <div class="nav-section-label">Admin</div>
                    <div class="nav-item" data-section="pending-requests" onclick="switchDashboardSection('pending-requests')" id="pendingRequestsNavItem" style="display:none;">
                        <div class="nav-icon"><i data-lucide="file-clock"></i></div>
                        <div class="nav-label">Pending Requests</div>
                    </div>
                    <div class="nav-item" data-section="transfer-requests" onclick="switchDashboardSection('transfer-requests')" id="transferRequestsNavItem" style="display:none;">
                        <div class="nav-icon"><i data-lucide="arrow-right-left"></i></div>
                        <div class="nav-label">Transfer Requests</div>
                    </div>
                    <div class="nav-item" id="qaNavItem" onclick="switchDashboardSection('qaView')" data-section="qaView" style="display:none;">
    <div class="nav-icon"><i data-lucide="shield-check"></i></div>
    <div class="nav-label">Quality Assurance</div>
</div>
<div class="nav-item" id="rnpsNavItem" onclick="switchDashboardSection('rnpsView')" data-section="rnpsView" style="display:none;">
    <div class="nav-icon"><i data-lucide="star"></i></div>
    <div class="nav-label">RNPS Dashboard</div>
</div>
                    <div class="nav-section-label">External</div>
                    <div class="nav-item" onclick="if(typeof analyticsTrackPage==='function')analyticsTrackPage('Customer Service');showCustomerService()">
                        <div class="nav-icon"><i data-lucide="headphones"></i></div>
                        <div class="nav-label">Customer Review</div>
                    </div>
                    <div class="nav-item" id="executiveHubNavItem" onclick="showExecutiveHub()" style="display:none;">
                        <div class="nav-icon"><i data-lucide="shield"></i></div>
                        <div class="nav-label">Executive Ops Hub</div>
                    </div>
                    <div class="nav-section-label" id="intelligenceNavLabel" style="display:none;">Intelligence</div>
                    <div class="nav-item" id="portalAnalyticsNavItem" onclick="showPortalAnalytics()" style="display:none;">
                        <div class="nav-icon"><i data-lucide="radar"></i></div>
                        <div class="nav-label">Portal Intelligence</div>
                    </div>
                    <div class="nav-section-label">Tools</div>
                    <div class="nav-item" data-section="eta-view" onclick="showETAView()">
                        <div class="nav-icon"><i data-lucide="layers"></i></div>
                        <div class="nav-label">Events / TT / Activity</div>
                    </div>
                  <div class="nav-item" id="projectMgmtNavItem" onclick="window.showProjectManagement()">
    <div class="nav-icon"><i data-lucide="bar-chart-3"></i></div>
    <div class="nav-label">Project Management</div>
</div>
                    <div class="nav-item" onclick="showComingSoon('synergy-360','Synergy 360','refresh-cw')">
                        <div class="nav-icon"><i data-lucide="refresh-cw"></i></div>
                        <div class="nav-label">Synergy 360</div>
                    </div>


                    <div class="nav-item" data-section="suggestionsView" onclick="switchDashboardSection('suggestionsView');sgLoadSuggestions();">
                        <div class="nav-icon"><i data-lucide="lightbulb"></i></div>
                        <div class="nav-label">Suggestion Box</div>
                    </div>

                    <div class="nav-item" data-section="processDocsView" onclick="switchDashboardSection('processDocsView');pdLoadDocs();">
                        <div class="nav-icon"><i data-lucide="folder-open"></i></div>
                        <div class="nav-label">Process Documents</div>
                    </div>
                    <div class="nav-item" id="psdNavItem" data-section="psdView" onclick="switchDashboardSection('psdView')" style="display:none;">
                        <div class="nav-icon"><i data-lucide="clipboard-list"></i></div>
                        <div class="nav-label">PSD Assignment</div>
                    </div>
<div class="nav-item" data-section="seMailboxView" onclick="switchDashboardSection('seMailboxView');initSEMailbox();">
    <div class="nav-icon"><i data-lucide="inbox"></i></div>
    <div class="nav-label">SE Mailbox</div>
</div>
                  <div class="nav-item" data-section="contactUsView" onclick="switchDashboardSection('contactUsView');">
                        <div class="nav-icon"><i data-lucide="phone"></i></div>
                        <div class="nav-label">Contact Us</div>
                    </div>
                    <div class="nav-item" id="tsmPendingNavItem" data-section="tsmPendingView" onclick="switchDashboardSection('tsmPendingView');initTSMPendingEscalation();" style="display:none;">
                        <div class="nav-icon"><i data-lucide="clipboard-list"></i></div>
                        <div class="nav-label">TSM Pending/Escalations</div>
                    </div>



                </div>
            </div>
            <div class="content">
                <div id="dashboard-view" class="dashboard-section">
                    <div id="loading" style="text-align:center; padding:60px; font-size:18px;">
                        <i data-lucide="loader" style="width: 24px; height: 24px; display: inline-block; vertical-align: middle; margin-right: 8px; animation: spin 1s linear infinite;"></i> Loading SharePoint data...
                    </div>

                    <div id="content" style="display:none;">
                        <div class="top-stats" id="stats"></div>

                        <div class="filters-section">
                            <div class="filters-grid">
                                <div class="filter-group">
                                    <label class="filter-label">Line Manager</label>
                                    <div class="custom-multiselect">
                                        <div class="multiselect-selected" onclick="smToggleMsDropdown('filterLMDropdown')">
                                            <span id="filterLMSelectedText">All Line Managers</span>
                                            <i data-lucide="chevron-down" style="width:16px;height:16px;opacity:.6;"></i>
                                        </div>
                                        <div class="multiselect-dropdown" id="filterLMDropdown" style="display:none;"></div>
                                    </div>
                                </div>
                                <div class="filter-group">
                                    <label class="filter-label">Service Manager</label>
                                    <div class="custom-multiselect">
                                        <div class="multiselect-selected" onclick="smToggleMsDropdown('filterSMDropdown')">
                                            <span id="filterSMSelectedText">All Service Managers</span>
                                            <i data-lucide="chevron-down" style="width:16px;height:16px;opacity:.6;"></i>
                                        </div>
                                        <div class="multiselect-dropdown" id="filterSMDropdown" style="display:none;"></div>
                                    </div>
                                </div>
                                <div class="filter-group">
                                    <label class="filter-label">Team</label>
                                    <div class="custom-multiselect">
                                        <div class="multiselect-selected" onclick="smToggleMsDropdown('filterTeamDropdown')">
                                            <span id="filterTeamSelectedText">All Teams</span>
                                            <i data-lucide="chevron-down" style="width:16px;height:16px;opacity:.6;"></i>
                                        </div>
                                        <div class="multiselect-dropdown" id="filterTeamDropdown" style="display:none;"></div>
                                    </div>
                                </div>
                                <div class="filter-group">
                                    <label class="filter-label">Revenue Status</label>
                                    <div class="custom-multiselect">
                                        <div class="multiselect-selected" onclick="smToggleMsDropdown('filterRevDropDropdown')">
                                            <span id="filterRevDropSelectedText">All Accounts</span>
                                            <i data-lucide="chevron-down" style="width:16px;height:16px;opacity:.6;"></i>
                                        </div>
                                        <div class="multiselect-dropdown" id="filterRevDropDropdown" style="display:none;">
                                            <div class="multiselect-option"><input type="checkbox" id="revDrop_drop" value="drop" onchange="smOnMsFilterChange('RevDrop','Revenue Status',true)"><label for="revDrop_drop">⚠ Revenue Drop Alerts</label></div>
                                            <div class="multiselect-option"><input type="checkbox" id="revDrop_upgrade" value="upgrade" onchange="smOnMsFilterChange('RevDrop','Revenue Status',true)"><label for="revDrop_upgrade">✓ Revenue Upgrade Alerts</label></div>
                                        </div>
                                    </div>
                                </div>
                                <div class="filter-group">
                                    <label class="filter-label">Account Structure</label>
                                    <div class="custom-multiselect">
                                        <div class="multiselect-selected" onclick="smToggleMsDropdown('filterStructureDropdown')">
                                            <span id="filterStructureSelectedText">All Accounts</span>
                                            <i data-lucide="chevron-down" style="width:16px;height:16px;opacity:.6;"></i>
                                        </div>
                                        <div class="multiselect-dropdown" id="filterStructureDropdown" style="display:none;">
                                            <div class="multiselect-option"><input type="checkbox" id="struct_with" value="with-children" onchange="smOnMsFilterChange('Structure','Account Structure',true)"><label for="struct_with">📊 Groups with Children</label></div>
                                            <div class="multiselect-option"><input type="checkbox" id="struct_standalone" value="standalone" onchange="smOnMsFilterChange('Structure','Account Structure',true)"><label for="struct_standalone">📄 Standalone Groups</label></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <!-- Legacy selects for tsm-se.js (hidden — do not remove) -->
                            <div aria-hidden="true" style="position:absolute;left:-9999px;width:1px;height:1px;overflow:hidden;opacity:0;">
                                <select class="filter-select" id="filterLM" onchange="applyFilters()"></select>
                                <select class="filter-select" id="filterSM" onchange="applyFilters()"></select>
                                <select class="filter-select" id="filterTeam" onchange="onTeamFilterChanged()"></select>
                            </div>
                            <div class="filter-actions">
                                <button type="button" class="export-btn tab-refresh-btn" data-section="dashboard-view" onclick="refreshTabData('dashboard-view')" style="padding:.5rem 1rem;font-size:.82rem;">
                                    <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
                                </button>
                                <button type="button" id="revenueToggleBtn" class="export-btn" onclick="toggleRevenueEnabled()" style="display:none;padding:.5rem 1rem;font-size:.82rem;">
                                    <i data-lucide="lock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Revenue: Disabled
                                </button>
                                <button type="button" id="pocToggleBtn" class="export-btn" onclick="togglePocDetailsEnabled()" style="display:none;padding:.5rem 1rem;font-size:.82rem;">
                                    <i data-lucide="lock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>POC Details: Disabled
                                </button>
                                <button type="button" id="rnpsEligibilityToggleBtn" class="export-btn" onclick="toggleRnpsEligibilityEnabled()" style="display:none;padding:.5rem 1rem;font-size:.82rem;">
                                    <i data-lucide="lock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>RNPS Eligibility: Disabled
                                </button>
                                <button type="button" class="reset-btn" onclick="resetFilters()">
                                    <i data-lucide="rotate-ccw" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Reset All Filters</button>
                            </div>
                        </div>

                      <!-- LINE MANAGER TILES - 5 in one row -->
                        <div class="line-managers-section">
                            <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:.75rem;margin-bottom:.85rem;">
                                <h2 class="section-title" style="margin:0!important;"><i data-lucide="users" style="width:20px;height:20px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>Line Manager Performance</h2>
                                <button type="button" id="loadAnalyticsBtn" onclick="loadAnalytics()" class="export-btn" style="padding:.5rem 1.1rem;font-size:.82rem;">
                                    <i data-lucide="bar-chart-2" style="width:15px;height:15px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>
                                    <span id="loadAnalyticsBtnText">Load RNPS &amp; ETA Analytics</span>
                                </button>
                            </div>
                            <div class="line-managers-grid" id="lineManagersGrid"></div>
                        </div>
                        <!-- SERVICE MANAGER PERFORMANCE - ROW FORMAT -->
                        <div class="sm-performance-section" id="smPerformanceSection" style="display:none;">
                            <h2 class="section-title"><i data-lucide="bar-chart-3" style="width: 20px; height: 20px; display: inline-block; vertical-align: middle; margin-right: 8px;"></i>Service Manager Performance</h2>
                            <div class="manager-list" id="serviceManagersList"></div>
                        </div>

                        <div style="text-align: center; margin-bottom: 16px;">
                            <button type="button" id="toggleChartsBtn" class="export-btn" onclick="toggleCharts()" style="padding: 12px 24px; font-size: 14px;">
                                <i data-lucide="eye" id="chartsIcon" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>
                                <span id="chartsText">Show Analytics Charts</span>
                            </button>
                        </div>

                        <div class="charts-section" id="chartsSection" style="display: none;">
                            <div class="chart-card">
                                <h3 class="chart-title" id="trendTitle"><i data-lucide="bar-chart-3" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Workload & Revenue Analysis</h3>
                                <div class="chart-container">
                                    <canvas id="trendChart"></canvas>
                                </div>
                            </div>

                            <div class="chart-card">
                                <h3 class="chart-title"><i data-lucide="pie-chart" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Group vs Child Accounts</h3>
                                <div class="chart-container">
                                    <canvas id="typeChart"></canvas>
                                </div>
                            </div>
                        </div>

                        <div class="table-section">
                            <div class="table-header">
                                <h3 class="table-title"><i data-lucide="clipboard-list" style="width: 20px; height: 20px; display: inline-block; vertical-align: left; margin-right: 8px;"></i>Detailed Account Information</h3>
                                <div class="table-actions">
                                    <select class="filter-select" id="transferStatusFilterMain" onchange="applyTransferFilter()" style="width:auto;font-size:12px;">
                                        <option value="">All Accounts</option>
                                        <option value="Transfer_Pending">⏳ Pending AM Approval</option>
                                        <option value="Transfer_Overdue_3D">⏰ Pending 3+ Days (Reminder)</option>
                                        <option value="AM_Approved">✅ AM Approved</option>
                                    </select>
                                    <input type="text" class="search-box" id="agGridSearch" placeholder="Search all columns..." oninput="searchAGGrid(this.value)">
                                    <button type="button" id="smExportExcelBtn" class="export-btn" onclick="smPromptExport()"><i data-lucide="file-spreadsheet" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Export to Excel</button>
                                </div>
                            </div>
                            <div id="myGrid" class="ag-theme-alpine" style="height: 600px; width: 100%;"></div>
                        </div>
                    </div>
                </div>
                <div class="dashboard-section" style="display:none;" id="customerServiceView">
                    <div class="header">
                        <div class="header-left">
                            <h1><i data-lucide="headphones" style="width:28px;height:28px;display:inline-block;vertical-align:middle;margin-right:10px;"></i>Customer Reviews</h1>
                            <p>Monthly &amp; Quarterly Review Performance Tracking</p>
                        </div>
                    </div>
                    <div id="csLoadingScreen" style="text-align:center;padding:80px 20px;color:var(--t2);">
                        <div style="margin-bottom:20px;"><i data-lucide="loader-2" style="width:48px;height:48px;animation:spin 1s linear infinite;color:var(--acc);"></i></div>
                        <div style="font-size:16px;font-weight:600;">Loading Customer Review Dashboard...</div>
                        <div style="font-size:13px;margin-top:8px;color:var(--t3);">Connecting to SharePoint...</div>
                    </div>
                    <div id="csMainContent" style="display:none;padding:1.25rem;">

                        <!-- TAB BAR -->
                        <div id="csTabBar" style="display:flex;gap:8px;margin-bottom:1.5rem;flex-wrap:wrap;">
                            <button type="button" id="csTabMSR" onclick="csSetTab('MSR')" style="padding:10px 24px;border-radius:10px;font-size:13px;font-weight:700;cursor:pointer;border:1px solid var(--border);background:var(--bg-input);color:var(--t1);display:none;align-items:center;transition:all .2s;">MSR</button>
                            <button type="button" id="csTabQSR" onclick="csSetTab('QSR')" style="padding:10px 24px;border-radius:10px;font-size:13px;font-weight:700;cursor:pointer;border:1px solid var(--border);background:var(--bg-input);color:var(--t1);display:none;align-items:center;transition:all .2s;">QSR</button>
                            <button type="button" id="csTabHC" onclick="csSetTab('HC')" style="padding:10px 24px;border-radius:10px;font-size:13px;font-weight:700;cursor:pointer;border:1px solid var(--border);background:var(--bg-input);color:var(--t1);display:none;align-items:center;transition:all .2s;">Health Check</button>
                        </div>

                        <!-- MSR / QSR CONTENT -->
                        <div id="csMSRQSRContent">
                            <div id="csBreadcrumb"></div>
                            <div id="csKPISection"></div>
                           
                            <div id="csTilesSection"></div>

                            <div class="table-section">
                                <div class="table-header">
                                    <h2 class="table-title"><i data-lucide="table" style="width:20px;height:20px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>Review Details</h2>
                                    <div class="table-actions">
                                        <select class="filter-select" id="csReviewTypeFilter" onchange="csReviewTypeChanged()" style="width:auto;">
                                            <option value="">All Types</option>
                                            <option value="MSR">MSR</option>
                                            <option value="QSR">QSR</option>
                                        </select>
                                        <select class="filter-select" id="csPeriodFilter" onchange="csApplyFilters()" style="width:auto;">
                                            <option value="">All Periods</option>
                                        </select>
                                        <select class="filter-select" id="csStatusFilter" onchange="csApplyFilters()" style="width:auto;">
                                            <option value="">All Status</option>
                                            <option value="Completed">✓ Completed</option>
                                            <option value="In Progress">⟳ In Progress</option>
                                            <option value="Pending">○ Pending</option>
                                            <option value="Overdue">✕ Overdue</option>
                                            <option value="Not Applicable">∅ Not Applicable</option>
                                            <option value="Customer Declined">− Declined</option>
                                            <option value="Failed">✕ Failed</option>
                                        </select>
                                        <input type="text" class="search-box" id="csSearchBox" placeholder="Search account, customer..." onkeyup="csApplyFilters()" style="width:200px;">
                                        <button type="button" onclick="csClearFilters()" class="reset-btn" style="padding:8px 16px;font-size:12px;">
                                            <i data-lucide="rotate-ccw" style="width:13px;height:13px;display:inline-block;vertical-align:middle;margin-right:4px;"></i>Reset Filters
                                        </button>
                                        <button type="button" class="export-btn tab-refresh-btn" data-section="customerServiceView" onclick="refreshTabData('customerServiceView')" style="padding:8px 16px;font-size:12px;">
                                            <i data-lucide="refresh-cw" style="width:13px;height:13px;display:inline-block;vertical-align:middle;margin-right:4px;"></i>Refresh
                                        </button>
                                        <button type="button" id="csAutoCreateBtn" onclick="csAutoCreateReviews();return false;" class="export-btn" style="display:none;">
                                            <i data-lucide="plus-circle" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Auto-Create
                                        </button>
                                        <button type="button" onclick="csExportToExcel();return false;" class="export-btn" style="background:linear-gradient(135deg,#27AE60,#229954);">
                                            <i data-lucide="file-spreadsheet" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Export
                                        </button>
                                    </div>
                                </div>
                                <div id="csGrid" class="ag-theme-alpine" style="height:600px;width:100%;"></div>
                            </div>
                        </div>
                    </div><!-- /csMSRQSRContent -->

                    <!-- HEALTH CHECK CONTENT -->
                    <div id="csHCContent" style="display:none;">
                        <div id="csHCKPIs"></div>
                        <div class="table-section" style="margin-top:1rem;">
                            <div class="table-header">
                                <h2 class="table-title"><i data-lucide="heart-pulse" style="width:20px;height:20px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>Health Check Interactions</h2>
                                <div class="table-actions">
                                    <input type="text" class="search-box" id="csHCSearchBox" placeholder="Search account, customer..." onkeyup="csHCApplyFilters()" style="width:200px;">
                                    <button type="button" id="csHCAddBtn" onclick="csShowHCModal(null)" class="export-btn" style="display:none;">
                                        <i data-lucide="plus-circle" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Add Interaction
                                    </button>
                                    <button type="button" onclick="csExportHCToExcel()" class="export-btn" style="background:linear-gradient(135deg,#27AE60,#229954);">
                                        <i data-lucide="file-spreadsheet" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Export
                                    </button>
                                </div>
                            </div>
                            <div id="csHCGrid" class="ag-theme-alpine" style="height:600px;width:100%;"></div>
                        </div>
                   </div><!-- /csHCContent -->

                </div><!-- /customerServiceView -->
<div class="dashboard-section" style="display:none;" id="contactUsView">
    <div style="display:flex;justify-content:flex-end;margin-bottom:.75rem;">
        <button type="button" class="export-btn tab-refresh-btn" data-section="contactUsView" onclick="refreshTabData('contactUsView')" style="padding:.45rem .9rem;font-size:.82rem;">
            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
        </button>
    </div>
    <div id="cuLoading" style="text-align:center;padding:20px;color:var(--t2);">
        <i data-lucide="loader-2" style="width:40px;height:40px;animation:spin 1s linear infinite;color:var(--acc);display:inline-block;"></i>
        <div style="margin-top:12px;font-weight:600;">Loading Directory...</div>
    </div>
    <div id="cuContent">
        <div id="cuViewContainer"></div>
    </div>
</div>
                <div class="dashboard-section" style="display:none;" id="suggestionsView">
                    <div style="display:flex;justify-content:flex-end;margin-bottom:.75rem;">
                        <button type="button" class="export-btn tab-refresh-btn" data-section="suggestionsView" onclick="refreshTabData('suggestionsView')" style="padding:.45rem .9rem;font-size:.82rem;">
                            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
                        </button>
                    </div>
                    <div id="sgLoading" style="text-align:center;padding:60px;color:var(--t2);">
                        <i data-lucide="loader-2" style="width:40px;height:40px;animation:spin 1s linear infinite;color:var(--acc);display:inline-block;"></i>
                        <div style="margin-top:12px;font-weight:600;">Loading Suggestions...</div>
                    </div>
                    <div id="sgContent" style="display:none;">
                        <div id="sgContainer"></div>
                    </div>
                </div>
<div class="dashboard-section" style="display:none;" id="seMailboxView">
    <div style="display:flex;justify-content:flex-end;margin-bottom:.75rem;">
        <button type="button" class="export-btn tab-refresh-btn" data-section="seMailboxView" onclick="refreshTabData('seMailboxView')" style="padding:.45rem .9rem;font-size:.82rem;">
            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
        </button>
    </div>
    <div id="semailbox-root"></div>
</div>
                <div class="dashboard-section" style="display:none;" id="processDocsView">
                    <div style="display:flex;justify-content:flex-end;margin-bottom:.75rem;">
                        <button type="button" class="export-btn tab-refresh-btn" data-section="processDocsView" onclick="refreshTabData('processDocsView')" style="padding:.45rem .9rem;font-size:.82rem;">
                            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
                        </button>
                    </div>
                    <div id="pdLoading" style="text-align:center;padding:60px;color:var(--t2);">
                        <i data-lucide="loader-2" style="width:40px;height:40px;animation:spin 1s linear infinite;color:var(--acc);display:inline-block;"></i>
                        <div style="margin-top:12px;font-weight:600;">Loading Documents...</div>
                    </div>
                    <div id="pdContent" style="display:none;">
                        <div id="pdContainer"></div>
                    </div>
                </div>
                
                <div class="dashboard-section" style="display:none;" id="qaView">
    <div style="display:flex;justify-content:flex-end;margin-bottom:.75rem;">
        <button type="button" class="export-btn tab-refresh-btn" data-section="qaView" onclick="refreshTabData('qaView')" style="padding:.45rem .9rem;font-size:.82rem;">
            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
        </button>
    </div>
    <div id="qaLoading" style="text-align:center;padding:20px;color:var(--t2);">
        <i data-lucide="loader-2" style="width:40px;height:40px;animation:spin 1s linear infinite;color:var(--acc);display:inline-block;"></i>
        <div style="margin-top:12px;font-weight:600;">Loading Quality Assurance...</div>
    </div>
    <div id="qaContent">
        <div id="qaContainer"></div>
    </div>
</div>
<div class="dashboard-section" style="display:none;" id="tsmPendingView">
    <div style="display:flex;justify-content:flex-end;margin-bottom:.75rem;">
        <button type="button" class="export-btn tab-refresh-btn" data-section="tsmPendingView" onclick="refreshTabData('tsmPendingView')" style="padding:.45rem .9rem;font-size:.82rem;">
            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
        </button>
    </div>
    <div id="tsmPeLoading" style="text-align:center;padding:60px;color:var(--t2);">
        <i data-lucide="loader-2" style="width:40px;height:40px;animation:spin 1s linear infinite;color:var(--acc);display:inline-block;"></i>
        <div style="margin-top:12px;font-weight:600;">Loading TSM Pending/Escalations...</div>
    </div>
    <div id="tsmPeContent" style="display:none;">
        <div id="tsmPeRoot"></div>
    </div>
</div>
<div class="dashboard-section" style="display:none;" id="rnpsView">
    <div style="display:flex;justify-content:flex-end;margin-bottom:.75rem;">
        <button type="button" class="export-btn tab-refresh-btn" data-section="rnpsView" onclick="refreshTabData('rnpsView')" style="padding:.45rem .9rem;font-size:.82rem;">
            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
        </button>
    </div>
    <div id="rnpsContent"><div id="rnpsContainer"></div></div>
</div>
            </div><!-- dashboard-view -->
            <div id="eta-view" class="dashboard-section" style="display:none; width:100%;"></div>
            <div id="pm-view" class="dashboard-section" style="display:none; width:100%;"></div>
            <!-- PSD ASSIGNMENT SECTION -->
            <div class="dashboard-section" style="display:none;" id="psdView">
                <div id="psdLoading" style="text-align:center;padding:40px;color:var(--t2);">
                    <i data-lucide="loader-2" style="width:40px;height:40px;animation:spin 1s linear infinite;color:var(--acc);display:inline-block;"></i>
                    <div style="margin-top:12px;font-weight:600;">Loading PSD Assignment...</div>
                </div>
                <div id="psdContent" style="display:none;"><div id="psdContainer"></div></div>
            </div>
            <!-- PENDING REQUESTS SECTION -->
            <div id="pending-requests" class="dashboard-section" style="display: none;">
                <div style="width:100%;">
                    <h2 style="font-size:1rem;font-weight:800;color:var(--t1);margin:0 0 .85rem!important;padding:0!important;display:flex;align-items:center;justify-content:space-between;gap:.4rem;">
                        <span style="display:flex;align-items:center;gap:.4rem;"><i data-lucide="file-clock" style="width: 24px; height: 24px;"></i> Pending Service Requests</span>
                        <button type="button" class="export-btn tab-refresh-btn" data-section="pending-requests" onclick="refreshTabData('pending-requests')" style="padding:.4rem .85rem;font-size:.78rem;">
                            <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:5px;"></i>Refresh
                        </button>
                    </h2>

                    <div id="requestsLoading" style="text-align:center; padding:60px; font-size:18px;">
                        <i data-lucide="loader" style="width: 24px; height: 24px; display: inline-block; vertical-align: middle; margin-right: 8px; animation: spin 1s linear infinite;"></i> Loading requests...
                    </div>

                    <div id="requestsContent" style="display:none;">
                        <div class="table-section">
                            <div class="table-header">
                              <h3 class="table-title">Pending Requests</h3>
                            <div class="table-actions">
                                <input type="text" class="search-box" id="requestSearchBox" placeholder="Search requests..." oninput="searchRequests()">
                                <button type="button" class="export-btn" onclick="exportPendingToExcel()"><i data-lucide="file-spreadsheet" style="width:16px;height:16px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Export to Excel</button>
                            </div>
                            </div>
                            <div id="pendingRequestsGrid" class="ag-theme-alpine" style="height:600px;width:100%;"></div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- EDIT ACCOUNT SECTION -->
            <div id="editRequestView" class="dashboard-section" style="display:none;width:100%;">
                <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:12px;margin-bottom:1.25rem;">
                        <h2 style="font-size:1rem;font-weight:800;color:var(--t1);margin:0!important;display:flex;align-items:center;gap:.5rem;">
                            <i data-lucide="edit-3" style="width:22px;height:22px;"></i> Edit Account Details
                        </h2>
                        <div style="display:flex;gap:10px;flex-wrap:wrap;">
                            <button type="button" class="export-btn" id="editViewHistoryBtn" onclick="smShowItemHistory()" style="display:none;">
                                <i data-lucide="history" style="width:16px;height:16px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>View History
                            </button>
                            <button type="button" class="reset-btn" onclick="backFromEditRequest()">
                                <i data-lucide="arrow-left" style="width:16px;height:16px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Back
                            </button>
                        </div>
                    </div>
                    <div class="table-section">
                        <div id="editCurrentInfo"></div>

                        <div class="edit-section-block" style="margin-top: 1.75rem;">
                        <h3 class="table-title"><i data-lucide="edit" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Update Account Assignment</h3>

                        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                            <div class="filter-group">
                                <label class="filter-label">Account Manager *</label>
                                <select class="filter-select" id="editAM"></select>
                            </div>
                            <div class="filter-group">
                                <label class="filter-label">Account Director *</label>
                                <select class="filter-select" id="editAD"></select>
                            </div>
                        </div>

                        <div style="display: flex; gap: 16px; margin-top: 32px;">
                            <button type="button" class="export-btn" onclick="reviewEditRequest()" style="flex: 1; font-size: 14px; padding: 14px; white-space: nowrap;"><i data-lucide="eye" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Review Changes</button>
                            <button type="button" class="reset-btn" onclick="backFromEditRequest()" style="font-size: 14px; padding: 14px 24px; white-space: nowrap;"><i data-lucide="x" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Cancel</button>
                        </div>
                        </div>

                        <div class="table-section" id="editReviewSection" style="display: none; margin-top: 24px;">
                            <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="check-circle" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Review Changes</h3>
                            <div id="editReviewContent"></div>
                            <div style="display: flex; gap: 16px; margin-top: 24px;">
                                <button type="button" class="export-btn" onclick="submitEditRequest()" style="flex: 1; font-size: 14px; padding: 14px; white-space: nowrap;"><i data-lucide="check" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Submit Changes</button>
                                <button type="button" class="reset-btn" onclick="document.getElementById('editReviewSection').style.display='none'" style="font-size: 14px; padding: 14px 24px; white-space: nowrap;"><i data-lucide="pencil" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Edit Again</button>
                            </div>
                            <div id="editSubmitMessage" style="margin-top: 16px; text-align: center; font-weight: 600;"></div>
                        </div>
                    </div>
            </div>
            <!-- REVIEW REQUEST VIEW (ADMIN) -->
            <div class="dashboard" style="display:none;" id="reviewRequestView">
                <div class="header">
                    <div class="header-left">
                        <h1><i data-lucide="eye" style="width: 28px; height: 28px; display: inline-block; vertical-align: middle; margin-right: 10px;"></i>Review Account Request</h1>
                        <p>Approve or reject account onboarding request</p>
                    </div>
                    <div class="header-actions">
                        <button type="button" class="theme-btn" onclick="backToRequestsList()" style="background: rgba(239, 68, 68, 0.15); color: #ef4444; padding: 10px 20px; border-radius: 12px; border: none; cursor: pointer; font-weight: 600;">
                            <i data-lucide="arrow-left" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Back
                        </button>
                    </div>
                </div>

                <div style="max-width: 1200px; margin: 0 auto;">
                    <div class="table-section">
                        <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="clipboard-list" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Request Details</h3>
                        <div id="reviewRequestDetails"></div>

                        <h3 class="table-title" style="margin: 32px 0 24px;"><i data-lucide="settings" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Approval Settings</h3>

                        <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 20px;">
                            <div class="filter-group">
                                <label class="filter-label">Request Status *</label>
                                <select class="filter-select" id="adminReqStatus" onchange="toggleApprovalFields()">
                                    <option value="">Select Status</option>
                                    <option value="OnBoarded">Approve & Onboard</option>
                                    <option value="Rejected">Reject</option>
                                </select>
                            </div>
                        </div>

                        <!-- APPROVE FIELDS -->
                        <div id="adminApproveFields" style="display:none; margin-top: 20px;">
                            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 20px;">
                                <div class="filter-group">
                                    <label class="filter-label">Team *</label>
                                    <select class="filter-select" id="adminReqTeam" onchange="adminTeamChanged()">
                                        <option value="">Select Team</option>
                                        <option value="DSM">DSM</option>
                                        <option value="TSM_ME">TSM_ME</option>
                                        <option value="TSM_SE">TSM_SE</option>
                                        <option value="PSD">PSD</option>
                                    </select>
                                </div>
                                <div class="filter-group">
                                    <label class="filter-label">Line Manager *</label>
                                    <select class="filter-select" id="adminReqLM" onchange="adminLMChanged()" disabled>
                                        <option value="">Select Line Manager</option>
                                    </select>
                                </div>
                                <div class="filter-group">
                                    <label class="filter-label">Service Manager *</label>
                                    <select class="filter-select" id="adminReqSM" disabled>
                                        <option value="">Select Service Manager</option>
                                    </select>
                                </div>
                            </div>
                        </div>
<div class="filter-group">
                    <label class="filter-label">Introduction Done *</label>
                    <select class="filter-select" id="adminIntroductionDone">
                        <option value="">Select</option>
                        <option value="Yes">Yes</option>
                        <option value="No">No</option>
                    </select>
                </div>
                        <!-- REJECT FIELDS -->
                        <div id="adminRejectFields" style="display:none; margin-top: 20px;">
                            <div class="filter-group">
                                <label class="filter-label">Rejection Reason *</label>
                                <textarea class="filter-select" id="adminRejectReason" rows="4" placeholder="Enter reason for rejection..." style="resize:vertical; cursor:text;"></textarea>
                            </div>
                        </div>
                        <div style="display: flex; gap: 16px; margin-top: 32px;">
                            <button type="button" class="export-btn" onclick="verifyAndSubmitApproval()" style="flex: 1;"><i data-lucide="check" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Review & Submit</button>
                            <button type="button" class="reset-btn" onclick="backToRequestsList()"><i data-lucide="x" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Cancel</button>
                        </div>

                        <div class="table-section" id="adminReviewSection" style="display: none; margin-top: 24px;">
                            <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="check-circle" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Confirm Decision</h3>
                            <div id="adminReviewContent"></div>
                            <div style="display: flex; gap: 16px; margin-top: 24px;">
                                <button type="button" class="export-btn" onclick="submitApproval()" style="flex: 1;"><i data-lucide="check" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Confirm & Submit</button>
                                <button type="button" class="reset-btn" onclick="document.getElementById('adminReviewSection').style.display='none'"><i data-lucide="pencil" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Edit</button>
                            </div>
                            <div id="adminSubmitMessage" style="margin-top: 16px; text-align: center; font-weight: 600;"></div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- TRANSFER REQUESTS SECTION: content injected by transfer.js (#transfer-requests, #sdReviewTransferView). -->

        </div><!-- /content -->
    </div><!-- /main-container -->
    </div><!-- /portal-shell dashboardContent -->
    <!-- ===== PORTAL INTELLIGENCE VIEW ===== -->
    <div class="dashboard" style="display:none;position:relative;z-index:10;" id="portalAnalyticsView">
        <div class="header">
            <div class="header-left">
                <h1><i data-lucide="radar" style="width:28px;height:28px;display:inline-block;vertical-align:middle;margin-right:10px;"></i>Portal Intelligence</h1>
                <p>Usage tracking · Utilization analytics · Session insights</p>
            </div>
            <div class="header-actions">
                <select id="analyticsPeriodFilter" class="filter-select" onchange="analyticsChangePeriod()" style="width:auto;">
                    <option value="7">Last 7 Days</option>
                    <option value="30" selected>Last 30 Days</option>
                    <option value="60">Last 60 Days</option>
                    <option value="90">Last 90 Days</option>
                </select>
                <button type="button" class="export-btn" style="background:linear-gradient(135deg,#27AE60,#229954);" onclick="analyticsExport()">
                    <i data-lucide="download" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Export CSV
                </button>
                <button type="button" class="export-btn" onclick="analyticsLoadDashboard()">
                    <i data-lucide="refresh-cw" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Refresh
                </button>
                <button type="button" onclick="showPortalAnalyticsBack()" style="background:rgba(239,68,68,0.15);color:#ef4444;padding:10px 20px;border-radius:12px;border:none;cursor:pointer;font-weight:600;display:flex;align-items:center;gap:6px;">
                    <i data-lucide="arrow-left" style="width:16px;height:16px;"></i>Back
                </button>
                <select class="color-scheme-selector" onchange="changeTheme(event)">
                    <option value="magenta">&#9670; Magenta</option>
                    <option value="duralux">&#9670; Duralux</option>
                </select>
                <div class="theme-toggle" onclick="toggleTheme()" title="Light/Dark">
                    <i data-lucide="moon"></i>
                </div>
            </div>
        </div>

        <!-- Loading -->
        <div id="analyticsLoading" style="display:flex;align-items:center;justify-content:center;min-height:60vh;flex-direction:column;gap:16px;">
            <div style="width:48px;height:48px;border:3px solid var(--border);border-top-color:var(--acc);border-radius:50%;animation:spin 0.8s linear infinite;"></div>
            <div style="font-weight:600;color:var(--t2);">Loading Portal Intelligence...</div>
        </div>

        <!-- Content -->
        <div id="analyticsContent" style="display:none;padding:1.25rem;">

            <!-- Role Filter — custom checkbox dropdown -->
            <div style="display:flex;align-items:center;gap:12px;margin-bottom:1.5rem;flex-wrap:wrap;">
                <span style="font-size:12px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.05em;white-space:nowrap;">Filter by role:</span>
                <div style="position:relative;display:inline-block;" id="analyticsRoleDropdownWrap">
                    <!-- Trigger button -->
                    <button type="button" onclick="analyticsToggleRoleDropdown(event)" id="analyticsRoleBtn" style="display:flex;align-items:center;gap:8px;padding:8px 14px;background:var(--bg-card);border:1px solid var(--border);border-radius:10px;color:var(--t1);font-size:13px;font-weight:500;cursor:pointer;min-width:200px;justify-content:space-between;">
                        <span id="analyticsRoleBtnText">All Roles</span>
                        <i data-lucide="chevron-down" style="width:14px;height:14px;flex-shrink:0;"></i>
                    </button>
                    <!-- Dropdown panel -->
                    <div id="analyticsRolePanel" style="display:none;position:absolute;top:calc(100% + 4px);left:0;z-index:999;background:var(--bg-card);border:1px solid var(--border);border-radius:10px;box-shadow:0 8px 24px rgba(0,0,0,0.15);min-width:220px;overflow:hidden;">
                        <div style="padding:8px 0;">
                            <label style="display:flex;align-items:center;gap:10px;padding:8px 14px;cursor:pointer;font-size:13px;color:var(--t1);font-weight:500;" onmouseover="this.style.background='var(--bg-secondary)'" onmouseout="this.style.background=''">
                                <input type="checkbox" value="All" checked onchange="analyticsRoleCheckboxChange(this)" style="width:15px;height:15px;accent-color:var(--acc);cursor:pointer;"> All Roles
                            </label>
                            <div style="height:1px;background:var(--border);margin:2px 0;"></div>
                            <label style="display:flex;align-items:center;gap:10px;padding:8px 14px;cursor:pointer;font-size:13px;color:var(--t1);" onmouseover="this.style.background='var(--bg-secondary)'" onmouseout="this.style.background=''">
                                <input type="checkbox" value="Service Manager" onchange="analyticsRoleCheckboxChange(this)" style="width:15px;height:15px;accent-color:var(--acc);cursor:pointer;"> Service Manager
                            </label>
                            <label style="display:flex;align-items:center;gap:10px;padding:8px 14px;cursor:pointer;font-size:13px;color:var(--t1);" onmouseover="this.style.background='var(--bg-secondary)'" onmouseout="this.style.background=''">
                                <input type="checkbox" value="Line Manager" onchange="analyticsRoleCheckboxChange(this)" style="width:15px;height:15px;accent-color:var(--acc);cursor:pointer;"> Line Manager
                            </label>
                            <label style="display:flex;align-items:center;gap:10px;padding:8px 14px;cursor:pointer;font-size:13px;color:var(--t1);" onmouseover="this.style.background='var(--bg-secondary)'" onmouseout="this.style.background=''">
                                <input type="checkbox" value="Admin" onchange="analyticsRoleCheckboxChange(this)" style="width:15px;height:15px;accent-color:var(--acc);cursor:pointer;"> Admin
                            </label>
                            <label style="display:flex;align-items:center;gap:10px;padding:8px 14px;cursor:pointer;font-size:13px;color:var(--t1);" onmouseover="this.style.background='var(--bg-secondary)'" onmouseout="this.style.background=''">
                                <input type="checkbox" value="Account Manager" onchange="analyticsRoleCheckboxChange(this)" style="width:15px;height:15px;accent-color:var(--acc);cursor:pointer;"> Account Manager
                            </label>
                            <label style="display:flex;align-items:center;gap:10px;padding:8px 14px;cursor:pointer;font-size:13px;color:var(--t1);" onmouseover="this.style.background='var(--bg-secondary)'" onmouseout="this.style.background=''">
                                <input type="checkbox" value="Account Director" onchange="analyticsRoleCheckboxChange(this)" style="width:15px;height:15px;accent-color:var(--acc);cursor:pointer;"> Account Director
                            </label>
                        </div>
                    </div>
                </div>
                <button type="button" onclick="analyticsResetRoleFilter()" class="reset-btn" style="padding:7px 14px;font-size:12px;">
                    <i data-lucide="rotate-ccw" style="width:12px;height:12px;display:inline-block;vertical-align:middle;margin-right:4px;"></i>Reset
                </button>
            </div>

            <!-- KPIs -->
            <div id="analyticsKPIs" style="margin-bottom:1.5rem;"></div>

            <!-- Charts 2-col grid -->
            <div id="analyticsCharts" class="charts-section" style="grid-template-columns:repeat(2,1fr);margin-bottom:1.5rem;"></div>

            <!-- Utilization League Table -->
            <div class="table-section" style="margin-bottom:1.5rem;">
                <div class="table-header">
                    <h2 class="table-title"><i data-lucide="trophy" style="width:20px;height:20px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>User Utilization League Table</h2>
                    <div style="font-size:12px;color:var(--t3);">Ranked by total sessions — shows who's actively using the portal</div>
                </div>
                <div class="manager-list" id="analyticsLeagueTable"></div>
            </div>

            <!-- Inactive Users Alert -->
            <div class="table-section" style="margin-bottom:1.5rem;">
                <div class="table-header">
                    <h2 class="table-title" style="color:var(--danger);"><i data-lucide="alert-triangle" style="width:20px;height:20px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>Inactive Users — Last 14 Days</h2>
                    <div style="font-size:12px;color:var(--t3);">Users who have previously logged in but not visited in 14+ days</div>
                </div>
                <div id="analyticsInactive"></div>
            </div>

            <!-- Recent Sessions -->
            <div class="table-section">
                <div class="table-header">
                    <h2 class="table-title"><i data-lucide="list" style="width:20px;height:20px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>Recent Sessions</h2>
                    <div style="font-size:12px;color:var(--t3);">Last 50 sessions across all users</div>
                </div>
                <div style="overflow-x:auto;">
                    <table style="width:100%;border-collapse:collapse;font-size:13px;">
                        <thead>
                            <tr>
                                <th style="background:var(--bg-secondary);padding:.6rem .9rem;text-align:left;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.04em;color:var(--t1);border-bottom:2px solid var(--border-s);">User</th>
                                <th style="background:var(--bg-secondary);padding:.6rem .9rem;text-align:left;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.04em;color:var(--t1);border-bottom:2px solid var(--border-s);">Role</th>
                                <th style="background:var(--bg-secondary);padding:.6rem .9rem;text-align:left;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.04em;color:var(--t1);border-bottom:2px solid var(--border-s);">Session Start</th>
                                <th style="background:var(--bg-secondary);padding:.6rem .9rem;text-align:left;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.04em;color:var(--t1);border-bottom:2px solid var(--border-s);">Duration</th>
                                <th style="background:var(--bg-secondary);padding:.6rem .9rem;text-align:left;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.04em;color:var(--t1);border-bottom:2px solid var(--border-s);">Pages</th>
                                <th style="background:var(--bg-secondary);padding:.6rem .9rem;text-align:left;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.04em;color:var(--t1);border-bottom:2px solid var(--border-s);">Last Page</th>
                                <th style="background:var(--bg-secondary);padding:.6rem .9rem;text-align:left;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.04em;color:var(--t1);border-bottom:2px solid var(--border-s);">Device</th>
                            </tr>
                        </thead>
                        <tbody id="analyticsRecentBody"></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <!--
  ============================================================
  ETA VIEW — Events / TT / Activity
  Drop this block INSIDE the portal-shell dashboardContent,
  just before the closing </div> of the main dashboard.
  Also add the <script> tag pointing to events-tt-activity.js
  at the bottom alongside the other module scripts.
  ============================================================
-->



    <style>
        /* ── ETA-specific styles (extend existing tokens — no overrides) ── */

        .eta-tab {
            transition: all .18s ease;
            white-space: nowrap;
        }

        .eta-tab-active {
            background: var(--grad) !important;
            color: #fff !important;
            box-shadow: 0 2px 10px var(--glow);
        }

        .eta-tab:not(.eta-tab-active):hover {
            background: var(--bg-hover) !important;
            color: var(--t1) !important;
        }

        .eta-kpi-card {
            min-width: 150px;
        }

        /* custom checkbox multiselect (dashboard filters) */
        .custom-multiselect { position: relative; z-index: 101; width: 100%; }
        .multiselect-selected {
            padding: .48rem .7rem;
            background: var(--bg-input);
            border: 1px solid var(--border-s);
            border-radius: 8px;
            color: var(--t1);
            font-size: .83rem;
            font-weight: 500;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 8px;
            transition: border-color .18s, box-shadow .18s;
            box-shadow: var(--cs);
        }
        .multiselect-selected:hover,
        .multiselect-selected:focus {
            border-color: var(--border-s);
            box-shadow: 0 0 0 3px var(--glow);
            outline: none;
        }
        .multiselect-dropdown {
            position: absolute;
            top: calc(100% + 4px);
            left: 0;
            right: 0;
            background: var(--bg-card);
            border: 1px solid var(--border-s);
            border-radius: 10px;
            box-shadow: var(--ch);
            max-height: 260px;
            overflow-y: auto;
            z-index: 9999;
        }
        .multiselect-option {
            padding: 8px 12px;
            display: flex;
            align-items: center;
            cursor: pointer;
            border-bottom: 1px solid var(--border);
        }
        .multiselect-option:last-child { border-bottom: none; }
        .multiselect-option:hover { background: var(--bg-hover); }
        .multiselect-option input[type="checkbox"] {
            margin-right: 10px;
            width: 15px;
            height: 15px;
            cursor: pointer;
            accent-color: var(--acc);
        }
        .multiselect-option label { cursor: pointer; flex: 1; font-size: .83rem; color: var(--t1); margin: 0; }

        /* searchable person select (Secondary SM, etc.) */
        .sm-person-wrap { position: relative; }
        .sm-person-dd {
            position: absolute;
            top: calc(100% + 4px);
            left: 0;
            right: 0;
            z-index: 800;
            background: var(--bg-card);
            border: 1px solid var(--border-s);
            border-radius: 10px;
            box-shadow: var(--ch);
            max-height: 240px;
            overflow-y: auto;
            display: none;
        }
        .sm-person-item {
            padding: .42rem .72rem;
            cursor: pointer;
            border-bottom: 1px solid var(--border);
        }
        .sm-person-item:last-child { border-bottom: none; }
        .sm-person-item:hover { background: var(--bg-hover); }
        .tab-refresh-btn.refreshing i { animation: spin 1s linear infinite; }

        /* version history modal */
        .sm-history-timeline { display: flex; flex-direction: column; gap: 14px; }
        .sm-history-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: var(--cs);
        }
        .sm-history-card-head {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
            flex-wrap: wrap;
            padding: 12px 16px;
            background: var(--bg-secondary);
            border-bottom: 1px solid var(--border);
        }
        .sm-history-version-badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-width: 44px;
            padding: 4px 10px;
            border-radius: 8px;
            background: var(--grad);
            color: #fff;
            font-size: .78rem;
            font-weight: 800;
        }
        .sm-history-changes-table {
            width: 100%;
            border-collapse: collapse;
            font-size: .82rem;
        }
        .sm-history-changes-table th {
            text-align: left;
            padding: 8px 14px;
            font-size: .68rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .04em;
            color: var(--t3);
            background: var(--bg-secondary);
            border-bottom: 1px solid var(--border);
        }
        .sm-history-changes-table td {
            padding: 10px 14px;
            color: var(--t2);
            border-bottom: 1px solid var(--border);
            vertical-align: top;
        }
        .sm-history-changes-table tr:last-child td { border-bottom: none; }
        .sm-history-old { color: var(--t3); text-decoration: line-through; }
        .sm-history-new { color: var(--t1); font-weight: 700; }

        #pending-requests {
            width: 100%;
            box-sizing: border-box;
        }

        #requestsContent {
            width: 100%;
        }

        #reviewRequestView {
            margin-left: 0 !important;
            width: 100% !important;
            position: relative !important;
            z-index: 1 !important;
        }
    </style>

    <!-- ===== COMING SOON VIEW ===== -->
    <div class="dashboard" style="display:none;" id="comingSoonView">
        <div class="header">
            <div class="header-left">
                <h1><i id="comingSoonIcon" data-lucide="zap" style="width:28px;height:28px;display:inline-block;vertical-align:middle;margin-right:10px;"></i><span id="comingSoonTitle">Activities</span></h1>
                <p>Service Management Platform</p>
            </div>
            <div class="header-actions">
                <button type="button" class="theme-btn" onclick="showComingSoonBack()" style="background:rgba(239,68,68,0.15);color:#ef4444;padding:10px 20px;border-radius:12px;border:none;cursor:pointer;font-weight:600;">
                    <i data-lucide="arrow-left" style="width:16px;height:16px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Back to Dashboard
                </button>
                <select class="color-scheme-selector" onchange="changeTheme(event)">
                    <option value="magenta">&#9670; Magenta</option>
                    <option value="duralux">&#9670; Duralux</option>
                </select>
                <div class="theme-toggle" onclick="toggleTheme()" title="Light/Dark">
                    <i data-lucide="moon"></i>
                </div>
            </div>
        </div>
        <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:60vh;text-align:center;padding:3rem;">
            <div style="background:var(--bg-card);border:1px solid var(--border);border-radius:20px;padding:3rem 4rem;box-shadow:var(--cs);max-width:500px;width:100%;position:relative;overflow:hidden;">
                <div style="position:absolute;top:0;left:0;right:0;height:4px;background:var(--grad);"></div>
                <div style="width:80px;height:80px;border-radius:50%;background:var(--grad);display:flex;align-items:center;justify-content:center;margin:0 auto 1.5rem;">
                    <i id="comingSoonBigIcon" data-lucide="zap" style="width:36px;height:36px;color:#fff;"></i>
                </div>
                <h2 id="comingSoonHeading" style="font-size:1.6rem;font-weight:900;color:var(--t1);margin-bottom:0.75rem;">Activities</h2>
                <div style="display:inline-block;background:var(--chip);color:var(--acc);border:1px solid var(--nab2);border-radius:20px;padding:4px 16px;font-size:12px;font-weight:700;margin-bottom:1.5rem;letter-spacing:0.05em;">COMING SOON</div>
                <p style="color:var(--t2);font-size:1rem;line-height:1.6;margin-bottom:0;">This feature is currently under development and will be available soon. Stay tuned for updates.</p>
            </div>
        </div>
    </div>

    <!-- ===== CUSTOMER SERVICE VIEW ===== -->
    <!-- /csMainContent -->
    <!-- ===== AM/AD VIEW ===== -->
    <!-- ===== AM/AD VIEW ===== -->
    <div class="dashboard" style="display:none;" id="amAdDashboard">
        <!-- Header same as admin but with Create Request button -->
        <div class="header">
            <div class="header-left">
                <h1><img src="http://sharedspaces:8086/sites/SM/Shared%20Documents/DUlogo%202026.png" alt="du Logo" style="width: 28px; height: 28px; object-fit: contain; vertical-align: middle; margin-right: 10px;">
                    My Accounts</h1>
                <p>Personal Revenue Analytics & Account Management</p>
            </div>
            <div class="header-actions">
                <button type="button" class="export-btn" onclick="showCreateRequestForm()">
                    <i data-lucide="plus" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Create New Request
                </button>
                <button type="button" class="export-btn" onclick="showAMTransferRequests()" style="background: linear-gradient(135deg, #f97316, #ea580c);">
                    <i data-lucide="repeat" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Pending Transfer Requests
                </button>

                <div class="date-badge">
                    <i data-lucide="calendar" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i><span id="amCurrentDate">Loading...</span>
                </div>
                <select class="color-scheme-selector" id="amColorSchemeSelector" onchange="changeTheme(event)">
                    <option value="magenta">Magenta Theme</option>
                    <option value="duralux">Duralux Theme</option>
                </select>
                <div class="theme-toggle" onclick="toggleTheme()">
                    <i data-lucide="moon" id="amThemeIcon"></i>
                </div>

            </div>
        </div>
        <div id="amLoading" style="text-align:center; padding:60px; font-size:18px;">
            <i data-lucide="loader" style="width: 24px; height: 24px; display: inline-block; vertical-align: middle; margin-right: 8px; animation: spin 1s linear infinite;"></i> Loading your accounts...
        </div>

        <div id="amContent" style="display:none;">
            <!-- 4 Stats Tiles -->
            <div class="top-stats" style="grid-template-columns: repeat(4, 1fr);">
                <div class="stat-card">
                    <div class="stat-label">Last Month Revenue</div>
                    <div class="stat-value" id="amLastMonthRev">0</div>
                    <div class="stat-subtitle">December 2025</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">Average Revenue</div>
                    <div class="stat-value" id="amAvgRev">0</div>
                    <div class="stat-subtitle">3-Month Average</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">Group Accounts</div>
                    <div class="stat-value" id="amGroupCount">0</div>
                    <div class="stat-subtitle">Parent Accounts</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">Child Accounts</div>
                    <div class="stat-value" id="amChildCount">0</div>
                    <div class="stat-subtitle">Sub Accounts</div>
                </div>
            </div>

            <!-- Performance Chart -->
            <div class="charts-section" style="grid-template-columns: 1fr 1fr;">
                <div class="chart-card">
                    <h3 class="chart-title"><i data-lucide="trending-up" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>3-Month Revenue Trend</h3>
                    <div class="chart-container">
                        <canvas id="amRevenueChart"></canvas>
                    </div>
                </div>
                <div class="chart-card">
                    <h3 class="chart-title"><i data-lucide="pie-chart" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Account Type Distribution</h3>
                    <div class="chart-container">
                        <canvas id="amTypeChart"></canvas>
                    </div>
                </div>
            </div>

            <!-- Accounts Table -->
            <div class="table-section">
                <div class="table-header">
                    <h3 class="table-title"><i data-lucide="clipboard-list" style="width: 20px; height: 20px; display: inline-block; vertical-align: middle; margin-right: 8px;"></i>My Accounts</h3>
                    <div class="table-actions">
                        <button type="button" class="export-btn" onclick="exportAMData()"><i data-lucide="file-spreadsheet" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Extract to Excel</button>
                        <input type="text" class="search-box" id="amSearchBox" placeholder="Search all columns..." oninput="searchAMGrid(this.value)">
                    </div>
                </div>
                <div id="amGrid" class="ag-theme-alpine" style="height: 600px; width: 100%;"></div>
            </div>
        </div>
    </div>
    <!-- ===== AM TRANSFER REQUESTS VIEW ===== -->
    <div class="dashboard" style="display:none;" id="amTransferRequestsView">
        <div class="header">
            <div class="header-left">
                <h1><i data-lucide="repeat" style="width: 28px; height: 28px; display: inline-block; vertical-align: middle; margin-right: 10px;"></i>Account Transfer Requests</h1>
                <p>Review and approve transfer requests</p>
            </div>
            <div class="header-actions">
                <button type="button" class="theme-btn" onclick="backToAMDashboard2()" style="background: rgba(239, 68, 68, 0.15); color: #ef4444; padding: 10px 20px; border-radius: 12px; border: none; cursor: pointer; font-weight: 600;">
                    <i data-lucide="arrow-left" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Back to Dashboard
                </button>
            </div>
        </div>

        <div id="amTransferLoading" style="text-align:center; padding:60px; font-size:18px;">
            <i data-lucide="loader" style="width: 24px; height: 24px; display: inline-block; vertical-align: middle; margin-right: 8px; animation: spin 1s linear infinite;"></i> Loading transfer requests...
        </div>

        <div id="amTransferContent" style="display:none;">
            <div class="table-section">
                <div class="table-header">
                    <h3 class="table-title">Pending Transfer Requests</h3>
                </div>
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th>Account Code</th>
                                <th>Customer</th>
                                <th>Current Team</th>
                                <th>Proposed Team</th>
                                <th>Dec Revenue</th>
                                <th>Requested By</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody id="amTransferTbody"></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <!-- ===== CREATE REQUEST FORM ===== -->
    <!-- ===== CREATE REQUEST FORM ===== -->
    <div class="dashboard" style="display:none;" id="createRequestView">
        <div class="header">
            <div class="header-left">
                <h1><i data-lucide="plus-circle" style="width: 28px; height: 28px; display: inline-block; vertical-align: middle; margin-right: 10px;"></i>Create New Account Request</h1>
                <p>Submit new account information to SharePoint</p>
            </div>
            <div class="header-actions">
                <button type="button" class="theme-btn" onclick="backToAMDashboard()" style="background: rgba(239, 68, 68, 0.15); color: #ef4444; padding: 10px 20px; border-radius: 12px; border: none; cursor: pointer; font-weight: 600;">
                    <i data-lucide="arrow-left" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Back to Dashboard
                </button>
            </div>
        </div>

        <div style="max-width: 900px; margin: 0 auto;">
            <!-- Form Section -->
            <div class="table-section" id="requestFormSection">
                <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="file-text" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Account Information</h3>

                <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px;">
                    <div class="filter-group">
                        <label class="filter-label">Account Code *</label>
                        <input type="text" class="filter-select" id="reqAccountCode" placeholder="e.g., 1.0129012" style="cursor: text; font-size: 14px; padding: 12px 16px;" oninput="updateL10OnAccountCodeChange()">
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">Group Account *</label>
                        <select class="filter-select" id="reqGroupAccount" onchange="toggleL10Field()" style="font-size: 14px; padding: 12px 16px;">
                            <option value="No" selected>No</option>
                            <option value="Yes">Yes</option>
                        </select>
                    </div>

                    <div class="filter-group" id="l10Field" style="display: block; grid-column: 1 / -1;">
                        <label class="filter-label">L-10 (Payment Responsible Account) *</label>
                        <input type="text" class="filter-select" id="reqParentCode" placeholder="e.g., 1.0129012" style="cursor: text; font-size: 14px; padding: 12px 16px;">
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">Customer Name *</label>
                        <input type="text" class="filter-select" id="reqCustomerName" placeholder="Enter customer name" style="cursor: text; font-size: 14px; padding: 12px 16px;">
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">POC Name *</label>
                        <input type="text" class="filter-select" id="reqPOCName" placeholder="Enter POC name" style="cursor: text; font-size: 14px; padding: 12px 16px;">
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">POC Email *</label>
                        <input type="email" class="filter-select" id="reqPOCEmail" placeholder="Enter POC email" style="cursor: text; font-size: 14px; padding: 12px 16px;">
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">POC Contact *</label>
                        <input type="text" class="filter-select" id="reqPOCContact" placeholder="+971501234567" value="+971" maxlength="13" oninput="formatPhoneNumber(this)" style="cursor: text; font-size: 14px; padding: 12px 16px;">
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">Account Director *</label>
                        <div id="reqADWrap">
                            <input type="text" class="filter-select" id="reqAD" readonly style="background: rgba(168, 85, 247, 0.1); cursor: not-allowed; font-size: 14px; padding: 12px 16px;">
                        </div>
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">Account Manager *</label>
                        <select class="filter-select" id="reqAM" style="font-size: 14px; padding: 12px 16px;"></select>
                    </div>
                </div>

                <h3 class="table-title" style="margin: 32px 0 24px;"><i data-lucide="dollar-sign" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Revenue Last 3 Months</h3>

                <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px;" id="revenueMonthsContainer">
                    <!-- Auto-generated month fields -->
                </div>

                <div style="display: flex; gap: 16px; margin-top: 32px;">
                    <button type="button" class="export-btn" onclick="reviewCreateRequest()" style="flex: 1; font-size: 14px; padding: 12px;"><i data-lucide="eye" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Review & Submit</button>
                    <button type="button" class="reset-btn" onclick="clearRequestForm()" style="font-size: 14px; padding: 12px;"><i data-lucide="rotate-ccw" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Clear Form</button>
                </div>
            </div>

            <!-- Review Section (Hidden Initially) -->
            <div class="table-section" id="reviewSection" style="display: none; margin-top: 24px;">
                <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="check-circle" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Review Your Request</h3>
                <div id="reviewContent"></div>
                <div style="display: flex; gap: 16px; margin-top: 24px;">
                    <button type="button" class="export-btn" onclick="submitRequest()" style="flex: 1; font-size: 14px; padding: 12px;"><i data-lucide="check" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Confirm & Submit</button>
                    <button type="button" class="reset-btn" onclick="editRequestForm()" style="font-size: 14px; padding: 12px;"><i data-lucide="pencil" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Edit</button>
                </div>
                <div id="submitMessage" style="margin-top: 16px; text-align: center; font-weight: 600;"></div>
            </div>
        </div>
    </div>

    <!-- Admin transfer list: #transfer-requests (ag-Grid) is injected by transfer.js — legacy full-page adminTransferView removed to avoid duplicate element IDs. -->

    <!-- ===== TRANSFER REQUEST FORM ===== -->
    <div class="dashboard" style="display:none;" id="transferRequestView">
        <div class="header">
            <div class="header-left">
                <h1><i data-lucide="repeat" style="width: 28px; height: 28px; display: inline-block; vertical-align: middle; margin-right: 10px;"></i>Account Transfer Request</h1>
                <p>Request account transfer due to revenue drop</p>
            </div>
            <div class="header-actions">
                <button type="button" class="theme-btn" onclick="backToDashboard()" style="background: rgba(239, 68, 68, 0.15); color: #ef4444;">
                    <i data-lucide="arrow-left" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Back to Dashboard
                </button>
            </div>
        </div>

        <div style="max-width: 1200px; margin: 0 auto;">
            <div class="table-section">
                <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="clipboard-list" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Account Information</h3>

                <div id="transferAccountInfo"></div>

                <h3 class="table-title" style="margin: 32px 0 24px;"><i data-lucide="repeat" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Transfer Details</h3>

                <div style="display: grid; grid-template-columns: 1fr; gap: 20px;">
                    <div class="filter-group">
                        <label class="filter-label">Proposed New Team *</label>
                        <select class="filter-select" id="transferNewTeam">
                            <option value="">Select New Team</option>
                            <option value="DSM">DSM</option>
                            <option value="TSM_ME">TSM_ME</option>
                            <option value="TSM_SE">TSM_SE</option>
                            <option value="PSD">PSD</option>
                            <option value="Call Centre">Call Centre</option>
                        </select>
                    </div>
                    <div class="filter-group">
                        <label class="filter-label">Transfer Reason</label>
                        <textarea class="filter-select" id="transferReason" rows="4" placeholder="Explain reason for transfer (optional)" style="cursor: text; resize: vertical;"></textarea>
                    </div>
                </div>

                <div style="display: flex; gap: 16px; margin-top: 32px;">
                    <button type="button" class="export-btn" onclick="submitTransferRequest()" style="flex: 1;"><i data-lucide="send" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Submit Transfer Request</button>
                    <button type="button" class="reset-btn" onclick="backToDashboard()"><i data-lucide="x" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Cancel</button>
                </div>

                <div id="transferSubmitMessage" style="margin-top: 16px; text-align: center; font-weight: 600;"></div>
            </div>
        </div>
    </div>
    <!-- ===== ACCOUNT SEARCH VIEW ===== -->
    <div class="dashboard" style="display:none;position:relative;z-index:10;" id="accountSearchView">
        <div class="header">
            <div class="header-left">
                <h1><i data-lucide="search" style="width: 28px; height: 28px; display: inline-block; vertical-align: middle; margin-right: 10px;"></i>Account Search</h1>
                <p>Search for any account and view its details</p>
            </div>
            <div class="header-actions">
                <select class="color-scheme-selector" id="searchColorSchemeSelector" onchange="changeTheme(event)">
                    <option value="magenta">Magenta Theme</option>
                    <option value="duralux">Duralux Theme</option>
                </select>
                <div class="theme-toggle" onclick="toggleTheme()">
                    <i data-lucide="moon" id="searchThemeIcon"></i>
                </div>
                <button type="button" class="theme-btn" onclick="backToLanding()" style="background: rgba(239, 68, 68, 0.15); color: #ef4444; padding: 10px 20px; border-radius: 12px; border: none; cursor: pointer; font-weight: 600;">
                    <i data-lucide="arrow-left" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Back to Landing
                </button>
            </div>
        </div>

        <div style="max-width: 1400px; margin: 0 auto;">
            <!-- Search Form -->
            <div class="table-section" style="margin-bottom: 24px;">
                <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="file-search" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Search Account</h3>

                <div style="display: flex; gap: 16px; align-items: flex-end; max-width: 700px;">
                    <div class="filter-group" style="flex: 1;">
                        <label class="filter-label">Account Code *</label>
                        <input type="text" class="filter-select" id="searchAccountCode" placeholder="e.g., 121.121.121.123" style="cursor: text; font-size: 14px; padding: 12px 16px;">
                    </div>
                    <button type="button" class="export-btn" onclick="searchAccount()" style="padding: 12px 24px; font-size: 14px;">
                        <i data-lucide="search" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Search
                    </button>
                    <button type="button" class="reset-btn" onclick="clearAccountSearch()" style="padding: 12px 24px; font-size: 14px;">
                        <i data-lucide="x" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Clear
                    </button>
                </div>
                <div id="searchErrorMessage" style="display: none; margin-top: 16px; padding: 16px; background: rgba(239, 68, 68, 0.1); border-left: 4px solid #ef4444; border-radius: 8px;">
                    <div style="display: flex; align-items: center; gap: 12px;">
                        <i data-lucide="alert-circle" style="width: 20px; height: 20px; color: #ef4444;"></i>
                        <div>
                            <div style="font-weight: 700; color: #ef4444; margin-bottom: 4px;">Account Not Found</div>
                            <div style="font-size: 13px; color: var(--text-secondary);">Please verify the account code and try again. Make sure the format is correct (e.g., 121.121.121.123)</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Account Details (Hidden Initially) -->
            <div class="table-section" id="accountDetailsSection" style="display: none; margin-bottom: 24px;">
                <h3 class="table-title" style="margin-bottom: 24px;"><i data-lucide="info" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Account Details</h3>
                <div id="accountDetailsContent" style="max-width: 1200px;"></div>
            </div>

            <!-- Related Accounts Grid -->
            <!-- Related Accounts Grid -->
            <div class="table-section" id="relatedAccountsSection" style="display: none;">
                <div class="table-header">
                    <h3 class="table-title"><i data-lucide="network" style="width: 20px; height: 20px; display: inline-block; vertical-align: middle; margin-right: 8px;"></i><span id="relatedAccountsTitle">Related Accounts</span></h3>
                    <div class="table-actions">
                        <input type="text" class="search-box" id="relatedAccountsSearch" placeholder="Search all columns..." oninput="searchRelatedAccounts(this.value)">
                    </div>
                </div>
                <div id="relatedAccountsGrid" class="ag-theme-alpine ag-theme-custom" style="height: 500px; width: 100%;"></div>
            </div>
        </div>
    </div>
    <!-- Edit-mode SharePoint item version history (Service Manager Request list) -->
    <div id="editHistoryModal" style="display:none;position:fixed;inset:0;z-index:10050;background:rgba(0,0,0,.55);backdrop-filter:blur(4px);align-items:center;justify-content:center;padding:24px;">
        <div style="background:var(--bg-card);border:1px solid var(--border);border-radius:18px;max-width:1100px;width:100%;max-height:90vh;display:flex;flex-direction:column;box-shadow:0 24px 64px rgba(0,0,0,.35);overflow:hidden;">
            <div style="display:flex;align-items:center;justify-content:space-between;padding:18px 22px;border-bottom:1px solid var(--border);background:var(--bg-secondary);">
                <div>
                    <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Item Version History</div>
                    <div id="editHistoryModalTitle" style="font-size:17px;font-weight:800;color:var(--t1);margin-top:4px;"></div>
                    <div style="font-size:12px;color:var(--t3);margin-top:4px;">Track who changed what and when — same data as SharePoint version history</div>
                </div>
                <button type="button" onclick="smCloseItemHistory()" style="background:rgba(239,68,68,.12);border:none;border-radius:10px;width:36px;height:36px;cursor:pointer;color:#ef4444;font-size:20px;line-height:1;">×</button>
            </div>
            <div id="editHistoryModalBody" style="padding:20px 22px;overflow-y:auto;flex:1;"></div>
        </div>
    </div>

    <div id="smExportModal" style="display:none;position:fixed;inset:0;z-index:10050;background:rgba(0,0,0,.55);backdrop-filter:blur(4px);align-items:center;justify-content:center;padding:24px;">
        <div style="background:var(--bg-card);border:1px solid var(--border);border-radius:18px;max-width:480px;width:100%;box-shadow:0 24px 64px rgba(0,0,0,.35);overflow:hidden;">
            <div style="padding:18px 22px;border-bottom:1px solid var(--border);background:var(--bg-secondary);">
                <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Export Options</div>
                <div style="font-size:17px;font-weight:800;color:var(--t1);margin-top:4px;">Choose export type</div>
            </div>
            <div style="padding:22px;display:flex;flex-direction:column;gap:12px;">
                <button type="button" class="export-btn" onclick="smCloseExportModal();exportToExcel(false);" style="width:100%;justify-content:center;padding:14px;">
                    <i data-lucide="file-spreadsheet" style="width:16px;height:16px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>Export to Excel
                </button>
                <button type="button" class="export-btn" onclick="smCloseExportModal();exportToExcel(true);" style="width:100%;justify-content:center;padding:14px;background:linear-gradient(135deg,#10b981,#059669);">
                    <i data-lucide="file-spreadsheet" style="width:16px;height:16px;display:inline-block;vertical-align:middle;margin-right:8px;"></i>Export RNPS to Excel
                </button>
                <button type="button" class="reset-btn" onclick="smCloseExportModal()" style="width:100%;justify-content:center;padding:12px;">Cancel</button>
            </div>
        </div>
    </div>

    <script>
        // ========================================
        // LOCAL DEVELOPMENT MODE CONFIGURATION
        // ========================================
        // Set to true to bypass SharePoint and use fake data
        // Set to false for production SharePoint integration
        if (typeof Chart !== 'undefined') Chart.defaults.plugins.datalabels = { display: false };
const LOCAL_DEV_MODE = false;

        // Development user profile (only used when LOCAL_DEV_MODE = true)
        const DEV_USER_PROFILE = {
            // Change this to test different roles:
            // 'Admin', 'Account Manager', 'Account Director', 'Line Manager', 'Service Manager'
            role: 'Admin',

            // User details (customize as needed)
            email: 'dev.user@duservicemanagement.com',
            name: 'Development User',

            // Optional: For LM/SM roles, specify which manager name to filter by
            // For LM: specify the Line Manager name from fake data
            // For SM: specify the Service Manager name from fake data
            managerName: 'Sarah Johnson' // Used for LM/SM filtering
        };

        // ========================================
        // EXISTING CODE CONTINUES
        // ========================================
        const SP_URL = 'http://sharedspaces:8086/sites/SM';
        const SP_LIST = 'Service Manager Request';
        const SP_ACCESS_LIST = 'Access_Control';
        const SP_MAPPING_LIST = 'Account Mapping';

        let ALL_DATA = [];
        let filtered = [];
        let SM_MAPPING_DATA = [];
        let DOJ_DATA = {}; // keyed by SM name → {doj, tenure}
        let selectedLM = null;
        let selectedSM = null;
let charts = {};

        // ══ ANALYTICS LAZY-LOAD STATE ══════════════════════════════
        window.ANALYTICS_LOADED = false;
        window.RNPS_CACHE = {};    // '_grouped_LMName' → raw grouped; 'LMName' → computed quarters
        window.ETA_LOADED  = false;

        async function loadAnalytics() {
            const btn     = document.getElementById('loadAnalyticsBtn');
            const btnText = document.getElementById('loadAnalyticsBtnText');

            // If already loaded — just refresh renders
            if (window.ANALYTICS_LOADED) {
                await renderLineManagers();
                await renderServiceManagers();
                return;
            }

            // ── Show loading state ─────────────────────────────────
            if (btn) {
                btn.disabled = true;
                btn.style.opacity = '.75';
                btn.innerHTML = '<i data-lucide="loader" style="width:15px;height:15px;display:inline-block;vertical-align:middle;animation:spin 1s linear infinite;margin-right:6px;"></i><span>Loading RNPS &amp; ETA...</span>';
                if (typeof lucide !== 'undefined') lucide.createIcons();
            }

            try {
                // ── 1. Collect which LMs to fetch RNPS for ────────
                let lms = [...new Set(ALL_DATA.map(a => a.lm))].filter(Boolean);
                if (USER_CONTEXT.role === 'Line Manager') {
                    lms = lms.filter(l => l === USER_CONTEXT.userName);
                } else if (USER_CONTEXT.role === 'Service Manager') {
                    const myLM = ALL_DATA.find(a => a.sm === USER_CONTEXT.userName)?.lm;
                    lms = myLM ? [myLM] : [];
                } else if (USER_CONTEXT.isTSMManager) {
                    const tsmData = ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE');
                    lms = [...new Set(tsmData.map(a => a.lm))].filter(Boolean);
                }

                // ── 2. Fetch RNPS for all LMs in parallel ─────────
                await Promise.all(lms.map(async lm => {
                    try {
                        const grouped = await fetchRNPSForLM(lm);
                        window.RNPS_CACHE['_grouped_' + lm] = grouped;   // for SM drill-down
                        window.RNPS_CACHE[lm] = computeLMRNPS(grouped);  // for LM tile
                    } catch(e) { console.warn('[RNPS] Failed for', lm, e); }
                }));

                // ── 3. Load ETA data ───────────────────────────────
              if (typeof etaLoadSPData === 'function' &&
    (!window.ETA_STATE || !window.ETA_STATE.allData || !window.ETA_STATE.allData.tts)) {
    await new Promise(function(resolve) {
        var run = async function() { await etaLoadSPData(); resolve(); };
        if (typeof requestIdleCallback === 'function') {
            requestIdleCallback(function() { run(); }, { timeout: 5000 });
        } else {
            setTimeout(run, 0);
        }
    });
}
                window.ETA_LOADED = true;
                window.ANALYTICS_LOADED = true;

                // ── 4. Update button → loaded ──────────────────────
                if (btn) {
                    btn.disabled = false;
                    btn.style.opacity = '1';
                    btn.style.background = 'linear-gradient(135deg,#10b981,#059669)';
                    btn.innerHTML = '<i data-lucide="check-circle" style="width:15px;height:15px;display:inline-block;vertical-align:middle;margin-right:6px;"></i><span>Analytics Loaded — Refresh</span>';
                    if (typeof lucide !== 'undefined') lucide.createIcons();
                }

                // ── 5. Re-render with real data ────────────────────
                await renderLineManagers();
                await renderServiceManagers();

            } catch (err) {
                console.error('[loadAnalytics]', err);
                if (btn) {
                    btn.disabled = false;
                    btn.style.opacity = '1';
                    btn.style.background = 'linear-gradient(135deg,#ef4444,#dc2626)';
                    btn.innerHTML = '<i data-lucide="alert-triangle" style="width:15px;height:15px;display:inline-block;vertical-align:middle;margin-right:6px;"></i><span>Error — Retry</span>';
                    if (typeof lucide !== 'undefined') lucide.createIcons();
                }
            }
        }
        function toggleCharts() {
            const chartsSection = document.getElementById('chartsSection');
            const icon = document.getElementById('chartsIcon');
            const text = document.getElementById('chartsText');

            if (chartsSection.style.display === 'none') {
                chartsSection.style.display = 'grid';
                icon.setAttribute('data-lucide', 'eye-off');
                text.textContent = 'Hide Analytics Charts';
            } else {
                chartsSection.style.display = 'none';
                icon.setAttribute('data-lucide', 'eye');
                text.textContent = 'Show Analytics Charts';
            }

            lucide.createIcons();
        }

        function setCurrentDate() {
            const now = new Date();
            const options = {
                weekday: 'long',
                year: 'numeric',
                month: 'long',
                day: 'numeric'
            };
            document.getElementById('currentDate').textContent = now.toLocaleDateString('en-US', options);
        }

        function parseAccessControlYesNo(val) {
            if (!val) return false;
            if (val === true || val === 'Yes' || val === 'yes') return true;
            if (val.results) return val.results.includes('Yes');
            return false;
        }

        function buildAccessControlYesNoPayload(fieldName, enabled, sampleItem) {
            const existing = sampleItem ? sampleItem[fieldName] : null;
            if (existing && typeof existing === 'object' && existing.results) {
                return {
                    [fieldName]: {
                        __metadata: {
                            type: 'Collection(Edm.String)'
                        },
                        results: enabled ? ['Yes'] : ['No']
                    }
                };
            }
            return {
                [fieldName]: enabled ? 'Yes' : 'No'
            };
        }

        function isAccessControlAdmin() {
            const email = (USER_CONTEXT.userEmail || '').toLowerCase();
            return email === 'ubaid.mir@du.ae' || email === 'tehleel.lone@du.ae';
        }

        async function fetchAccessControlSettings() {
            try {
                let url = SP_URL + "/_api/web/lists/getbytitle('" + SP_ACCESS_LIST + "')/items?" +
                    "$select=ID,UserEmailID,RevenueEnabled,POCDetailsEnabled,RNPSEligibilityEnabled&" +
                    "$filter=(UserEmailID eq 'ubaid.mir@du.ae' or UserEmailID eq 'tehleel.lone@du.ae')&$top=2";
                let res = await fetch(url, {
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });
                if (!res.ok) {
                    url = SP_URL + "/_api/web/lists/getbytitle('" + SP_ACCESS_LIST + "')/items?" +
                        "$select=ID,UserEmailID,RevenueEnabled,POCDetailsEnabled,RNPSEligibilityEnabled&" +
                        "$filter=(UserEmailID eq 'ubaid.mir@du.ae' or UserEmailID eq 'tehleel.lone@du.ae')&$top=2";
                    res = await fetch(url, {
                        headers: {
                            'Accept': 'application/json;odata=verbose'
                        },
                        credentials: 'include'
                    });
                }
                if (!res.ok) return;
                const data = await res.json();
                const results = data.d.results;
                window.REVENUE_ENABLED = results.some(r => parseAccessControlYesNo(r.RevenueEnabled));
                window.POC_DETAILS_ENABLED = results.some(r => parseAccessControlYesNo(r.POCDetailsEnabled));
                window.RNPS_ELIGIBILITY_EDIT_ENABLED = results.some(r => parseAccessControlYesNo(r.RNPSEligibilityEnabled));
                window.REVENUE_CONTROL_ITEMS = results;
                console.log('[RevenueEnabled]', window.REVENUE_ENABLED);
                console.log('[POCDetailsEnabled]', window.POC_DETAILS_ENABLED, '(global for all users)');
                console.log('[RNPSEligibilityEnabled]', window.RNPS_ELIGIBILITY_EDIT_ENABLED, '(global for all users in Edit screen)');
                updateRevenueToggleBtn();
                updatePocToggleBtn();
                updateRnpsEligibilityToggleBtn();
            } catch (e) {
                console.warn('fetchAccessControlSettings failed:', e);
                window.REVENUE_ENABLED = false;
                window.POC_DETAILS_ENABLED = false;
                window.RNPS_ELIGIBILITY_EDIT_ENABLED = false;
            }
        }

        async function fetchRevenueEnabled() {
            return fetchAccessControlSettings();
        }

        async function toggleRevenueEnabled() {
            if (!isAccessControlAdmin()) {
                alert('Only authorised admins can change this setting.');
                return;
            }
            try {
                const newValue = !window.REVENUE_ENABLED;
                const items = window.REVENUE_CONTROL_ITEMS || [];
                if (items.length === 0) {
                    alert('Control rows not found');
                    return;
                }

                // Get digest once
                const digestRes = await fetch(SP_URL + '/_api/contextinfo', {
                    method: 'POST',
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });
                if (!digestRes.ok) throw new Error('Failed to get digest');
                const digest = (await digestRes.json()).d.GetContextWebInformation.FormDigestValue;

                // Update BOTH rows
                for (const item of items) {
                    const updateRes = await fetch(SP_URL + "/_api/web/lists/getbytitle('" + SP_ACCESS_LIST + "')/items(" + item.ID + ")", {
                        method: 'POST',
                        headers: {
                            'Accept': 'application/json;odata=verbose',
                            'Content-Type': 'application/json;odata=verbose',
                            'X-RequestDigest': digest,
                            'IF-MATCH': '*',
                            'X-HTTP-Method': 'MERGE'
                        },
                        credentials: 'include',
                        body: JSON.stringify({
                            __metadata: {
                                type: 'SP.Data.Access_x005f_ControlListItem'
                            },
                            RevenueEnabled: {
                                __metadata: {
                                    type: 'Collection(Edm.String)'
                                },
                                results: newValue ? ['Yes'] : ['No']
                            }
                        })
                    });
                    if (!updateRes.ok) throw new Error('Update failed for ' + item.UserEmailID);
                }

                window.REVENUE_ENABLED = newValue;
                // Update both rows in memory too
                window.REVENUE_CONTROL_ITEMS = items.map(i => ({
                    ...i,
                    RevenueEnabled: newValue
                }));
                updateRevenueToggleBtn();
                console.log('[RevenueEnabled] updated to', newValue);
            } catch (e) {
                alert('Error updating revenue setting: ' + e.message);
            }
        }

        function updateRevenueToggleBtn() {
            const btn = document.getElementById('revenueToggleBtn');
            if (!btn) return;
            if (window.REVENUE_ENABLED) {
                btn.innerHTML = '<i data-lucide="unlock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Revenue: Enabled';
                btn.style.background = 'linear-gradient(135deg,#10b981,#059669)';
            } else {
                btn.innerHTML = '<i data-lucide="lock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Revenue: Disabled';
                btn.style.background = 'linear-gradient(135deg,#ef4444,#dc2626)';
            }
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }

        async function togglePocDetailsEnabled() {
            if (!isAccessControlAdmin()) {
                alert('Only authorised admins can change this setting.');
                return;
            }
            try {
                const newValue = !window.POC_DETAILS_ENABLED;
                const items = window.REVENUE_CONTROL_ITEMS || [];
                if (items.length === 0) {
                    alert('Control rows not found');
                    return;
                }

                const digestRes = await fetch(SP_URL + '/_api/contextinfo', {
                    method: 'POST',
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });
                if (!digestRes.ok) throw new Error('Failed to get digest');
                const digest = (await digestRes.json()).d.GetContextWebInformation.FormDigestValue;

                for (const item of items) {
                    const updateRes = await fetch(SP_URL + "/_api/web/lists/getbytitle('" + SP_ACCESS_LIST + "')/items(" + item.ID + ")", {
                        method: 'POST',
                        headers: {
                            'Accept': 'application/json;odata=verbose',
                            'Content-Type': 'application/json;odata=verbose',
                            'X-RequestDigest': digest,
                            'IF-MATCH': '*',
                            'X-HTTP-Method': 'MERGE'
                        },
                        credentials: 'include',
                        body: JSON.stringify(Object.assign({
                            __metadata: {
                                type: 'SP.Data.Access_x005f_ControlListItem'
                            }
                        }, buildAccessControlYesNoPayload('POCDetailsEnabled', newValue, item)))
                    });
                    if (!updateRes.ok) {
                        const errText = await updateRes.text();
                        throw new Error('Update failed for ' + item.UserEmailID + ': ' + errText);
                    }
                }

                window.POC_DETAILS_ENABLED = newValue;
                window.REVENUE_CONTROL_ITEMS = items.map(i => ({
                    ...i,
                    POCDetailsEnabled: newValue ? 'Yes' : 'No'
                }));
                updatePocToggleBtn();
                if (typeof renderEditPocFields === 'function' && EDIT_ACCOUNT_DATA) renderEditPocFields();
                console.log('[POCDetailsEnabled] updated to', newValue);
            } catch (e) {
                alert('Error updating POC details setting: ' + e.message);
            }
        }

        function updatePocToggleBtn() {
            const btn = document.getElementById('pocToggleBtn');
            if (!btn) return;
            if (window.POC_DETAILS_ENABLED) {
                btn.innerHTML = '<i data-lucide="unlock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>POC Details: Enabled';
                btn.style.background = 'linear-gradient(135deg,#10b981,#059669)';
            } else {
                btn.innerHTML = '<i data-lucide="lock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>POC Details: Disabled';
                btn.style.background = 'linear-gradient(135deg,#ef4444,#dc2626)';
            }
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }

        async function toggleRnpsEligibilityEnabled() {
            if (!isAccessControlAdmin()) {
                alert('Only authorised admins can change this setting.');
                return;
            }
            try {
                const newValue = !window.RNPS_ELIGIBILITY_EDIT_ENABLED;
                const items = window.REVENUE_CONTROL_ITEMS || [];
                if (items.length === 0) {
                    alert('Control rows not found');
                    return;
                }

                const digestRes = await fetch(SP_URL + '/_api/contextinfo', {
                    method: 'POST',
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });
                if (!digestRes.ok) throw new Error('Failed to get digest');
                const digest = (await digestRes.json()).d.GetContextWebInformation.FormDigestValue;

                for (const item of items) {
                    const updateRes = await fetch(SP_URL + "/_api/web/lists/getbytitle('" + SP_ACCESS_LIST + "')/items(" + item.ID + ")", {
                        method: 'POST',
                        headers: {
                            'Accept': 'application/json;odata=verbose',
                            'Content-Type': 'application/json;odata=verbose',
                            'X-RequestDigest': digest,
                            'IF-MATCH': '*',
                            'X-HTTP-Method': 'MERGE'
                        },
                        credentials: 'include',
                        body: JSON.stringify(Object.assign({
                            __metadata: {
                                type: 'SP.Data.Access_x005f_ControlListItem'
                            }
                        }, buildAccessControlYesNoPayload('RNPSEligibilityEnabled', newValue, item)))
                    });
                    if (!updateRes.ok) {
                        const errText = await updateRes.text();
                        throw new Error('Update failed for ' + item.UserEmailID + ': ' + errText);
                    }
                }

                window.RNPS_ELIGIBILITY_EDIT_ENABLED = newValue;
                window.REVENUE_CONTROL_ITEMS = items.map(i => ({
                    ...i,
                    RNPSEligibilityEnabled: newValue ? 'Yes' : 'No'
                }));
                updateRnpsEligibilityToggleBtn();
                if (typeof smRefreshEditRnpsEligibilityFields === 'function' && EDIT_ACCOUNT_DATA) {
                    smRefreshEditRnpsEligibilityFields();
                }
                console.log('[RNPSEligibilityEnabled] updated to', newValue);
            } catch (e) {
                alert('Error updating RNPS Eligibility setting: ' + e.message);
            }
        }

        function updateRnpsEligibilityToggleBtn() {
            const btn = document.getElementById('rnpsEligibilityToggleBtn');
            if (!btn) return;
            if (window.RNPS_ELIGIBILITY_EDIT_ENABLED) {
                btn.innerHTML = '<i data-lucide="unlock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>RNPS Eligibility: Enabled';
                btn.style.background = 'linear-gradient(135deg,#10b981,#059669)';
            } else {
                btn.innerHTML = '<i data-lucide="lock" style="width:14px;height:14px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>RNPS Eligibility: Disabled';
                btn.style.background = 'linear-gradient(135deg,#ef4444,#dc2626)';
            }
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }
        async function fetchDOJData() {
            if (LOCAL_DEV_MODE) return; // skip in dev mode
            try {
                const url = SP_URL + "/_api/web/lists/getbytitle('DOJ')/items?$select=Title,DOJ,Status&$top=500";
                const res = await fetch(url, {
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    }
                });
                if (!res.ok) return;
                const data = await res.json();
                DOJ_DATA = {};
                (data.d.results || []).forEach(function(item) {
                    const name = (item.Title || '').trim();
                    if (!name || !item.DOJ) return;
                    const dojDate = new Date(item.DOJ);
                    const now = new Date();
                    const totalMonths = Math.floor((now - dojDate) / (1000 * 60 * 60 * 24 * 30.44));
                    const years = Math.floor(totalMonths / 12);
                    const months = totalMonths % 12;
                    const tenure = years > 0 ? (months > 0 ? years + 'y ' + months + 'm' : years + 'y') : months + 'm';
                    const dojFormatted = dojDate.toLocaleDateString('en-GB', {
                        month: 'short',
                        year: 'numeric'
                    });
                    DOJ_DATA[name] = {
                        doj: dojFormatted,
                        tenure: tenure
                    };
                });
                console.log('DOJ data loaded:', Object.keys(DOJ_DATA).length, 'records');
            } catch (e) {
                console.warn('DOJ fetch failed:', e);
            }
        }

        async function fetchAccountMapping() {
            if (LOCAL_DEV_MODE) {
                const devNames = ['Ahmed Al-Mansouri', 'Fatima Hassan', 'Omar Abdullah', 'Layla Mohamed', 'Sarah Johnson', 'Michael Chen'];
                SM_MAPPING_DATA = devNames.map(function(name, idx) {
                    return {
                        name: name,
                        team: idx % 2 === 0 ? 'DSM' : 'TSM_ME',
                        email: name.toLowerCase().replace(/ /g, '.') + '@du.ae',
                        userId: String(100 + idx)
                    };
                });
                return;
            }
            try {
                const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_MAPPING_LIST + "')/items?" +
                    "$select=Service_Manager_Name,Email_ID,Team,User_ID,Status&$top=5000&$orderby=Service_Manager_Name asc";
                const res = await fetch(url, {
                    headers: { 'Accept': 'application/json;odata=verbose' },
                    credentials: 'include'
                });
                if (!res.ok) {
                    console.warn('Account Mapping fetch failed:', res.status);
                    return;
                }
                const data = await res.json();
                const seen = {};
                SM_MAPPING_DATA = [];
                (data.d.results || []).forEach(function(item) {
                    const name = (item.Service_Manager_Name || '').trim();
                    if (!name) return;
                    const key = name + '|' + (item.Team || '');
                    if (seen[key]) return;
                    seen[key] = true;
                    SM_MAPPING_DATA.push({
                        name: name,
                        team: item.Team || '',
                        email: item.Email_ID || '',
                        userId: item.User_ID || '',
                        status: item.Status || ''
                    });
                });
                SM_MAPPING_DATA.sort(function(a, b) { return a.name.localeCompare(b.name); });
                console.log('Account Mapping loaded:', SM_MAPPING_DATA.length, 'records');
            } catch (e) {
                console.warn('Account Mapping fetch failed:', e);
            }
        }

        function smNormalizePersonName(name) {
            return (name || '').trim().replace(/\s+/g, ' ');
        }

        async function smResolvePersonId(name) {
            const norm = smNormalizePersonName(name);
            if (!norm) return null;

            const mapped = SM_MAPPING_DATA.find(function(a) {
                return smNormalizePersonName(a.name) === norm;
            });
            if (mapped && mapped.userId) {
                const uid = parseInt(String(mapped.userId).trim(), 10);
                if (!isNaN(uid) && uid > 0) return uid;
            }

            if (typeof getUserId === 'function') {
                const byTitle = await getUserId(name);
                if (byTitle) return byTitle;
            }

            if (mapped && mapped.email) {
                const eUrl = SP_URL + "/_api/web/siteusers?$filter=Email eq '" + mapped.email.replace(/'/g, "''") + "'&$select=Id&$top=1";
                const eRes = await fetch(eUrl, { headers: { 'Accept': 'application/json;odata=verbose' }, credentials: 'include' });
                if (eRes.ok) {
                    const eData = await eRes.json();
                    if (eData.d.results && eData.d.results.length > 0) return eData.d.results[0].Id;
                }
            }
            return null;
        }

        async function smPatchListItem(itemId, digest, fields) {
            const updateUrl = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items(" + itemId + ")";
            const res = await fetch(updateUrl, {
                method: 'POST',
                headers: {
                    'Accept': 'application/json;odata=verbose',
                    'Content-Type': 'application/json;odata=verbose',
                    'X-RequestDigest': digest,
                    'IF-MATCH': '*',
                    'X-HTTP-Method': 'MERGE'
                },
                credentials: 'include',
                body: JSON.stringify(Object.assign({
                    __metadata: { type: 'SP.Data.Service_x0020_Manager_x0020_RequestListItem' }
                }, fields))
            });
            if (!res.ok) {
                const errorText = await res.text();
                throw new Error('Update failed: ' + errorText);
            }
        }

        window.smSecondarySmSearch = function(q) {
            const dd = document.getElementById('editSecondarySMDD');
            if (!dd) return;
            const lower = (q || '').toLowerCase();
            const pool = SM_MAPPING_DATA.slice();
            const results = lower
                ? pool.filter(function(a) {
                    return a.name.toLowerCase().indexOf(lower) >= 0 ||
                        (a.team || '').toLowerCase().indexOf(lower) >= 0 ||
                        (a.email || '').toLowerCase().indexOf(lower) >= 0;
                })
                : pool.slice(0, 40);

            dd.innerHTML = '';
            if (!results.length) {
                dd.innerHTML = '<div class="sm-person-item" style="color:var(--t3);font-size:.82rem;">No service managers found</div>';
                dd.style.display = 'block';
                return;
            }
            const clearItem = document.createElement('div');
            clearItem.className = 'sm-person-item';
            clearItem.style.color = 'var(--t3)';
            clearItem.textContent = '— Clear Secondary SM —';
            clearItem.addEventListener('mousedown', function(e) {
                e.preventDefault();
                smClearSecondarySm();
            });
            dd.appendChild(clearItem);
            results.forEach(function(a) {
                const item = document.createElement('div');
                item.className = 'sm-person-item';
                item.innerHTML = '<div style="font-size:.83rem;font-weight:600;color:var(--t1);">' + a.name + '</div>' +
                    '<div style="font-size:.72rem;color:var(--t3);">' + (a.team || '') + (a.email ? ' · ' + a.email : '') + '</div>';
                item.addEventListener('mousedown', function(e) {
                    e.preventDefault();
                    smSelectSecondarySm(a.name, a.team);
                });
                dd.appendChild(item);
            });
            dd.style.display = 'block';
        };

        window.smSelectSecondarySm = function(name, team) {
            const hidden = document.getElementById('editSecondarySM');
            const inp = document.getElementById('editSecondarySMInput');
            const dd = document.getElementById('editSecondarySMDD');
            const tag = document.getElementById('editSecondarySMTag');
            const normalized = smNormalizePersonName(name);
            if (hidden) hidden.value = normalized;
            if (inp) inp.value = '';
            if (dd) dd.style.display = 'none';
            if (tag) {
                if (!normalized) {
                    tag.innerHTML = '';
                    tag.style.display = 'none';
                    return;
                }
                tag.innerHTML = '<span style="display:inline-flex;align-items:center;gap:.35rem;background:var(--nab);border:1px solid var(--nab2);border-radius:20px;padding:.22rem .65rem;font-size:.78rem;font-weight:600;color:var(--acc);">' +
                    normalized + (team ? ' <span style="font-size:.65rem;opacity:.7;">(' + team + ')</span>' : '') +
                    '<span style="cursor:pointer;margin-left:.2rem;font-size:.95rem;" onclick="smClearSecondarySm()">×</span></span>';
                tag.style.display = 'block';
            }
        };

        window.smClearSecondarySm = function() {
            smSelectSecondarySm('', '');
        };

        function smSecondarySmEditHtml() {
            return '<div class="edit-field-card">' +
                '<div class="edit-field-label">Secondary SM</div>' +
                '<div class="sm-person-wrap">' +
                    '<input type="text" id="editSecondarySMInput" class="filter-select" placeholder="Search service manager..." style="font-size:14px;padding:10px;width:100%;cursor:text;" autocomplete="off" ' +
                    'oninput="smSecondarySmSearch(this.value)" onfocus="smSecondarySmSearch(this.value)" ' +
                    'onblur="setTimeout(function(){var d=document.getElementById(\'editSecondarySMDD\');if(d)d.style.display=\'none\';},250)">' +
                    '<div id="editSecondarySMDD" class="sm-person-dd"></div>' +
                '</div>' +
                '<div id="editSecondarySMTag" style="display:none;margin-top:.35rem;"></div>' +
                '<input type="hidden" id="editSecondarySM" value="">' +
            '</div>';
        }

async function fetchRNPSForLM(lmName) {
    try {
        const url = SP_URL + "/_api/web/lists/getbytitle('RNPSScores')/items?" +
            "$select=Period,ServiceManager,ResponseRate,RNPS,ResponseTarget,RNPSTarget&" +
            "$filter=LineManager eq '" + lmName.replace(/'/g, "''") + "'&" +
            "$top=500";
        const res = await fetch(url, {
            headers: { 'Accept': 'application/json;odata=verbose' },
            credentials: 'include'
        });
        if (!res.ok) return {};
        const data = await res.json();
        const currentYear = new Date().getFullYear();
        const validQuarters = ['Q1-' + currentYear, 'Q2-' + currentYear, 'Q3-' + currentYear, 'Q4-' + currentYear];

        const grouped = {};
        (data.d.results || []).forEach(row => {
            if (!validQuarters.includes(row.Period)) return;
            const sm = (row.ServiceManager || '').trim();
            if (!sm) return;
            if (!grouped[sm]) grouped[sm] = [];
            grouped[sm].push(row);
        });
        return grouped;
    } catch (e) {
        console.warn('fetchRNPSForLM failed:', e);
        return {};
    }
}

// ── ETA TILE HELPERS ──────────────────────────────────────────

function etaGetCurrentYearData(arr) {
    var currentYear = new Date().getFullYear().toString();
    return (arr || []).filter(function(r) { return r.Year === currentYear; });
}

function etaClassifyTT(r) {
    var s = r.TT_STATUS || '';
    if (['Closed','Resolved'].includes(s)) return 'completed';
    if (['Assigned','In Queue','Pending','Pending For Closure'].includes(s)) return 'assigned';
    if (['Open','In Progress'].includes(s)) return 'inprogress';
    return 'other';
}

function etaClassifyEvent(r) {
    var s = r.STATUS || '';
    if (['Completed','Closed'].includes(s)) return 'completed';
    if (['Open','In Queue'].includes(s)) return 'assigned';
    if (['In Progress','Escalated','Pending'].includes(s)) return 'inprogress';
    return 'other';
}

function etaClassifyActivity(r) {
    var s = r.ACTIVITY_STATUS || '';
    if (s === 'Done') return 'completed';
    if (['Assigned','Unassigned'].includes(s)) return 'assigned';
    if (['In Progress','Open'].includes(s)) return 'inprogress';
    return 'other';
}

function etaGetManagerStats(filterFn) {
    if (!ETA_STATE.allData) return null;

    var tts        = etaGetCurrentYearData(ETA_STATE.allData.tts).filter(filterFn);
    var events     = etaGetCurrentYearData(ETA_STATE.allData.events).filter(filterFn);
    var activities = etaGetCurrentYearData(ETA_STATE.allData.activities).filter(filterFn);

    function countBy(arr, classifyFn) {
        var counts = { assigned: 0, inprogress: 0, completed: 0, other: 0, total: 0 };
        arr.forEach(function(r) {
            var c = classifyFn(r);
            counts[c]++;
            counts.total++;
        });
        return counts;
    }

    // Detailed breakdown for (i) popup
    function detailBy(arr, statusField) {
        var map = {};
        arr.forEach(function(r) {
            var s = r[statusField] || 'Unknown';
            map[s] = (map[s] || 0) + 1;
        });
        return map;
    }

    return {
        tt:         countBy(tts, etaClassifyTT),
        events:     countBy(events, etaClassifyEvent),
        activities: countBy(activities, etaClassifyActivity),
        ttDetail:   detailBy(tts, 'TT_STATUS'),
        evDetail:   detailBy(events, 'STATUS'),
        actDetail:  detailBy(activities, 'ACTIVITY_STATUS')
    };
}

function etaStatsTileHtml(stats, title) {
    if (!stats) {
        return `<div class="lm-tile" style="cursor:default;opacity:.7;">
            <div class="lm-header"><h3 style="font-size:.85rem;">${title}</h3>
            <div class="lm-meta">ETA data loading...</div></div>
        </div>`;
    }

    var currentYear = new Date().getFullYear();

    function statRow(label, s) {
        return `
        <div style="margin-bottom:6px;">
            <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:3px;">
                <span style="font-size:.65rem;font-weight:700;color:var(--t2);">${label}</span>
                <span style="font-size:.65rem;color:var(--t3);">Total: ${s.total}</span>
            </div>
            <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:3px;">
                <div style="background:rgba(245,158,11,.12);border:1px solid rgba(245,158,11,.3);border-radius:5px;padding:3px 4px;text-align:center;">
                    <div style="font-size:.58rem;color:#b45309;font-weight:600;">Assigned</div>
                    <div style="font-size:.78rem;font-weight:800;color:#f59e0b;">${s.assigned}</div>
                </div>
                <div style="background:rgba(59,130,246,.12);border:1px solid rgba(59,130,246,.3);border-radius:5px;padding:3px 4px;text-align:center;">
                    <div style="font-size:.58rem;color:#1d4ed8;font-weight:600;">In Prog</div>
                    <div style="font-size:.78rem;font-weight:800;color:#3b82f6;">${s.inprogress}</div>
                </div>
                <div style="background:rgba(16,185,129,.12);border:1px solid rgba(16,185,129,.3);border-radius:5px;padding:3px 4px;text-align:center;">
                    <div style="font-size:.58rem;color:#065f46;font-weight:600;">Done</div>
                    <div style="font-size:.78rem;font-weight:800;color:#10b981;">${s.completed}</div>
                </div>
                <div style="background:rgba(107,114,128,.12);border:1px solid rgba(107,114,128,.3);border-radius:5px;padding:3px 4px;text-align:center;">
                    <div style="font-size:.58rem;color:var(--t3);font-weight:600;">Other</div>
                    <div style="font-size:.78rem;font-weight:800;color:var(--t3);">${s.other}</div>
                </div>
            </div>
        </div>`;
    }

    return `
    <div class="lm-tile" style="cursor:default;border-color:var(--border-s);min-width:220px;">
        <div class="lm-header" style="margin-bottom:8px;">
            <div style="display:flex;align-items:center;justify-content:space-between;">
                <h3 style="font-size:.85rem;">${title}</h3>
                <span style="font-size:.7rem;color:var(--t3);font-weight:500;">${currentYear}</span>
                <div style="position:relative;display:inline-block;">
                    <span id="etaInfoBtn_${title.replace(/\s/g,'_')}"
                        onclick="etaShowInfoPopup(event,'${title.replace(/\s/g,'_')}')"
                        style="cursor:pointer;width:18px;height:18px;border-radius:50%;background:var(--bg-secondary);border:1px solid var(--border);display:inline-flex;align-items:center;justify-content:center;font-size:.65rem;font-weight:800;color:var(--acc);">i</span>
                    <div id="etaInfoPopup_${title.replace(/\s/g,'_')}" 
                        style="display:none;position:absolute;right:0;top:24px;z-index:999;background:var(--bg-card);border:1px solid var(--border);border-radius:10px;box-shadow:0 8px 24px rgba(0,0,0,.2);padding:10px 12px;min-width:220px;max-width:260px;">
                        <div style="font-size:.7rem;font-weight:800;color:var(--t1);margin-bottom:6px;border-bottom:1px solid var(--border);padding-bottom:4px;">Detailed Breakdown</div>
                        ${etaDetailHtml('TTs', stats.ttDetail)}
                        ${etaDetailHtml('Events', stats.evDetail)}
                        ${etaDetailHtml('Activities', stats.actDetail)}
                    </div>
                </div>
            </div>
            <div class="lm-meta">ETA Data — Current Year</div>
        </div>
        ${statRow('Trouble Tickets', stats.tt)}
        ${statRow('Events', stats.events)}
        ${statRow('Activities', stats.activities)}
    </div>`;
}

function etaDetailHtml(label, detailMap) {
    var entries = Object.entries(detailMap).sort(function(a,b){ return b[1]-a[1]; });
    if (!entries.length) return `<div style="font-size:.68rem;color:var(--t3);margin-bottom:6px;">${label}: No data</div>`;
    return `
    <div style="margin-bottom:8px;">
        <div style="font-size:.65rem;font-weight:700;color:var(--acc);margin-bottom:3px;">${label}</div>
        ${entries.map(function(e) {
            return `<div style="display:flex;justify-content:space-between;font-size:.65rem;padding:1px 0;">
                <span style="color:var(--t2);">${e[0]}</span>
                <span style="font-weight:700;color:var(--t1);">${e[1]}</span>
            </div>`;
        }).join('')}
    </div>`;
}

// Close info popups when clicking outside
document.addEventListener('click', function(e) {
    if (!e.target.closest('[id^="etaInfoBtn_"]') && !e.target.closest('[id^="etaInfoPopup_"]')) {
        document.querySelectorAll('[id^="etaInfoPopup_"]').forEach(function(p) {
            p.style.display = 'none';
        });
    }
});

window.etaShowInfoPopup = function(e, key) {
    e.stopPropagation();
    // Close all other popups first
    document.querySelectorAll('[id^="etaInfoPopup_"]').forEach(function(p) {
        p.style.display = 'none';
    });
    var popup = document.getElementById('etaInfoPopup_' + key);
    if (popup) popup.style.display = popup.style.display === 'none' ? 'block' : 'none';
};
function computeLMRNPS(rnpsGrouped) {
    // rnpsGrouped is { smName: [rows], ... } — flatten and average by period
    const byPeriod = {};
    Object.values(rnpsGrouped).forEach(rows => {
        rows.forEach(row => {
            if (!byPeriod[row.Period]) byPeriod[row.Period] = [];
            byPeriod[row.Period].push(row);
        });
    });

    const currentYear = new Date().getFullYear();
    const validQuarters = ['Q1-' + currentYear, 'Q2-' + currentYear, 'Q3-' + currentYear, 'Q4-' + currentYear];

    const result = [];
    validQuarters.forEach(q => {
        if (!byPeriod[q] || byPeriod[q].length === 0) return;
        const rows = byPeriod[q];
        const count = rows.length;
        result.push({
            Period: q,
            ResponseRate: parseFloat((rows.reduce((s, r) => s + (r.ResponseRate || 0), 0) / count).toFixed(1)),
            RNPS: parseFloat((rows.reduce((s, r) => s + (r.RNPS || 0), 0) / count).toFixed(1)),
            ResponseTarget: rows[0].ResponseTarget,
            RNPSTarget: rows[0].RNPSTarget
        });
    });
    return result;
}
        function parseChoiceField(raw) {
            if (raw === null || raw === undefined) return '';
            if (typeof raw === 'object') {
                if (Array.isArray(raw)) return raw.length ? parseChoiceField(raw[0]) : '';
                return raw.Label || raw.Value || raw.Title || '';
            }
            return String(raw).replace(/\u00a0/g, ' ').trim();
        }

        async function fetchData() {
            // ===== LOCAL DEV MODE: Return fake data =====
            if (LOCAL_DEV_MODE) {
                console.log('🎭 LOCAL DEV MODE: Using fake data');
                return generateFakeData();
            }

            // ===== EXISTING SHAREPOINT DATA FETCH =====
            const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                "$select=ID,Title,Parent_x0020_Code,Customer_x0020_Name,Team," +
                "Proposed_x0020_Team,Transfer_Request_Date," +
                "Line_x0020_Manager/Title,Line_x0020_Manager/EMail," +
                "Service_x0020_Manager/Title,Service_x0020_Manager/EMail," +
                "Secondary_x0020_Service_x0020_Ma/Title,Secondary_x0020_Service_x0020_Ma/EMail," +
                "Account_x0020_Manager/Title,Account_x0020_Manager/EMail," +
                "Account_x0020_Director/Title,Account_x0020_Director/EMail," +
                "POC_x0020_Name,POC_x0020_Email_x0020_ID,POC_x0020_Contact_x0020_No," +
                "Secondary_x0020_POC_x0020_Name,Secondary_x0020_POC_x0020_Email,Secondary_x0020_POC_x0020_Contac," +
                "Technical_x0020_POC_x0020_Name,Technical_x0020_POC_x0020_Email,Technical_x0020_POC_x0020_Contac," +
                "AccountType,RNPS_Eligibility,RNPS_POC,RNPS_Not_Eligible_Reason," +
                // 2025 months
                "Oct_x002d_25,Nov_x002d_25,Dec_x002d_25," +
                // 2026 months
                "Jan_x002d_26,Feb_x002d_26,Mar_x002d_26,Apr_x002d_26,May_x002d_26,Jun_x002d_26," +
                "Jul_x002d_26,Aug_x002d_26,Sep_x002d_26,Oct_x002d_26,Nov_x002d_26,Dec_x002d_26," +
                "Request_x0020_Status,Request_x0020_Type,Reject_New_Account_Reason&" +
                "$expand=Line_x0020_Manager,Service_x0020_Manager,Secondary_x0020_Service_x0020_Ma,Account_x0020_Manager,Account_x0020_Director&" +
                "$top=10000";

            const res = await fetch(url, {
                headers: {
                    'Accept': 'application/json;odata=verbose'
                },
                credentials: 'include'
            });

            if (!res.ok) throw new Error('Failed: ' + res.statusText);

            const data = await res.json();
            return data.d.results.map(item => {
                const isApproved = item.Request_x0020_Status === 'OnBoarded' || item.Request_x0020_Status === 'AM_Approved';

                // [NEW] Get last 3 completed months dynamically
                const lastThreeMonths = getLastThreeCompletedMonths();
                const monthRevenues = [];

                lastThreeMonths.forEach(month => {
                    const value = parseFloat(item[month.field]) || 0;
                    monthRevenues.push(value);
                });

                // Calculate average from last 3 months
                const avgRevenue = monthRevenues.length > 0 ?
                    monthRevenues.reduce((sum, val) => sum + val, 0) / monthRevenues.length :
                    0;

                const team = item.Team || '';
                const lastMonthValue = monthRevenues[2] || 0; // Most recent month

                // Calculate if revenue drop or upgrade (only for approved PARENT accounts)
                let isRevDrop = false;
                let isRevUpgrade = false;
                const isGroup = (item.Title === item.Parent_x0020_Code) || !item.Parent_x0020_Code;
                // NOTE: isRevDrop/isRevUpgrade will be recalculated after ALL data loads
                // using combined parent+children revenue. Set placeholder here.
                if (isApproved && isGroup) {
                    // Placeholder - real check done in recalculateRevFlags() after full load
                }
                const result = {
                    spItemId: item.ID || item.Id || null,
                    code: item.Title || '',
                    parent: item.Parent_x0020_Code || null,
                    customer: item.Customer_x0020_Name || '',
                    team: team,
                    lm: item.Line_x0020_Manager ? item.Line_x0020_Manager.Title : '',
                    sm: item.Service_x0020_Manager ? item.Service_x0020_Manager.Title : '',
                    secondarySm: item.Secondary_x0020_Service_x0020_Ma ? item.Secondary_x0020_Service_x0020_Ma.Title : '',
                    secondarySmEmail: item.Secondary_x0020_Service_x0020_Ma ? (item.Secondary_x0020_Service_x0020_Ma.EMail || '') : '',
                    am: item.Account_x0020_Manager ? item.Account_x0020_Manager.Title : '',
                    ad: item.Account_x0020_Director ? item.Account_x0020_Director.Title : '',
                    lmEmail: item.Line_x0020_Manager ? (item.Line_x0020_Manager.EMail || '') : '',
                    smEmail: item.Service_x0020_Manager ? (item.Service_x0020_Manager.EMail || '') : '',
                    amEmail: item.Account_x0020_Manager ? (item.Account_x0020_Manager.EMail || '') : '',
                    adEmail: item.Account_x0020_Director ? (item.Account_x0020_Director.EMail || '') : '',
                    proposedTeam: item.Proposed_x0020_Team || '',
                    transferRequestDate: item.Transfer_Request_Date ? new Date(item.Transfer_Request_Date) : null,
                    pocName: item.POC_x0020_Name || '',
                    pocEmail: item.POC_x0020_Email_x0020_ID || '',
                    pocPhone: item.POC_x0020_Contact_x0020_No || '',
                    secondaryPocName: item.Secondary_x0020_POC_x0020_Name || '',
                    secondaryPocEmail: item.Secondary_x0020_POC_x0020_Email || '',
                    secondaryPocPhone: item.Secondary_x0020_POC_x0020_Contac || '',
                    technicalPocName: item.Technical_x0020_POC_x0020_Name || '',
                    technicalPocEmail: item.Technical_x0020_POC_x0020_Email || '',
                    technicalPocPhone: item.Technical_x0020_POC_x0020_Contac || '',
                    accountType: parseChoiceField(item.AccountType) || '',
                    rnpsEligibility: parseChoiceField(item.RNPS_Eligibility) || '',
                    rnpsPoc: smNormalizeRnpsPoc(parseChoiceField(item.RNPS_POC) || 'Primary'),
                    rnpsNotEligibleReason: smPlainTextFromSharePoint(item.RNPS_Not_Eligible_Reason),

                    // [NEW] Store last 3 months values with keys
                    [lastThreeMonths[0].key]: monthRevenues[0] || 0, // e.g., 'oct25'
                    [lastThreeMonths[1].key]: monthRevenues[1] || 0,
                    [lastThreeMonths[2].key]: monthRevenues[2] || 0,

                    avg: avgRevenue,
                    type: (item.Title === item.Parent_x0020_Code) ? 'Group' : 'Child',
                    isRevDrop: isRevDrop,
                    requestStatus: item.Request_x0020_Status || 'Not Onboarded',
                    requestType: item.Request_x0020_Type || 'New Account',
                    isApproved: isApproved,
                    isRevUpgrade: isRevUpgrade,
                    rejectReason: item.Reject_New_Account_Reason || '',
                    transferDaysPassed: item.Transfer_Request_Date ? Math.floor((new Date() - new Date(item.Transfer_Request_Date)) / 86400000) : null,

                    // [NEW] Store ALL month values for edit view
                    allMonths: {
                        'Oct_x002d_25': parseFloat(item.Oct_x002d_25) || 0,
                        'Nov_x002d_25': parseFloat(item.Nov_x002d_25) || 0,
                        'Dec_x002d_25': parseFloat(item.Dec_x002d_25) || 0,
                        'Jan_x002d_26': parseFloat(item.Jan_x002d_26) || 0,
                        'Feb_x002d_26': parseFloat(item.Feb_x002d_26) || 0,
                        'Mar_x002d_26': parseFloat(item.Mar_x002d_26) || 0,
                        'Apr_x002d_26': parseFloat(item.Apr_x002d_26) || 0,
                        'May_x002d_26': parseFloat(item.May_x002d_26) || 0,
                        'Jun_x002d_26': parseFloat(item.Jun_x002d_26) || 0,
                        'Jul_x002d_26': parseFloat(item.Jul_x002d_26) || 0,
                        'Aug_x002d_26': parseFloat(item.Aug_x002d_26) || 0,
                        'Sep_x002d_26': parseFloat(item.Sep_x002d_26) || 0,
                        'Oct_x002d_26': parseFloat(item.Oct_x002d_26) || 0,
                        'Nov_x002d_26': parseFloat(item.Nov_x002d_26) || 0,
                        'Dec_x002d_26': parseFloat(item.Dec_x002d_26) || 0
                    }
                };

                return result;
            });
        }

        function recalculateRevFlags() {
            // For each Group account, sum its own revenue + all children's revenue
            ALL_DATA.forEach(account => {
                if (account.type !== 'Group') return;

                const lastThree = getLastThreeCompletedMonths();
                const latestKey = lastThree[2].key;

                // Find all children
                const children = ALL_DATA.filter(a =>
                    a.type === 'Child' &&
                    a.parent === account.code &&
                    a.code !== account.code
                );

                // Combined latest month revenue
                const combinedLatest = (account[latestKey] || 0) +
                    children.reduce((sum, c) => sum + (c[latestKey] || 0), 0);

                account.isRevDrop = false;
                account.isRevUpgrade = false;

                if (!account.isApproved) return;

                const team = account.team;
                if (team === 'DSM' && combinedLatest < 50000) account.isRevDrop = true;
                if (team === 'TSM_ME' && combinedLatest < 10000) account.isRevDrop = true;
                if (team === 'TSM_SE' && combinedLatest >= 10000) account.isRevUpgrade = true;
                if (team === 'TSM_ME' && combinedLatest >= 50000) account.isRevUpgrade = true;

                // Store combined revenue for use in transfer form
                account.combinedLatestRev = combinedLatest;
            });
        }
        // ========================================
        // DYNAMIC REVENUE MONTH FUNCTIONS
        // ========================================

        /**
         * Get the last 3 COMPLETED months
         * Example: If today is Feb 15, 2026 → Returns [Nov-25, Dec-25, Jan-26]
         */
        function getLastThreeCompletedMonths() {
            const today = new Date();
            const months = [];

            for (let i = 3; i >= 1; i--) {
                const date = new Date(today.getFullYear(), today.getMonth() - i, 1);
                months.push(getMonthInfo(date));
            }

            return months;
        }

        /**
         * Get the PREVIOUS month (editable month)
         * Example: If today is Feb 15, 2026 → Returns Jan-26
         */
        function getPreviousMonth() {
            const today = new Date();
            const date = new Date(today.getFullYear(), today.getMonth() - 1, 1);
            return getMonthInfo(date);
        }

        /**
         * Get the CURRENT month
         */
        function getCurrentMonth() {
            const today = new Date();
            return getMonthInfo(today);
        }

        /**
         * Convert a Date object to month info
         */
        function getMonthInfo(date) {
            const monthNames = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun',
                'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'
            ];

            const monthName = monthNames[date.getMonth()];
            const year = date.getFullYear().toString().slice(-2); // "26"

            return {
                label: `${monthName}-${year}`, // "Jan-26"
                field: `${monthName}_x002d_${year}`, // "Jan_x002d_26"
                key: `${monthName.toLowerCase()}${year}`, // "jan26" (for storing in result object)
                date: date,
                month: date.getMonth(),
                year: date.getFullYear()
            };
        }

        /**
         * Check if deadline has passed for a given month
         * Deadline = Last day of the NEXT month
         * Example: Jan-26 deadline = Feb 28/29, 2026 23:59:59
         */
        function isDeadlinePassed(monthInfo) {
            const today = new Date();

            // Deadline = Last day of next month
            const deadlineMonth = monthInfo.month + 1; // Next month
            const deadlineYear = monthInfo.year;

            // Get last day of next month
            const lastDay = new Date(deadlineYear, deadlineMonth + 1, 0); // Day 0 = last day of previous month
            lastDay.setHours(23, 59, 59, 999);

            return today > lastDay;
        }

        /**
         * Check if a month is locked (cannot edit)
         * Locked if: value > 0 OR deadline passed
         */
        function isMonthLocked(value, monthInfo) {
            if (window.REVENUE_ENABLED) return false; // override — always unlocked
            if (value > 0) return true;
            if (isDeadlinePassed(monthInfo)) return true;
            return false;
        }

        /**
         * Get revenue months for display/edit
         */
        function getRevenueMonthsForEdit() {
            const lastThree = getLastThreeCompletedMonths();
            const previous = getPreviousMonth();
            const current = getCurrentMonth();

            // Check if previous month is still editable
            const canEditPrevious = !isDeadlinePassed(previous);

            return {
                lastThree: lastThree, // Array of 3 months (disabled)
                editable: canEditPrevious ? previous : null, // Single month or null
                current: current // For reference
            };
        }
        // ===== USER CONTEXT =====
        let USER_CONTEXT = {
            userEmail: '',
            userName: '',
            role: 'none',
            isActive: false
        };
        window.USER_CONTEXT = USER_CONTEXT; // expose for external JS modules

        function normalizeSmRole(role) {
            if (!role) return 'none';
            var r = String(role).trim();
            if (r === 'PSD Admin') return 'PSD_Admin';
            if (r === 'PSD Agent') return 'PSD_Agent';
            return r;
        }
        function isPsdRole(role) {
            return role === 'PSD_Admin' || role === 'PSD_Agent';
        }
        function applyPsdNavRestrictions() {
            var psdAllowedLabels = [
                'Newsletter',
                'Events / TT / Activity',
                'Project Management',
                'Synergy 360',
                'Suggestion Box',
                'Process Documents',
                'Contact Us',
                'PSD Assignment'
            ];
            document.querySelectorAll('.nav-item').forEach(function(item) {
                var labelEl = item.querySelector('.nav-label');
                var label = labelEl ? labelEl.textContent.trim() : '';
                item.style.display = (psdAllowedLabels.indexOf(label) >= 0) ? 'flex' : 'none';
            });
            document.querySelectorAll('.nav-section-label').forEach(function(lbl) {
                lbl.style.display = 'none';
            });
        }

        // ===== VISIT TRACKING =====
        function getVisitCount() {
            try {
                const visitCount = localStorage.getItem('sm_visit_count_' + USER_CONTEXT.userEmail);
                return visitCount ? parseInt(visitCount, 10) : 0;
            } catch (e) {
                return 0;
            }
        }

        function incrementVisitCount() {
            try {
                const currentCount = getVisitCount();
                const newCount = currentCount + 1;
                localStorage.setItem('sm_visit_count_' + USER_CONTEXT.userEmail, newCount.toString());
                return newCount;
            } catch (e) {
                return 1;
            }
        }

        function getLastVisit() {
            try {
                const lastVisit = localStorage.getItem('sm_last_visit_' + USER_CONTEXT.userEmail);
                if (lastVisit) {
                    const date = new Date(lastVisit);
                    const now = new Date();
                    const diffMs = now - date;
                    const diffMins = Math.floor(diffMs / 60000);
                    const diffHours = Math.floor(diffMs / 3600000);
                    const diffDays = Math.floor(diffMs / 86400000);

                    if (diffMins < 60) return diffMins + ' min ago';
                    else if (diffHours < 24) return diffHours + ' hr ago';
                    else if (diffDays < 7) return diffDays + ' days ago';
                    else return date.toLocaleDateString();
                }
                return 'First visit';
            } catch (e) {
                return 'First visit';
            }
        }

        function saveLastVisit() {
            try {
                localStorage.setItem('sm_last_visit_' + USER_CONTEXT.userEmail, new Date().toISOString());
            } catch (e) {}
        }

        function getUserInitials(name) {
            if (!name) return '?';
            const parts = name.split(' ');
            if (parts.length >= 2) {
                return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
            }
            return name[0].toUpperCase();
        }

        // ========================================
        // 🎭 FAKE DATA GENERATOR (LOCAL DEV MODE)
        // ========================================
        function generateFakeData() {
            const teams = ['DSM', 'TSM_ME', 'TSM_SE', 'PSD'];
            const lineManagers = ['Sarah Johnson', 'Michael Chen', 'David Williams', 'Emma Thompson'];
            const serviceManagers = ['Ahmed Al-Mansouri', 'Fatima Hassan', 'Omar Abdullah', 'Layla Mohamed'];
            const accountManagers = ['James Smith', 'Lisa Anderson', 'Robert Brown'];
            const accountDirectors = ['Jennifer Davis', 'William Martinez'];

            const customers = [
                'Emirates Airlines', 'ADNOC Group', 'Dubai Municipality', 'Etisalat Business',
                'Abu Dhabi Police', 'Dubai Airports', 'Majid Al Futtaim', 'Emaar Properties',
                'DP World', 'Dubai Health Authority', 'DEWA', 'RTA Dubai', 'Dubai Customs',
                'Abu Dhabi Education', 'Sharjah Government', 'RAK Properties', 'Ajman Municipality',
                'Fujairah Government', 'Dubai Tourism', 'Abu Dhabi Tourism', 'Emirates NBD',
                'First Abu Dhabi Bank', 'Dubai Islamic Bank', 'Mashreq Bank', 'ADCB',
                'RAK Bank', 'Abu Dhabi Commercial Bank', 'Dubai World', 'Mubadala Investment', 'TDIC Abu Dhabi'
            ];

            const pocNames = ['Ahmed Ali', 'Mohammed Hassan', 'Fatima Abdullah', 'Sarah Ibrahim',
                'Omar Khalid', 'Layla Rashid', 'Khalid Ahmed', 'Mariam Said',
                'Hassan Mohammad', 'Noura Abdulla'
            ];

            const requestStatuses = ['OnBoarded', 'AM_Approved', 'Pending', 'Not Onboarded'];
            const requestTypes = ['New Account', 'Transfer Request', 'Account Update'];

            const fakeAccounts = [];
            let accountCode = 10000;

            // Generate 30 accounts
            for (let i = 0; i < 30; i++) {
                const isGroup = Math.random() > 0.6;
                const team = teams[Math.floor(Math.random() * teams.length)];
                const status = requestStatuses[Math.floor(Math.random() * requestStatuses.length)];
                const isApproved = status === 'OnBoarded' || status === 'AM_Approved';

                const code = (accountCode + i).toString();
                const parentCode = isGroup ? code : (10000 + Math.floor(i / 3)).toString();

                // Revenue based on team thresholds
                let baseRevenue;
                if (team === 'DSM') baseRevenue = 50000 + Math.random() * 150000;
                else if (team === 'TSM_ME') baseRevenue = 1000 + Math.random() * 48000;
                else if (team === 'TSM_SE') baseRevenue = 100 + Math.random() * 900;
                else baseRevenue = 5000 + Math.random() * 25000;

                const oct = baseRevenue * (0.9 + Math.random() * 0.2);
                const nov = baseRevenue * (0.85 + Math.random() * 0.3);
                const dec = baseRevenue * (0.95 + Math.random() * 0.15);
                const avg = (oct + nov + dec) / 3;

                // Revenue flags
                let isRevDrop = false,
                    isRevUpgrade = false;
                if (isApproved && isGroup) {
                    if (team === 'DSM' && dec < 50000) isRevDrop = true;
                    if (team === 'TSM_ME' && dec < 1000) isRevDrop = true;
                    if (team === 'TSM_SE' && dec >= 1000) isRevUpgrade = true;
                    if (team === 'TSM_ME' && dec >= 50000) isRevUpgrade = true;
                }

                fakeAccounts.push({
                    spItemId: 1000 + i,
                    code,
                    parent: parentCode,
                    customer: customers[i % customers.length],
                    team,
                    lm: lineManagers[Math.floor(Math.random() * lineManagers.length)],
                    sm: serviceManagers[Math.floor(Math.random() * serviceManagers.length)],
                    secondarySm: serviceManagers[Math.floor(Math.random() * serviceManagers.length)],
                    am: accountManagers[Math.floor(Math.random() * accountManagers.length)],
                    ad: accountDirectors[Math.floor(Math.random() * accountDirectors.length)],
                    pocName: pocNames[Math.floor(Math.random() * pocNames.length)],
                    pocEmail: pocNames[Math.floor(Math.random() * pocNames.length)].toLowerCase().replace(' ', '.') + '@customer.ae',
                    pocPhone: '+971-50-' + Math.floor(1000000 + Math.random() * 9000000),
                    secondaryPocName: '',
                    secondaryPocEmail: '',
                    secondaryPocPhone: '',
                    technicalPocName: '',
                    technicalPocEmail: '',
                    technicalPocPhone: '',
                    rnpsPoc: 'Primary',
                    oct: Math.round(oct),
                    nov: Math.round(nov),
                    dec: Math.round(dec),
                    avg: Math.round(avg),
                    type: isGroup ? 'Group' : 'Child',
                    isRevDrop,
                    isRevUpgrade,
                    requestStatus: status,
                    requestType: requestTypes[Math.floor(Math.random() * requestTypes.length)],
                    isApproved
                });
            }

            console.log('🎭 Generated', fakeAccounts.length, 'fake accounts');
            console.log('Groups:', fakeAccounts.filter(a => a.type === 'Group').length);
            console.log('Children:', fakeAccounts.filter(a => a.type === 'Child').length);
            console.log('Approved:', fakeAccounts.filter(a => a.isApproved).length);
            console.log('Pending:', fakeAccounts.filter(a => !a.isApproved).length);

            return fakeAccounts;
        }

        // ========================================
        // 🔐 MOCK AUTHENTICATION (LOCAL DEV MODE)
        // ========================================
        function mockAuthentication() {
            console.log('🔓 LOCAL DEV MODE: Bypassing SharePoint authentication');

            USER_CONTEXT.userEmail = DEV_USER_PROFILE.email;
            USER_CONTEXT.userName = DEV_USER_PROFILE.name;
            USER_CONTEXT.role = DEV_USER_PROFILE.role;
            USER_CONTEXT.isActive = true;
USER_CONTEXT.isAdmin = (DEV_USER_PROFILE.role === 'Admin');
USER_CONTEXT.isAM = (DEV_USER_PROFILE.role === 'Account Manager');
USER_CONTEXT.isAD = (DEV_USER_PROFILE.role === 'Account Director');
USER_CONTEXT.isLM = (DEV_USER_PROFILE.role === 'Line Manager');
USER_CONTEXT.isSM = (DEV_USER_PROFILE.role === 'Service Manager');
USER_CONTEXT.isReadOnly = (DEV_USER_PROFILE.role === 'Read Only');
USER_CONTEXT.isTSMManager = (DEV_USER_PROFILE.role === 'TSM Manager');
USER_CONTEXT.isTSMSEViewer = (DEV_USER_PROFILE.role === 'TSM_SE_Viewer');
            // For LM/SM roles, use manager name from config
            if (USER_CONTEXT.isLM || USER_CONTEXT.isSM) {
                USER_CONTEXT.userName = DEV_USER_PROFILE.managerName;
            }

            // Show admin-only nav items immediately
            if (USER_CONTEXT.isAdmin) {
           ['portalAnalyticsNavItem', 'transferRequestsNavItem', 'pendingRequestsNavItem'].forEach(function(id) {
                    var el = document.getElementById(id);
                    if (el) el.style.display = 'flex';
                });
                var lbl = document.getElementById('intelligenceNavLabel');
                if (lbl) lbl.style.display = 'block';
            }

            console.log('[✓] Mock user:', USER_CONTEXT.userName, '| Role:', USER_CONTEXT.role);
            return true;
        }

        // ===== ACCESS CONTROL =====
        function loadUserAccess(callback) {
            // ===== LOCAL DEV MODE: Skip SharePoint authentication =====
            if (LOCAL_DEV_MODE) {
                mockAuthentication();
                callback(true);
                return;
            }

            // ===== EXISTING SHAREPOINT AUTHENTICATION (UNCHANGED) =====
            const siteUrl = 'http://sharedspaces:8086/sites/SM';

            const xhrUser = new XMLHttpRequest();
            xhrUser.open('GET', siteUrl + '/_api/web/currentuser?$select=Title,Email', true);
            xhrUser.setRequestHeader('Accept', 'application/json;odata=verbose');
            xhrUser.onreadystatechange = function() {
                if (xhrUser.readyState !== 4) return;
                if (xhrUser.status >= 200 && xhrUser.status < 300) {
                    const currentUser = JSON.parse(xhrUser.responseText).d;
                    const userEmail = currentUser.Email;
                    const userName = currentUser.Title;

                    USER_CONTEXT.userEmail = userEmail;
                    USER_CONTEXT.userName = userName;

                    fetchAccessControl(userEmail, userName, callback);
                } else {
                    console.error('Error loading current user');
                    callback(false);
                }
            };
            xhrUser.send();
        }

        function fetchAccessControl(userEmail, userName, callback) {
            const siteUrl = 'http://sharedspaces:8086/sites/SM';
            const accessListTitle = 'Access_Control';

            const url = siteUrl +
                "/_api/web/lists/getbytitle('" + accessListTitle + "')/items" +
                "?$select=UserEmailID,Role,OCC_SEARCH&$top=5000";

            const xhr = new XMLHttpRequest();
            xhr.open('GET', url, true);
            xhr.setRequestHeader('Accept', 'application/json;odata=verbose');
            xhr.onreadystatechange = function() {
                if (xhr.readyState !== 4) return;
                if (xhr.status >= 200 && xhr.status < 300) {
                    const json = JSON.parse(xhr.responseText);
                    const allResults = (json.d && json.d.results) ? json.d.results : [];
                    const emailLower = (userEmail || '').toLowerCase();
                    const accessRecord = allResults.find(function(r) {
                        return (r.UserEmailID || '').toLowerCase() === emailLower;
                    });

                    if (accessRecord) {
                        // [OK] Found in Access_Control list
                        USER_CONTEXT.role = normalizeSmRole(accessRecord.Role || 'none');
                        USER_CONTEXT.isAM = (USER_CONTEXT.role === 'Account Manager');
                        USER_CONTEXT.isAD = (USER_CONTEXT.role === 'Account Director');
                        USER_CONTEXT.isAdmin = (USER_CONTEXT.role === 'Admin');
USER_CONTEXT.isReadOnly = (USER_CONTEXT.role === 'Read Only');
USER_CONTEXT.isTSMManager = (USER_CONTEXT.role === 'TSM Manager');
USER_CONTEXT.isTSMSEViewer = (USER_CONTEXT.role === 'TSM_SE_Viewer');
USER_CONTEXT.isPsdAdmin = (USER_CONTEXT.role === 'PSD_Admin');
USER_CONTEXT.isPsdAgent = (USER_CONTEXT.role === 'PSD_Agent');
USER_CONTEXT.occSearchEnabled = parseAccessControlYesNo(accessRecord.OCC_SEARCH);
USER_CONTEXT.isActive = true;

                        localStorage.setItem('sm_user_email', userEmail);
                        localStorage.setItem('sm_user_name', userName);
                        localStorage.setItem('sm_user_role', USER_CONTEXT.role);

                        // Show admin-only nav items immediately
                        if (USER_CONTEXT.isAdmin) {
                         ['portalAnalyticsNavItem', 'transferRequestsNavItem', 'pendingRequestsNavItem'].forEach(function(id) {
                                var el = document.getElementById(id);
                                if (el) el.style.display = 'flex';
                            });
                            var lbl = document.getElementById('intelligenceNavLabel');
                            if (lbl) lbl.style.display = 'block';
                        }

                        console.log('[✓] Access granted:', userEmail, '| Role:', USER_CONTEXT.role);
                        callback(true);
                    } else {
                        // [NOT] NOT in Access_Control - Check if they're LM or SM in accounts
                        console.log('Not in Access_Control, checking if LM/SM...');
                        checkIfLineOrServiceManager(userName, callback);
                    }
                } else {
                    console.error('Error loading access control:', xhr.status, xhr.statusText);
                    callback(false);
                }
            };
            xhr.send();
        }
        async function checkIfLineOrServiceManager(userName, callback) {
            try {
                // Fetch all accounts and check if userName is LM or SM
                const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                    "$select=Line_x0020_Manager/Title,Service_x0020_Manager/Title&" +
                    "$expand=Line_x0020_Manager,Service_x0020_Manager&" +
                    "$filter=(Request_x0020_Status eq 'OnBoarded')&" +
                    "$top=10000";

                const res = await fetch(url, {
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });

                if (!res.ok) {
                    console.error('Failed to check LM/SM status');
                    callback(false);
                    return;
                }

                const data = await res.json();
                const accounts = data.d.results;

                // Check if userName appears as Line Manager
                const isLM = accounts.some(a =>
                    a.Line_x0020_Manager && a.Line_x0020_Manager.Title === userName
                );

                // Check if userName appears as Service Manager
                const isSM = accounts.some(a =>
                    a.Service_x0020_Manager && a.Service_x0020_Manager.Title === userName
                );

                if (isLM || isSM) {
                    // [OK] They are LM or SM - give them access
               USER_CONTEXT.role = isLM ? 'Line Manager' : 'Service Manager';
USER_CONTEXT.isLM = isLM;
USER_CONTEXT.isSM = isSM;
USER_CONTEXT.isAdmin = false;
USER_CONTEXT.isAM = false;
USER_CONTEXT.isAD = false;
USER_CONTEXT.isReadOnly = false;
USER_CONTEXT.isTSMManager = false;
USER_CONTEXT.occSearchEnabled = false;
USER_CONTEXT.isActive = true;

                    localStorage.setItem('sm_user_email', USER_CONTEXT.userEmail);
                    localStorage.setItem('sm_user_name', userName);
                    localStorage.setItem('sm_user_role', USER_CONTEXT.role);

                    console.log('[✓] Access granted:', userName, '| Role:', USER_CONTEXT.role);
                    callback(true);
                } else {
                    // [NOT] Not LM/SM either - no access
                    console.log('[✗] User not found as LM/SM:', userName);
                    USER_CONTEXT.role = 'none';
                    USER_CONTEXT.isActive = false;
                    USER_CONTEXT.occSearchEnabled = false;
                    callback(false);
                }
            } catch (err) {
                console.error('Error checking LM/SM:', err);
                callback(false);
            }
        }

        function clearAccountSearch() {
            document.getElementById('searchAccountCode').value = '';
            document.getElementById('accountDetailsSection').style.display = 'none';
            document.getElementById('relatedAccountsSection').style.display = 'none';
            document.getElementById('relatedAccountsSearch').value = '';
            document.getElementById('searchErrorMessage').style.display = 'none'; // Hide error
        }
        // ===== LANDING PAGE FUNCTIONS =====
        function populateLandingPage() {
            // Existing user data population (unchanged)
            document.getElementById('landingUserName').textContent = USER_CONTEXT.userName || 'User';
            document.getElementById('landingUserEmail').textContent = USER_CONTEXT.userEmail || '';

            // Enhanced role badge with gradients and icons
            const roleEl = document.getElementById('landingUserRole');
            if (USER_CONTEXT.role === 'Admin') {
                roleEl.innerHTML = '<i data-lucide="crown" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Administrator';
                roleEl.style.background = 'linear-gradient(135deg, rgba(234, 179, 8, 0.2), rgba(251, 191, 36, 0.2))';
                roleEl.style.borderColor = 'rgba(234, 179, 8, 0.6)';
            } else if (USER_CONTEXT.role === 'Account Manager') {
                roleEl.innerHTML = '<i data-lucide="user" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Account Manager';
                roleEl.style.background = 'linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(96, 165, 250, 0.2))';
                roleEl.style.borderColor = 'rgba(59, 130, 246, 0.6)';
            } else if (USER_CONTEXT.role === 'Account Director') {
                roleEl.innerHTML = '<i data-lucide="briefcase" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Account Director';
                roleEl.style.background = 'linear-gradient(135deg, rgba(168, 85, 247, 0.2), rgba(139, 92, 246, 0.2))';
                roleEl.style.borderColor = 'rgba(168, 85, 247, 0.6)';
            } else if (USER_CONTEXT.role === 'Line Manager') {
                roleEl.innerHTML = '<i data-lucide="bar-chart-2" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Line Manager';
                roleEl.style.background = 'linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(5, 150, 105, 0.2))';
                roleEl.style.borderColor = 'rgba(16, 185, 129, 0.6)';
            } else if (USER_CONTEXT.role === 'Service Manager') {
                roleEl.innerHTML = '<i data-lucide="target" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Service Manager';
                roleEl.style.background = 'linear-gradient(135deg, rgba(245, 158, 11, 0.2), rgba(217, 119, 6, 0.2))';
                roleEl.style.borderColor = 'rgba(245, 158, 11, 0.6)';
            } 
            else if (USER_CONTEXT.role === 'TSM Manager') {
    roleEl.innerHTML = '<i data-lucide="shield" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>TSM Manager';
    roleEl.style.background = 'linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(8, 145, 178, 0.2))';
    roleEl.style.borderColor = 'rgba(6, 182, 212, 0.6)'; }
            else if (USER_CONTEXT.role === 'Read Only') {
    roleEl.innerHTML = '<i data-lucide="eye" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Read Only';
    roleEl.style.background = 'linear-gradient(135deg, rgba(99, 102, 241, 0.2), rgba(139, 92, 246, 0.2))';
    roleEl.style.borderColor = 'rgba(99, 102, 241, 0.6)';
} else if (USER_CONTEXT.role === 'TSM_SE_Viewer') {
    roleEl.innerHTML = '<i data-lucide="building-2" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>TSM SE Viewer';
    roleEl.style.background = 'linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(5, 150, 105, 0.2))';
    roleEl.style.borderColor = 'rgba(16, 185, 129, 0.6)';
}
 else if (USER_CONTEXT.role === 'Auditor') {
    roleEl.innerHTML = '<i data-lucide="clipboard-check" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Auditor';
    roleEl.style.background = 'linear-gradient(135deg, rgba(20, 184, 166, 0.2), rgba(13, 148, 136, 0.2))';
    roleEl.style.borderColor = 'rgba(20, 184, 166, 0.6)';
} else if (USER_CONTEXT.role === 'PSD_Admin') {
    roleEl.innerHTML = '<i data-lucide="clipboard-list" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>PSD Admin';
    roleEl.style.background = 'linear-gradient(135deg, rgba(124, 58, 237, 0.2), rgba(139, 92, 246, 0.2))';
    roleEl.style.borderColor = 'rgba(124, 58, 237, 0.6)';
} else if (USER_CONTEXT.role === 'PSD_Agent') {
    roleEl.innerHTML = '<i data-lucide="inbox" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>PSD Agent';
    roleEl.style.background = 'linear-gradient(135deg, rgba(8, 145, 178, 0.2), rgba(6, 182, 212, 0.2))';
    roleEl.style.borderColor = 'rgba(8, 145, 178, 0.6)';
}
else {
    roleEl.innerHTML = '<i data-lucide="user" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Guest User';
    roleEl.style.background = 'rgba(156, 163, 175, 0.2)';
    roleEl.style.borderColor = 'rgba(156, 163, 175, 0.6)';
}
            // Avatar initials
            document.getElementById('avatarPlaceholder').textContent = getUserInitials(USER_CONTEXT.userName);

            // Stats
            document.getElementById('landingLastVisit').textContent = getLastVisit();
            document.getElementById('landingVisitCount').textContent = getVisitCount().toString();

    // ===== DASHBOARD BUTTON (Role-based) =====
            const dashboardBtn = document.getElementById('goToDashboard');
            if (USER_CONTEXT.isActive && USER_CONTEXT.role !== 'none') {
                dashboardBtn.disabled = false;

                if (USER_CONTEXT.role === 'Admin') {
                    dashboardBtn.innerHTML = `
                        <span class="btn-icon"><i data-lucide="layout-dashboard" style="width: 20px; height: 20px;"></i></span>
                        <span class="btn-text">Launch Admin Dashboard</span>
                        <span class="btn-shine"></span>
                    `;
                } else if (USER_CONTEXT.role === 'Line Manager' || USER_CONTEXT.role === 'Service Manager') {
                    dashboardBtn.innerHTML = `
                        <span class="btn-icon"><i data-lucide="layout-dashboard" style="width: 20px; height: 20px;"></i></span>
                        <span class="btn-text">Launch My Dashboard</span>
                        <span class="btn-shine"></span>
                    `;
                } else if (USER_CONTEXT.role === 'TSM Manager') {
    dashboardBtn.innerHTML = `
        <span class="btn-icon"><i data-lucide="layout-dashboard" style="width: 20px; height: 20px;"></i></span>
        <span class="btn-text">Launch TSM Dashboard</span>
        <span class="btn-shine"></span>
    `;
                } else if (USER_CONTEXT.role === 'Auditor') {
    dashboardBtn.innerHTML = `
        <span class="btn-icon"><i data-lucide="shield-check" style="width: 20px; height: 20px;"></i></span>
        <span class="btn-text">Launch QA Dashboard</span>
        <span class="btn-shine"></span>
    `;
                } else if (USER_CONTEXT.role === 'Read Only') {
                    dashboardBtn.innerHTML = `
                        <span class="btn-icon"><i data-lucide="eye" style="width: 20px; height: 20px;"></i></span>
                        <span class="btn-text">Launch Dashboard</span>
                        <span class="btn-shine"></span>
                    `;
                }else if (USER_CONTEXT.role === 'TSM_SE_Viewer') {
    dashboardBtn.innerHTML = `
        <span class="btn-icon"><i data-lucide="building-2" style="width: 20px; height: 20px;"></i></span>
        <span class="btn-text">Launch TSM SE Dashboard</span>
        <span class="btn-shine"></span>
    `;
} else if (USER_CONTEXT.role === 'PSD_Admin') {
                    dashboardBtn.innerHTML = `
                        <span class="btn-icon"><i data-lucide="clipboard-list" style="width: 20px; height: 20px;"></i></span>
                        <span class="btn-text">Launch PSD Assignment</span>
                        <span class="btn-shine"></span>
                    `;
                } else if (USER_CONTEXT.role === 'PSD_Agent') {
                    dashboardBtn.innerHTML = `
                        <span class="btn-icon"><i data-lucide="inbox" style="width: 20px; height: 20px;"></i></span>
                        <span class="btn-text">Launch My PSD Queue</span>
                        <span class="btn-shine"></span>
                    `;
                }
                else if (USER_CONTEXT.role === 'Account Manager' || USER_CONTEXT.role === 'Account Director') {
                    dashboardBtn.innerHTML = `
                        <span class="btn-icon"><i data-lucide="bar-chart-3" style="width: 20px; height: 20px;"></i></span>
                        <span class="btn-text">Launch My Dashboard</span>
                        <span class="btn-shine"></span>
                    `;
                }

                document.getElementById('accessNote').innerHTML = `
                    <i data-lucide="check-circle" style="width: 14px; height: 14px;"></i>
                    <span>Access granted - Click above to proceed</span>
                `;

                dashboardBtn.onclick = function() {
       if (USER_CONTEXT.role === 'Admin' || USER_CONTEXT.role === 'Line Manager' || USER_CONTEXT.role === 'Service Manager' || USER_CONTEXT.role === 'Read Only' || USER_CONTEXT.role === 'TSM Manager' || USER_CONTEXT.role === 'Auditor' || USER_CONTEXT.role === 'TSM_SE_Viewer' || isPsdRole(USER_CONTEXT.role)) {
    showDashboard();
}

else if (USER_CONTEXT.role === 'Account Manager' || USER_CONTEXT.role === 'Account Director') {
                        showAMDashboard();
                    }
                };

            } else {
                // NO ACCESS TO DASHBOARD
                dashboardBtn.disabled = true;
                dashboardBtn.style.opacity = '0.6';
                dashboardBtn.style.cursor = 'not-allowed';
                dashboardBtn.innerHTML = `
                    <span class="btn-icon"><i data-lucide="layout-dashboard" style="width: 20px; height: 20px;"></i></span>
                    <span class="btn-text">Launch Dashboard</span>
                    <span class="btn-shine"></span>
                `;
                document.getElementById('accessNote').innerHTML = `
                    <i data-lucide="lock" style="width: 14px; height: 14px; color: #ef4444;"></i>
                    <span style="color: #ef4444; font-weight: 600;">Access Denied - Dashboard Not Available</span>
                    <br>
                    <span style="font-size: 12px; color: var(--text-secondary); margin-top: 4px; display: inline-block;">You do not have permission to access the dashboard. Please contact your administrator to request access.</span>
                `;
            }

            // ===== ACCOUNT SEARCH BUTTON (ALWAYS ENABLED FOR ALL USERS) =====
            const searchBtn = document.getElementById('goToAccountSearch');
            if (searchBtn) {
                searchBtn.disabled = false; // ✅ ALWAYS ENABLED
                searchBtn.onclick = function() {
                    showAccountSearch();
                };
            }

            // ===== OCC SEARCH BUTTON (Access_Control.OCC_SEARCH = Yes) =====
            const occBtn = document.getElementById('goToOccSearch');
            if (occBtn) {
                var occEnabled = parseAccessControlYesNo(USER_CONTEXT.occSearchEnabled) || USER_CONTEXT.occSearchEnabled === true;
                occBtn.style.display = occEnabled ? 'flex' : 'none';
                occBtn.disabled = !occEnabled;
                occBtn.onclick = function() {
                    window.location.href = 'http://sharedspaces:8081/sites/OCC/SitePages/occ-search.aspx';
                };
            }

// Reinitialize Lucide icons
            if (typeof lucide !== 'undefined') {
                lucide.createIcons();
            }

            // System status indicator animation
            const systemStatus = document.getElementById('systemStatus');
            if (systemStatus) {
                setTimeout(() => {
                    systemStatus.textContent = 'All Systems Online';
                }, 1500);
            }

            // Show owner panel only for tehleel & ubaid
            var ownerPanel = document.getElementById('ownerViewAsPanel');
            if (ownerPanel) {
                var ownerEmails = ['tehleel.lone@du.ae', 'ubaid.mir@du.ae'];
                ownerPanel.style.display = ownerEmails.includes(
                    (USER_CONTEXT.userEmail || '').toLowerCase()
                ) ? 'block' : 'none';
            }
        }

        function showDashboard() {
            const landing = document.getElementById('landingPage');
            if (landing) {
                landing.style.display = 'none';
                landing.style.visibility = 'hidden';
                landing.style.position = 'absolute';
                landing.style.zIndex = '-9999';
            }

            document.getElementById('dashboardContent').style.display = 'block';

            // Auditor: jump straight to QA view
            if (USER_CONTEXT.role === 'Auditor') {
                setTimeout(function() {
                    switchDashboardSection('qaView');
                    if (typeof qaInit === 'function') qaInit();
                }, 150);
            }

            // PSD roles: jump straight to PSD Assignment
            if (isPsdRole(USER_CONTEXT.role)) {
                setTimeout(function() {
                    switchDashboardSection('psdView');
                }, 150);
            }

            const loading = document.getElementById('loading');
            const content = document.getElementById('content');
            if (loading) loading.style.display = 'none';
            if (content) content.style.display = 'block';
            // Re-initialize icons and ensure nav is visible
            setTimeout(() => {
                // Only show nav items appropriate to role
                // Pending/Transfer nav items are hidden by default (style="display:none") 
                // and only shown for Admin in init()
                if (typeof lucide !== 'undefined') {
                    lucide.createIcons();
                }
            }, 100);
            incrementVisitCount();
            saveLastVisit();
        }
        // ========================================
        // AM/AD VIEW FUNCTIONS
        // ========================================


        function initLandingPage() {
            // Button click handler is now set in populateLandingPage()
            // This function can remain empty or be removed
        }

        function initPsdShell() {
            try {
                setCurrentDate();
                var loading = document.getElementById('loading');
                var content = document.getElementById('content');
                if (loading) loading.style.display = 'none';
                if (content) content.style.display = 'block';

                var psdNav = document.getElementById('psdNavItem');
                if (psdNav) psdNav.style.display = 'flex';
                applyPsdNavRestrictions();

                if (typeof lucide !== 'undefined') lucide.createIcons();
                if (typeof analyticsStartSession === 'function') analyticsStartSession();
                if (typeof nlCheckOnLoad === 'function') nlCheckOnLoad();
            } catch (err) {
                console.error('PSD shell init error:', err);
            }
        }

        async function init() {
            try {
                console.log('Loading SharePoint...');
                await fetchDOJData();
                await fetchAccountMapping();
                await fetchRevenueEnabled();
          ALL_DATA = await fetchData();
window.ALL_DATA = ALL_DATA;
recalculateRevFlags();

                // Filter data by role on initial load
                if (USER_CONTEXT.role === 'Line Manager') {
                    // ✅ STEP 1: Get ALL accounts first (groups + children)
                    window.ALL_FILTERED = ALL_DATA.filter(a => a.lm === USER_CONTEXT.userName);
                    // ✅ STEP 2: Then filter to groups only for stats
                    filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
                } else if (USER_CONTEXT.role === 'Service Manager') {
                    // ✅ STEP 1: Get ALL accounts first (groups + children)
                    window.ALL_FILTERED = ALL_DATA.filter(a => a.sm === USER_CONTEXT.userName);
                    // ✅ STEP 2: Then filter to groups only for stats
                    filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
                } else if (USER_CONTEXT.isTSMManager) {
    window.ALL_FILTERED = ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE');
    filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
} else {
    // Admin
    window.ALL_FILTERED = ALL_DATA.slice();
    filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
}

              setCurrentDate();
populateFilters({ preserve: false });
updateServiceManagerFilter();
applyFilters();
if (!window._smMsDropdownClickBound) {
    window._smMsDropdownClickBound = true;
    document.addEventListener('click', function (event) {
        document.querySelectorAll('.custom-multiselect').forEach(function (ms) {
            if (!ms.contains(event.target)) {
                var dd = ms.querySelector('.multiselect-dropdown');
                if (dd) dd.style.display = 'none';
            }
        });
    });
}
// Check for overdue transfers (Admin only, runs once on load)

                document.getElementById('loading').style.display = 'none';
                document.getElementById('content').style.display = 'block';
                // [OK] REMOVED THE LINE THAT WAS SHOWING DASHBOARD
                // Show landing page on initial load
                // document.getElementById('landingPage').style.display = 'grid';
                // [OK] Show Account Requests button only for Admin
                if (USER_CONTEXT.isAdmin) {
                    const btn = document.getElementById('accountRequestsBtn');
                    if (btn) btn.style.display = 'block';
                }
                // Initialize Lucide icons
                if (typeof lucide !== 'undefined') {
                    lucide.createIcons();
                }
                // Show Transfer Requests only for Admin
                if (USER_CONTEXT.isAdmin) {
                    const transferNavItem = document.getElementById('transferRequestsNavItem');
                    if (transferNavItem) transferNavItem.style.display = 'flex';
                }
                // Show Pending Requests only for Admin
                if (USER_CONTEXT.isAdmin) {
                    const pendingNavItem = document.getElementById('pendingRequestsNavItem');
                    if (pendingNavItem) pendingNavItem.style.display = 'flex';
                }
               if (USER_CONTEXT.isAdmin || USER_CONTEXT.role === 'Service Director' || USER_CONTEXT.isTSMManager) {
    var ehNav = document.getElementById('executiveHubNavItem');
    if (ehNav) ehNav.style.display = 'flex';
}
                // Show Portal Intelligence only for Admin/SD
                if (USER_CONTEXT.isAdmin || USER_CONTEXT.role === 'Admin') {
                    const analyticsNav = document.getElementById('portalAnalyticsNavItem');
                    const analyticsLabel = document.getElementById('intelligenceNavLabel');
                    if (analyticsNav) analyticsNav.style.display = 'flex';
                    if (analyticsLabel) analyticsLabel.style.display = 'block';
                }
         if (USER_CONTEXT.isAdmin || USER_CONTEXT.role === 'Service Director' || USER_CONTEXT.role === 'Auditor' || USER_CONTEXT.isTSMManager || USER_CONTEXT.role === 'Line Manager' || USER_CONTEXT.role === 'Service Manager') {
    var qaNav = document.getElementById('qaNavItem');
    if (qaNav) qaNav.style.display = 'flex';
}

if (USER_CONTEXT.isAdmin || USER_CONTEXT.role === 'Service Director' || USER_CONTEXT.role === 'Auditor' || USER_CONTEXT.isTSMManager || USER_CONTEXT.role === 'Line Manager' || USER_CONTEXT.role === 'Service Manager' || USER_CONTEXT.role === 'Read Only' || USER_CONTEXT.role === 'TSM_SE_Viewer') {
    var tsmPendingNav = document.getElementById('tsmPendingNavItem');
    if (tsmPendingNav) tsmPendingNav.style.display = 'flex';
}

var rnpsNav = document.getElementById('rnpsNavItem');
if (rnpsNav) rnpsNav.style.display = 'flex';

                // PSD Assignment: visible to SM Admin, PSD_Admin and PSD_Agent only
                if (USER_CONTEXT.isAdmin || USER_CONTEXT.role === 'PSD_Admin' || USER_CONTEXT.role === 'PSD_Agent') {
                    var psdNav = document.getElementById('psdNavItem');
                    if (psdNav) psdNav.style.display = 'flex';
                }
                // PSD_Admin / PSD_Agent: restrict the sidebar to an allowed set of menus
                if (isPsdRole(USER_CONTEXT.role)) {
                    applyPsdNavRestrictions();
                }

                // Auditor: hide all nav items except allowed ones
                if (USER_CONTEXT.role === 'Auditor') {
                    var allowedSections = ['qaView', 'suggestionsView', 'processDocsView', 'contactUsView', 'tsmPendingView'];
                    document.querySelectorAll('.nav-item').forEach(function(item) {
                        var section = item.getAttribute('data-section');
                        var labelEl = item.querySelector('.nav-label');
                        var isNewsletter = labelEl && labelEl.textContent.trim() === 'Newsletter';
                        if (!allowedSections.includes(section) && !isNewsletter) {
                            item.style.display = 'none';
                        }
                    });
                    document.querySelectorAll('.nav-section-label').forEach(function(lbl) {
                        lbl.style.display = 'none';
                    });
                }
 // TSM Manager: hide Project Management
                if (USER_CONTEXT.isTSMManager) {
                    var projNav = document.getElementById('projectMgmtNavItem');
                    if (projNav) projNav.style.display = 'none';
                }

                // TSM SE Viewer: no export
                if (USER_CONTEXT.isTSMSEViewer) {
                    var exportBtn = document.getElementById('smExportExcelBtn');
                    if (exportBtn) exportBtn.style.display = 'none';
                }

                // Start session tracking
                if (typeof analyticsStartSession === 'function') {
                    analyticsStartSession();
                }

                if (typeof nlCheckOnLoad === 'function') {
                    nlCheckOnLoad();
                }
            } catch (err) {
                console.error('Error:', err);
                document.getElementById('loading').innerHTML = '<div style="color:#ef4444;">Error: ' + err.message + '</div>';
            }


        }

  const SM_MS_FILTER_CFG = {
            LM: { dropdown: 'filterLMDropdown', text: 'filterLMSelectedText', label: 'Line Managers' },
            SM: { dropdown: 'filterSMDropdown', text: 'filterSMSelectedText', label: 'Service Managers' },
            Team: { dropdown: 'filterTeamDropdown', text: 'filterTeamSelectedText', label: 'Teams' },
            RevDrop: { dropdown: 'filterRevDropDropdown', text: 'filterRevDropSelectedText', label: 'Accounts' },
            Structure: { dropdown: 'filterStructureDropdown', text: 'filterStructureSelectedText', label: 'Accounts' }
        };

        function smGetMsFilterValues(key) {
            const cfg = SM_MS_FILTER_CFG[key];
            if (!cfg) return [];
            return Array.from(document.querySelectorAll('#' + cfg.dropdown + ' input[type="checkbox"]:checked')).map(function (cb) { return cb.value; });
        }

        function smToggleMsDropdown(dropdownId) {
            document.querySelectorAll('.multiselect-dropdown').forEach(function (d) {
                if (d.id !== dropdownId) d.style.display = 'none';
            });
            const dropdown = document.getElementById(dropdownId);
            if (dropdown) dropdown.style.display = dropdown.style.display === 'none' ? 'block' : 'none';
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }
        window.smToggleMsDropdown = smToggleMsDropdown;

        function smUpdateMsFilterDisplay(key, label) {
            const cfg = SM_MS_FILTER_CFG[key];
            if (!cfg) return;
            const selected = smGetMsFilterValues(key);
            const displayText = selected.length === 0 ? 'All ' + label :
                selected.length === 1 ? selected[0] :
                selected.length + ' selected';
            const textEl = document.getElementById(cfg.text);
            if (textEl) textEl.textContent = displayText;
        }

        function smUpdateLegacySelect(selectId, values, selectedValues) {
            var el = document.getElementById(selectId);
            if (!el) return;
            el.innerHTML = '<option value=""></option>';
            values.forEach(function (v) {
                var opt = document.createElement('option');
                opt.value = v;
                opt.textContent = v;
                el.appendChild(opt);
            });
            if (selectedValues && selectedValues.length === 1) el.value = selectedValues[0];
            else el.value = '';
        }

        function smSyncLegacyFilterSelects(fireTeamChange) {
            var teamSel = smGetMsFilterValues('Team');
            var lmSel = smGetMsFilterValues('LM');
            var smSel = smGetMsFilterValues('SM');
            var teamEl = document.getElementById('filterTeam');
            var lmEl = document.getElementById('filterLM');
            var smEl = document.getElementById('filterSM');
            if (lmEl) lmEl.value = lmSel.length === 1 ? lmSel[0] : '';
            if (smEl) smEl.value = smSel.length === 1 ? smSel[0] : '';
            if (!teamEl) return;
            var newTeam = teamSel.length === 1 ? teamSel[0] : '';
            var teamChanged = teamEl.value !== newTeam;
            teamEl.value = newTeam;
            if (!fireTeamChange || window._smSyncingLegacyTeam) return;
            window._smSyncingLegacyTeam = true;
            try {
                // tsm-se.js listens to the native #filterTeam change event — dispatch it
                if (teamChanged) {
                    teamEl.dispatchEvent(new Event('change', { bubbles: true }));
                } else {
                    onTeamFilterChanged();
                }
            } finally {
                window._smSyncingLegacyTeam = false;
            }
        }

        function smOnMsFilterChange(key, label, applyNow) {
            smUpdateMsFilterDisplay(key, label);
            if (key === 'Team') {
                selectedLM = null;
                selectedSM = null;
                var perfSec = document.getElementById('smPerformanceSection');
                if (perfSec) perfSec.style.display = 'none';
                smSyncLegacyFilterSelects(true);
                return;
            }
            if (applyNow !== false) applyFilters();
        }
        window.smOnMsFilterChange = smOnMsFilterChange;

        function smBuildMsDropdown(key, values, savedValues) {
            const cfg = SM_MS_FILTER_CFG[key];
            if (!cfg) return;
            const dropdown = document.getElementById(cfg.dropdown);
            if (!dropdown) return;
            dropdown.innerHTML = '';
            values.forEach(function (val) {
                const div = document.createElement('div');
                div.className = 'multiselect-option';
                const safeId = key + '_' + String(val).replace(/\s+/g, '_').replace(/[^a-zA-Z0-9_]/g, '');
                const checked = savedValues && savedValues.includes(val) ? ' checked' : '';
                div.innerHTML = '<input type="checkbox" id="' + safeId + '" value="' + String(val).replace(/"/g, '&quot;') + '"' + checked + ' onchange="smOnMsFilterChange(\'' + key + '\',\'' + cfg.label + '\',true)">' +
                    '<label for="' + safeId + '">' + val + '</label>';
                dropdown.appendChild(div);
            });
            smUpdateMsFilterDisplay(key, cfg.label);
        }

        function smClearMsDropdown(key) {
            const cfg = SM_MS_FILTER_CFG[key];
            if (!cfg) return;
            document.querySelectorAll('#' + cfg.dropdown + ' input[type="checkbox"]').forEach(function (cb) { cb.checked = false; });
            smUpdateMsFilterDisplay(key, cfg.label);
        }

        function smSetMsDropdownDisabled(key, disabled) {
            const cfg = SM_MS_FILTER_CFG[key];
            if (!cfg) return;
            document.querySelectorAll('#' + cfg.dropdown + ' input[type="checkbox"]').forEach(function (cb) { cb.disabled = disabled; });
            const wrap = document.getElementById(cfg.dropdown);
            if (wrap && wrap.parentElement) {
                const sel = wrap.parentElement.querySelector('.multiselect-selected');
                if (sel) {
                    sel.style.opacity = disabled ? '0.6' : '1';
                    sel.style.pointerEvents = disabled ? 'none' : 'auto';
                }
            }
        }

  function populateFilters(options) {
    options = options || {};
    const preserve = options.preserve !== false;
    const savedLM = preserve ? smGetMsFilterValues('LM') : [];
    const savedSM = preserve ? smGetMsFilterValues('SM') : [];
    const savedTeam = preserve ? smGetMsFilterValues('Team') : [];
console.log('[populateFilters] role:', USER_CONTEXT.role, '| isTSMManager:', USER_CONTEXT.isTSMManager);
    const role = USER_CONTEXT.role;
            const userName = USER_CONTEXT.userName;

           let filteredData = ALL_DATA;

// Role-based data filtering
if (role === 'Line Manager') {
    filteredData = ALL_DATA.filter(a => a.lm === userName);
} else if (role === 'Service Manager') {
    filteredData = ALL_DATA.filter(a => a.sm === userName);
} else if (USER_CONTEXT.isTSMManager) {
    filteredData = ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE');
}
            if (role === 'Line Manager') {
                smBuildMsDropdown('LM', [userName], [userName]);
                smSetMsDropdownDisabled('LM', true);
            } else if (role === 'Service Manager') {
                const myLM = ALL_DATA.find(a => a.sm === userName)?.lm;
                smBuildMsDropdown('LM', myLM ? [myLM] : [], myLM ? [myLM] : []);
                smSetMsDropdownDisabled('LM', true);
            } else {
    const sourceData = USER_CONTEXT.isTSMManager
        ? ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE')
        : ALL_DATA;
    const lms = [...new Set(sourceData.map(a => a.lm))].filter(Boolean).sort();
    smBuildMsDropdown('LM', lms, preserve ? savedLM.filter(v => lms.includes(v)) : []);
    smSetMsDropdownDisabled('LM', false);
}

            if (role === 'Service Manager') {
                smBuildMsDropdown('SM', [userName], [userName]);
                smSetMsDropdownDisabled('SM', true);
            } else if (role === 'Line Manager') {
                const mySMs = [...new Set(filteredData.map(a => a.sm))].filter(Boolean).sort();
                smBuildMsDropdown('SM', mySMs, preserve ? savedSM.filter(v => mySMs.includes(v)) : []);
                smSetMsDropdownDisabled('SM', false);
            } else {
    const sourceData = USER_CONTEXT.isTSMManager
        ? ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE')
        : ALL_DATA;
    const sms = [...new Set(sourceData.map(a => a.sm))].filter(Boolean).sort();
    smBuildMsDropdown('SM', sms, preserve ? savedSM.filter(v => sms.includes(v)) : []);
    smSetMsDropdownDisabled('SM', false);
}
            const teams = [...new Set(filteredData.map(a => a.team))].filter(Boolean).sort();
            smBuildMsDropdown('Team', teams, preserve ? savedTeam.filter(v => teams.includes(v)) : []);
            smSetMsDropdownDisabled('Team', false);
            smUpdateLegacySelect('filterLM', role === 'Line Manager' ? [userName] : (role === 'Service Manager' ? (ALL_DATA.find(a => a.sm === userName)?.lm ? [ALL_DATA.find(a => a.sm === userName).lm] : []) : [...new Set((USER_CONTEXT.isTSMManager ? ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE') : ALL_DATA).map(a => a.lm))].filter(Boolean).sort()), smGetMsFilterValues('LM'));
            smUpdateLegacySelect('filterSM', role === 'Service Manager' ? [userName] : [...new Set(filteredData.map(a => a.sm))].filter(Boolean).sort(), smGetMsFilterValues('SM'));
            smUpdateLegacySelect('filterTeam', teams, smGetMsFilterValues('Team'));
            smSyncLegacyFilterSelects(false);
        }

        function updateServiceManagerFilter() {
            const role = USER_CONTEXT.role;
            const userName = USER_CONTEXT.userName;

            if (role === 'Line Manager' || role === 'Service Manager') {
                return;
            }

            const lmFilter = smGetMsFilterValues('LM');
            const teamFilter = smGetMsFilterValues('Team');
            let sms, lms;

            if (teamFilter.length) {
                const teamData = ALL_DATA.filter(a => teamFilter.includes(a.team));
                lms = [...new Set(teamData.map(a => a.lm))].filter(Boolean).sort();

                if (lmFilter.length) {
                    sms = [...new Set(teamData.filter(a => lmFilter.includes(a.lm)).map(a => a.sm))].filter(Boolean).sort();
                } else {
                    sms = [...new Set(teamData.map(a => a.sm))].filter(Boolean).sort();
                }
            } else {
    const baseData = USER_CONTEXT.isTSMManager
        ? ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE')
        : ALL_DATA;

    lms = [...new Set(baseData.map(a => a.lm))].filter(Boolean).sort();

    if (lmFilter.length) {
        sms = [...new Set(baseData.filter(a => lmFilter.includes(a.lm)).map(a => a.sm))].filter(Boolean).sort();
    } else {
        sms = [...new Set(baseData.map(a => a.sm))].filter(Boolean).sort();
    }
}

            const currentLM = smGetMsFilterValues('LM');
            if (!document.querySelector('#filterLMDropdown input[disabled]')) {
                smBuildMsDropdown('LM', lms, currentLM.filter(v => lms.includes(v)));
            }

            const currentSM = smGetMsFilterValues('SM');
            if (!document.querySelector('#filterSMDropdown input[disabled]')) {
                smBuildMsDropdown('SM', sms, currentSM.filter(v => sms.includes(v)));
            }
        }

        function onTeamFilterChanged() {
            selectedLM = null;
            selectedSM = null;
            var teamEl = document.getElementById('filterTeam');
            var wasTsmSe = !!window.TSM_SE_ACTIVE;
            window.TSM_SE_ACTIVE = !!(teamEl && teamEl.value === 'TSM_SE');
            if (wasTsmSe && !window.TSM_SE_ACTIVE) {
                smExitTsmSeMode();
            }
            var perfSec = document.getElementById('smPerformanceSection');
            if (perfSec) perfSec.style.display = 'none';
            applyFilters();
        }

        function smDestroyTsmSeGridOnly() {
            ['tsmSeDestroy', 'tsmSeReset', 'tsmSeClear', 'tsmSeRestoreMainGrid', 'tsmSeHide'].forEach(function (name) {
                if (typeof window[name] === 'function') {
                    try { window[name](); } catch (e) { console.warn('[smDestroyTsmSeGridOnly]', name, e); }
                }
            });
            if (window.tsmSeGridApi) {
                try { window.tsmSeGridApi.destroy(); } catch (e) {}
                window.tsmSeGridApi = null;
            }
            var gridDiv = document.getElementById('myGrid');
            if (gridDiv) gridDiv.innerHTML = '';
            if (agGridApi) {
                try {
                    agGridApi.setFilterModel(null);
                    agGridApi.setGridOption('quickFilterText', '');
                    agGridApi.destroy();
                } catch (e) {}
                agGridApi = null;
            }
        }

        function smExitTsmSeMode() {
            window.TSM_SE_ACTIVE = false;
            smDestroyTsmSeGridOnly();
            window.PARENT_ROWS = null;
            window.CURRENT_TABLE_DATA = null;
        }

        function resetFilters() {
            smClearMsDropdown('LM');
            smClearMsDropdown('SM');
            smClearMsDropdown('Team');
            smClearMsDropdown('RevDrop');
            smClearMsDropdown('Structure');
            var tfEl = document.getElementById('transferStatusFilterMain');
            if (tfEl) tfEl.value = '';
            var searchEl = document.getElementById('agGridSearch');
            if (searchEl) searchEl.value = '';
            window._TRANSFER_FILTER_VAL = '';
            selectedLM = null;
            selectedSM = null;
            document.getElementById('smPerformanceSection').style.display = 'none';

            smExitTsmSeMode();
            populateFilters({ preserve: false });

            var teamEl = document.getElementById('filterTeam');
            var lmEl = document.getElementById('filterLM');
            var smEl = document.getElementById('filterSM');
            if (lmEl) lmEl.value = '';
            if (smEl) smEl.value = '';
            if (teamEl) {
                teamEl.value = '';
                try {
                    teamEl.dispatchEvent(new Event('change', { bubbles: true }));
                } catch (e) {
                    onTeamFilterChanged();
                }
            } else {
                applyFilters();
            }
        }

        function applyFilters() {
            smSyncLegacyFilterSelects(false);
            updateServiceManagerFilter();
            const lmSel = smGetMsFilterValues('LM');
            const smSel = smGetMsFilterValues('SM');
            const teamSel = smGetMsFilterValues('Team');
            const revDropSel = smGetMsFilterValues('RevDrop');
            const structureSel = smGetMsFilterValues('Structure');

            window.TSM_SE_ACTIVE = teamSel.length === 1 && teamSel[0] === 'TSM_SE';

            // First filter ALL data based on criteria
           const allFiltered = ALL_DATA.filter(a => {
    if (USER_CONTEXT.role === 'Line Manager') {
        if (a.lm !== USER_CONTEXT.userName) return false;
    } else if (USER_CONTEXT.role === 'Service Manager') {
        if (a.sm !== USER_CONTEXT.userName) return false;
    } else if (USER_CONTEXT.isTSMManager) {
        if (a.team !== 'TSM_ME' && a.team !== 'TSM_SE') return false;
    }
                if (selectedLM && a.lm !== selectedLM) return false;
                if (selectedSM && a.sm !== selectedSM) return false;
                if (lmSel.length && !lmSel.includes(a.lm)) return false;
                if (smSel.length && !smSel.includes(a.sm)) return false;
                if (teamSel.length && !teamSel.includes(a.team)) return false;
                if (revDropSel.length) {
                    const revMatch = (revDropSel.includes('drop') && a.isRevDrop) ||
                        (revDropSel.includes('upgrade') && a.isRevUpgrade);
                    if (!revMatch) return false;
                }
                return true;
            });

            // PARENTS ONLY for stats, charts, tiles
            filtered = allFiltered.filter(a => a.type === 'Group');

            // ✅ NEW: Apply structure filter (Groups with/without children)
            if (structureSel.length) {
                filtered = filtered.filter(parent => {
                    const hasChildren = allFiltered.some(child =>
                        child.type === 'Child' &&
                        child.parent === parent.code &&
                        child.code !== parent.code
                    );
                    const wantWith = structureSel.includes('with-children');
                    const wantStandalone = structureSel.includes('standalone');
                    if (wantWith && wantStandalone) return true;
                    if (wantWith) return hasChildren;
                    if (wantStandalone) return !hasChildren;
                    return true;
                });
            }

            // Store all filtered (including children) for table expand
            window.ALL_FILTERED = allFiltered;
            // ✅ ADD THIS
            console.log('[applyFilters] window.ALL_FILTERED now has:', window.ALL_FILTERED.length, 'accounts');
            console.log('[applyFilters] Groups:', window.ALL_FILTERED.filter(a => a.type === 'Group').length);
            console.log('[applyFilters] Children:', window.ALL_FILTERED.filter(a => a.type === 'Child').length);
            updateStats();
            renderLineManagers();
            renderServiceManagers();
            renderCharts();
            if (window.TSM_SE_ACTIVE && typeof window.tsmSeRenderTable === 'function') {
                window.tsmSeRenderTable();
            } else {
                renderTable();
            }
        }

     async function selectLM(name) {
    selectedLM = selectedLM === name ? null : name;
    selectedSM = null;

    if (selectedLM) {
        document.getElementById('smPerformanceSection').style.display = 'block';
    } else {
        document.getElementById('smPerformanceSection').style.display = 'none';
    }

    updateServiceManagerFilter();
    applyFilters();
    await renderServiceManagers(); // ← add this
}

        function selectSM(name) {
            selectedSM = selectedSM === name ? null : name;
            applyFilters();
        }

        function formatCurrency(v) {
            if (v === null || v === undefined || isNaN(v)) return '0';
            if (v >= 1000000) return (v / 1000000).toFixed(2) + 'M';
            if (v >= 1000) return (v / 1000).toFixed(1) + 'K';
            return v.toFixed(0);
        }

        function updateStats() {
            const total = filtered.length;
            const totalRev = filtered.reduce((sum, a) => sum + a.avg, 0);
            const groups = filtered.filter(a => a.type === 'Group').length;

            // ✅ FIX: Count children from ALL_FILTERED (not filtered which only has Groups)
            const allFiltered = window.ALL_FILTERED || [];
            const children = allFiltered.filter(a => a.type === 'Child').length;

            // ✅ FIX: Display total as Groups + Children
            const displayTotal = groups + children;

            // [NEW] Get dynamic last 3 months
            const lastThree = getLastThreeCompletedMonths();
            const month1Key = lastThree[0].key;
            const month2Key = lastThree[1].key;
            const month3Key = lastThree[2].key;

            const month1 = filtered.reduce((sum, a) => sum + (a[month1Key] || 0), 0);
            const month2 = filtered.reduce((sum, a) => sum + (a[month2Key] || 0), 0);
            const month3 = filtered.reduce((sum, a) => sum + (a[month3Key] || 0), 0);

            const growth = month2 > 0 ? ((month3 - month2) / month2 * 100).toFixed(1) : 0;

            document.getElementById('stats').innerHTML = `
    <div class="stat-card">
        <div class="stat-label">Total Accounts</div>
        <div class="stat-value">${displayTotal}</div>
        <div class="stat-subtitle">Active Accounts</div>
        <div class="stat-breakdown">
            <div class="breakdown-item">
                <div class="breakdown-label">Groups</div>
                <div class="breakdown-value">${groups}</div>
            </div>
            <div class="breakdown-item">
                <div class="breakdown-label">Children</div>
                <div class="breakdown-value">${children}</div>
            </div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Total Revenue</div>
        <div class="stat-value">${formatCurrency(totalRev)}</div>
        <div class="stat-subtitle">Monthly Average</div>
        <div class="stat-breakdown">
            <div class="breakdown-item">
                <div class="breakdown-label">Growth</div>
                <div class="breakdown-value" style="color:${growth >= 0 ? '#10b981' : '#ef4444'}">${growth}%</div>
            </div>
            <div class="breakdown-item">
                <div class="breakdown-label">Per Account</div>
                <div class="breakdown-value">${total > 0 ? formatCurrency(totalRev/total) : 0}</div>
            </div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-label">${lastThree[2].label} Revenue</div>
        <div class="stat-value">${formatCurrency(month3)}</div>
        <div class="stat-subtitle">Latest Month</div>
        <div class="stat-breakdown">
            <div class="breakdown-item">
                <div class="breakdown-label">${lastThree[1].label}</div>
                <div class="breakdown-value">${formatCurrency(month2)}</div>
            </div>
            <div class="breakdown-item">
                <div class="breakdown-label">${lastThree[0].label}</div>
                <div class="breakdown-value">${formatCurrency(month1)}</div>
            </div>
        </div>
    </div>
                <div class="stat-card">
                    <div class="stat-label">Line Managers</div>
                    <div class="stat-value">${new Set(filtered.map(a => a.lm).filter(Boolean)).size}</div>
                    <div class="stat-subtitle">Active Managers</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">Service Managers</div>
                    <div class="stat-value">${new Set(filtered.map(a => a.sm).filter(Boolean)).size}</div>
                    <div class="stat-subtitle">Active Managers</div>
                </div>
            `;
        }

async function renderLineManagers() {
    const scopeGroups = filtered.filter(a => a.type === 'Group');
    let lms = [...new Set(scopeGroups.map(a => a.lm))].filter(Boolean);

    if (USER_CONTEXT.role === 'Line Manager') {
        lms = lms.filter(lm => lm === USER_CONTEXT.userName);
    } else if (USER_CONTEXT.role === 'Service Manager') {
        const myLM = ALL_DATA.find(a => a.sm === USER_CONTEXT.userName)?.lm;
        if (myLM) lms = [myLM];
        else lms = [];
    }

    const lastThree = getLastThreeCompletedMonths();

    // Use cached RNPS — populated only when loadAnalytics() is called
    const rnpsMap = window.RNPS_CACHE || {};

    const stats = lms.map(lm => {
        const accs = filtered.filter(a => a.lm === lm);
        const totalR = accs.reduce((sum, a) => sum + a.avg, 0);

        const month1 = accs.reduce((sum, a) => sum + (a[lastThree[0].key] || 0), 0);
        const month2 = accs.reduce((sum, a) => sum + (a[lastThree[1].key] || 0), 0);
        const month3 = accs.reduce((sum, a) => sum + (a[lastThree[2].key] || 0), 0);

        const growth = month2 > 0 ? ((month3 - month2) / month2 * 100).toFixed(1) : 0;

        const totalC = filtered.length;
        const totalRev = filtered.reduce((sum, a) => sum + a.avg, 0);
        const cShare = totalC > 0 ? (accs.length / totalC * 100).toFixed(1) : 0;
        const rShare = totalRev > 0 ? (totalR / totalRev * 100).toFixed(1) : 0;

        return { name: lm, count: accs.length, totalR, month1, month2, month3, growth, cShare, rShare };
    })
    .filter(m => m.count > 0)
    .sort((a, b) => b.totalR - a.totalR);

    const grid = document.getElementById('lineManagersGrid');

    if (stats.length === 0) {
        grid.innerHTML = `
            <div style="grid-column: 1 / -1; text-align: center; padding: 60px 20px; color: var(--text-secondary);">
                <div style="font-size: 48px; margin-bottom: 16px;">📭</div>
                <div style="font-size: 18px; font-weight: 600; margin-bottom: 8px;">No Accounts Found</div>
                <div style="font-size: 14px;">No accounts match the current filters</div>
            </div>`;
        return;
    }

    grid.innerHTML = stats.map(m => {
        const isSelected = selectedLM === m.name;
        const currentYear = new Date().getFullYear();
        const key = m.name.replace(/\s/g, '_');

const lmQuarters = rnpsMap[m.name] || [];
let rnpsTileHtml = '';
if (isSelected && !window.TSM_SE_ACTIVE) {
            if (!window.ANALYTICS_LOADED) {
                rnpsTileHtml = `
                <div class="lm-tile" style="cursor:default;opacity:.85;min-width:200px;">
                    <div class="lm-header">
                        <h3 style="font-size:.85rem;">RNPS Performance</h3>
                        <div class="lm-meta" style="margin-top:6px;line-height:1.5;">
                            <i data-lucide="bar-chart-2" style="width:12px;height:12px;display:inline-block;vertical-align:middle;margin-right:3px;"></i>
                            Click <strong style="color:var(--acc);">Load RNPS &amp; ETA Analytics</strong> above to view
                        </div>
                    </div>
                </div>`;
            } else if (lmQuarters.length > 0) {
                rnpsTileHtml = `
            <div class="lm-tile" style="border-color:var(--border-s);cursor:default;">
                <div class="lm-header">
                    <h3 style="font-size:.85rem;">RNPS Performance</h3>
                    <div class="lm-meta">Avg across all SMs</div>
                </div>
                <div style="display:flex;flex-direction:column;gap:6px;margin-top:4px;">
                    ${lmQuarters.map(row => {
                        const rnpsColor = row.RNPS >= row.RNPSTarget ? '#10b981' : '#ef4444';
                        const rrColor = row.ResponseRate >= row.ResponseTarget ? '#10b981' : '#ef4444';
                        return `
                        <div style="background:var(--bg-secondary);border:1px solid var(--border);border-radius:8px;padding:8px 10px;">
                            <div style="font-size:.68rem;font-weight:800;color:var(--acc);margin-bottom:5px;">${row.Period}</div>
                            <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:3px;">
                                <span style="font-size:.63rem;color:var(--t3);">Avg Response Rate</span>
                                <span style="font-size:.7rem;font-weight:700;color:${rrColor};">
                                    ${row.ResponseRate}%
                                    <span style="color:var(--t3);font-weight:400;">/ ${row.ResponseTarget}%</span>
                                </span>
                            </div>
                            <div style="display:flex;justify-content:space-between;align-items:center;">
                                <span style="font-size:.63rem;color:var(--t3);">Avg RNPS Score</span>
                                <span style="font-size:.7rem;font-weight:700;color:${rnpsColor};">
                                    ${row.RNPS}
                                    <span style="color:var(--t3);font-weight:400;">/ ${row.RNPSTarget}</span>
                                </span>
                            </div>
                        </div>`;
                    }).join('')}
                </div>
            </div>`;
            } else {
                rnpsTileHtml = `
                <div class="lm-tile" style="cursor:default;opacity:.75;min-width:200px;">
                    <div class="lm-header">
                        <h3 style="font-size:.85rem;">RNPS Performance</h3>
                        <div class="lm-meta" style="margin-top:6px;">No RNPS data found for this period.</div>
                    </div>
                </div>`;
            }
        }

       // ── ETA tile (only when selected) ────────────────────
    let etaTileHtml = '';
if (isSelected && !window.TSM_SE_ACTIVE) {
            if (!window.ETA_LOADED) {
                etaTileHtml = `
                <div class="lm-tile" style="cursor:default;opacity:.85;min-width:200px;">
                    <div class="lm-header">
                        <h3 style="font-size:.85rem;">ETA Overview</h3>
                        <div class="lm-meta" style="margin-top:6px;line-height:1.5;">
                            <i data-lucide="layers" style="width:12px;height:12px;display:inline-block;vertical-align:middle;margin-right:3px;"></i>
                            Click <strong style="color:var(--acc);">Load RNPS &amp; ETA Analytics</strong> above to view
                        </div>
                    </div>
                </div>`;
            } else {
                const etaStats = etaGetManagerStats(function(r) { return r.LM === m.name; });
                const hasData = etaStats && (etaStats.tt.total > 0 || etaStats.events.total > 0 || etaStats.activities.total > 0);

                function lmMiniRow(label, s) {
                    if (!s || s.total === 0) return '';
                    return `
                    <div style="margin-bottom:6px;">
                        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:2px;">
                            <span style="font-size:.62rem;font-weight:700;color:var(--t2);">${label}</span>
                            <span style="font-size:.6rem;color:var(--t3);">Total: ${s.total}</span>
                        </div>
                        <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:3px;">
                            <div style="background:rgba(245,158,11,.12);border:1px solid rgba(245,158,11,.25);border-radius:5px;padding:3px 4px;text-align:center;">
                                <div style="font-size:.58rem;color:#b45309;font-weight:600;">Assigned</div>
                                <div style="font-size:.78rem;font-weight:800;color:#f59e0b;">${s.assigned}</div>
                            </div>
                            <div style="background:rgba(59,130,246,.12);border:1px solid rgba(59,130,246,.25);border-radius:5px;padding:3px 4px;text-align:center;">
                                <div style="font-size:.58rem;color:#1d4ed8;font-weight:600;">In Prog</div>
                                <div style="font-size:.78rem;font-weight:800;color:#3b82f6;">${s.inprogress}</div>
                            </div>
                            <div style="background:rgba(16,185,129,.12);border:1px solid rgba(16,185,129,.25);border-radius:5px;padding:3px 4px;text-align:center;">
                                <div style="font-size:.58rem;color:#065f46;font-weight:600;">Done</div>
                                <div style="font-size:.78rem;font-weight:800;color:#10b981;">${s.completed}</div>
                            </div>
                            <div style="background:rgba(107,114,128,.12);border:1px solid rgba(107,114,128,.25);border-radius:5px;padding:3px 4px;text-align:center;">
                                <div style="font-size:.58rem;color:var(--t3);font-weight:600;">Other</div>
                                <div style="font-size:.78rem;font-weight:800;color:var(--t3);">${s.other}</div>
                            </div>
                        </div>
                    </div>`;
                }

                etaTileHtml = `
                <div class="lm-tile" style="border-color:var(--border-s);cursor:default;min-width:220px;">
                    <div class="lm-header" style="margin-bottom:8px;">
                        <div style="display:flex;align-items:center;justify-content:space-between;gap:6px;">
                            <h3 style="font-size:.85rem;">${m.name}</h3>
                            <span style="font-size:.7rem;color:var(--t3);font-weight:500;">${currentYear}</span>
                            <div style="position:relative;display:inline-block;">
                                <span id="etaInfoBtn_lm_${key}"
                                    onclick="etaShowInfoPopup(event,'lm_${key}')"
                                    style="cursor:pointer;width:18px;height:18px;border-radius:50%;background:var(--bg-secondary);border:1px solid var(--border);display:inline-flex;align-items:center;justify-content:center;font-size:.65rem;font-weight:800;color:var(--acc);">i</span>
                                <div id="etaInfoPopup_lm_${key}"
                                    style="display:none;position:absolute;right:0;top:24px;z-index:999;background:var(--bg-card);border:1px solid var(--border);border-radius:10px;box-shadow:0 8px 24px rgba(0,0,0,.2);padding:10px 12px;min-width:220px;max-width:260px;">
                                    <div style="font-size:.7rem;font-weight:800;color:var(--t1);margin-bottom:6px;border-bottom:1px solid var(--border);padding-bottom:4px;">Detailed Breakdown</div>
                                    ${hasData
                                        ? etaDetailHtml('TTs', etaStats.ttDetail) + etaDetailHtml('Events', etaStats.evDetail) + etaDetailHtml('Activities', etaStats.actDetail)
                                        : '<div style="font-size:.72rem;color:var(--t3);">No ETA data found</div>'
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="lm-meta">ETA Data — Current Year</div>
                    </div>
                    ${hasData ? `
                        ${lmMiniRow('Trouble Tickets', etaStats.tt)}
                        ${lmMiniRow('Events', etaStats.events)}
                        ${lmMiniRow('Activities', etaStats.activities)}
                    ` : `
                        <div style="font-size:.75rem;color:var(--t3);text-align:center;padding:1rem 0;">No ETA data found for this LM.</div>
                    `}
                </div>`;
            }
        }

        // ── Main LM tile ──────────────────────────────────────
        return `
        <div class="lm-tile ${isSelected ? 'selected' : ''}" onclick="selectLM('${m.name}')">
            <div class="lm-header">
                <h3>${m.name}</h3>
            </div>

            <div class="lm-stats-grid">
                <div class="lm-stat-box">
                    <div class="lm-stat-label">Count</div>
                    <div class="lm-stat-value">${m.count}</div>
                </div>
                <div class="lm-stat-box">
                    <div class="lm-stat-label">Revenue</div>
                    <div class="lm-stat-value">${formatCurrency(m.totalR)}</div>
                </div>
            </div>

            <div class="lm-revenue">
                <div class="lm-revenue-label">${lastThree[2].label} Revenue</div>
                <div class="lm-revenue-count">${formatCurrency(m.month3)}</div>
                <div class="lm-revenue-breakdown">
                    <div class="lm-revenue-item">
                        <div class="lm-revenue-item-label">Growth</div>
                        <div class="lm-revenue-item-value" style="color:${m.growth >= 0 ? '#34d399' : '#fca5a5'};">${m.growth >= 0 ? '+' : ''}${m.growth}%</div>
                    </div>
                    <div class="lm-revenue-item">
                        <div class="lm-revenue-item-label">${lastThree[1].label}</div>
                        <div class="lm-revenue-item-value">${formatCurrency(m.month2)}</div>
                    </div>
                    <div class="lm-revenue-item">
                        <div class="lm-revenue-item-label">${lastThree[0].label}</div>
                        <div class="lm-revenue-item-value">${formatCurrency(m.month1)}</div>
                    </div>
                </div>
            </div>

            <div class="lm-progress-section">
                <div class="lm-progress-row">
                    <span class="lm-progress-label">Account Share</span>
                    <span class="lm-progress-value">${m.cShare}%</span>
                </div>
                <div class="progress-bar">
                    <div class="progress-fill" style="width:${m.cShare}%"></div>
                </div>
                <div class="lm-progress-row">
                    <span class="lm-progress-label">Revenue Share</span>
                    <span class="lm-progress-value">${m.rShare}%</span>
                </div>
                <div class="progress-bar">
                    <div class="progress-fill" style="width:${m.rShare}%"></div>
                </div>
            </div>
        </div>
        ${rnpsTileHtml}
        ${etaTileHtml}`;
    }).join('');
}
async function renderServiceManagers() {
    if (!selectedLM) {
        document.getElementById('smPerformanceSection').style.display = 'none';
        return;
    }

    document.getElementById('smPerformanceSection').style.display = 'block';

    const sms = [...new Set(filtered.map(a => a.sm))].filter(Boolean);
    const lastThree = getLastThreeCompletedMonths();
    const currentYear = new Date().getFullYear();

    // Fetch RNPS data for selected LM
    const rnpsData = await fetchRNPSForLM(selectedLM);

    const stats = sms.map(sm => {
        const accs = filtered.filter(a => a.sm === sm);
        const totalR = accs.reduce((sum, a) => sum + a.avg, 0);

        const month1 = accs.reduce((sum, a) => sum + (a[lastThree[0].key] || 0), 0);
        const month2 = accs.reduce((sum, a) => sum + (a[lastThree[1].key] || 0), 0);
        const month3 = accs.reduce((sum, a) => sum + (a[lastThree[2].key] || 0), 0);

        const growth = month2 > 0 ? ((month3 - month2) / month2 * 100).toFixed(1) : 0;

        const totalC = filtered.length;
        const totalRev = filtered.reduce((sum, a) => sum + a.avg, 0);
        const cShare = totalC > 0 ? (accs.length / totalC * 100).toFixed(1) : 0;
        const rShare = totalRev > 0 ? (totalR / totalRev * 100).toFixed(1) : 0;

        return { name: sm, count: accs.length, totalR, month1, month2, month3, growth, cShare, rShare };
    }).sort((a, b) => b.totalR - a.totalR);

    document.getElementById('serviceManagersList').innerHTML = stats.map((m, i) => {
        const rankClass = i === 0 ? 'rank-1' : i === 1 ? 'rank-2' : i === 2 ? 'rank-3' : 'rank-other';
        const isSelected = selectedSM === m.name;
        const tag = i === 0 ? 'Top Performer' : i === 1 ? 'High Achiever' : 'Key Contributor';
        const key = m.name.replace(/\s/g, '_');

      // ── RNPS block ────────────────────────────────────────
    const smRnps = rnpsData[m.name] || [];
let rnpsHtml = '';
if (window.TSM_SE_ACTIVE) {
    rnpsHtml = '';
} else if (!window.ANALYTICS_LOADED) {
            rnpsHtml = `
                <div style="margin-top:8px;padding-top:8px;border-top:1px solid var(--border);font-size:.72rem;color:var(--t3);">
                    <i data-lucide="bar-chart-2" style="width:11px;height:11px;display:inline-block;vertical-align:middle;margin-right:3px;"></i>
                    Click <strong style="color:var(--acc);">Load RNPS &amp; ETA Analytics</strong> above to view RNPS data
                </div>`;
        } else if (smRnps.length > 0) {
            const sorted = smRnps.slice().sort((a, b) => {
                const qa = parseInt(a.Period.replace('Q', '').split('-')[0]);
                const qb = parseInt(b.Period.replace('Q', '').split('-')[0]);
                return qa - qb;
            });
            rnpsHtml = `
                <div style="margin-top:8px;padding-top:8px;border-top:1px solid var(--border);display:flex;flex-wrap:wrap;gap:6px;">
                    <div style="width:100%;font-size:.62rem;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.05em;margin-bottom:2px;">RNPS Performance</div>
                    ${sorted.map(row => {
                        const rnpsColor = row.RNPS >= row.RNPSTarget ? '#10b981' : '#ef4444';
                        const rrColor = row.ResponseRate >= row.ResponseTarget ? '#10b981' : '#ef4444';
                        return `
                        <div style="background:var(--bg-secondary);border:1px solid var(--border);border-radius:8px;padding:6px 10px;min-width:120px;flex:1;">
                            <div style="font-size:.65rem;font-weight:800;color:var(--acc);margin-bottom:4px;">${row.Period}</div>
                            <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:2px;">
                                <span style="font-size:.62rem;color:var(--t3);">Response Rate</span>
                                <span style="font-size:.68rem;font-weight:700;color:${rrColor};">${row.ResponseRate != null ? row.ResponseRate + '%' : 'N/A'} <span style="color:var(--t3);font-weight:400;">/ ${row.ResponseTarget != null ? row.ResponseTarget + '%' : 'N/A'}</span></span>
                            </div>
                            <div style="display:flex;justify-content:space-between;align-items:center;">
                                <span style="font-size:.62rem;color:var(--t3);">RNPS Score</span>
                                <span style="font-size:.68rem;font-weight:700;color:${rnpsColor};">${row.RNPS != null ? row.RNPS : 'N/A'} <span style="color:var(--t3);font-weight:400;">/ ${row.RNPSTarget != null ? row.RNPSTarget : 'N/A'}</span></span>
                            </div>
                        </div>`;
                    }).join('')}
                </div>`;
        }

    // ── ETA block ─────────────────────────────────────────
    const etaStats = window.ETA_LOADED ? etaGetManagerStats(function(r) { return r.SM === m.name; }) : null;
let etaHtml = '';
if (window.TSM_SE_ACTIVE) {
    etaHtml = '';
} else if (!window.ANALYTICS_LOADED || !window.ETA_LOADED) {
            etaHtml = `
                <div style="margin-top:8px;padding-top:8px;border-top:1px solid var(--border);font-size:.72rem;color:var(--t3);">
                    <i data-lucide="layers" style="width:11px;height:11px;display:inline-block;vertical-align:middle;margin-right:3px;"></i>
                    Click <strong style="color:var(--acc);">Load RNPS &amp; ETA Analytics</strong> above to view ETA data
                </div>`;
        } else if (etaStats && (etaStats.tt.total > 0 || etaStats.events.total > 0 || etaStats.activities.total > 0)) {
            function miniRow(label, s) {
                if (s.total === 0) return '';
                return `
                <div style="margin-bottom:5px;">
                    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:2px;">
                        <span style="font-size:.62rem;font-weight:700;color:var(--t2);">${label}</span>
                        <span style="font-size:.6rem;color:var(--t3);">Total: ${s.total}</span>
                    </div>
                    <div style="display:flex;gap:3px;">
                        <span style="flex:1;background:rgba(245,158,11,.12);border:1px solid rgba(245,158,11,.25);border-radius:4px;padding:2px 3px;text-align:center;font-size:.6rem;font-weight:700;color:#f59e0b;" title="Assigned">${s.assigned}</span>
                        <span style="flex:1;background:rgba(59,130,246,.12);border:1px solid rgba(59,130,246,.25);border-radius:4px;padding:2px 3px;text-align:center;font-size:.6rem;font-weight:700;color:#3b82f6;" title="In Progress">${s.inprogress}</span>
                        <span style="flex:1;background:rgba(16,185,129,.12);border:1px solid rgba(16,185,129,.25);border-radius:4px;padding:2px 3px;text-align:center;font-size:.6rem;font-weight:700;color:#10b981;" title="Completed">${s.completed}</span>
                        <span style="flex:1;background:rgba(107,114,128,.12);border:1px solid rgba(107,114,128,.25);border-radius:4px;padding:2px 3px;text-align:center;font-size:.6rem;font-weight:700;color:var(--t3);" title="Other">${s.other}</span>
                    </div>
                </div>`;
            }

            etaHtml = `
            <div style="margin-top:8px;padding-top:8px;border-top:1px solid var(--border);">
                <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:5px;">
                    <div style="font-size:.62rem;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.05em;">ETA — ${currentYear}</div>
                    <div style="display:flex;gap:4px;font-size:.55rem;color:var(--t3);">
                        <span style="background:rgba(245,158,11,.15);border-radius:3px;padding:1px 4px;">🟡 Assigned</span>
                        <span style="background:rgba(59,130,246,.15);border-radius:3px;padding:1px 4px;">🔵 In Prog</span>
                        <span style="background:rgba(16,185,129,.15);border-radius:3px;padding:1px 4px;">🟢 Done</span>
                        <span style="background:rgba(107,114,128,.15);border-radius:3px;padding:1px 4px;">⚫ Other</span>
                    </div>
                    <div style="position:relative;display:inline-block;">
                        <span id="etaInfoBtn_sm_${key}"
                            onclick="etaShowInfoPopup(event,'sm_${key}')"
                            style="cursor:pointer;width:16px;height:16px;border-radius:50%;background:var(--bg-secondary);border:1px solid var(--border);display:inline-flex;align-items:center;justify-content:center;font-size:.6rem;font-weight:800;color:var(--acc);">i</span>
                        <div id="etaInfoPopup_sm_${key}"
                            style="display:none;position:absolute;right:0;top:20px;z-index:999;background:var(--bg-card);border:1px solid var(--border);border-radius:10px;box-shadow:0 8px 24px rgba(0,0,0,.2);padding:10px 12px;min-width:200px;">
                            <div style="font-size:.7rem;font-weight:800;color:var(--t1);margin-bottom:6px;border-bottom:1px solid var(--border);padding-bottom:4px;">Detailed Breakdown</div>
                            ${etaDetailHtml('TTs', etaStats.ttDetail)}
                            ${etaDetailHtml('Events', etaStats.evDetail)}
                            ${etaDetailHtml('Activities', etaStats.actDetail)}
                        </div>
                    </div>
                </div>
                ${miniRow('Trouble Tickets', etaStats.tt)}
                ${miniRow('Events', etaStats.events)}
                ${miniRow('Activities', etaStats.activities)}
            </div>`;
        }

        return `
            <div class="manager-row ${isSelected ? 'selected' : ''}" onclick="selectSM('${m.name}')">
                <div class="rank-badge ${rankClass}">${i + 1}</div>
                <div class="manager-name-col">
                    <div class="manager-name">${m.name}</div>
                    <div class="manager-tag">${tag}</div>
                </div>
                <div class="stat-col">
                    <div class="stat-label">Accounts</div>
                    <div class="stat-value">${m.count}</div>
                </div>
                <div class="stat-col">
                    <div class="stat-label">Revenue</div>
                    <div class="stat-value">${formatCurrency(m.totalR)}</div>
                </div>
                <div class="stat-col">
                    <div class="stat-label">${lastThree[2].label}</div>
                    <div class="stat-value">${formatCurrency(m.month3)}</div>
                </div>
                <div class="stat-col">
                    <div class="stat-label">Growth</div>
                    <div class="stat-value" style="color: ${m.growth >= 0 ? '#10b981' : '#ef4444'}">${m.growth}%</div>
                </div>
                <div class="progress-bar-col">
                    <div class="progress-label">Account Share: ${m.cShare}%</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: ${m.cShare}%"></div>
                    </div>
                </div>
                <div class="progress-bar-col">
                    <div class="progress-label">Revenue Share: ${m.rShare}%</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: ${m.rShare}%"></div>
                    </div>
                </div>
                ${DOJ_DATA[m.name] ? `<div class="sm-doj-col"><div class="stat-label">Joined</div><div class="stat-value">${DOJ_DATA[m.name].doj}</div><div class="stat-sub">${DOJ_DATA[m.name].tenure} exp</div></div>` : ''}
                ${rnpsHtml}
                ${etaHtml}
            </div>`;
    }).join('');
}        function getColors() {
            const theme = document.body.getAttribute('data-theme');
            const isDuralux = theme === 'duralux' || theme === 'duralux-dark';
            const isDark = theme === 'dark' || theme === 'duralux-dark';
            return {
                text: isDark ? '#f3e8ff' : (isDuralux ? '#0f1a3e' : '#1e1133'),
                secondary: isDark ? '#c4b5fd' : (isDuralux ? '#3d4f8a' : '#4b3f6b'),
                grid: isDark ? 'rgba(255,255,255,0.08)' : 'rgba(0,0,0,0.06)',
                primary: isDuralux ? '#4c6fff' : (isDark ? '#c084fc' : '#a855f7'),
                accent: isDuralux ? '#818cf8' : (isDark ? '#c084fc' : '#a855f7'),
                teamBlue: isDuralux ? '#60a5fa' : '#a855f7',
                teamPurple: isDuralux ? '#3b82f6' : '#c724b1'
            };
        }

        function renderCharts() {
            const colors = getColors();

            // ✅ Calculate totals for percentages
            const totalRevenue = filtered.reduce((sum, a) => sum + a.avg, 0);

            // ✅ Get managers based on current selection
            let managers = [];
            let revenueData = [];
            let accountsData = [];
            let revenueShareData = [];

            // 🔥 SCALING FACTOR: Make accounts visible on chart (multiply by 100)
            const ACCOUNT_SCALE = 100;

            if (selectedLM) {
                // Show Service Managers under selected Line Manager
                const sms = [...new Set(filtered.filter(a => a.lm === selectedLM).map(a => a.sm))].filter(Boolean);
                managers = sms;

                sms.forEach(sm => {
                    const smAccounts = filtered.filter(a => a.sm === sm);
                    const smRevenue = smAccounts.reduce((sum, a) => sum + a.avg, 0);

                    revenueData.push(smRevenue / 1000); // in K
                    accountsData.push(smAccounts.length * ACCOUNT_SCALE); // 🔥 SCALED
                    revenueShareData.push(totalRevenue > 0 ? (smRevenue / totalRevenue * 100) : 0);
                });
            } else if (selectedSM) {
                // Show only selected Service Manager
                managers = [selectedSM];
                const smAccounts = filtered.filter(a => a.sm === selectedSM);
                const smRevenue = smAccounts.reduce((sum, a) => sum + a.avg, 0);

                revenueData.push(smRevenue / 1000);
                accountsData.push(smAccounts.length * ACCOUNT_SCALE); // 🔥 SCALED
                revenueShareData.push(totalRevenue > 0 ? (smRevenue / totalRevenue * 100) : 0);
            } else {
                // Show all Line Managers
                const lms = [...new Set(filtered.map(a => a.lm))].filter(Boolean);
                managers = lms;

                lms.forEach(lm => {
                    const lmAccounts = filtered.filter(a => a.lm === lm);
                    const lmRevenue = lmAccounts.reduce((sum, a) => sum + a.avg, 0);

                    revenueData.push(lmRevenue / 1000);
                    accountsData.push(lmAccounts.length * ACCOUNT_SCALE); // 🔥 SCALED
                    revenueShareData.push(totalRevenue > 0 ? (lmRevenue / totalRevenue * 100) : 0);
                });
            }

            // ✅ WORKLOAD & REVENUE CHART (Bars + Lines) - ENHANCED
            // ✅ WORKLOAD & REVENUE CHART (Bars + Lines) - ENHANCED WITH LABELS
            if (charts.trend) charts.trend.destroy();

            // 🔧 Calculate account share percentages
            const totalAccounts = filtered.length;
            const accountShareData = [];

            if (selectedLM) {
                const sms = [...new Set(filtered.filter(a => a.lm === selectedLM).map(a => a.sm))].filter(Boolean);
                sms.forEach(sm => {
                    const smAccounts = filtered.filter(a => a.sm === sm);
                    const accountShare = totalAccounts > 0 ? (smAccounts.length / totalAccounts * 100) : 0;
                    accountShareData.push(accountShare);
                });
            } else if (selectedSM) {
                const smAccounts = filtered.filter(a => a.sm === selectedSM);
                const accountShare = totalAccounts > 0 ? (smAccounts.length / totalAccounts * 100) : 0;
                accountShareData.push(accountShare);
            } else {
                const lms = [...new Set(filtered.map(a => a.lm))].filter(Boolean);
                lms.forEach(lm => {
                    const lmAccounts = filtered.filter(a => a.lm === lm);
                    const accountShare = totalAccounts > 0 ? (lmAccounts.length / totalAccounts * 100) : 0;
                    accountShareData.push(accountShare);
                });
            }

            charts.trend = new Chart(document.getElementById('trendChart'), {
                type: 'bar',
                data: {
                    labels: managers,
                    datasets: [{
                            label: 'Revenue (K AED)',
                            data: revenueData,
                            backgroundColor: colors.teamBlue,
                            borderColor: colors.teamBlue,
                            borderWidth: 2,
                            borderRadius: 8,
                            maxBarThickness: 30,
                            yAxisID: 'y',
                            order: 3
                        },
                        {
                            label: 'Total Accounts',
                            data: accountsData,
                            backgroundColor: colors.teamPurple,
                            borderColor: colors.teamPurple,
                            borderWidth: 2,
                            borderRadius: 8,
                            maxBarThickness: 30,
                            yAxisID: 'y',
                            order: 4
                        },
                        {
                            label: 'Revenue Share (%)',
                            data: revenueShareData,
                            type: 'line',
                            borderColor: '#f97316',
                            backgroundColor: 'rgba(249, 115, 22, 0.1)',
                            borderWidth: 3,
                            borderDash: [8, 4],
                            pointRadius: 6,
                            pointBackgroundColor: '#f97316',
                            pointBorderColor: '#fff',
                            pointBorderWidth: 2,
                            pointHoverRadius: 8,
                            tension: 0.3,
                            yAxisID: 'y1',
                            order: 1
                        },
                        {
                            label: 'Account Share (%)',
                            data: accountShareData,
                            type: 'line',
                            borderColor: '#10b981',
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            borderDash: [8, 4],
                            pointRadius: 6,
                            pointBackgroundColor: '#10b981',
                            pointBorderColor: '#fff',
                            pointBorderWidth: 2,
                            pointHoverRadius: 8,
                            tension: 0.3,
                            yAxisID: 'y1',
                            order: 2
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            display: true,
                            position: 'bottom',
                            labels: {
                                color: colors.text,
                                font: {
                                    size: 11,
                                    weight: '600'
                                },
                                padding: 8,
                                usePointStyle: true,
                                pointStyle: 'circle'
                            }
                        },
                        tooltip: {
                            backgroundColor: 'rgba(0, 0, 0, 0.8)',
                            titleFont: {
                                size: 14,
                                weight: 'bold'
                            },
                            bodyFont: {
                                size: 13
                            },
                            padding: 12,
                            callbacks: {
                                label: (context) => {
                                    const label = context.dataset.label;
                                    const value = context.parsed.y;
                                    const managerName = context.label;

                                    if (label.includes('Revenue (K')) {
                                        return `💰 Revenue: ${formatCurrency(value * 1000)}`;
                                    } else if (label.includes('Total Accounts')) {
                                        // ✅ Show both parent and child counts
                                        const allFiltered = window.ALL_FILTERED || [];
                                        let parentCount = 0;
                                        let childCount = 0;

                                        if (selectedLM) {
                                            const accounts = allFiltered.filter(a => a.sm === managerName);
                                            parentCount = accounts.filter(a => a.type === 'Group').length;
                                            childCount = accounts.filter(a => a.type === 'Child').length;
                                        } else {
                                            const accounts = allFiltered.filter(a => a.lm === managerName);
                                            parentCount = accounts.filter(a => a.type === 'Group').length;
                                            childCount = accounts.filter(a => a.type === 'Child').length;
                                        }

                                        return `📊 Groups: ${parentCount} | Children: ${childCount} | Total: ${parentCount + childCount}`;
                                    } else if (label.includes('Revenue Share')) {
                                        return `📈 Revenue Share: ${value.toFixed(1)}%`;
                                    } else if (label.includes('Account Share')) {
                                        return `🎯 Account Share: ${value.toFixed(1)}%`;
                                    }
                                    return `${label}: ${value}`;
                                }
                            }
                        },
                    },
                    scales: {
                        x: {
                            ticks: {
                                color: colors.secondary,
                                font: {
                                    size: 10,
                                    weight: '600'
                                },
                                maxRotation: 35,
                                minRotation: 0
                            },
                            grid: {
                                display: false
                            }
                        },
                        y: {
                            type: 'linear',
                            display: false,
                            position: 'left',
                            beginAtZero: true,
                            grid: {
                                color: colors.grid,
                                lineWidth: 1
                            }
                        },
                        y1: {
                            type: 'linear',
                            display: true,
                            position: 'right',
                            beginAtZero: true,
                            max: 100,
                            ticks: {
                                color: colors.secondary,
                                font: {
                                    size: 10,
                                    weight: '700'
                                },
                                callback: function(value) {
                                    return value + '%';
                                }
                            },
                            grid: {
                                display: false
                            },
                            title: {
                                display: true,
                                text: 'Share (%)',
                                color: colors.text,
                                font: {
                                    size: 14,
                                    weight: '700'
                                },
                                padding: {
                                    top: 10
                                }
                            }
                        }
                    },
                    interaction: {
                        mode: 'index',
                        intersect: false
                    }
                },
                plugins: [{
                    id: 'barLabels',
                    afterDatasetsDraw: function(chart) {
                        const ctx = chart.ctx;

                        chart.data.datasets.forEach((dataset, datasetIndex) => {
                            // Only show labels on bar datasets (not lines)
                            if (dataset.type === 'line') return;

                            const meta = chart.getDatasetMeta(datasetIndex);

                            meta.data.forEach((bar, index) => {
                                const value = dataset.data[index];

                                // Get the corresponding percentage from lines
                                let percentage = '';
                                if (dataset.label.includes('Revenue')) {
                                    const revenueShareDataset = chart.data.datasets.find(d => d.label === 'Revenue Share (%)');
                                    if (revenueShareDataset) {
                                        percentage = revenueShareDataset.data[index].toFixed(1) + '%';
                                    }
                                } else if (dataset.label.includes('Accounts')) {
                                    const accountShareDataset = chart.data.datasets.find(d => d.label === 'Account Share (%)');
                                    if (accountShareDataset) {
                                        percentage = accountShareDataset.data[index].toFixed(1) + '%';
                                    }
                                }

                                // Format the main value
                                let mainLabel;
                                if (dataset.label.includes('Revenue')) {
                                    mainLabel = formatCurrency(value * 1000);
                                } else if (dataset.label.includes('Accounts')) {
                                    mainLabel = Math.round(value / 100).toString();
                                }

                                // Position - MORE space above bars
                                const x = bar.x;
                                let y;
                                if (dataset.label.includes('Revenue')) {
                                    y = bar.y - 26;
                                } else {
                                    y = bar.y - 14;
                                }

                                // Draw text with stroke for visibility
                                ctx.save();
                                ctx.textAlign = 'center';
                                ctx.textBaseline = 'bottom';
                                ctx.font = 'bold 9px Arial';

                                // White stroke (outline) for better visibility
                                ctx.strokeStyle = 'rgba(255, 255, 255, 0.8)';
                                ctx.lineWidth = 3;

                                // Use bright, contrasting colors
                                const textColor = dataset.label.includes('Revenue') ? '#1e40af' : '#7c3aed';
                                ctx.fillStyle = textColor;

                                // Line 1: Main value (with stroke)
                                ctx.strokeText(mainLabel, x, y);
                                ctx.fillText(mainLabel, x, y);

                                // Line 2: Percentage (with stroke)
                                if (percentage) {
                                    ctx.strokeText(percentage, x, y + 15);
                                    ctx.fillText(percentage, x, y + 15);
                                }

                                ctx.restore();
                            });
                        });
                    }
                }]
            });

            // TYPE CHART (unchanged)
            if (charts.type) charts.type.destroy();
            const allFiltered = window.ALL_FILTERED || [];
            const groupC = allFiltered.filter(a => a.type === 'Group').length;
            const childC = allFiltered.filter(a => a.type === 'Child').length;

            charts.type = new Chart(document.getElementById('typeChart'), {
                type: 'doughnut',
                data: {
                    labels: ['Group', 'Child'],
                    datasets: [{
                        data: [groupC, childC],
                        backgroundColor: [colors.teamBlue, colors.teamPurple],
                        borderWidth: 0
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'bottom',
                            labels: {
                                color: colors.secondary,
                                font: {
                                    size: 13,
                                    weight: '600'
                                },
                                padding: 15
                            }
                        }
                    }
                }
            });
        }
        let agGridApi = null;

        function SmSetColumnFilter() {}
        SmSetColumnFilter.prototype.init = function (params) {
            this.params = params;
            this.selected = new Set();
            this.gui = document.createElement('div');
            this.gui.className = 'sm-ag-set-filter';
            this._buildGui();
        };
        SmSetColumnFilter.prototype._cellValue = function (data) {
            var field = this.params.colDef.field;
            var v = data[field];
            if (v === null || v === undefined || v === '') return '—';
            if (this.params.colDef.type === 'numericColumn' || typeof v === 'number') {
                return typeof formatCurrency === 'function' ? formatCurrency(v) : String(v);
            }
            return String(v);
        };
        SmSetColumnFilter.prototype._allValues = function () {
            var values = new Set(), self = this;
            this.params.api.forEachNode(function (node) {
                if (node.data) values.add(self._cellValue(node.data));
            });
            return Array.from(values).sort(function (a, b) { return a.localeCompare(b); });
        };
        SmSetColumnFilter.prototype._buildGui = function () {
            var self = this, all = this._allValues();
            this.gui.innerHTML = '';
            var search = document.createElement('input');
            search.type = 'text';
            search.placeholder = 'Search...';
            search.className = 'sm-ag-set-search';
            this.gui.appendChild(search);
            var list = document.createElement('div');
            list.className = 'sm-ag-set-list';
            this.gui.appendChild(list);
            var render = function (term) {
                list.innerHTML = '';
                all.filter(function (v) { return !term || v.toLowerCase().indexOf(term.toLowerCase()) >= 0; }).forEach(function (v) {
                    var row = document.createElement('label');
                    row.className = 'sm-ag-set-option';
                    var cb = document.createElement('input');
                    cb.type = 'checkbox';
                    cb.checked = self.selected.has(v);
                    cb.onchange = function () {
                        if (cb.checked) self.selected.add(v); else self.selected.delete(v);
                        self.params.filterChangedCallback();
                    };
                    row.appendChild(cb);
                    row.appendChild(document.createTextNode(' ' + v));
                    list.appendChild(row);
                });
            };
            render('');
            search.oninput = function () { render(search.value); };
            var actions = document.createElement('div');
            actions.className = 'sm-ag-set-actions';
            var btnAll = document.createElement('button');
            btnAll.type = 'button';
            btnAll.textContent = 'Select all';
            btnAll.onclick = function () { all.forEach(function (v) { self.selected.add(v); }); render(search.value); self.params.filterChangedCallback(); };
            var btnClear = document.createElement('button');
            btnClear.type = 'button';
            btnClear.textContent = 'Clear';
            btnClear.onclick = function () { self.selected.clear(); render(search.value); self.params.filterChangedCallback(); };
            actions.appendChild(btnAll);
            actions.appendChild(btnClear);
            this.gui.appendChild(actions);
        };
        SmSetColumnFilter.prototype.getGui = function () { return this.gui; };
        SmSetColumnFilter.prototype.isFilterActive = function () { return this.selected.size > 0; };
        SmSetColumnFilter.prototype.doesFilterPass = function (params) {
            if (!this.selected.size) return true;
            return this.selected.has(this._cellValue(params.data));
        };
        SmSetColumnFilter.prototype.getModel = function () { return this.selected.size ? { values: Array.from(this.selected) } : null; };
        SmSetColumnFilter.prototype.setModel = function (model) {
            this.selected = new Set(model && model.values ? model.values : []);
            this._buildGui();
        };
        SmSetColumnFilter.prototype.destroy = function () {};

        function smCanEditRnpsEligibility() {
            return !!window.RNPS_ELIGIBILITY_EDIT_ENABLED;
        }

        function smBuildEditRnpsEligibilityHtml(account) {
            var html = '<div id="editRnpsEligibilityWrap">';
            if (smCanEditRnpsEligibility()) {
                html += '<div class="edit-field-card">' +
                    '<div class="edit-field-label">RNPS Eligibility</div>' +
                    '<select class="filter-select" id="editRnpsEligibility" onchange="onEditRnpsEligibilityChange()" style="font-size: 14px; padding: 10px; width: 100%;">' +
                    '<option value="">Select...</option>' +
                    '<option value="Yes"' + (account.rnpsEligibility === 'Yes' ? ' selected' : '') + '>Yes</option>' +
                    '<option value="No"' + (account.rnpsEligibility === 'No' ? ' selected' : '') + '>No</option>' +
                    '</select></div>';
                html += '<div class="edit-field-card" id="editRnpsNotEligibleReasonWrap" style="display:none;grid-column:1/-1;">' +
                    '<div class="edit-field-label">Reason for RNPS not Eligible *</div>' +
                    '<textarea class="filter-select" id="editRnpsNotEligibleReason" rows="3" placeholder="Enter reason..." style="resize:vertical;cursor:text;width:100%;font-size:14px;padding:12px 16px;">' +
                    smEscapeHtml(account.rnpsNotEligibleReason || '') +
                    '</textarea></div>';
            } else {
                html += '<div class="edit-field-card">' +
                    '<div class="edit-field-label">RNPS Eligibility</div>' +
                    '<div class="edit-field-value">' + (account.rnpsEligibility || '—') + '</div></div>';
                if (smIsRnpsNotEligible(account.rnpsEligibility)) {
                    html += '<div class="edit-field-card" style="grid-column:1/-1;">' +
                        '<div class="edit-field-label">Reason for RNPS not Eligible</div>' +
                        '<div class="edit-field-value">' + smEscapeHtml(account.rnpsNotEligibleReason || '—') + '</div></div>';
                }
            }
            html += '</div>';
            return html;
        }

        function smRefreshEditRnpsEligibilityFields() {
            if (!EDIT_ACCOUNT_DATA) return;
            var wrap = document.getElementById('editRnpsEligibilityWrap');
            if (!wrap) return;
            wrap.outerHTML = smBuildEditRnpsEligibilityHtml(EDIT_ACCOUNT_DATA);
            smSyncEditRnpsFieldsUi();
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }
        window.smRefreshEditRnpsEligibilityFields = smRefreshEditRnpsEligibilityFields;

        function smIsRnpsEligible(val) {
            return String(val || '').trim().toLowerCase() === 'yes';
        }

        function smIsRnpsNotEligible(val) {
            return String(val || '').trim().toLowerCase() === 'no';
        }

        function smPlainTextFromSharePoint(val) {
            if (val == null || val === '') return '';
            if (typeof val === 'object') {
                if (val.results && val.results.length) return smPlainTextFromSharePoint(val.results[0]);
                if (val.Value != null) return smPlainTextFromSharePoint(val.Value);
                return '';
            }
            var s = String(val).trim();
            if (!s) return '';
            if (/<[a-z][\s\S]*>/i.test(s)) {
                var tmp = document.createElement('div');
                tmp.innerHTML = s;
                return (tmp.textContent || tmp.innerText || '').trim();
            }
            return s;
        }

        function smEscapeHtml(val) {
            return String(val == null ? '' : val)
                .replace(/&/g, '&amp;')
                .replace(/</g, '&lt;')
                .replace(/>/g, '&gt;')
                .replace(/"/g, '&quot;');
        }

        function onEditRnpsEligibilityChange() {
            smSyncEditRnpsFieldsUi();
        }

        function smSyncEditRnpsFieldsUi() {
            var el = document.getElementById('editRnpsEligibility');
            var wrap = document.getElementById('editRnpsPocWrap');
            var reasonWrap = document.getElementById('editRnpsNotEligibleReasonWrap');
            if (!wrap || !reasonWrap) return;

            var eligibility = el ? el.value : ((EDIT_ACCOUNT_DATA && EDIT_ACCOUNT_DATA.rnpsEligibility) || '');
            var isYes = smIsRnpsEligible(eligibility);
            var isNo = smIsRnpsNotEligible(eligibility);

            wrap.style.display = isNo ? 'none' : '';
            reasonWrap.style.display = isNo ? '' : 'none';

            document.querySelectorAll('input[name="editRnpsPoc"]').forEach(function (radio) {
                radio.disabled = isNo;
                if (isNo) radio.checked = false;
            });

            if (isYes && !document.querySelector('input[name="editRnpsPoc"]:checked')) {
                var defaultOpt = document.querySelector('input[name="editRnpsPoc"][value="Primary"]');
                if (defaultOpt) defaultOpt.checked = true;
            }

            var reasonEl = document.getElementById('editRnpsNotEligibleReason');
            if (reasonEl) reasonEl.required = isNo;
        }
        window.onEditRnpsEligibilityChange = onEditRnpsEligibilityChange;
        window.smSyncEditRnpsFieldsUi = smSyncEditRnpsFieldsUi;

        function smNormalizeRnpsPoc(val) {
            const v = String(val || 'Primary').trim();
            if (/^secondary$/i.test(v)) return 'Secondary';
            if (/^technical$/i.test(v)) return 'Technical';
            return 'Primary';
        }

        function formatPhoneNumber(input) {
            if (!input) return;
            let value = input.value || '';
            if (!value.startsWith('+971')) {
                input.value = '+971';
                return;
            }
            const numbers = value.slice(4).replace(/\D/g, '');
            input.value = '+971' + numbers.slice(0, 9);
        }
        window.formatPhoneNumber = formatPhoneNumber;

        function smIsValidUaePhone(phone) {
            return /^\+971\d{9}$/.test(String(phone || '').trim());
        }

        function smValidatePocPhoneFields(values, changedFieldKeys) {
            const fields = [
                ['pocPhone', 'POC Contact', true],
                ['secondaryPocPhone', 'Secondary POC Contact', false],
                ['technicalPocPhone', 'Technical POC Contact', false]
            ];
            const changedSet = new Set(changedFieldKeys || []);
            for (let i = 0; i < fields.length; i++) {
                const key = fields[i][0];
                const label = fields[i][1];
                const primaryRequired = fields[i][2];
                const val = String((values && values[key]) || '').trim();
                const isChanged = changedSet.has(key);
                if (!val || val === '+971') {
                    if (isChanged && primaryRequired) {
                        return label + ' is required (UAE format: +971 followed by 9 digits, e.g. +971501234567)';
                    }
                    continue;
                }
                if (isChanged && !smIsValidUaePhone(val)) {
                    return label + ' must be UAE format: +971 followed by 9 digits (e.g. +971501234567)';
                }
            }
            return null;
        }

        function smGetRnpsPocDetails(row) {
            if (smIsRnpsNotEligible(row && row.rnpsEligibility)) {
                return { name: '', email: '', phone: '' };
            }
            const tag = smNormalizeRnpsPoc(row && row.rnpsPoc);
            if (tag === 'Secondary') {
                return {
                    name: (row && row.secondaryPocName) || '',
                    email: (row && row.secondaryPocEmail) || '',
                    phone: (row && row.secondaryPocPhone) || ''
                };
            }
            if (tag === 'Technical') {
                return {
                    name: (row && row.technicalPocName) || '',
                    email: (row && row.technicalPocEmail) || '',
                    phone: (row && row.technicalPocPhone) || ''
                };
            }
            return {
                name: (row && row.pocName) || '',
                email: (row && row.pocEmail) || '',
                phone: (row && row.pocPhone) || ''
            };
        }

        function smApplyRnpsExportRow(row) {
            const poc = smGetRnpsPocDetails(row);
            row._rnpsPocName = poc.name;
            row._rnpsPocEmail = poc.email;
            row._rnpsPocPhone = poc.phone;
            return row;
        }

        function smGetPocColumnDefs() {
            return [
                { field: 'pocName', headerName: 'Primary POC Name', width: 150, hide: false },
                { field: 'pocEmail', headerName: 'Primary POC Email', width: 180, hide: false },
                { field: 'pocPhone', headerName: 'Primary POC Contact', width: 140, hide: false },
                { field: 'secondaryPocName', headerName: 'Secondary POC Name', width: 150, hide: false },
                { field: 'secondaryPocEmail', headerName: 'Secondary POC Email', width: 180, hide: false },
                { field: 'secondaryPocPhone', headerName: 'Secondary POC Contact', width: 140, hide: false },
                { field: 'technicalPocName', headerName: 'Technical POC Name', width: 150, hide: false },
                { field: 'technicalPocEmail', headerName: 'Technical POC Email', width: 180, hide: false },
                { field: 'technicalPocPhone', headerName: 'Technical POC Contact', width: 140, hide: false }
            ];
        }

        function smEnsurePocColumnsVisible(api) {
            if (!api) return;
            smGetPocColumnDefs().forEach(function (col) {
                try {
                    api.setColumnsVisible([col.field], true);
                } catch (e) {}
            });
        }

        function smEnhanceColDefTree(col) {
            if (col.children && col.children.length) {
                return Object.assign({}, col, {
                    children: col.children.map(function (child) {
                        return smEnhanceColDefTree(child);
                    })
                });
            }
            if (!col.field || col.field === 'actions') return col;
            return smEnhanceColDef(Object.assign({}, col));
        }

        function smEnhanceColDef(col) {
            if (col.field === 'actions') return col;
            col.filter = SmSetColumnFilter;
            return col;
        }

        function renderTable() {
            if (window.TSM_SE_ACTIVE && typeof window.tsmSeRenderTable === 'function') {
                return window.tsmSeRenderTable();
            }

            smDestroyTsmSeGridOnly();

            const gridDiv = document.getElementById('myGrid');
            if (!gridDiv) return;
            const lastThree = getLastThreeCompletedMonths();

            // Build parent rows with children nested
            const parentRows = filtered.map(parent => {
                const children = (window.ALL_FILTERED || []).filter(a =>
                    a.type === 'Child' && a.parent === parent.code && a.code !== parent.code
                );

          
                return {
                    ...parent,
                    _children: children,
                    _hasChildren: children.length > 0,
                    _isParent: true
                };
            });

         
            const columnDefs = [{
                    field: 'code',
                    headerName: 'Account Code',
                    pinned: 'left',
                    width: 180,
                    cellRenderer: params => {
                        const data = params.data;
                        if (data._isParent && data._hasChildren) {
                            const expanded = data._expanded ? true : false;
                            const icon = expanded ? '▼' : '▶';
                            const count = data._children ? data._children.length : 0;
                            return `<span style="cursor:pointer; font-weight:700;" onclick="toggleChildren('${data.code}')">
                <span style="display:inline-block; width:24px; color:var(--accent); font-size:16px; font-weight:900;">${icon}</span>
                ${data.code} <span style="font-size:10px; color:var(--text-secondary); font-weight:500;">(${count})</span>
            </span>`;
                        } else if (data._isChild) {
                            return `<span style="padding-left:28px; color:var(--text-secondary); font-weight:500;">↳ ${data.code}</span>`;
                        }
                        return `<span style="font-weight:700;">${data.code}</span>`;
                    }
                },
                {
                    field: 'parent',
                    headerName: 'Parent Code',
                    width: 140
                },
                {
                    field: 'customer',
                    headerName: 'Customer Name',
                    width: 200
                },
                {
                    field: 'type',
                    headerName: 'Type',
                    width: 100,
                    cellRenderer: params => {
                        if (params.data._isChild) {
                            return `<span class="status-badge badge-warning" style="opacity:0.7;">Child</span>`;
                        }
                        const badge = params.value === 'Group' ? 'badge-success' : 'badge-warning';
                        return `<span class="status-badge ${badge}">${params.value}</span>`;
                    }
                },
                {
                    field: 'team',
                    headerName: 'Team',
                    width: 110
                },
                {
                    field: 'lm',
                    headerName: 'Line Manager',
                    width: 150
                },
                {
                    field: 'sm',
                    headerName: 'Service Manager',
                    width: 150
                },
                {
                    field: 'secondarySm',
                    headerName: 'Secondary SM',
                    width: 150
                },
                {
                    field: 'am',
                    headerName: 'Account Manager',
                    width: 150
                },
                {
                    field: 'ad',
                    headerName: 'Account Director',
                    width: 150
                },
                {
                    headerName: 'POC Details',
                    children: smGetPocColumnDefs()
                },
                {
                    field: 'accountType',
                    headerName: 'Account Type',
                    width: 150
                },
                {
                    field: 'rnpsEligibility',
                    headerName: 'RNPS Eligibility',
                    width: 150
                },
                {
                    field: 'rnpsNotEligibleReason',
                    headerName: 'Reason for RNPS not Eligible',
                    width: 220,
                    valueFormatter: function (params) {
                        return smPlainTextFromSharePoint(params.value) || '';
                    }
                },
                {
                    field: 'rnpsPoc',
                    headerName: 'RNPS POC',
                    width: 120,
                    valueFormatter: function (params) {
                        if (smIsRnpsNotEligible(params.data && params.data.rnpsEligibility)) return '—';
                        return smNormalizeRnpsPoc(params.value);
                    }
                },
                ...lastThree.map((month, index) => ({
                    field: month.key,
                    headerName: month.label,
                    width: 150,
                    type: 'numericColumn',
                    autoHeight: true, // ✅ Let row expand for 2 lines
                    cellRenderer: params => {
                        const value = params.value || 0;
                        const data = params.data;

                        // ✅ CHILD rows - simple display
                        if (data._isChild) {
                            return `<div style="opacity: 0.6; text-align: right; padding: 8px 0;">${formatCurrency(value)}</div>`;
                        }

                        // ✅ PARENT rows WITH children - show parent + sum
                        if (data._isParent && data._hasChildren && data._children && data._children.length > 0) {
                            const childrenSum = data._children.reduce((sum, child) => {
                                return sum + (child[month.key] || 0);
                            }, 0);

                            return `
                <div style="text-align: right; padding: 8px 0;">
                    <div style="font-weight: 700; font-size: 14px;">${formatCurrency(value)}</div>
                    <div style="font-size: 10px; color: var(--text-secondary); font-weight: 500; margin-top: 4px;">Child Sum: ${formatCurrency(childrenSum)}</div>
                </div>
            `;
                        }

                        // ✅ PARENT rows WITHOUT children - simple display
                        return `<div style="font-weight: 700; text-align: right; padding: 8px 0;">${formatCurrency(value)}</div>`;
                    },
                    cellStyle: params => {
                        if (index === 2 && params.data.isRevDrop && params.data._isParent) {
                            return {
                                color: '#ef4444'
                            };
                        }
                        return null;
                    }
                })),
                {
                    field: 'avg',
                    headerName: 'Avg Revenue',
                    width: 130,
                    valueFormatter: params => formatCurrency(params.value),
                    type: 'numericColumn',
                    cellStyle: params => {
                        if (params.data._isChild) return {
                            opacity: '0.6'
                        };
                        return {
                            fontWeight: '700'
                        };
                    }
                },
                {
                    field: 'actions',
                    headerName: 'Actions',
                    width: 120,
                    pinned: 'right',
                    sortable: false,
                    filter: false,
                    autoHeight: true,
                   cellRenderer: params => {
    const a = params.data;

    // Read Only — no actions at all
if (USER_CONTEXT.isReadOnly || USER_CONTEXT.isTSMManager) return '<div></div>';
    // Children only get Edit button
    if (a._isChild) {
                            let childHtml = '<div style="display:flex;flex-direction:column;gap:3px;padding:4px 2px;">';
                            let canEdit = false;
                            if (USER_CONTEXT.isAdmin) canEdit = true;
                            else if (USER_CONTEXT.role === 'Line Manager') canEdit = (a.lm === USER_CONTEXT.userName);
                            else if (USER_CONTEXT.role === 'Service Manager') canEdit = (a.sm === USER_CONTEXT.userName);

                            if (canEdit) {
                                childHtml += `<button type="button" class="export-btn" style="padding:3px 7px;font-size:10px;white-space:nowrap;width:100%;box-sizing:border-box;" onclick="openEditRequest('${a.code}')">
                    <i data-lucide="pencil" style="width: 12px; height: 12px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Edit
                </button>`;
                            }
                            childHtml += '</div>';
                            return childHtml;
                        }

                        let canEdit = false;
                        if (USER_CONTEXT.isAdmin) canEdit = true;
                        else if (USER_CONTEXT.role === 'Line Manager') canEdit = (a.lm === USER_CONTEXT.userName);
                        else if (USER_CONTEXT.role === 'Service Manager') canEdit = (a.sm === USER_CONTEXT.userName);

                        const canTransfer = USER_CONTEXT.isAdmin || (a.lm === USER_CONTEXT.userName) || (a.sm === USER_CONTEXT.userName);

                        const hasTransfer = (a.isRevDrop || a.isRevUpgrade) && canTransfer && a._isParent;

                  const transferAlreadyRaised = a._isParent && a.requestType === 'Transfer' &&
                            (a.requestStatus === 'Not Onboarded' || a.requestStatus === 'AM_Approved' || a.requestStatus === 'Transfer_Pending');
                        let html = `<div style="display:flex;flex-direction:column;gap:3px;padding:4px 2px;">`;
if (transferAlreadyRaised) {
                            var tStatus = a.requestStatus;
                            var tLabel, tColor, tIcon;
                            if (tStatus === 'AM_Approved') {
                                tLabel = 'Transfer: AM Approved';
                                tColor = 'linear-gradient(135deg,#10b981,#059669)';
                                tIcon = 'check-circle';
                            } else {
                                tLabel = 'Pending AM Approval';
                                tColor = 'linear-gradient(135deg,#f97316,#ea580c)';
                                tIcon = 'hourglass';
                            }
                            html += '<button type="button" class="export-btn" style="padding:3px 7px;font-size:10px;white-space:nowrap;width:100%;box-sizing:border-box;background:' + tColor + ';color:#fff;cursor:default;opacity:0.85;" disabled>' +
                                '<i data-lucide="' + tIcon + '" style="width:12px;height:12px;display:inline-block;vertical-align:middle;margin-right:4px;"></i>' + tLabel + '</button>';

                            // Send reminder button: 3+ days pending AM action (Transfer_Pending / Not Onboarded)
                            const isPendingAM = (a.requestStatus === 'Transfer_Pending' || a.requestStatus === 'Not Onboarded');
                            const overdue = (a.transferDaysPassed !== null && a.transferDaysPassed !== undefined && a.transferDaysPassed >= 3);
                            if (isPendingAM && overdue) {
                                const esc = (v) => String(v || '').replace(/\\/g, '\\\\').replace(/'/g, "\\'");
                                html += `<button type="button" class="reset-btn" style="padding:3px 7px;font-size:10px;white-space:nowrap;width:100%;box-sizing:border-box;"
onclick="sendTransferReminderEmailMain('${esc(a.amEmail)}','${esc(a.adEmail)}','${esc(a.smEmail)}','${esc(a.lmEmail)}','${esc(a.code)}','${esc(a.customer)}','${esc(a.team)}','${esc(a.proposedTeam)}',${a.transferDaysPassed || 0})">
<i data-lucide="mail" style="width:12px;height:12px;display:inline-block;vertical-align:middle;margin-right:4px;"></i>Send Reminder</button>`;
                            }
                        } else if (hasTransfer) {
                            html += `<button type="button" class="export-btn" style="padding:3px 7px;font-size:10px;white-space:nowrap;width:100%;box-sizing:border-box;background:linear-gradient(135deg,#f97316,#ea580c);color:#fff;"
            onclick="openTransferRequest('${a.code}', '${a.customer.replace(/'/g, "\\'")}', '${a.team}', '${a.lm}', '${a.sm}', '${a.am}', '${a.ad}')">
                <i data-lucide="repeat" style="width: 12px; height: 12px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Transfer
            </button>`;
                        }
                        if (canEdit) {
                            html += `<button type="button" class="export-btn" style="padding:3px 7px;font-size:10px;white-space:nowrap;width:100%;box-sizing:border-box;" onclick="openEditRequest('${a.code}')">
                <i data-lucide="pencil" style="width: 12px; height: 12px; display: inline-block; vertical-align: middle; margin-right: 4px;"></i>Edit
            </button>`;
                        }

                        html += '</div>';
                        return html;
                    },
                    onCellClicked: params => {
                        setTimeout(() => lucide.createIcons(), 100);
                    }
                }
            ];

            window.SM_DASHBOARD_COLUMN_DEFS = columnDefs.slice();

            // Store parent data globally for expand/collapse
            window.PARENT_ROWS = parentRows;
            window.CURRENT_TABLE_DATA = [...parentRows];

            const gridOptions = {
                columnDefs: columnDefs.map(function (col) {
                    return smEnhanceColDefTree(col);
                }),
                rowData: parentRows,
                defaultColDef: {
                    sortable: true,
                    filter: true,
                    resizable: true,
                    minWidth: 100
                },
                pagination: true,
                paginationPageSize: 50,
                paginationPageSizeSelector: [25, 50, 100, 200],
                rowHeight: 60,
                groupHeaderHeight: 36,
                headerHeight: 44,
                animateRows: true,
                enableCellTextSelection: true,
                suppressMenuHide: false,
               onGridReady: params => {
                    agGridApi = params.api;
                    smEnsurePocColumnsVisible(params.api);
                },
                isExternalFilterPresent: () => {
                    return !!window._TRANSFER_FILTER_VAL;
                },
                doesExternalFilterPass: node => {
                    var val = window._TRANSFER_FILTER_VAL;
                    if (!val) return true;
                    var d = node.data;
                    if (val === 'Transfer_Pending') {
                        return d.requestType === 'Transfer' && (d.requestStatus === 'Transfer_Pending' || d.requestStatus === 'Not Onboarded');
                    }
                    if (val === 'Transfer_Overdue_3D') {
                        return d.requestType === 'Transfer' &&
                            (d.requestStatus === 'Transfer_Pending' || d.requestStatus === 'Not Onboarded') &&
                            (d.transferDaysPassed !== null && d.transferDaysPassed !== undefined && d.transferDaysPassed >= 3);
                    }
                    if (val === 'AM_Approved') {
                        return d.requestType === 'Transfer' && d.requestStatus === 'AM_Approved';
                    }
                    return true;
                },
                getRowStyle: params => {
                    if (params.data._isChild) {
                        return {
                            background: 'rgba(168, 85, 247, 0.06)',
                            borderLeft: '3px solid var(--accent)'
                        };
                    }
                    if (params.data.isRevDrop) {
                        return {
                            background: 'rgba(239, 68, 68, 0.2)'
                        };
                    }
                    if (params.data.isRevUpgrade) {
                        return {
                            background: 'rgba(16, 185, 129, 0.2)'
                        };
                    }
                    return null;
                },
                getRowId: params => params.data._isChild ? 'child_' + params.data.code : 'parent_' + params.data.code
            };

            if (agGridApi) {
                agGridApi.destroy();
            }

            gridDiv.innerHTML = '';
            agGrid.createGrid(gridDiv, gridOptions);
        }

        function toggleChildren(parentCode) {
            if (!agGridApi) return;

            const parentRow = window.PARENT_ROWS.find(p => p.code === parentCode);
            if (!parentRow || !parentRow._hasChildren) return;

            // Toggle expanded state
            parentRow._expanded = !parentRow._expanded;

            // Rebuild row data
            const newRowData = [];
            window.PARENT_ROWS.forEach(parent => {
                newRowData.push(parent);
                if (parent._expanded && parent._children) {
                    parent._children.forEach(child => {
                        newRowData.push({
                            ...child,
                            _isChild: true,
                            _isParent: false,
                            _hasChildren: false
                        });
                    });
                }
            });

            agGridApi.setGridOption('rowData', newRowData);

            // ✅ FORCE REFRESH to update the icon
            agGridApi.refreshCells({
                force: true
            });

            setTimeout(() => {
                if (typeof lucide !== 'undefined') lucide.createIcons();
            }, 100);
        }

       function searchAGGrid(searchText) {
            if (!agGridApi) return;
            agGridApi.setGridOption('quickFilterText', searchText);
        }

        function applyTransferFilter() {
            var val = document.getElementById('transferStatusFilterMain').value;
            if (!agGridApi) return;
            if (!val) {
                // IMPORTANT: this grid uses an external filter via window._TRANSFER_FILTER_VAL.
                // If we don't clear it, "All accounts" stays filtered even after resetting.
                window._TRANSFER_FILTER_VAL = '';
                agGridApi.setFilterModel(null);
                agGridApi.onFilterChanged();
                return;
            }
            // Use external filter
            window._TRANSFER_FILTER_VAL = val;
            agGridApi.onFilterChanged();
        }

        function sendTransferReminderEmailMain(amEmail, adEmail, smEmail, lmEmail, code, customer, oldTeam, newTeam, daysPassed) {
            const safeDays = (daysPassed === null || daysPassed === undefined) ? '3+' : String(daysPassed);
            const subj = encodeURIComponent(`[REMINDER] Transfer Request Pending AM Action - ACC# ${code} | ${customer}`);
            const body = encodeURIComponent(
                `Dear AM / AD,

This is a reminder that the following transfer request has been pending AM action for ${safeDays} day(s).

Account: ${code} - ${customer}
Current Team: ${oldTeam}
Proposed Team: ${newTeam}

Please log in to the Service Management Dashboard and approve or reject the request.

Note: If no action is taken, the transfer request will be auto-approved.

Best regards,
${USER_CONTEXT.userName}`
            );

            const to = [amEmail, adEmail].filter(Boolean).join(';');
            const cc = [smEmail, lmEmail].filter(Boolean).join(';');
            window.location.href = `mailto:${encodeURIComponent(to)}?subject=${subj}&body=${body}` + (cc ? `&cc=${encodeURIComponent(cc)}` : '');
        }

        function searchTable() {
            const search = document.getElementById('searchBox').value.toLowerCase();
            const tbody = document.getElementById('tbody');
            if (!tbody) return;

            const rows = tbody.getElementsByTagName('tr');
            Array.from(rows).forEach(row => {
                // Get only td elements, exclude buttons
                const cells = row.getElementsByTagName('td');
                let rowText = '';
                for (let cell of cells) {
                    // Skip the last cell (Actions column with buttons)
                    if (cell !== cells[cells.length - 1]) {
                        rowText += cell.textContent.toLowerCase() + ' ';
                    }
                }
                row.style.display = rowText.includes(search) ? '' : 'none';
            });
        }

        function smFlattenColumnDefs(defs) {
            var flat = [];
            (defs || []).forEach(function (col) {
                if (col.children && col.children.length) {
                    flat = flat.concat(smFlattenColumnDefs(col.children));
                } else if (col.field && col.field !== 'actions') {
                    flat.push(col);
                }
            });
            return flat;
        }

        function smGetRnpsExportColumnDefs() {
            return [
                { field: 'rnpsPoc', headerName: 'RNPS POC' },
                { field: '_rnpsPocName', headerName: 'POC Name' },
                { field: '_rnpsPocEmail', headerName: 'POC Email' },
                { field: '_rnpsPocPhone', headerName: 'POC Contact' }
            ];
        }

        function smGetExportColumns(rnpsMode) {
            var pocFields = {};
            smGetPocColumnDefs().forEach(function (col) {
                pocFields[col.field] = true;
            });

            var cols = [];
            if (window.SM_DASHBOARD_COLUMN_DEFS && window.SM_DASHBOARD_COLUMN_DEFS.length) {
                cols = smFlattenColumnDefs(window.SM_DASHBOARD_COLUMN_DEFS);
            } else if (agGridApi) {
                cols = agGridApi.getColumns().filter(function (col) {
                    return col.getColId() !== 'actions';
                }).map(function (col) {
                    return col.getColDef();
                });
            }

            if (rnpsMode) {
                cols = cols.filter(function (col) {
                    return col.field && !pocFields[col.field];
                });
                return cols.concat(smGetRnpsExportColumnDefs());
            }

            var existing = {};
            cols.forEach(function (col) {
                if (col.field) existing[col.field] = true;
            });
            smGetPocColumnDefs().forEach(function (pocCol) {
                if (!existing[pocCol.field]) cols.push(pocCol);
            });
            return cols;
        }

        window.smGetPocColumnDefs = smGetPocColumnDefs;
        window.smEnsurePocColumnsVisible = smEnsurePocColumnsVisible;

        function smFormatExportCellValue(col, row) {
            const colDef = col.getColDef ? col.getColDef() : col;
            const field = colDef.field;
            let raw = field ? row[field] : '';

            if (typeof colDef.valueGetter === 'function') {
                try {
                    raw = colDef.valueGetter({ data: row, colDef: colDef });
                } catch (e) {
                    raw = field ? row[field] : '';
                }
            }

            if (field === 'code') {
                return row._isExportChild ? '  ↳ ' + (row.code || '') : (row.code || '');
            }
            if (field === 'type') {
                return row._isExportChild ? 'Child' : (row.type || '');
            }
            if (field === 'parent') {
                return row.parent || '-';
            }
            if (colDef.type === 'numericColumn' || field === 'avg') {
                return formatCurrency(raw || 0);
            }
            if (field === 'accountType' || field === 'rnpsEligibility' || field === 'rnpsPoc' || field === 'rnpsNotEligibleReason') {
                if (field === 'rnpsNotEligibleReason') return smPlainTextFromSharePoint(raw) || '';
                return raw || '';
            }
            if (field === '_rnpsPocName' || field === '_rnpsPocEmail' || field === '_rnpsPocPhone') {
                return raw != null && raw !== '' ? raw : '';
            }
            return raw != null && raw !== '' ? raw : '';
        }

        function smPromptExport() {
            if (isAccessControlAdmin()) {
                smShowExportModal();
            } else {
                exportToExcel(false);
            }
        }

        function smShowExportModal() {
            const modal = document.getElementById('smExportModal');
            if (!modal) {
                exportToExcel(false);
                return;
            }
            modal.style.display = 'flex';
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }

        function smCloseExportModal() {
            const modal = document.getElementById('smExportModal');
            if (modal) modal.style.display = 'none';
        }

        function exportToExcel(rnpsMode) {
            if (!agGridApi) return;
            const isRnpsExport = rnpsMode === true;

            const today = new Date();
            const dateStr = today.toLocaleDateString('en-GB') + " " + today.toLocaleTimeString('en-GB', {
                hour: '2-digit',
                minute: '2-digit'
            });

            const exportColumns = smGetExportColumns(isRnpsExport);
            const colCount = exportColumns.length || 1;
            const thStyle = 'background-color:#a855f7;color:white;font-weight:bold;padding:10px;';

            // Build export data: parents with their children underneath
            const exportData = [];
            const parentAccounts = filtered; // Parents only
            const allAccounts = window.ALL_FILTERED || [];

            parentAccounts.forEach(parent => {
                if (isRnpsExport && !smIsRnpsEligible(parent.rnpsEligibility)) return;
                exportData.push(smApplyRnpsExportRow({
                    ...parent,
                    _isExportParent: true
                }));
                // Find and add children
                const children = allAccounts.filter(a =>
                    a.type === 'Child' && a.parent === parent.code && a.code !== parent.code
                );
                children.forEach(child => {
                    exportData.push(smApplyRnpsExportRow({
                        ...child,
                        _isExportChild: true
                    }));
                });
            });

            let html = '<html><head><meta charset="utf-8"></head><body>';
            html += '<table border="1" cellspacing="0" cellpadding="4">';

            html += '<tr><td colspan="' + colCount + '" style="background-color:#a855f7;color:white;font-size:16px;font-weight:bold;text-align:center;padding:12px;">';
            html += (isRnpsExport ? 'Service Management Dashboard - RNPS Export' : 'Service Management Dashboard - Export') + '</td></tr>';

            html += '<tr><td colspan="' + colCount + '" style="background-color:#e9d5ff;font-size:12px;padding:8px;text-align:center;">';
            html += '<b>Generated:</b> ' + dateStr + ' | <b>Parents:</b> ' + parentAccounts.length + ' | <b>Total (with children):</b> ' + exportData.length + '</td></tr>';

            html += '<tr><td colspan="' + colCount + '" style="height:10px;"></td></tr>';

            html += '<tr>';
            exportColumns.forEach(function (col) {
                const colDef = col.getColDef ? col.getColDef() : col;
                const headerName = colDef.headerName || colDef.field || (col.getColId ? col.getColId() : '');
                html += '<th style="' + thStyle + '">' + headerName + '</th>';
            });
            html += '</tr>';

            exportData.forEach((a, i) => {
                let rowColor;
                if (a._isExportChild) {
                    rowColor = '#fef3c7'; // Light yellow for children
                } else {
                    rowColor = (i % 2 === 0) ? '#f3e8ff' : '#ffffff';
                }

                html += '<tr>';
                exportColumns.forEach(function (col) {
                    const colDef = col.getColDef ? col.getColDef() : col;
                    const field = colDef.field;
                    const isNumeric = colDef.type === 'numericColumn' || field === 'avg';
                    const cellValue = smFormatExportCellValue(col, a);
                    const fontWeight = field === 'code' ? ';font-weight:' + (a._isExportChild ? '400' : '700') : '';
                    const align = isNumeric ? ';text-align:right' : '';
                    html += '<td style="background-color:' + rowColor + ';padding:8px' + fontWeight + align + ';">' + cellValue + '</td>';
                });
                html += '</tr>';
            });

            html += '<tr><td colspan="' + colCount + '" style="background-color:#e9d5ff;font-weight:bold;padding:10px;text-align:center;">';
            html += 'Parent Accounts: ' + parentAccounts.length + ' | Total Records (with children): ' + exportData.length + '</td></tr>';
            html += '</table></body></html>';

            const blob = new Blob([html], {
                type: "application/vnd.ms-excel"
            });
            const link = document.createElement("a");
            const url = URL.createObjectURL(blob);
            const filename = (isRnpsExport ? 'SM_Dashboard_RNPS_' : 'SM_Dashboard_') + today.toISOString().split("T")[0] + ".xls";
            link.setAttribute("href", url);
            link.setAttribute("download", filename);
            link.style.visibility = "hidden";
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }

        function changeTheme(event) {
            const selectedTheme = event.target.value;
            const body = document.body;
            const wrapper = document.querySelector('.sm-dashboard-container');
            const icon = document.getElementById('themeIcon');
            const currentTheme = (wrapper || body).getAttribute('data-theme');

            // Check if we're currently in dark mode
            const isDarkMode = currentTheme === 'dark' || currentTheme === 'duralux-dark';

            // Switch to the selected theme, maintaining dark mode state if applicable
            if (selectedTheme === 'magenta') {
                if (isDarkMode) {
                    body.setAttribute('data-theme', 'dark');
                    if (wrapper) wrapper.setAttribute('data-theme', 'dark');
                    icon.setAttribute('data-lucide', 'sun');
                } else {
                    body.removeAttribute('data-theme');
                    if (wrapper) wrapper.removeAttribute('data-theme');
                    icon.setAttribute('data-lucide', 'moon');
                }
                try {
                    localStorage.setItem('sm_base_theme', 'magenta');
                    localStorage.setItem('sm_dark_mode', isDarkMode ? 'true' : 'false');
                } catch (e) {}
            } else if (selectedTheme === 'duralux') {
                if (isDarkMode) {
                    body.setAttribute('data-theme', 'duralux-dark');
                    if (wrapper) wrapper.setAttribute('data-theme', 'duralux-dark');
                    icon.setAttribute('data-lucide', 'sun');
                } else {
                    body.setAttribute('data-theme', 'duralux');
                    if (wrapper) wrapper.setAttribute('data-theme', 'duralux');
                    icon.setAttribute('data-lucide', 'moon');
                }
                try {
                    localStorage.setItem('sm_base_theme', 'duralux');
                    localStorage.setItem('sm_dark_mode', isDarkMode ? 'true' : 'false');
                } catch (e) {}
            }

            lucide.createIcons();

            setTimeout(() => {
                if (typeof renderCharts === 'function' && charts.trend) {
                    renderCharts();
                }
                if (typeof renderAMCharts === 'function' && amCharts.revenue) {
                    renderAMCharts();
                }
            }, 100);
        }

        function toggleSidebar() {
            var sb = document.getElementById('sidebar');
            var icon = document.getElementById('sidebarToggleIcon');
            if (!sb) return;
            sb.classList.toggle('collapsed');
            var collapsed = sb.classList.contains('collapsed');
            icon.setAttribute('data-lucide', collapsed ? 'chevrons-right' : 'chevrons-left');
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }

        function toggleTheme() {
            const body = document.body;
            const wrapper = document.querySelector('.sm-dashboard-container');
            const icon = document.getElementById('themeIcon');
            const amIcon = document.getElementById('amThemeIcon');
            const currentTheme = (wrapper || body).getAttribute('data-theme');

            // Toggle between light and dark of the CURRENT base theme
            if (!currentTheme || currentTheme === 'default') {
                // Currently on Magenta light → switch to Magenta dark
                body.setAttribute('data-theme', 'dark');
                if (wrapper) wrapper.setAttribute('data-theme', 'dark');
                icon.setAttribute('data-lucide', 'sun');
                if (amIcon) amIcon.setAttribute('data-lucide', 'sun');
                try {
                    localStorage.setItem('sm_dark_mode', 'true');
                } catch (e) {}
            } else if (currentTheme === 'dark') {
                // Currently on Magenta dark → switch to Magenta light
                body.removeAttribute('data-theme');
                if (wrapper) wrapper.removeAttribute('data-theme');
                icon.setAttribute('data-lucide', 'moon');
                if (amIcon) amIcon.setAttribute('data-lucide', 'moon');
                try {
                    localStorage.setItem('sm_dark_mode', 'false');
                } catch (e) {}
            } else if (currentTheme === 'duralux') {
                // Currently on Duralux light → switch to Duralux dark
                body.setAttribute('data-theme', 'duralux-dark');
                if (wrapper) wrapper.setAttribute('data-theme', 'duralux-dark');
                icon.setAttribute('data-lucide', 'sun');
                if (amIcon) amIcon.setAttribute('data-lucide', 'sun');
                try {
                    localStorage.setItem('sm_dark_mode', 'true');
                } catch (e) {}
            } else if (currentTheme === 'duralux-dark') {
                // Currently on Duralux dark → switch to Duralux light
                body.setAttribute('data-theme', 'duralux');
                if (wrapper) wrapper.setAttribute('data-theme', 'duralux');
                icon.setAttribute('data-lucide', 'moon');
                if (amIcon) amIcon.setAttribute('data-lucide', 'moon');
                try {
                    localStorage.setItem('sm_dark_mode', 'false');
                } catch (e) {}
            }

            lucide.createIcons();

            setTimeout(() => {
                if (typeof renderCharts === 'function' && charts.trend) {
                    renderCharts();
                }
                if (typeof renderAMCharts === 'function' && amCharts.revenue) {
                    renderAMCharts();
                }
            }, 100);
        }

        function switchColorScheme() {
            const scheme = document.getElementById('colorSchemeSelector').value;

            if (scheme === 'cyan') {
                document.body.classList.add('cyan-theme');
            } else {
                document.body.classList.remove('cyan-theme');
            }

            try {
                localStorage.setItem('sm_color_scheme', scheme);
            } catch (e) {}

            setTimeout(() => {
                if (typeof renderCharts === 'function' && charts.trend) {
                    renderCharts();
                }
                if (typeof renderAMCharts === 'function' && amCharts.revenue) {
                    renderAMCharts();
                }
            }, 100);
        }


        function switchDashboardSection(sectionId) {
            // Track page view
            if (typeof analyticsTrackPage === 'function') {
                const pageNames = {
                    'dashboard-view': 'Dashboard',
                    'pending-requests': 'Pending Requests',
                    'transfer-requests': 'Transfer Requests'
                };
                analyticsTrackPage(pageNames[sectionId] || sectionId);
            }

            // ── RESET ALL OVERLAY/FULL-PAGE VIEWS FIRST ──────────────────
            const overlayViews = [
                'reviewRequestView',
                'sdReviewTransferView',
                'transferRequestView'
            ];
            overlayViews.forEach(function(id) {
                var el = document.getElementById(id);
                if (el) el.style.display = 'none';
            });

            // Reset pending requests internal state
            CURRENT_REQUEST_ITEM = null;
            if (typeof pendingGridApi !== 'undefined' && pendingGridApi) {
                // grid stays, just reset the review state
            }

            // ── RESET section-mode on content div ────────────────────────
            var contentDiv = document.querySelector('.content');
            if (contentDiv) {
                contentDiv.classList.remove('section-mode');
                contentDiv.classList.remove('psd-full-mode');
            }
            var portalShell = document.querySelector('.portal-shell');
            if (portalShell) portalShell.classList.remove('psd-full-shell');

            // ── HIDE ALL SECTIONS ─────────────────────────────────────────
            document.querySelectorAll('.dashboard-section').forEach(function(section) {
                section.style.display = 'none';
            });

            // ── REMOVE ACTIVE FROM ALL NAV ITEMS ─────────────────────────
            document.querySelectorAll('.nav-item').forEach(function(item) {
                item.classList.remove('active');
            });

            // ── SHOW TARGET SECTION ───────────────────────────────────────
            var targetSection = document.getElementById(sectionId);
            if (targetSection) {
                targetSection.style.display = 'block';
            }

            // ── SET ACTIVE NAV ITEM ───────────────────────────────────────
            var activeNav = document.querySelector('[data-section="' + sectionId + '"]');
            if (activeNav) {
                activeNav.classList.add('active');
            }

            // ── SECTION-MODE for pending/transfer ────────────────────────
            if (contentDiv) {
                if (sectionId === 'pending-requests' || sectionId === 'transfer-requests') {
                    contentDiv.classList.add('section-mode');
                }
                if (sectionId === 'psdView') {
                    contentDiv.classList.add('psd-full-mode');
                    if (portalShell) portalShell.classList.add('psd-full-shell');
                }
            }

            // ── LOAD DATA FOR SPECIFIC SECTIONS ──────────────────────────
            if (sectionId === 'pending-requests') {
                if (typeof loadAccountRequests === 'function') loadAccountRequests();
            } 
            else if (sectionId === 'transfer-requests') {
                if (typeof loadAdminTransferRequests === 'function') loadAdminTransferRequests();
            } 
           
        else if (sectionId === 'processDocsView') {
    if (typeof pdLoadDocs === 'function') pdLoadDocs();
}
else if (sectionId === 'qaView') {
    if (typeof qaInit === 'function') qaInit();
}
else if (sectionId === 'rnpsView') {
    if (typeof rnpsInit === 'function') rnpsInit();
}
else if (sectionId === 'tsmPendingView') {
    if (typeof initTSMPendingEscalation === 'function') initTSMPendingEscalation();
}
else if (sectionId === 'contactUsView') {
    if (typeof cuLoadContacts === 'function') cuLoadContacts();
}
          else if (sectionId === 'seMailboxView') {
    if (typeof SEMailbox !== 'undefined') SEMailbox.init();
}
else if (sectionId === 'psdView') {
    if (typeof psdInit === 'function') psdInit();
    else {
        var psdLoading = document.getElementById('psdLoading');
        if (psdLoading) {
            psdLoading.innerHTML = '<div style="text-align:center;padding:40px;color:#ef4444;font-weight:700;">PSD script failed to load. Re-upload psd-assignment.js to SharePoint and hard-refresh (Ctrl+F5).</div>';
        }
    }
}

        }

        function getVisibleDashboardSectionId() {
            var sections = document.querySelectorAll('.dashboard-section');
            for (var i = 0; i < sections.length; i++) {
                var section = sections[i];
                if (section.style.display !== 'none' && section.offsetParent !== null && section.id) {
                    return section.id;
                }
            }
            return 'dashboard-view';
        }

        window.refreshCurrentTab = function() {
            var activeNav = document.querySelector('.nav-item.active[data-section]');
            var sectionId = activeNav ? activeNav.getAttribute('data-section') : getVisibleDashboardSectionId();
            refreshTabData(sectionId || 'dashboard-view');
        };

        window.refreshTabData = async function(sectionId) {
            var btn = document.querySelector('.tab-refresh-btn[data-section="' + sectionId + '"]') || document.getElementById('headerRefreshBtn');
            if (btn) {
                btn.disabled = true;
                btn.classList.add('refreshing');
            }
            try {
                switch (sectionId) {
                    case 'dashboard-view':
                        await init();
                        break;
                    case 'pending-requests':
                        if (typeof loadAccountRequests === 'function') await loadAccountRequests();
                        break;
                    case 'transfer-requests':
                        if (typeof loadAdminTransferRequests === 'function') await loadAdminTransferRequests();
                        break;
                    case 'processDocsView':
                        if (typeof pdLoadDocs === 'function') pdLoadDocs();
                        break;
                    case 'qaView':
                        if (typeof qaInit === 'function') qaInit();
                        break;
                    case 'rnpsView':
                        if (typeof rnpsInit === 'function') rnpsInit();
                        break;
                    case 'tsmPendingView':
                        if (typeof tspeInit === 'function') await tspeInit(true);
                        else if (typeof initTSMPendingEscalation === 'function') await initTSMPendingEscalation(true);
                        break;
                    case 'contactUsView':
                        if (typeof cuLoadContacts === 'function') cuLoadContacts();
                        break;
                    case 'suggestionsView':
                        if (typeof sgLoadSuggestions === 'function') sgLoadSuggestions();
                        break;
                    case 'seMailboxView':
                        if (typeof SEMailbox !== 'undefined' && SEMailbox.init) SEMailbox.init();
                        break;
                    case 'psdView':
                        if (typeof psdInit === 'function') psdInit();
                        break;
                    case 'customerServiceView':
                        if (typeof csFetchSharePointData === 'function') {
                            var csLoading = document.getElementById('csLoadingScreen');
                            var csMain = document.getElementById('csMainContent');
                            if (csLoading) csLoading.style.display = 'block';
                            if (csMain) csMain.style.display = 'none';
                            await csFetchSharePointData();
                        }
                        break;
                    default:
                        await init();
                        break;
                }
                if (typeof lucide !== 'undefined') lucide.createIcons();
            } catch (err) {
                console.error('Tab refresh failed:', err);
                alert('Refresh failed: ' + err.message);
            } finally {
                if (btn) {
                    btn.disabled = false;
                    btn.classList.remove('refreshing');
                }
            }
        };

        // Load user access first, then initialize
        // ===== MAIN INITIALIZATION =====
        function initAll() {
            injectAGGridThemeStyles();
            initLandingPage();

            loadUserAccess(function(hasAccess) {
                populateLandingPage();
                try {
                    const savedBaseTheme = localStorage.getItem('sm_base_theme') || 'magenta'; // ✅ DEFAULT TO MAGENTA
                    const savedDarkMode = localStorage.getItem('sm_dark_mode') === 'true'; // ✅ DEFAULT TO LIGHT (false)
                    const selector = document.getElementById('colorSchemeSelector');
                    const icon = document.getElementById('themeIcon');
                    const wrapper = document.querySelector('.sm-dashboard-container');

                    // Set the dropdown to the saved base theme
                    if (selector) {
                        selector.value = savedBaseTheme;
                    }

                    // Apply the appropriate theme based on base theme + dark mode
                    if (savedBaseTheme === 'duralux') {
                        if (savedDarkMode) {
                            document.body.setAttribute('data-theme', 'duralux-dark');
                            if (wrapper) wrapper.setAttribute('data-theme', 'duralux-dark');
                            if (icon) icon.setAttribute('data-lucide', 'sun');
                        } else {
                            document.body.setAttribute('data-theme', 'duralux');
                            if (wrapper) wrapper.setAttribute('data-theme', 'duralux');
                            if (icon) icon.setAttribute('data-lucide', 'moon');
                        }
                    } else {
                        // Magenta theme
                        if (savedDarkMode) {
                            document.body.setAttribute('data-theme', 'dark');
                            if (wrapper) wrapper.setAttribute('data-theme', 'dark');
                            if (icon) icon.setAttribute('data-lucide', 'sun');
                        } else {
                            document.body.removeAttribute('data-theme');
                            if (wrapper) wrapper.removeAttribute('data-theme');
                            if (icon) icon.setAttribute('data-lucide', 'moon');
                        }
                    }
                } catch (e) {}

                // Initialize Lucide icons
                if (typeof lucide !== 'undefined') {
                    lucide.createIcons();
                }
                // Show revenue/POC/RNPS toggles only to authorised admins (global effect applies to all users)
                if (isAccessControlAdmin()) {
                    const btn = document.getElementById('revenueToggleBtn');
                    if (btn) btn.style.display = 'inline-flex';
                    const pocBtn = document.getElementById('pocToggleBtn');
                    if (pocBtn) pocBtn.style.display = 'inline-flex';
                    const rnpsBtn = document.getElementById('rnpsEligibilityToggleBtn');
                    if (rnpsBtn) rnpsBtn.style.display = 'inline-flex';
                }
                // ✅ ALWAYS show landing page for ALL users (even without access)
                console.log('[INFO] User:', USER_CONTEXT.userName, '| Role:', USER_CONTEXT.role);

                // Show landing page ALWAYS
                document.getElementById('landingPage').style.display = 'grid';

                // Load dashboard data ONLY if user has access
              if (hasAccess) {
    console.log('[✓] Access granted - Loading dashboard data');

    fetchAccessControlSettings().finally(function () {
        if (USER_CONTEXT.role === 'Admin' || USER_CONTEXT.role === 'Line Manager' || USER_CONTEXT.role === 'Service Manager' || USER_CONTEXT.role === 'Read Only' || USER_CONTEXT.role === 'TSM Manager' || USER_CONTEXT.role === 'Auditor' || USER_CONTEXT.role === 'TSM_SE_Viewer') {
            init();
        } else if (USER_CONTEXT.role === 'Account Manager' || USER_CONTEXT.role === 'Account Director') {
            initAMView();
        } else if (isPsdRole(USER_CONTEXT.role)) {
            initPsdShell();
        }
        if (typeof tsmSeInit === 'function') tsmSeInit();
    });
                } else {
                    console.log('[!] No dashboard access - Account Search only');
                }
            });
        }
        // Start the app
        if (document.readyState === 'complete') {
            initAll();
        } else {
            window.addEventListener('load', initAll);
        }
// ===== OWNER: VIEW AS USER =====
async function ownerFetchUserRole(email) {
            // Step 1: Check Access_Control list
            const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_ACCESS_LIST + "')/items?" +
                "$select=UserEmailID,Role,OCC_SEARCH&" +
                "$filter=UserEmailID eq '" + email + "'&$top=1";
            const res = await fetch(url, {
                headers: { 'Accept': 'application/json;odata=verbose' },
                credentials: 'include'
            });
            if (res.ok) {
                const data = await res.json();
                const results = data.d.results;
                if (results.length > 0) return { role: results[0].Role, name: null, occSearch: results[0].OCC_SEARCH };
            }

            // Step 2: Get their display name from the main SP list directly
            // Search by email in Line_Manager or Service_Manager expanded fields
            const spListUrl = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                "$select=Line_x0020_Manager/Title,Line_x0020_Manager/EMail," +
                "Service_x0020_Manager/Title,Service_x0020_Manager/EMail&" +
                "$expand=Line_x0020_Manager,Service_x0020_Manager&" +
                "$filter=(Line_x0020_Manager/EMail eq '" + email + "') or " +
                "(Service_x0020_Manager/EMail eq '" + email + "')&$top=1";

            const spRes = await fetch(spListUrl, {
                headers: { 'Accept': 'application/json;odata=verbose' },
                credentials: 'include'
            });

            if (spRes.ok) {
                const spData = await spRes.json();
                const spResults = spData.d.results;
                if (spResults.length > 0) {
                    const item = spResults[0];
                    if (item.Line_x0020_Manager && 
                        item.Line_x0020_Manager.EMail && 
                        item.Line_x0020_Manager.EMail.toLowerCase() === email) {
                        return { role: 'Line Manager', name: item.Line_x0020_Manager.Title };
                    }
                    if (item.Service_x0020_Manager && 
                        item.Service_x0020_Manager.EMail && 
                        item.Service_x0020_Manager.EMail.toLowerCase() === email) {
                        return { role: 'Service Manager', name: item.Service_x0020_Manager.Title };
                    }
                }
            }

            return null;
        }        async function ownerFetchUserName(email) {
            const url = SP_URL + "/_api/web/siteusers?$filter=Email eq '" + email + "'&$select=Title,Email&$top=1";
            const res = await fetch(url, {
                headers: { 'Accept': 'application/json;odata=verbose' },
                credentials: 'include'
            });
            if (!res.ok) return null;
            const data = await res.json();
            const results = data.d.results;
            if (results.length > 0) return results[0].Title;
            return null;
        }

        window.ownerSwitchView = async function() {
            const email = document.getElementById('ownerViewAsEmail').value.trim().toLowerCase();
            const msgEl = document.getElementById('ownerViewAsMsg');

            if (!email) {
                msgEl.innerHTML = '<span style="color:#ef4444;">Please enter an email.</span>';
                return;
            }

            msgEl.innerHTML = '<span style="color:var(--t3);">Checking access...</span>';

          const result = await ownerFetchUserRole(email);
            if (!result) {
                msgEl.innerHTML = '<span style="color:#ef4444;">No access found for this email.</span>';
                return;
            }

            const role = result.role;
            let displayName = result.name;

            // If name not returned (Admin/AM/AD roles), fetch from siteusers
            if (!displayName) {
                displayName = await ownerFetchUserName(email);
            }
            if (!displayName) displayName = email.split('@')[0];
            msgEl.innerHTML = '<span style="color:#10b981;">Found: ' + role + ' — loading view...</span>';

            msgEl.innerHTML = '<span style="color:#10b981;">Found: ' + role + ' — loading view...</span>';

            USER_CONTEXT.userEmail    = email;
            USER_CONTEXT.userName     = displayName;
            USER_CONTEXT.role         = role;
            USER_CONTEXT.isActive     = true;
            USER_CONTEXT.isAdmin      = role === 'Admin';
            USER_CONTEXT.isAM         = role === 'Account Manager';
            USER_CONTEXT.isAD         = role === 'Account Director';
            USER_CONTEXT.isLM         = role === 'Line Manager';
            USER_CONTEXT.isSM         = role === 'Service Manager';
            USER_CONTEXT.isReadOnly   = role === 'Read Only';
            USER_CONTEXT.isTSMManager = role === 'TSM Manager';
            USER_CONTEXT.occSearchEnabled = result.occSearch ? parseAccessControlYesNo(result.occSearch) : false;
            window.USER_CONTEXT       = USER_CONTEXT;

            document.getElementById('ownerViewAsPanel').style.display = 'none';

            setTimeout(async function() {
                if (role === 'Account Manager' || role === 'Account Director') {
                    showAMDashboard();
                } else {
                    if (role === 'Line Manager') {
                        window.ALL_FILTERED = ALL_DATA.filter(a => a.lm === displayName);
                        filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
                    } else if (role === 'Service Manager') {
                        window.ALL_FILTERED = ALL_DATA.filter(a => a.sm === displayName);
                        filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
                    } else if (role === 'TSM Manager') {
                        window.ALL_FILTERED = ALL_DATA.filter(a => a.team === 'TSM_ME' || a.team === 'TSM_SE');
                        filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
                   } else {
                        window.ALL_FILTERED = ALL_DATA.slice();
                        filtered = window.ALL_FILTERED.filter(a => a.type === 'Group');
                    }

                    populateLandingPage();
                    showDashboard();
                    populateFilters({ preserve: false });
                    updateServiceManagerFilter();
                    applyFilters();

                    // Reset all nav items first
                    ['portalAnalyticsNavItem','transferRequestsNavItem','pendingRequestsNavItem',
                     'intelligenceNavLabel','executiveHubNavItem','qaNavItem','rnpsNavItem',
                     'projectMgmtNavItem'].forEach(function(id) {
                        var el = document.getElementById(id);
                        if (el) el.style.display = 'none';
                    });

                    // Show nav items for the impersonated role
                    if (USER_CONTEXT.isAdmin) {
                        ['portalAnalyticsNavItem','transferRequestsNavItem','pendingRequestsNavItem',
                         'executiveHubNavItem','qaNavItem','projectMgmtNavItem'].forEach(function(id) {
                            var el = document.getElementById(id);
                            if (el) el.style.display = 'flex';
                        });
                        var lbl = document.getElementById('intelligenceNavLabel');
                        if (lbl) lbl.style.display = 'block';
                    }

                    if (USER_CONTEXT.isTSMManager) {
                        var ehNav = document.getElementById('executiveHubNavItem');
                        if (ehNav) ehNav.style.display = 'flex';
                        var qaNav = document.getElementById('qaNavItem');
                        if (qaNav) qaNav.style.display = 'flex';
                    }

                    if (USER_CONTEXT.role === 'Line Manager' || USER_CONTEXT.role === 'Service Manager') {
                        var qaNavLmSm = document.getElementById('qaNavItem');
                        if (qaNavLmSm) qaNavLmSm.style.display = 'flex';
                    }

                    var rnpsNav = document.getElementById('rnpsNavItem');
                    if (rnpsNav) rnpsNav.style.display = 'flex';

                    if (typeof lucide !== 'undefined') lucide.createIcons();
                }
            }, 300);
        };


        // ========================================
        // ACCOUNT REQUESTS FUNCTIONS (ADMIN)
        // ========================================

        let CURRENT_REQUEST_ITEM = null;

        // ── SharePoint item version history (Service Manager Request) ──
        const SM_HISTORY_FIELDS = {
            'Title': 'Account Code',
            'Customer_x0020_Name': 'Customer Name',
            'Team': 'Team',
            'Line_x0020_Manager': 'Line Manager',
            'Service_x0020_Manager': 'Service Manager',
            'Account_x0020_Manager': 'Account Manager',
            'Account_x0020_Director': 'Account Director',
            'POC_x0020_Name': 'POC Name',
            'POC_x0020_Email_x0020_ID': 'POC Email',
            'POC_x0020_Contact_x0020_No': 'POC Contact',
            'Secondary_x0020_POC_x0020_Name': 'Secondary POC Name',
            'Secondary_x0020_POC_x0020_Email': 'Secondary POC Email',
            'Secondary_x0020_POC_x0020_Contac': 'Secondary POC Contact',
            'Technical_x0020_POC_x0020_Name': 'Technical POC Name',
            'Technical_x0020_POC_x0020_Email': 'Technical POC Email',
            'Technical_x0020_POC_x0020_Contac': 'Technical POC Contact',
            'AccountType': 'Account Type',
            'RNPS_Eligibility': 'RNPS Eligibility',
            'RNPS_Not_Eligible_Reason': 'Reason for RNPS not Eligible',
            'RNPS_POC': 'RNPS POC',
            'Service_Required': 'Service Required',
            'Parent_x0020_Code': 'Parent Code',
            'Request_x0020_Status': 'Request Status',
            'Request_x0020_Type': 'Request Type',
            'Proposed_x0020_Team': 'Proposed Team',
            'Oct_x002d_25': 'Oct-25', 'Nov_x002d_25': 'Nov-25', 'Dec_x002d_25': 'Dec-25',
            'Jan_x002d_26': 'Jan-26', 'Feb_x002d_26': 'Feb-26', 'Mar_x002d_26': 'Mar-26',
            'Apr_x002d_26': 'Apr-26', 'May_x002d_26': 'May-26', 'Jun_x002d_26': 'Jun-26',
            'Jul_x002d_26': 'Jul-26', 'Aug_x002d_26': 'Aug-26', 'Sep_x002d_26': 'Sep-26',
            'Oct_x002d_26': 'Oct-26', 'Nov_x002d_26': 'Nov-26', 'Dec_x002d_26': 'Dec-26'
        };

        function smHistoryFmtDate(iso) {
            if (!iso) return 'N/A';
            const d = new Date(iso);
            if (isNaN(d.getTime())) return String(iso);
            return d.toLocaleString('en-GB', {
                day: '2-digit', month: 'short', year: 'numeric',
                hour: '2-digit', minute: '2-digit'
            });
        }

        function smHistoryNorm(val) {
            if (val === null || val === undefined) return '';
            return String(val).replace(/\u00a0/g, ' ').trim();
        }

        function smCleanSpValue(val) {
            val = smHistoryNorm(val);
            if (val.indexOf(';#') >= 0) val = val.split(';#').pop();
            return val;
        }

        function smParseVersionFields(data) {
            if (!data || !data.d) return {};
            var d = data.d;
            if (d.FieldValuesAsText && typeof d.FieldValuesAsText === 'object' && !d.FieldValuesAsText.__deferred) {
                return d.FieldValuesAsText;
            }
            var copy = Object.assign({}, d);
            delete copy.__metadata;
            if (copy.FieldValuesAsText && copy.FieldValuesAsText.__deferred) delete copy.FieldValuesAsText;
            return copy;
        }

        function smEditorFromFields(fields) {
            return smCleanSpValue(fields.Editor || fields['Modified By'] || fields.Author || '');
        }

        async function smFetchListItemId(accountCode) {
            const safeCode = String(accountCode || '').replace(/'/g, "''");
            const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                "$select=ID&$filter=Title eq '" + safeCode + "'&$top=1";
            const res = await fetch(url, {
                headers: { 'Accept': 'application/json;odata=verbose' },
                credentials: 'include'
            });
            if (!res.ok) throw new Error('Could not find list item');
            const data = await res.json();
            if (!data.d.results.length) throw new Error('Account not found in SharePoint list');
            return data.d.results[0].ID;
        }

        async function smFetchItemVersions(itemId) {
            const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items(" + itemId + ")/versions";
            const res = await fetch(url, {
                headers: { 'Accept': 'application/json;odata=verbose' },
                credentials: 'include'
            });
            if (!res.ok) throw new Error('Failed to load version history (' + res.status + ')');
            const data = await res.json();
            var versions = data.d.results || [];
            versions.sort(function (a, b) { return new Date(b.Created) - new Date(a.Created); });
            return versions;
        }

        async function smFetchVersionsPageHtml(itemId) {
            const listGuid = await smGetListGuid();
            const url = SP_URL + '/_layouts/15/Versions.aspx?list=' + encodeURIComponent(listGuid) + '&ID=' + itemId;
            const res = await fetch(url, { credentials: 'include' });
            if (!res.ok) throw new Error('Failed to load version history page (' + res.status + ')');
            return res.text();
        }

        function smHistoryFieldLabel(raw) {
            var key = String(raw || '').trim();
            if (!key) return key;
            if (SM_HISTORY_FIELDS[key]) return SM_HISTORY_FIELDS[key];
            if (SM_HISTORY_LABEL_LOOKUP[key]) return SM_HISTORY_LABEL_LOOKUP[key];
            return key.replace(/_x0020_/g, ' ').replace(/_x002d_/g, '-').replace(/_/g, ' ').trim();
        }

        var SM_HISTORY_LABEL_LOOKUP = {};
        Object.keys(SM_HISTORY_FIELDS).forEach(function (k) {
            SM_HISTORY_LABEL_LOOKUP[SM_HISTORY_FIELDS[k]] = SM_HISTORY_FIELDS[k];
            SM_HISTORY_LABEL_LOOKUP[k] = SM_HISTORY_FIELDS[k];
        });
        ['Service_Required', 'Parent Code', 'Parent_x0020_Code', 'Customer Name'].forEach(function (k) {
            if (!SM_HISTORY_LABEL_LOOKUP[k]) SM_HISTORY_LABEL_LOOKUP[k] = smHistoryFieldLabel(k);
        });

        function smLooksLikeDate(text) {
            return /^\d{1,2}\/\d{1,2}\/\d{4}/.test(String(text || '').trim()) ||
                /^\d{1,2}\s+[A-Za-z]{3}\s+\d{4}/.test(String(text || '').trim());
        }

        function smStripHtml(html) {
            var tmp = document.createElement('div');
            tmp.innerHTML = html || '';
            return (tmp.textContent || tmp.innerText || '').replace(/\s+/g, ' ').trim();
        }

        function smCellTextNoNestedTables(cell) {
            if (!cell) return '';
            var clone = cell.cloneNode(true);
            clone.querySelectorAll('table').forEach(function (t) { t.remove(); });
            return smStripHtml(clone.innerHTML);
        }

        function smParseVersionsPageHtml(html) {
            const doc = new DOMParser().parseFromString(html, 'text/html');
            const entries = [];
            const versionTable = doc.querySelector('table.ms-settingsframe') ||
                doc.querySelector('table[id*="versions"]') ||
                doc.querySelector('table.ms-formtable');

            if (!versionTable) {
                console.warn('[smParseVersionsPageHtml] ms-settingsframe not found, using fallback scan');
                return smParseVersionsPageHtmlFallback(doc);
            }

            const rows = versionTable.querySelectorAll('tbody > tr, tr');
            var dataStart = 0;
            for (var h = 0; h < Math.min(rows.length, 6); h++) {
                var ht = (rows[h].textContent || '').toLowerCase();
                if (ht.indexOf('modified') >= 0 && ht.indexOf('no') >= 0) {
                    dataStart = h + 1;
                    break;
                }
            }

            var current = null;
            for (let i = dataStart; i < rows.length; i++) {
                const row = rows[i];
                const cells = row.querySelectorAll(':scope > td');
                if (!cells.length) continue;

                const versionRaw = smStripHtml(cells[0].innerHTML).replace(/\s*View\s*$/i, '').trim();
                const verMatch = versionRaw.match(/^(\d+\.\d+)/);

                if (verMatch && cells.length >= 2) {
                    if (current) entries.push(current);
                    let modified = '';
                    let editor = '';
                    const dateEl = row.querySelector('table[ctxname="ctxVer"] a, table[ctxname="ctxVer"] td, .ms-vb-time');
                    if (dateEl) modified = smStripHtml(dateEl.innerHTML);
                    const userEl = row.querySelector('.ms-imnSpan a:nth-child(2), .ms-imnSpan a, .ms-subtleLink');
                    if (userEl) editor = smStripHtml(userEl.innerHTML);
                    if (!modified && cells[1]) modified = smCellTextNoNestedTables(cells[1]);
                    if (!editor && cells[2]) editor = smCellTextNoNestedTables(cells[2]);

                    current = {
                        versionLabel: verMatch[1],
                        versionNo: smExtractVersionNoFromRow(row) || smVersionLabelToNo(verMatch[1]),
                        viewUrl: smExtractVersionViewUrl(row),
                        created: modified,
                        editor: editor,
                        changes: []
                    };
                    smCollectVersionChanges(row, current.changes, {});
                    continue;
                }

                if (current) {
                    smCollectVersionChanges(row, current.changes, {});
                }
            }
            if (current) entries.push(current);

            entries.sort(function (a, b) {
                return parseFloat(b.versionLabel) - parseFloat(a.versionLabel);
            });
            entries.forEach(function (e, idx) { e.isCurrent = idx === 0; });
            return entries;
        }

        function smVersionLabelToNo(label) {
            return Math.round(parseFloat(String(label)) * 512);
        }

        function smExtractVersionNoFromRow(verRow) {
            if (!verRow) return null;
            const html = verRow.innerHTML || '';
            var m = html.match(/VersionNo=(\d+)/i);
            if (m) return parseInt(m[1], 10);
            m = html.match(/VersionNo['"%3D\s:=]+(\d{3,6})/i);
            if (m) return parseInt(m[1], 10);
            return null;
        }

        function smResolveSpUrl(href) {
            if (!href) return null;
            if (href.indexOf('http') === 0) return href;
            try {
                return new URL(href, SP_URL + '/').href;
            } catch (e) {
                return SP_URL + (href.charAt(0) === '/' ? href : '/' + href);
            }
        }

        function smExtractVersionViewUrl(verRow) {
            if (!verRow) return null;
            var links = verRow.querySelectorAll('a[href]');
            for (var i = 0; i < links.length; i++) {
                var href = links[i].getAttribute('href') || '';
                if (/VersionNo=|listform\.aspx|DispForm\.aspx|displayifs\.aspx/i.test(href)) {
                    return smResolveSpUrl(href);
                }
            }
            return null;
        }

        function smCollectVersionChanges(root, changes, seen) {
            if (!root) return;
            seen = seen || {};

            root.querySelectorAll('tr[id^="SPField"]').forEach(function (tr) {
                if (tr.closest('table[ctxname="ctxVer"]')) return;
                var rowId = tr.getAttribute('id') || '';
                var idMatch = rowId.match(/SPField(?:\w+?)_(.+)$/i);
                var label = idMatch ? smHistoryFieldLabel(idMatch[1]) : '';
                var body = tr.querySelector('.ms-formbody');
                var tds = tr.querySelectorAll('td');
                var newVal = '';
                if (body) newVal = smStripHtml(body.innerHTML);
                else if (tds.length >= 2) newVal = smStripHtml(tds[tds.length - 1].innerHTML);
                else if (tds.length === 1) newVal = smStripHtml(tds[0].innerHTML);
                if (!label && tds.length >= 1) label = smHistoryFieldLabel(smStripHtml(tds[0].innerHTML));
                if (!label || smLooksLikeDate(label) || seen[label]) return;
                seen[label] = true;
                changes.push({ label: label, oldVal: '—', newVal: newVal || '—' });
            });

            root.querySelectorAll('.ms-formlabel').forEach(function (lab) {
                var tr = lab.closest('tr');
                if (!tr || tr.closest('table[ctxname="ctxVer"]')) return;
                var body = tr.querySelector('.ms-formbody');
                if (!body) return;
                var label = smHistoryFieldLabel(smStripHtml(lab.innerHTML));
                var newVal = smStripHtml(body.innerHTML);
                if (!label || smLooksLikeDate(label) || seen[label]) return;
                seen[label] = true;
                changes.push({ label: label, oldVal: '—', newVal: newVal || '—' });
            });

            root.querySelectorAll('tr').forEach(function (tr) {
                if (tr.id && tr.id.indexOf('SPField') === 0) return;
                if (tr.closest('table[ctxname="ctxVer"]')) return;
                var tds = tr.querySelectorAll(':scope > td');
                if (tds.length < 2) return;
                var label = smHistoryFieldLabel(smStripHtml(tds[0].innerHTML));
                var newVal = smStripHtml(tds[1].innerHTML);
                if (!label || smLooksLikeDate(label) || /^no\.?$/i.test(label) || /^modified/i.test(label)) return;
                if (seen[label]) return;
                seen[label] = true;
                changes.push({ label: label, oldVal: '—', newVal: newVal || '—' });
            });

            root.querySelectorAll('td[colspan]').forEach(function (td) {
                if (td.querySelector('table')) {
                    td.querySelectorAll('table tr').forEach(function (tr) {
                        var tds = tr.querySelectorAll('td');
                        if (tds.length >= 2) {
                            var label = smHistoryFieldLabel(smStripHtml(tds[0].innerHTML));
                            var newVal = smStripHtml(tds[1].innerHTML);
                            if (label && !smLooksLikeDate(label) && !seen[label]) {
                                seen[label] = true;
                                changes.push({ label: label, oldVal: '—', newVal: newVal || '—' });
                            }
                        }
                    });
                } else {
                    smParsePlainVersionChangeText(smStripHtml(td.innerHTML), changes, seen);
                }
            });

            if (!changes.length && root.innerHTML) {
                smParseChangesFromRawFragment(root.innerHTML, changes, seen);
            }
        }

        function smParseChangesFromRawFragment(html, changes, seen) {
            seen = seen || {};
            Object.keys(SM_HISTORY_FIELDS).forEach(function (key) {
                var label = SM_HISTORY_FIELDS[key];
                if (seen[label]) return;
                var re = new RegExp('SPField\\w+_' + key.replace(/[.*+?^${}()|[\]\\]/g, '\\$&') + '[\\s\\S]{0,400}?ms-formbody[^>]*>([\\s\\S]*?)<', 'i');
                var m = html.match(re);
                if (m) {
                    seen[label] = true;
                    changes.push({ label: label, oldVal: '—', newVal: smStripHtml(m[1]) || '—' });
                }
            });
            var monthRe = /(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)-(\d{2})[\s\S]{0,160}?([\d,\.]+)/gi;
            var mm;
            while ((mm = monthRe.exec(html)) !== null) {
                var mLabel = mm[1] + '-' + mm[2];
                if (!seen[mLabel]) {
                    seen[mLabel] = true;
                    changes.push({ label: mLabel, oldVal: '—', newVal: mm[3].trim() });
                }
            }
        }

        function smParsePlainVersionChangeText(text, changes, seen) {
            if (!text) return;
            seen = seen || {};
            Object.keys(SM_HISTORY_FIELDS).forEach(function (key) {
                if (text.indexOf(key) === 0) {
                    var label = SM_HISTORY_FIELDS[key];
                    if (seen[label]) return;
                    seen[label] = true;
                    changes.push({
                        label: label,
                        oldVal: '—',
                        newVal: text.slice(key.length).trim() || '—'
                    });
                }
            });
            var monthMatch = text.match(/^(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)-\d{2}\s+(.+)$/i);
            if (monthMatch && !seen[monthMatch[0].split(/\s/)[0]]) {
                var mLabel = monthMatch[0].split(/\s/)[0];
                seen[mLabel] = true;
                changes.push({ label: mLabel, oldVal: '—', newVal: monthMatch[2].trim() });
            }
        }

        function smParseDispFormFields(html) {
            const doc = new DOMParser().parseFromString(html, 'text/html');
            const fields = {};
            function addField(key, val) {
                if (!key) return;
                val = smCleanSpValue(val);
                if (val) fields[key] = val;
            }
            doc.querySelectorAll('tr[id^="SPField"], [id^="SPField"]').forEach(function (el) {
                var idMatch = (el.getAttribute('id') || '').match(/SPField(?:\w+?)_(.+)$/i);
                if (!idMatch) return;
                var key = idMatch[1];
                var body = el.querySelector('.ms-formbody');
                if (body) { addField(key, smStripHtml(body.innerHTML)); return; }
                var tds = el.querySelectorAll('td');
                if (tds.length >= 2) addField(key, smStripHtml(tds[tds.length - 1].innerHTML));
                else addField(key, smStripHtml(el.innerHTML));
            });
            doc.querySelectorAll('.ms-formlabel').forEach(function (lab) {
                var tr = lab.closest('tr');
                if (!tr) return;
                var body = tr.querySelector('.ms-formbody');
                if (!body) return;
                var labelText = smStripHtml(lab.innerHTML);
                Object.keys(SM_HISTORY_FIELDS).forEach(function (key) {
                    if (SM_HISTORY_FIELDS[key] === labelText || key === labelText) {
                        addField(key, smStripHtml(body.innerHTML));
                    }
                });
            });
            return fields;
        }

        function smDecodeSoapInnerXml(text) {
            if (!text) return '';
            return String(text)
                .replace(/&lt;/g, '<').replace(/&gt;/g, '>')
                .replace(/&amp;/g, '&').replace(/&quot;/g, '"')
                .replace(/<!\[CDATA\[([\s\S]*?)\]\]>/g, '$1');
        }

        function smParseSoapVersionCollection(xmlText, fieldName) {
            const versions = [];
            var innerMatch = xmlText.match(/<GetVersionCollectionResult[^>]*>([\s\S]*?)<\/GetVersionCollectionResult>/i);
            if (!innerMatch) return versions;
            var inner = smDecodeSoapInnerXml(innerMatch[1].trim());
            if (!inner) return versions;

            var doc;
            try {
                doc = new DOMParser().parseFromString(inner, 'text/xml');
            } catch (e) {
                return versions;
            }
            if (doc.querySelector('parsererror')) return versions;

            doc.querySelectorAll('Version').forEach(function (v) {
                var label = v.getAttribute('Version') || v.getAttribute('VersionLabel') || v.getAttribute('version') || '';
                if (!label || !/\d/.test(label)) {
                    var verId = parseInt(v.getAttribute('VersionId') || v.getAttribute('versionid') || '0', 10);
                    if (verId) label = (verId / 512).toFixed(1);
                }
                var fieldEl = v.querySelector('Field[Name="' + fieldName + '"]') ||
                    v.querySelector('Field[name="' + fieldName + '"]') ||
                    v.querySelector('Field');
                var val = '';
                if (fieldEl) {
                    val = fieldEl.getAttribute('Value') || fieldEl.getAttribute('value') || smStripHtml(fieldEl.textContent);
                }
                if (label && /\d/.test(String(label))) {
                    versions.push({
                        versionLabel: String(label).replace(',', '.').trim(),
                        value: val,
                        fieldName: fieldName
                    });
                }
            });
            return versions;
        }

        function smFormatListGuid(guid) {
            if (!guid) return guid;
            var g = String(guid).trim();
            if (g.charAt(0) !== '{') g = '{' + g;
            if (g.charAt(g.length - 1) !== '}') g = g + '}';
            return g;
        }

        async function smSoapGetVersionCollection(listGuid, itemId, fieldName) {
            const envelope = '<?xml version="1.0" encoding="utf-8"?>' +
                '<soap:Envelope xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">' +
                '<soap:Body><GetVersionCollection xmlns="http://schemas.microsoft.com/sharepoint/soap/">' +
                '<strlistID>' + smFormatListGuid(listGuid) + '</strlistID>' +
                '<strlistItemID>' + itemId + '</strlistItemID>' +
                '<strFieldName>' + fieldName.replace(/&/g, '&amp;').replace(/</g, '&lt;') + '</strFieldName>' +
                '</GetVersionCollection></soap:Body></soap:Envelope>';

            const res = await fetch(SP_URL + '/_vti_bin/lists.asmx', {
                method: 'POST',
                credentials: 'include',
                headers: {
                    'Content-Type': 'text/xml; charset=utf-8',
                    'SOAPAction': 'http://schemas.microsoft.com/sharepoint/soap/GetVersionCollection'
                },
                body: envelope
            });
            if (!res.ok) return [];
            const text = await res.text();
            return smParseSoapVersionCollection(text, fieldName);
        }

        function smSoapValueAtVersion(hist, versionLabel) {
            if (!hist || !hist.length) return '';
            var target = parseFloat(versionLabel);
            for (var i = 0; i < hist.length; i++) {
                if (parseFloat(hist[i].versionLabel) === target) {
                    return smCleanSpValue(hist[i].value);
                }
            }
            return '';
        }

        function smResolveFieldValueAtVersion(fieldHistories, fieldKey, versionLabel, allLabels) {
            var exact = smSoapValueAtVersion(fieldHistories[fieldKey], versionLabel);
            if (exact) return exact;
            var target = parseFloat(versionLabel);
            var labels = allLabels.slice().sort(function (a, b) { return parseFloat(b) - parseFloat(a); });
            for (var i = 0; i < labels.length; i++) {
                if (parseFloat(labels[i]) > target) continue;
                var val = smSoapValueAtVersion(fieldHistories[fieldKey], labels[i]);
                if (val) return val;
            }
            return '';
        }

        function smChangesFromSoapHistories(fieldHistories, versionLabel, olderVersionLabel, allLabels) {
            const changes = [];
            const seen = {};
            Object.keys(fieldHistories).forEach(function (fieldKey) {
                var newerVal = smResolveFieldValueAtVersion(fieldHistories, fieldKey, versionLabel, allLabels);
                if (!olderVersionLabel) {
                    if (newerVal) {
                        var lbl = SM_HISTORY_FIELDS[fieldKey] || smHistoryFieldLabel(fieldKey);
                        if (!seen[lbl]) {
                            seen[lbl] = true;
                            changes.push({ label: lbl, oldVal: '—', newVal: newerVal });
                        }
                    }
                    return;
                }
                var olderVal = smResolveFieldValueAtVersion(fieldHistories, fieldKey, olderVersionLabel, allLabels);
                if (newerVal !== olderVal) {
                    var label = SM_HISTORY_FIELDS[fieldKey] || smHistoryFieldLabel(fieldKey);
                    if (!seen[label]) {
                        seen[label] = true;
                        changes.push({
                            label: label,
                            oldVal: olderVal || '—',
                            newVal: newerVal || '—'
                        });
                    }
                }
            });
            return changes;
        }

        async function smBuildHistoryChangesViaSoap(itemId, entries) {
            const listGuid = await smGetListGuid();
            const fieldKeys = Object.keys(SM_HISTORY_FIELDS);
            const fieldHistories = {};
            const allLabels = entries.map(function (e) { return e.versionLabel; });

            await Promise.all(fieldKeys.map(async function (fieldKey) {
                try {
                    const hist = await smSoapGetVersionCollection(listGuid, itemId, fieldKey);
                    if (hist.length) fieldHistories[fieldKey] = hist;
                } catch (e) {
                    console.warn('[smSoapGetVersionCollection]', fieldKey, e);
                }
            }));

            if (!Object.keys(fieldHistories).length) {
                console.warn('[smBuildHistoryChangesViaSoap] No field histories returned from lists.asmx');
                return null;
            }
            console.log('[smBuildHistoryChangesViaSoap] Loaded', Object.keys(fieldHistories).length, 'field histories via SOAP');

            return entries.map(function (e, idx) {
                var olderLabel = idx < entries.length - 1 ? entries[idx + 1].versionLabel : null;
                var soapChanges = smChangesFromSoapHistories(fieldHistories, e.versionLabel, olderLabel, allLabels);
                var changes = soapChanges.length ? soapChanges : (e.changes || []);
                return Object.assign({}, e, { changes: changes });
            });
        }

        async function smFetchItemFieldsAtVersion(itemId, listGuid, versionNo, viewUrl) {
            const urls = [];
            if (viewUrl) urls.push(viewUrl);
            urls.push(
                SP_URL + '/_layouts/15/listform.aspx?PageType=3&ListId=' + encodeURIComponent(listGuid) +
                    '&ID=' + itemId + '&VersionNo=' + versionNo,
                SP_URL + '/Lists/' + encodeURIComponent(SP_LIST) + '/DispForm.aspx?ID=' + itemId + '&VersionNo=' + versionNo
            );
            for (var u = 0; u < urls.length; u++) {
                try {
                    const res = await fetch(urls[u], { credentials: 'include' });
                    if (!res.ok) continue;
                    const html = await res.text();
                    const fields = smParseDispFormFields(html);
                    if (Object.keys(fields).length) return fields;
                } catch (e) {
                    console.warn('[smFetchItemFieldsAtVersion]', urls[u], e);
                }
            }
            return {};
        }

        function smInitialVersionChanges(fields) {
            const changes = [];
            const seen = {};
            Object.keys(fields || {}).forEach(function (key) {
                if (!key || key.indexOf('__') === 0) return;
                const val = smCleanSpValue(fields[key]);
                if (!val) return;
                const label = SM_HISTORY_FIELDS[key] || smHistoryFieldLabel(key);
                if (seen[label]) return;
                seen[label] = true;
                changes.push({ label: label, oldVal: '—', newVal: val });
            });
            return changes;
        }

        async function smEnrichVersionEntriesByCompare(itemId, entries) {
            var soapEntries = await smBuildHistoryChangesViaSoap(itemId, entries);
            if (soapEntries && soapEntries.some(function (e) { return e.changes && e.changes.length; })) {
                return soapEntries;
            }

            const listGuid = await smGetListGuid();
            const snapshots = [];
            for (var i = 0; i < entries.length; i++) {
                var e = entries[i];
                var versionNo = e.versionNo || smVersionLabelToNo(e.versionLabel);
                snapshots.push(await smFetchItemFieldsAtVersion(itemId, listGuid, versionNo, e.viewUrl));
            }

            var hasSnapshotData = snapshots.some(function (s) { return Object.keys(s).length > 0; });
            if (!hasSnapshotData && soapEntries) return soapEntries;

            return entries.map(function (e, idx) {
                if (e.changes && e.changes.length) return e;
                var newer = snapshots[idx] || {};
                var older = idx < snapshots.length - 1 ? snapshots[idx + 1] : null;
                var changes = older ? smCompareVersionFields(newer, older) : smInitialVersionChanges(newer);
                return Object.assign({}, e, { changes: changes });
            });
        }

        function smParseVersionPropertiesRow(propsRow, changes) {
            smCollectVersionChanges(propsRow, changes, {});
        }

        function smParseVersionsPageHtmlFallback(doc) {
            const entries = [];
            const rows = doc.querySelectorAll('tr');
            for (let i = 0; i < rows.length; i++) {
                const row = rows[i];
                const cells = row.querySelectorAll(':scope > td');
                if (cells.length < 3) continue;
                const firstText = smStripHtml(cells[0].innerHTML).replace(/\s*View\s*$/i, '').trim();
                const verMatch = firstText.match(/^(\d+\.\d+)/);
                if (!verMatch || /^no\.?$/i.test(firstText)) continue;

                const changes = [];
                const nextRow = rows[i + 1];
                if (nextRow) {
                    smParseVersionPropertiesRow(nextRow, changes);
                    i++;
                }
                entries.push({
                    versionLabel: verMatch[1],
                    created: smStripHtml(cells[1].innerHTML),
                    editor: smStripHtml(cells[2].innerHTML),
                    changes: changes
                });
            }
            entries.sort(function (a, b) { return parseFloat(b.versionLabel) - parseFloat(a.versionLabel); });
            entries.forEach(function (e, idx) { e.isCurrent = idx === 0; });
            return entries;
        }

        async function smBuildItemHistoryFromVersionsPage(itemId) {
            const html = await smFetchVersionsPageHtml(itemId);
            let entries = smParseVersionsPageHtml(html);
            if (!entries.length) throw new Error('No version history found');
            entries = await smEnrichVersionEntriesByCompare(itemId, entries);
            return entries;
        }

        async function smFetchVersionFields(itemId, versionId) {
            const textUrl = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items(" + itemId + ")/versions(" + versionId + ")/FieldValuesAsText";
            try {
                const res = await fetch(textUrl, {
                    headers: { 'Accept': 'application/json;odata=verbose' },
                    credentials: 'include'
                });
                if (res.ok) {
                    const data = await res.json();
                    const parsed = smParseVersionFields(data);
                    if (parsed && Object.keys(parsed).length) return parsed;
                }
            } catch (e) {
                console.warn('[smFetchVersionFields] FieldValuesAsText failed', e);
            }

            const rawUrl = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items(" + itemId + ")/versions(" + versionId + ")";
            const rawRes = await fetch(rawUrl, {
                headers: { 'Accept': 'application/json;odata=verbose' },
                credentials: 'include'
            });
            if (!rawRes.ok) return {};
            const rawData = await rawRes.json();
            return smParseVersionFields(rawData);
        }

        function smGetHistoryFieldVal(fields, internalKey) {
            if (!fields) return '';
            if (fields[internalKey] !== undefined && fields[internalKey] !== null) return fields[internalKey];
            var label = SM_HISTORY_FIELDS[internalKey];
            if (label && fields[label] !== undefined && fields[label] !== null) return fields[label];
            return '';
        }

        function smCompareVersionFields(newerFields, olderFields) {
            const changes = [];
            const checked = {};
            function pushChange(key) {
                if (checked[key] || !key || key.indexOf('__') === 0) return;
                checked[key] = true;
                const oldVal = smCleanSpValue((olderFields || {})[key]);
                const newVal = smCleanSpValue((newerFields || {})[key]);
                if (oldVal === newVal) return;
                changes.push({
                    label: SM_HISTORY_FIELDS[key] || smHistoryFieldLabel(key),
                    oldVal: oldVal || '—',
                    newVal: newVal || '—'
                });
            }
            Object.keys(SM_HISTORY_FIELDS).forEach(pushChange);
            Object.keys(newerFields || {}).forEach(pushChange);
            Object.keys(olderFields || {}).forEach(pushChange);
            return changes;
        }

        function smHistoryVersionLabel(version) {
            if (!version) return '';
            if (version.VersionLabel) return version.VersionLabel;
            if (version.VersionId) return (version.VersionId / 512).toFixed(1);
            return String(version.ID || '');
        }

        function smRenderItemHistoryTimeline(entries, container) {
            if (!entries.length) {
                container.innerHTML = '<div style="text-align:center;padding:48px 20px;color:var(--t3);">' +
                    '<i data-lucide="inbox" style="width:40px;height:40px;display:block;margin:0 auto 12px;"></i>' +
                    '<div style="font-size:15px;font-weight:700;">No version history</div>' +
                    '<div style="font-size:13px;margin-top:6px;">This item has not been modified since creation.</div></div>';
                if (typeof lucide !== 'undefined') lucide.createIcons();
                return;
            }

            let html = '<div class="sm-history-timeline">';

            entries.forEach(function (entry, idx) {
                const hasChanges = entry.changes && entry.changes.length > 0;
                const versionText = entry.versionLabel || entry.versionId || ('v' + (idx + 1));
                html += '<div class="sm-history-card">';
                html += '<div class="sm-history-card-head">';
                html += '<div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;">';
                html += '<span class="sm-history-version-badge">' + versionText + '</span>';
                html += '<div><div style="font-size:13px;font-weight:700;color:var(--t1);">' +
                    (entry.isCurrent ? 'Current version' : 'Previous version') + '</div>';
                html += '<div style="font-size:12px;color:var(--t3);margin-top:2px;">Modified by <strong style="color:var(--t2);">' +
                    (entry.editor || 'Unknown') + '</strong></div></div></div>';
                html += '<div style="font-size:12px;color:var(--t3);font-weight:600;white-space:nowrap;">' +
                    smHistoryFmtDate(entry.created) + '</div>';
                html += '</div>';

                if (hasChanges) {
                    const showValueOnly = entry.changes.every(function (ch) { return ch.oldVal === '—' || !ch.oldVal; });
                    html += '<table class="sm-history-changes-table"><thead><tr>';
                    if (showValueOnly) {
                        html += '<th style="width:40%;">Field</th><th style="width:60%;">Value</th>';
                    } else {
                        html += '<th style="width:28%;">Field</th><th style="width:36%;">Previous</th><th style="width:36%;">New value</th>';
                    }
                    html += '</tr></thead><tbody>';
                    entry.changes.forEach(function (ch) {
                        html += '<tr><td style="font-weight:700;color:var(--t1);">' + ch.label + '</td>';
                        if (showValueOnly) {
                            html += '<td><span class="sm-history-new">' + ch.newVal + '</span></td></tr>';
                        } else {
                            html += '<td><span class="sm-history-old">' + ch.oldVal + '</span></td>' +
                                '<td><span class="sm-history-new">' + ch.newVal + '</span></td></tr>';
                        }
                    });
                    html += '</tbody></table>';
                } else if (idx === entries.length - 1) {
                    html += '<div style="padding:14px 16px;font-size:13px;color:var(--t3);">Initial item creation — no field details captured.</div>';
                } else {
                    html += '<div style="padding:14px 16px;font-size:13px;color:var(--t3);">No tracked field changes in this version.</div>';
                }
                html += '</div>';
            });

            html += '</div>';
            container.innerHTML = html;
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }

        async function smBuildItemHistoryEntries(itemId) {
            try {
                return await smBuildItemHistoryFromVersionsPage(itemId);
            } catch (pageErr) {
                console.warn('[smBuildItemHistoryEntries] Versions.aspx parse failed, trying REST', pageErr);
            }
            try {
                const versions = await smFetchItemVersions(itemId);
                if (!versions.length) return [];

                const fieldSets = await Promise.all(versions.map(function (v) {
                    return smFetchVersionFields(itemId, v.ID);
                }));

                return versions.map(function (v, idx) {
                    const fields = fieldSets[idx] || {};
                    const olderFields = idx < versions.length - 1 ? (fieldSets[idx + 1] || {}) : null;
                    const changes = olderFields ? smCompareVersionFields(fields, olderFields) : [];
                    let editor = smEditorFromFields(fields);
                    if (!editor && v.Editor && typeof v.Editor === 'object' && v.Editor.Title) editor = v.Editor.Title;
                    return {
                        versionId: v.ID,
                        versionLabel: smHistoryVersionLabel(v),
                        created: v.Created,
                        editor: editor,
                        changes: changes,
                        isCurrent: idx === 0
                    };
                });
            } catch (restErr) {
                throw new Error('Could not load version history from SharePoint');
            }
        }

        async function smGetListGuid() {
            if (window._SM_LIST_GUID) return window._SM_LIST_GUID;
            const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')?$select=Id";
            const res = await fetch(url, {
                headers: { 'Accept': 'application/json;odata=verbose' },
                credentials: 'include'
            });
            if (!res.ok) throw new Error('Could not resolve SharePoint list ID');
            const data = await res.json();
            window._SM_LIST_GUID = data.d.Id;
            return window._SM_LIST_GUID;
        }

        async function smShowItemHistory() {
            const account = EDIT_ACCOUNT_DATA;
            const code = account && account.code;
            if (!code) { alert('No account selected'); return; }

            const modal = document.getElementById('editHistoryModal');
            const body = document.getElementById('editHistoryModalBody');
            const title = document.getElementById('editHistoryModalTitle');
            if (!modal || !body) return;

            title.textContent = code + (account.customer ? ' · ' + account.customer : '');
            body.innerHTML = '<div style="text-align:center;padding:40px;"><i data-lucide="loader" style="width:24px;height:24px;animation:spin 1s linear infinite;"></i><div style="margin-top:12px;font-size:13px;color:var(--t3);">Loading version history...</div><div style="font-size:11px;color:var(--t3);margin-top:6px;">Fetching each version snapshot from SharePoint…</div></div>';
            modal.style.display = 'flex';
            if (typeof lucide !== 'undefined') lucide.createIcons();

            try {
                let itemId = account.spItemId;
                if (!itemId) itemId = await smFetchListItemId(code);
                account.spItemId = itemId;

                const entries = await smBuildItemHistoryEntries(itemId);
                smRenderItemHistoryTimeline(entries, body);
            } catch (err) {
                console.error('[smShowItemHistory]', err);
                var openLink = '';
                try {
                    var itemId = account.spItemId;
                    if (!itemId) itemId = await smFetchListItemId(code);
                    var listGuid = await smGetListGuid();
                    openLink = SP_URL + '/_layouts/15/Versions.aspx?list=' + encodeURIComponent(listGuid) + '&ID=' + itemId;
                } catch (e) {}
                body.innerHTML = '<div style="color:#ef4444;padding:16px;line-height:1.5;">Error loading item history: ' + err.message +
                    '<div style="font-size:12px;color:var(--t3);margin-top:8px;">On-prem SharePoint may not expose the REST /versions API. Versioning must be enabled on the Service Manager Request list.</div>' +
                    (openLink ? '<div style="margin-top:12px;"><a href="' + openLink + '" target="_blank" rel="noopener" style="color:var(--accent);font-weight:600;">Open SharePoint Version History</a></div>' : '') +
                    '</div>';
            }
        }

        function smCloseItemHistory() {
            const modal = document.getElementById('editHistoryModal');
            if (modal) modal.style.display = 'none';
        }

        window.smShowItemHistory = smShowItemHistory;
        window.smCloseItemHistory = smCloseItemHistory;

        async function openEditRequest(accountCode) {
            try {
                await fetchAccessControlSettings();
                document.querySelectorAll('.dashboard-section').forEach(function (s) { s.style.display = 'none'; });
                document.getElementById('editRequestView').style.display = 'block';
                var contentDiv = document.querySelector('.content');
                var portalShell = document.querySelector('.portal-shell');
                if (contentDiv) {
                    contentDiv.classList.remove('section-mode');
                    contentDiv.classList.add('edit-full-mode');
                }
                if (portalShell) portalShell.classList.add('edit-full-shell');
                // ✅ STEP 2: NOW reset the button (it exists now)
                const submitBtn = document.querySelector('#editReviewSection button[onclick="submitEditRequest()"]');
                if (submitBtn) {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = '<i data-lucide="check" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Submit Changes';
                }

                // ✅ STEP 3: Hide review section
                document.getElementById('editReviewSection').style.display = 'none';
                document.getElementById('editSubmitMessage').innerHTML = '';
                document.getElementById('editReviewContent').innerHTML = '';

                // ✅ STEP 4: Clear global variables
                window.EDIT_REVENUE_DATA = null;
                EDIT_ACCOUNT_DATA = null;

                // ✅ STEP 5: Reset dropdowns
                document.getElementById('editAM').selectedIndex = 0;
                document.getElementById('editAD').selectedIndex = 0;

                // ✅ STEP 6: Load fresh account data
                const account = ALL_DATA.find(a => a.code === accountCode);
                if (!account) {
                    alert('Account not found');
                    return;
                }

                EDIT_ACCOUNT_DATA = account;
                window.EDIT_ACCOUNT_CODE = account.code;
                initEditPocValues(account);

                if (!SM_MAPPING_DATA.length) {
                    await fetchAccountMapping();
                }

                const histBtn = document.getElementById('editViewHistoryBtn');
                if (histBtn) histBtn.style.display = 'inline-flex';

                // Display current info
                let html = '<div class="edit-section-block">';
                html += '<h3 class="table-title"><i data-lucide="building-2" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Account Information</h3>';
                html += '<div class="edit-info-grid">';
                const fields = [
                    ['Account Code', account.code],
                    ['Customer Name', account.customer],
                    ['Team', account.team],
                    ['Line Manager', account.lm],
                    ['Service Manager', account.sm],
                    ['Account Manager', account.am],
                    ['Account Director', account.ad]
                ];

                fields.forEach(function (pair) {
                    html += '<div class="edit-field-card">' +
                        '<div class="edit-field-label">' + pair[0] + '</div>' +
                        '<div class="edit-field-value">' + (pair[1] || '—') + '</div></div>';
                    if (pair[0] === 'Service Manager') {
                        html += smSecondarySmEditHtml();
                    }
                });

                html += '<div class="edit-field-card">' +
                    '<div class="edit-field-label">Account Type</div>' +
                    '<div class="edit-field-value">' + (account.accountType || '—') + '</div></div>';

                html += smBuildEditRnpsEligibilityHtml(account);

                html += '</div></div>';

                html += '<div class="edit-section-block">';
                html += '<h3 class="table-title"><i data-lucide="bar-chart-3" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Monthly Revenue</h3>';

                const revenueMonths = getRevenueMonthsForEdit();

                html += '<h4>Past 3 Months (Read-Only)</h4>';
                html += '<div class="edit-revenue-grid">';

                revenueMonths.lastThree.forEach(function (month) {
                    const value = account.allMonths ? (account.allMonths[month.field] || 0) : 0;
                    const isLocked = !window.REVENUE_ENABLED;
                    html += '<div class="edit-revenue-card">' +
                        '<div class="edit-field-label">' + month.label + '</div>' +
                        '<input type="number" id="hist_' + month.field + '" value="' + value + '" ' + (isLocked ? 'disabled' : '') +
                        ' style="width: 100%; padding: 10px; background: var(--bg-input); border: 1px solid var(--border-s); border-radius: 8px; font-size: 16px; font-weight: 700; color: var(--t1); cursor: ' + (isLocked ? 'not-allowed' : 'text') + ';">' +
                        '</div>';
                });
                html += '</div>';

                if (revenueMonths.editable && !window.REVENUE_ENABLED) {
                    const editMonth = revenueMonths.editable;
                    const currentValue = account.allMonths ? (account.allMonths[editMonth.field] || 0) : 0;
                    const isLocked = isMonthLocked(currentValue, editMonth);

                    html += '<h4 style="margin-top: 1.25rem;">' +
                        (isLocked ? 'Previous Month (Locked)' : 'Previous Month (You Can Submit Revenue)') +
                        '</h4>';

                    if (isLocked) {
                        html += '<div class="edit-revenue-card" style="border-color: rgba(16,185,129,.45); background: rgba(16,185,129,.08);">' +
                            '<div style="display:flex;align-items:center;gap:12px;margin-bottom:8px;">' +
                            '<i data-lucide="lock" style="width:20px;height:20px;color:var(--success);"></i>' +
                            '<div class="edit-field-label" style="color:var(--success);margin:0;">' + editMonth.label + ' — LOCKED</div></div>' +
                            '<div class="edit-field-value" style="font-size:1.25rem;color:var(--success);">' + formatCurrency(currentValue) + '</div>' +
                            '<div style="font-size:12px;color:var(--t3);margin-top:8px;">Revenue submitted and locked. Contact admin to modify.</div></div>';
                    } else {
                        html += '<div class="edit-revenue-card" style="border-color: var(--accent); background: rgba(168,85,247,.06); margin-top: .5rem;">' +
                            '<div style="display:flex;align-items:center;gap:12px;margin-bottom:12px;">' +
                            '<i data-lucide="edit-3" style="width:20px;height:20px;color:var(--accent);"></i>' +
                            '<div class="edit-field-label" style="color:var(--accent);margin:0;">' + editMonth.label + ' Revenue</div></div>' +
                            '<input type="number" step="0.01" id="editRevenue_' + editMonth.field + '" value="' + currentValue + '" ' +
                            'style="width:100%;padding:14px;background:var(--bg-input);border:2px solid var(--accent);border-radius:10px;font-size:18px;font-weight:700;color:var(--accent);" placeholder="Enter revenue amount">' +
                            '<div style="font-size:12px;color:var(--t3);margin-top:8px;">Deadline: ' + getDeadlineText(editMonth) + ' | Once submitted, cannot be edited.</div></div>';
                    }
                } else if (!window.REVENUE_ENABLED) {
                    html += '<div class="edit-revenue-card" style="text-align:center;color:var(--danger);margin-top:1rem;">No editable month available (deadline passed).</div>';
                }

                html += '</div>';

                html += '<div class="edit-section-block">';
                html += '<h3 class="table-title"><i data-lucide="users" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>POC Details</h3>';
                html += '<div class="filter-group" id="editRnpsPocWrap" style="margin-bottom: 1rem;">' +
                    '<label class="filter-label">RNPS POC</label>' +
                    '<div style="display:flex;gap:1.25rem;flex-wrap:wrap;padding:8px 0;">';
                ['Primary', 'Secondary', 'Technical'].forEach(function (opt) {
                    const selected = smNormalizeRnpsPoc(account.rnpsPoc) === opt;
                    html += '<label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-size:14px;font-weight:600;color:var(--t1);">' +
                        '<input type="radio" name="editRnpsPoc" value="' + opt + '"' + (selected ? ' checked' : '') + '> ' + opt +
                        '</label>';
                });
                html += '</div></div>';
                html += '<div class="filter-group" style="margin-bottom: 1rem; max-width: 360px;">' +
                    '<label class="filter-label">POC Category</label>' +
                    '<select class="filter-select" id="editPocCategory" onchange="onEditPocCategoryChange()" style="font-size: 14px; padding: 10px;">' +
                    '<option value="primary" selected>Primary POC Details</option>' +
                    '<option value="secondary">Secondary POC Details</option>' +
                    '<option value="technical">Technical POC Details</option>' +
                    '</select></div>' +
                    '<div id="editPocFields"></div></div>';

                document.getElementById('editCurrentInfo').innerHTML = html;
                renderEditPocFields();
                smSyncEditRnpsFieldsUi();

                if (account.secondarySm) {
                    const mappedExisting = SM_MAPPING_DATA.find(function(a) {
                        return smNormalizePersonName(a.name) === smNormalizePersonName(account.secondarySm);
                    });
                    smSelectSecondarySm(
                        mappedExisting ? mappedExisting.name : account.secondarySm,
                        mappedExisting ? mappedExisting.team : ''
                    );
                } else {
                    smClearSecondarySm();
                }

                // Populate AM/AD dropdowns
                const allAMs = [...new Set(ALL_DATA.map(a => a.am))].filter(Boolean).sort();
                const allADs = [...new Set(ALL_DATA.map(a => a.ad))].filter(Boolean).sort();

                const amSelect = document.getElementById('editAM');
                amSelect.innerHTML = '<option value="">Select Account Manager</option>';
                allAMs.forEach(am => {
                    const opt = document.createElement('option');
                    opt.value = am;
                    opt.textContent = am;
                    if (am === account.am) opt.selected = true;
                    amSelect.appendChild(opt);
                });

                const adSelect = document.getElementById('editAD');
                adSelect.innerHTML = '<option value="">Select Account Director</option>';
                allADs.forEach(ad => {
                    const opt = document.createElement('option');
                    opt.value = ad;
                    opt.textContent = ad;
                    if (ad === account.ad) opt.selected = true;
                    adSelect.appendChild(opt);
                });

                // Initialize Lucide icons
                if (typeof lucide !== 'undefined') {
                    lucide.createIcons();
                }

            } catch (err) {
                console.error('Error:', err);
                alert('Error opening edit view: ' + err.message);
            }
        }

        /**
         * Helper: Get deadline text for display
         */
        function getDeadlineText(monthInfo) {
            const deadlineMonth = monthInfo.month + 1;
            const deadlineYear = monthInfo.year;
            const lastDay = new Date(deadlineYear, deadlineMonth + 1, 0);

            const monthNames = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
            return `${monthNames[lastDay.getMonth()]} ${lastDay.getDate()}, ${deadlineYear}`;
        }
        async function populateEditDropdowns(account) {
            // Get all unique AMs and ADs
            const allAMs = [...new Set(ALL_DATA.map(a => a.am))].filter(Boolean).sort();
            const allADs = [...new Set(ALL_DATA.map(a => a.ad))].filter(Boolean).sort();

            const amSelect = document.getElementById('editAM');
            amSelect.innerHTML = '<option value="">Select Account Manager</option>';
            allAMs.forEach(am => {
                const opt = document.createElement('option');
                opt.value = am;
                opt.textContent = am;
                if (am === account.am) opt.selected = true;
                amSelect.appendChild(opt);
            });

            const adSelect = document.getElementById('editAD');
            adSelect.innerHTML = '<option value="">Select Account Director</option>';
            allADs.forEach(ad => {
                const opt = document.createElement('option');
                opt.value = ad;
                opt.textContent = ad;
                if (ad === account.ad) opt.selected = true;
                adSelect.appendChild(opt);
            });
        }

        function initEditPocValues(account) {
            window.EDIT_POC_VALUES = {
                pocName: account.pocName || '',
                pocEmail: account.pocEmail || '',
                pocPhone: account.pocPhone || '',
                secondaryPocName: account.secondaryPocName || '',
                secondaryPocEmail: account.secondaryPocEmail || '',
                secondaryPocPhone: account.secondaryPocPhone || '',
                technicalPocName: account.technicalPocName || '',
                technicalPocEmail: account.technicalPocEmail || '',
                technicalPocPhone: account.technicalPocPhone || ''
            };
        }

        function syncEditPocFromDom() {
            if (!window.EDIT_POC_VALUES) return;
            [
                ['pocName', 'editPrimaryName'],
                ['pocEmail', 'editPrimaryEmail'],
                ['pocPhone', 'editPrimaryContact'],
                ['secondaryPocName', 'editSecondaryName'],
                ['secondaryPocEmail', 'editSecondaryEmail'],
                ['secondaryPocPhone', 'editSecondaryContact'],
                ['technicalPocName', 'editTechnicalName'],
                ['technicalPocEmail', 'editTechnicalEmail'],
                ['technicalPocPhone', 'editTechnicalContact']
            ].forEach(function (pair) {
                const el = document.getElementById(pair[1]);
                if (el) window.EDIT_POC_VALUES[pair[0]] = el.value.trim();
            });
        }

        function onEditPocCategoryChange() {
            syncEditPocFromDom();
            renderEditPocFields(true);
        }

        function renderEditPocFields(skipSync) {
            const account = EDIT_ACCOUNT_DATA;
            if (!account) return;
            if (!skipSync) syncEditPocFromDom();
            if (!window.EDIT_POC_VALUES) initEditPocValues(account);
            const cat = (document.getElementById('editPocCategory') || {}).value || 'primary';
            const wrap = document.getElementById('editPocFields');
            if (!wrap) return;
            const values = window.EDIT_POC_VALUES;
            // Global flag — when enabled by admin, all users with edit access can change POC fields
            const editable = !!window.POC_DETAILS_ENABLED;

            const configs = {
                primary: {
                    title: 'Primary POC Details',
                    prefix: 'primary',
                    fields: [
                        ['POC Name', 'pocName'],
                        ['POC Email', 'pocEmail'],
                        ['POC Contact', 'pocPhone']
                    ]
                },
                secondary: {
                    title: 'Secondary POC Details',
                    prefix: 'secondary',
                    fields: [
                        ['Secondary POC Name', 'secondaryPocName'],
                        ['Secondary POC Email', 'secondaryPocEmail'],
                        ['Secondary POC Contact', 'secondaryPocPhone']
                    ]
                },
                technical: {
                    title: 'Technical POC Details',
                    prefix: 'technical',
                    fields: [
                        ['Technical POC Name', 'technicalPocName'],
                        ['Technical POC Email', 'technicalPocEmail'],
                        ['Technical POC Contact', 'technicalPocPhone']
                    ]
                }
            };
            const cfg = configs[cat] || configs.primary;
            let html = '<div class="edit-revenue-card" style="margin-top:.5rem;">';
            html += '<div class="edit-field-label" style="color:var(--accent);margin-bottom:14px;">' + cfg.title + (editable ? ' <span style="font-size:11px;color:var(--success);font-weight:600;">(Editable)</span>' : ' <span style="font-size:11px;color:var(--t3);font-weight:600;">(Read-only)</span>') + '</div>';
            html += '<div class="edit-info-grid" style="grid-template-columns:repeat(3,minmax(0,1fr));">';

            if (!editable) {
                cfg.fields.forEach(function (pair) {
                    const val = values[pair[1]] || '';
                    html += '<div class="edit-field-card"><div class="edit-field-label">' + pair[0] + '</div><div class="edit-field-value">' + (val || '—') + '</div></div>';
                });
            } else {
                const ids = ['Name', 'Email', 'Contact'];
                cfg.fields.forEach(function (pair, idx) {
                    const inputId = 'edit' + cfg.prefix.charAt(0).toUpperCase() + cfg.prefix.slice(1) + ids[idx];
                    const inputType = idx === 1 ? 'email' : 'text';
                    const isContact = idx === 2;
                    const val = isContact ? (values[pair[1]] || '+971') : (values[pair[1]] || '');
                    const contactAttrs = isContact
                        ? ' placeholder="+971501234567" maxlength="13" oninput="formatPhoneNumber(this)"'
                        : '';
                    html += '<div class="filter-group"><label class="filter-label">' + pair[0] + '</label><input type="' + inputType + '" class="filter-select" id="' + inputId + '" value="' + String(val).replace(/"/g, '&quot;') + '"' + contactAttrs + ' style="cursor:text;font-size:14px;padding:12px 16px;"></div>';
                });
            }
            html += '</div></div>';
            wrap.innerHTML = html;
        }

        function getEditPocChanges() {
            const changes = [];
            if (!EDIT_ACCOUNT_DATA || !window.POC_DETAILS_ENABLED) return changes;
            syncEditPocFromDom();
            const values = window.EDIT_POC_VALUES || {};
            const pairs = [
                ['POC Name', 'pocName'],
                ['POC Email', 'pocEmail'],
                ['POC Contact', 'pocPhone'],
                ['Secondary POC Name', 'secondaryPocName'],
                ['Secondary POC Email', 'secondaryPocEmail'],
                ['Secondary POC Contact', 'secondaryPocPhone'],
                ['Technical POC Name', 'technicalPocName'],
                ['Technical POC Email', 'technicalPocEmail'],
                ['Technical POC Contact', 'technicalPocPhone']
            ];
            pairs.forEach(function (p) {
                const oldVal = EDIT_ACCOUNT_DATA[p[1]] || '';
                const newVal = values[p[1]] || '';
                if (newVal !== oldVal) changes.push({ label: p[0], oldVal: oldVal || '—', newVal: newVal || '—', field: p[1] });
            });
            return changes;
        }

        function backFromEditRequest() {
            document.getElementById('editRequestView').style.display = 'none';
            var dashView = document.getElementById('dashboard-view');
            if (dashView) dashView.style.display = 'block';
            var contentDiv = document.querySelector('.content');
            var portalShell = document.querySelector('.portal-shell');
            if (contentDiv) {
                contentDiv.classList.remove('section-mode');
                contentDiv.classList.remove('edit-full-mode');
            }
            if (portalShell) portalShell.classList.remove('edit-full-shell');
            const histBtn = document.getElementById('editViewHistoryBtn');
            if (histBtn) histBtn.style.display = 'none';
            window.EDIT_ACCOUNT_CODE = null;
            window.EDIT_POC_VALUES = null;
            window.EDIT_RNPS_POC = null;
            window.EDIT_RNPS_NOT_ELIGIBLE_REASON = null;
            window.EDIT_RNPS_ELIGIBILITY = null;
        }


        function reviewEditRequest() {
            const newAM = document.getElementById('editAM').value;
            const newAD = document.getElementById('editAD').value;
            const newSecondarySm = smNormalizePersonName((document.getElementById('editSecondarySM') || {}).value);
            const oldSecondarySm = smNormalizePersonName(EDIT_ACCOUNT_DATA.secondarySm);

            if (!newAM || !newAD) {
                alert('Please fill in all required fields');
                return;
            }
            // Collect past 3 months if revenue enabled
            const lastThree = getLastThreeCompletedMonths();
            window.EDIT_ALL_MONTHS = [];
            if (window.REVENUE_ENABLED) {
                lastThree.forEach(month => {
                    const inputEl = document.getElementById('hist_' + month.field);
                    if (inputEl) {
                        window.EDIT_ALL_MONTHS.push({
                            field: month.field,
                            value: parseFloat(inputEl.value) || 0
                        });
                    }
                });
            }

            // rest of reviewEditRequest continues here...
            const revenueMonths = getRevenueMonthsForEdit();
            let newRevenue = null;
            let revenueField = null;

            if (revenueMonths.editable) {
                const editMonth = revenueMonths.editable;
                const inputId = 'editRevenue_' + editMonth.field;
                const inputEl = document.getElementById(inputId);
                if (inputEl) {
                    newRevenue = parseFloat(inputEl.value);
                    revenueField = editMonth.field;
                    if (isNaN(newRevenue) || newRevenue < 0) {
                        alert('Please enter a valid revenue amount (0 or greater)');
                        return;
                    }
                }
            }

            const amChanged = newAM !== EDIT_ACCOUNT_DATA.am;
            const adChanged = newAD !== EDIT_ACCOUNT_DATA.ad;
            const secondarySmChanged = newSecondarySm !== oldSecondarySm;
            const revenueChanged = newRevenue !== null && newRevenue !== (EDIT_ACCOUNT_DATA.allMonths[revenueField] || 0);
            let newRnpsEligibility = EDIT_ACCOUNT_DATA.rnpsEligibility || '';
            let rnpsChanged = false;
            if (smCanEditRnpsEligibility()) {
                const rnpsEl = document.getElementById('editRnpsEligibility');
                newRnpsEligibility = rnpsEl ? rnpsEl.value : (EDIT_ACCOUNT_DATA.rnpsEligibility || '');
                rnpsChanged = newRnpsEligibility !== (EDIT_ACCOUNT_DATA.rnpsEligibility || '');
            }

            const effectiveRnpsEligibility = smCanEditRnpsEligibility() ? newRnpsEligibility : (EDIT_ACCOUNT_DATA.rnpsEligibility || '');
            const isRnpsYes = smIsRnpsEligible(effectiveRnpsEligibility);
            const isRnpsNo = smIsRnpsNotEligible(effectiveRnpsEligibility);

            let newRnpsNotEligibleReason = EDIT_ACCOUNT_DATA.rnpsNotEligibleReason || '';
            if (smCanEditRnpsEligibility() && isRnpsNo) {
                newRnpsNotEligibleReason = (document.getElementById('editRnpsNotEligibleReason') || {}).value.trim();
                if (!newRnpsNotEligibleReason) {
                    alert('Reason for RNPS not Eligible is required when RNPS Eligibility is No');
                    return;
                }
            } else if (smCanEditRnpsEligibility() && isRnpsYes) {
                newRnpsNotEligibleReason = '';
            }

            const pocChanges = getEditPocChanges();
            const pocChanged = pocChanges.length > 0;
            if (window.POC_DETAILS_ENABLED && pocChanged) {
                const phoneErr = smValidatePocPhoneFields(
                    window.EDIT_POC_VALUES,
                    pocChanges.map(function (c) { return c.field; })
                );
                if (phoneErr) {
                    alert(phoneErr);
                    return;
                }
            }

            let newRnpsPoc = '';
            if (isRnpsYes) {
                const pocChecked = document.querySelector('input[name="editRnpsPoc"]:checked');
                if (!pocChecked) {
                    alert('Please select RNPS POC when RNPS Eligibility is Yes');
                    return;
                }
                newRnpsPoc = smNormalizeRnpsPoc(pocChecked.value);
            }

            const rnpsReasonChanged = newRnpsNotEligibleReason !== (EDIT_ACCOUNT_DATA.rnpsNotEligibleReason || '');
            const oldRnpsPocDisplay = smIsRnpsNotEligible(EDIT_ACCOUNT_DATA.rnpsEligibility) ? '' : smNormalizeRnpsPoc(EDIT_ACCOUNT_DATA.rnpsPoc);
            const rnpsPocChanged = isRnpsYes
                ? newRnpsPoc !== oldRnpsPocDisplay
                : (EDIT_ACCOUNT_DATA.rnpsPoc && !smIsRnpsNotEligible(EDIT_ACCOUNT_DATA.rnpsEligibility));

            if (!amChanged && !adChanged && !secondarySmChanged && !revenueChanged && !rnpsChanged && !rnpsReasonChanged && !pocChanged && !rnpsPocChanged && window.EDIT_ALL_MONTHS.length === 0) {
                alert('No changes detected');
                return;
            }

            window.EDIT_SECONDARY_SM = newSecondarySm;

            let html = '<div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16px;">';
            const changes = [
                ['Account Code', EDIT_ACCOUNT_DATA.code, EDIT_ACCOUNT_DATA.code],
                ['Customer', EDIT_ACCOUNT_DATA.customer, EDIT_ACCOUNT_DATA.customer],
                ['Team', EDIT_ACCOUNT_DATA.team, EDIT_ACCOUNT_DATA.team],
                ['Line Manager', EDIT_ACCOUNT_DATA.lm, EDIT_ACCOUNT_DATA.lm],
                ['Service Manager', EDIT_ACCOUNT_DATA.sm, EDIT_ACCOUNT_DATA.sm],
                ['Secondary SM', EDIT_ACCOUNT_DATA.secondarySm || '—', newSecondarySm || '—'],
                ['Account Manager', EDIT_ACCOUNT_DATA.am, newAM],
                ['Account Director', EDIT_ACCOUNT_DATA.ad, newAD],
                ['Account Type', EDIT_ACCOUNT_DATA.accountType || '—', EDIT_ACCOUNT_DATA.accountType || '—'],
                ['RNPS Eligibility', EDIT_ACCOUNT_DATA.rnpsEligibility || '—', newRnpsEligibility || '—'],
                ['Reason for RNPS not Eligible', EDIT_ACCOUNT_DATA.rnpsNotEligibleReason || '—', newRnpsNotEligibleReason || '—'],
                ['RNPS POC', oldRnpsPocDisplay || '—', isRnpsYes ? newRnpsPoc : '—']
            ];

            if (revenueChanged) {
                const monthLabel = revenueMonths.editable.label;
                const oldValue = EDIT_ACCOUNT_DATA.allMonths[revenueField] || 0;
                changes.push([`${monthLabel} Revenue`, formatCurrency(oldValue), formatCurrency(newRevenue)]);
            }
            if (window.REVENUE_ENABLED && window.EDIT_ALL_MONTHS && window.EDIT_ALL_MONTHS.length > 0) {
                window.EDIT_ALL_MONTHS.forEach(m => {
                    const oldValue = EDIT_ACCOUNT_DATA.allMonths[m.field] || 0;
                    if (m.value !== oldValue) {
                        const monthLabel = m.field.replace('_x002d_', '-').replace('_', ' ');
                        changes.push([monthLabel + ' Revenue', formatCurrency(oldValue), formatCurrency(m.value)]);
                    }
                });
            }
            pocChanges.forEach(function (pc) {
                changes.push([pc.label, pc.oldVal, pc.newVal]);
            });
            window.EDIT_POC_CHANGES = pocChanges;
            window.EDIT_RNPS_POC = isRnpsYes ? newRnpsPoc : '';
            window.EDIT_RNPS_NOT_ELIGIBLE_REASON = newRnpsNotEligibleReason;
            window.EDIT_RNPS_ELIGIBILITY = effectiveRnpsEligibility;
            changes.forEach(([label, oldVal, newVal]) => {
                const hasChanged = oldVal !== newVal;
                const safeLabel = smEscapeHtml(label);
                const safeOld = smEscapeHtml(oldVal);
                const safeNew = smEscapeHtml(newVal);
                html += `<div style="padding: 12px; background: ${hasChanged ? 'rgba(16, 185, 129, 0.15)' : 'rgba(168, 85, 247, 0.1)'}; border-radius: 8px; border: ${hasChanged ? '2px solid var(--success)' : 'none'};">
            <div style="font-size: 11px; color: var(--text-secondary); margin-bottom: 4px; font-weight: 600;">${safeLabel}</div>
            <div style="font-size: 14px; font-weight: 600;">${hasChanged ? `${safeOld} <i data-lucide="arrow-right" style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin: 0 4px;"></i> ${safeNew}` : safeNew}</div>
        </div>`;
            });

            html += '</div>';
            window.EDIT_REVENUE_DATA = {
                field: revenueField,
                value: newRevenue
            };
            document.getElementById('editReviewContent').innerHTML = html;
            document.getElementById('editReviewSection').style.display = 'block';
            document.getElementById('editSubmitMessage').innerHTML = '';
            if (typeof lucide !== 'undefined') lucide.createIcons();
        }

        async function submitEditRequest() {
            const submitBtn = event.target;
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i data-lucide="loader" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px; animation: spin 1s linear infinite;"></i>Submitting...';

            try {
                const newAM = document.getElementById('editAM').value;
                const newAD = document.getElementById('editAD').value;
                const newSecondarySm = smNormalizePersonName((document.getElementById('editSecondarySM') || {}).value);
                const oldSecondarySm = smNormalizePersonName(EDIT_ACCOUNT_DATA.secondarySm);

                // Find item ID
                const searchUrl = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                    "$select=ID&$filter=Title eq '" + EDIT_ACCOUNT_DATA.code + "'&$top=1";

                const searchRes = await fetch(searchUrl, {
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });

                if (!searchRes.ok) throw new Error('Cannot find account');
                const searchData = await searchRes.json();
                if (searchData.d.results.length === 0) throw new Error('Account not found');

                const itemId = searchData.d.results[0].ID;

                // Get user IDs
                const amId = await getUserId(newAM);
                const adId = await getUserId(newAD);
                const secondarySmChanged = newSecondarySm !== oldSecondarySm;
                let secondarySmId = null;
                if (secondarySmChanged && newSecondarySm) {
                    secondarySmId = await smResolvePersonId(newSecondarySm);
                    if (!secondarySmId) throw new Error('Could not resolve Secondary SM user: ' + newSecondarySm);
                }

                // Get form digest
                const digestRes = await fetch(SP_URL + "/_api/contextinfo", {
                    method: 'POST',
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });
                if (!digestRes.ok) throw new Error('Failed to get form digest');
                const digestData = await digestRes.json();
                const digest = digestData.d.GetContextWebInformation.FormDigestValue;

                // Build update data
                const updateUrl = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items(" + itemId + ")";
                const updateData = {
                    __metadata: {
                        type: 'SP.Data.Service_x0020_Manager_x0020_RequestListItem'
                    },
                    Account_x0020_ManagerId: amId,
                    Account_x0020_DirectorId: adId
                };
                if (secondarySmChanged) {
                    const replacingExisting = !!oldSecondarySm && !!newSecondarySm;
                    if (replacingExisting) {
                        // SharePoint on-prem person fields often fail on direct swap — clear first, then set new
                        await smPatchListItem(itemId, digest, { Secondary_x0020_Service_x0020_MaId: null });
                        updateData.Secondary_x0020_Service_x0020_MaId = secondarySmId;
                    } else {
                        updateData.Secondary_x0020_Service_x0020_MaId = newSecondarySm ? secondarySmId : null;
                    }
                }

                if (window.EDIT_REVENUE_DATA && window.EDIT_REVENUE_DATA.field) {
                    updateData[window.EDIT_REVENUE_DATA.field] = window.EDIT_REVENUE_DATA.value;
                }

                if (smCanEditRnpsEligibility()) {
                    const rnpsEl = document.getElementById('editRnpsEligibility');
                    if (rnpsEl) {
                        updateData.RNPS_Eligibility = rnpsEl.value;
                        if (smIsRnpsNotEligible(rnpsEl.value)) {
                            updateData.RNPS_POC = null;
                            updateData.RNPS_Not_Eligible_Reason = (document.getElementById('editRnpsNotEligibleReason') || {}).value.trim();
                        } else if (smIsRnpsEligible(rnpsEl.value)) {
                            updateData.RNPS_Not_Eligible_Reason = '';
                        }
                    }
                }

                // Save all 3 months if revenue enabled
                if (window.REVENUE_ENABLED && window.EDIT_ALL_MONTHS && window.EDIT_ALL_MONTHS.length > 0) {
                    window.EDIT_ALL_MONTHS.forEach(m => {
                        updateData[m.field] = m.value;
                    });
                }

                var spPocMap = {
                    pocName: 'POC_x0020_Name',
                    pocEmail: 'POC_x0020_Email_x0020_ID',
                    pocPhone: 'POC_x0020_Contact_x0020_No',
                    secondaryPocName: 'Secondary_x0020_POC_x0020_Name',
                    secondaryPocEmail: 'Secondary_x0020_POC_x0020_Email',
                    secondaryPocPhone: 'Secondary_x0020_POC_x0020_Contac',
                    technicalPocName: 'Technical_x0020_POC_x0020_Name',
                    technicalPocEmail: 'Technical_x0020_POC_x0020_Email',
                    technicalPocPhone: 'Technical_x0020_POC_x0020_Contac'
                };
                (window.EDIT_POC_CHANGES || []).forEach(function (pc) {
                    if (spPocMap[pc.field]) updateData[spPocMap[pc.field]] = pc.newVal === '—' ? '' : pc.newVal;
                });

                if (window.EDIT_RNPS_POC) {
                    updateData.RNPS_POC = window.EDIT_RNPS_POC;
                } else if (smIsRnpsEligible(window.EDIT_RNPS_ELIGIBILITY || (EDIT_ACCOUNT_DATA && EDIT_ACCOUNT_DATA.rnpsEligibility))) {
                    const rnpsPocEl = document.querySelector('input[name="editRnpsPoc"]:checked');
                    if (rnpsPocEl) updateData.RNPS_POC = smNormalizeRnpsPoc(rnpsPocEl.value);
                } else if (smIsRnpsNotEligible(window.EDIT_RNPS_ELIGIBILITY || (EDIT_ACCOUNT_DATA && EDIT_ACCOUNT_DATA.rnpsEligibility))) {
                    updateData.RNPS_POC = null;
                }

                const updateRes = await fetch(updateUrl, {
                    method: 'POST',
                    headers: {
                        'Accept': 'application/json;odata=verbose',
                        'Content-Type': 'application/json;odata=verbose',
                        'X-RequestDigest': digest,
                        'IF-MATCH': '*',
                        'X-HTTP-Method': 'MERGE'
                    },
                    credentials: 'include',
                    body: JSON.stringify(updateData)
                });

                if (!updateRes.ok) {
                    const errorText = await updateRes.text();
                    throw new Error('Update failed: ' + errorText);
                }

                document.getElementById('editSubmitMessage').innerHTML = '<span style="color: var(--success);">✅ Account updated successfully!</span>';

                // Clear stored revenue data
                window.EDIT_REVENUE_DATA = null;
                window.EDIT_POC_VALUES = null;
                window.EDIT_RNPS_POC = null;
                window.EDIT_RNPS_NOT_ELIGIBLE_REASON = null;
                window.EDIT_RNPS_ELIGIBILITY = null;
                EDIT_ACCOUNT_DATA = null;
                setTimeout(() => {
                    // RESET EVERYTHING
                    document.getElementById('editReviewSection').style.display = 'none';
                    document.getElementById('editSubmitMessage').innerHTML = '';
                    window.EDIT_REVENUE_DATA = null;
                    window.EDIT_POC_VALUES = null;
                    window.EDIT_RNPS_POC = null;
                    window.EDIT_RNPS_NOT_ELIGIBLE_REASON = null;
                    window.EDIT_RNPS_ELIGIBILITY = null;
                    EDIT_ACCOUNT_DATA = null;

                    // Reset dropdowns to placeholder
                    document.getElementById('editAM').selectedIndex = 0;
                    document.getElementById('editAD').selectedIndex = 0;
                    smClearSecondarySm();

                    // Clear current info display
                    document.getElementById('editCurrentInfo').innerHTML = '';

                    backFromEditRequest();
                    init();
                }, 2000);

            } catch (err) {
                console.error('Error:', err);
                document.getElementById('editSubmitMessage').innerHTML = '<span style="color: var(--danger);">Error: ' + err.message + '</span>';
                submitBtn.disabled = false;
                submitBtn.innerHTML = '<i data-lucide="check" style="width: 16px; height: 16px; display: inline-block; vertical-align: middle; margin-right: 6px;"></i>Submit Changes';
                lucide.createIcons();
            }
        }
        // ========================================
        // ACCOUNT SEARCH FUNCTIONS
        // ========================================

        function showAccountSearch() {
            const landing = document.getElementById('landingPage');
            if (landing) {
                landing.style.display = 'none';
                landing.style.visibility = 'hidden';
                landing.style.position = 'absolute';
                landing.style.zIndex = '-1';
            }
            const dc = document.getElementById('dashboardContent');
            if (dc) dc.style.display = 'none';

            document.getElementById('accountSearchView').style.display = 'block';
            window.scrollTo(0, 0);

            // Sync theme selector with current theme
            const currentTheme = document.body.getAttribute('data-theme');
            const searchSelector = document.getElementById('searchColorSchemeSelector');
            const searchIcon = document.getElementById('searchThemeIcon');

            if (searchSelector) {
                if (currentTheme === 'duralux' || currentTheme === 'duralux-dark') {
                    searchSelector.value = 'duralux';
                } else {
                    searchSelector.value = 'magenta';
                }
            }

            if (searchIcon) {
                if (currentTheme === 'dark' || currentTheme === 'duralux-dark') {
                    searchIcon.setAttribute('data-lucide', 'sun');
                } else {
                    searchIcon.setAttribute('data-lucide', 'moon');
                }
            }

            // Initialize Lucide icons
            if (typeof lucide !== 'undefined') {
                lucide.createIcons();
            }
        }

        // ─── COMING SOON VIEWS ──────────────────────────────────────────────
        var ALL_VIEW_IDS = ['dashboardContent', 'landingPage', 'customerServiceView',
            'reviewRequestView', 'transferRequestView', 'sdReviewTransferView',
            'amAdDashboard', 'amTransferRequestsView', 'amMyRequestsView', 'amRequestDetailView',
            'createRequestView', 'accountSearchView', 'comingSoonView', 'portalAnalyticsView',
            'editRequestView', 'executiveHubView', 'contactUsView', 'newsletterView'
        ];

        window.showPortalAnalytics = function() {
            ALL_VIEW_IDS.forEach(function(vid) {
                var el = document.getElementById(vid);
                if (el) {
                    el.style.display = 'none';
                    el.style.position = 'absolute';
                    el.style.zIndex = '-1';
                }
            });
            var view = document.getElementById('portalAnalyticsView');
            if (view) {
                view.style.position = '';
                view.style.zIndex = '';
                view.style.display = 'block';
            }
            window.scrollTo(0, 0);
            if (typeof analyticsLoadDashboard === 'function') analyticsLoadDashboard();
            if (typeof lucide !== 'undefined') lucide.createIcons();
        };

        window.showPortalAnalyticsBack = function() {
            var view = document.getElementById('portalAnalyticsView');
            if (view) {
                view.style.display = 'none';
                view.style.position = 'absolute';
                view.style.zIndex = '-1';
            }
            var dc = document.getElementById('dashboardContent');
            if (dc) {
                dc.style.position = '';
                dc.style.zIndex = '';
                dc.style.display = 'block';
            }
            if (typeof switchDashboardSection === 'function') switchDashboardSection('dashboard-view');
            window.scrollTo(0, 0);
        };

        window.showComingSoon = function(id, title, icon) {
            if (typeof analyticsTrackPage === 'function') analyticsTrackPage(title);
            ALL_VIEW_IDS.forEach(function(vid) {
                var el = document.getElementById(vid);
                if (el) {
                    el.style.display = 'none';
                    el.style.position = 'absolute';
                    el.style.zIndex = '-1';
                }
            });

            // Update the single shared view
            var view = document.getElementById('comingSoonView');
            document.getElementById('comingSoonTitle').textContent = title;
            document.getElementById('comingSoonHeading').textContent = title;
            document.getElementById('comingSoonIcon').setAttribute('data-lucide', icon);
            document.getElementById('comingSoonBigIcon').setAttribute('data-lucide', icon);

            view.style.position = '';
            view.style.zIndex = '';
            view.style.display = 'block';
            window.scrollTo(0, 0);
            if (typeof lucide !== 'undefined') lucide.createIcons();
        };

        window.showComingSoonBack = function() {
            var view = document.getElementById('comingSoonView');
            if (view) {
                view.style.display = 'none';
                view.style.position = 'absolute';
                view.style.zIndex = '-1';
            }
            var dc = document.getElementById('dashboardContent');
            if (dc) {
                dc.style.position = '';
                dc.style.zIndex = '';
                dc.style.display = 'block';
            }
            if (typeof switchDashboardSection === 'function') switchDashboardSection('dashboard-view');
            window.scrollTo(0, 0);
            if (typeof lucide !== 'undefined') lucide.createIcons();
        };

        function backToLanding() {
            document.getElementById('accountSearchView').style.display = 'none';
            const landing = document.getElementById('landingPage');
            if (landing) {
                landing.style.position = '';
                landing.style.visibility = '';
                landing.style.zIndex = '';
                landing.style.display = 'grid';
            }

            // Clear search results
            document.getElementById('searchAccountCode').value = '';
            document.getElementById('accountDetailsSection').style.display = 'none';
            document.getElementById('relatedAccountsSection').style.display = 'none';

            if (typeof lucide !== 'undefined') {
                lucide.createIcons();
            }
        }

        async function searchAccount() {
            const accountCode = document.getElementById('searchAccountCode').value.trim();

            if (!accountCode) {
                alert('Please enter an account code');
                return;
            }

            try {
                // Fetch account from SharePoint
                const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                    "$select=Title,Parent_x0020_Code,Customer_x0020_Name,Team," +
                    "Line_x0020_Manager/Title,Service_x0020_Manager/Title," +
                    "Secondary_x0020_Service_x0020_Ma/Title," +
                    "Account_x0020_Manager/Title,Account_x0020_Director/Title," +
                    "Service_x0020_Director/Title,Jan_x002d_26&" +
                    "$expand=Line_x0020_Manager,Service_x0020_Manager," +
                    "Secondary_x0020_Service_x0020_Ma,Account_x0020_Manager," +
                    "Account_x0020_Director,Service_x0020_Director&" +
                    "$filter=Title eq '" + accountCode + "'&" +
                    "$top=1";

                const res = await fetch(url, {
                    headers: {
                        'Accept': 'application/json;odata=verbose'
                    },
                    credentials: 'include'
                });

                if (!res.ok) throw new Error('Failed to search account');

                const data = await res.json();

                if (data.d.results.length === 0) {
                    // Show error message below search box
                    document.getElementById('searchErrorMessage').style.display = 'block';
                    document.getElementById('accountDetailsSection').style.display = 'none';
                    document.getElementById('relatedAccountsSection').style.display = 'none';

                    // Re-initialize Lucide icons
                    if (typeof lucide !== 'undefined') {
                        lucide.createIcons();
                    }
                    return;
                }

                // Hide error message if account found
                document.getElementById('searchErrorMessage').style.display = 'none';

                const account = data.d.results[0];
                displayAccountDetails(account);
                await loadRelatedAccounts(account);

            } catch (err) {
                console.error('Search error:', err);
                alert('Error searching account: ' + err.message);
            }
        }

        function displayAccountDetails(account) {
            const isGroup = (account.Title === account.Parent_x0020_Code) || !account.Parent_x0020_Code;

            let html = '<div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px;">';

            const fields = [
                ['Account Code', account.Title],
                ['Parent Code', account.Parent_x0020_Code || account.Title],
                ['Account Type', isGroup ?
                    '<span style="color: var(--success); font-weight: 700; font-size: 16px;">🟢 Group Account</span>' :
                    '<span style="color: var(--accent); font-weight: 700; font-size: 16px;">🔵 Child Account</span>'
                ],
                ['Line Manager', account.Line_x0020_Manager?.Title || 'N/A'],
                ['Service Manager', account.Service_x0020_Manager?.Title || 'N/A'],
                ['Secondary SM', account.Secondary_x0020_Service_x0020_Ma?.Title || 'N/A'],
                ['Account Manager', account.Account_x0020_Manager?.Title || 'N/A'],
                ['Account Director', account.Account_x0020_Director?.Title || 'N/A'],
                ['Service Director', account.Service_x0020_Director?.Title || 'N/A']
            ];

            fields.forEach(([label, value]) => {
                html += `
            <div style="padding: 12px; background: rgba(168, 85, 247, 0.1); border-radius: 8px;">
                <div style="font-size: 11px; color: var(--text-secondary); margin-bottom: 4px; font-weight: 600;">${label}</div>
                <div style="font-size: 14px; font-weight: 600;">${value}</div>
            </div>
        `;
            });

            html += '</div>';

            document.getElementById('accountDetailsContent').innerHTML = html;
            document.getElementById('accountDetailsSection').style.display = 'block';
        }

        let relatedAccountsGridApi = null;

        async function loadRelatedAccounts(account) {
            const isGroup = (account.Title === account.Parent_x0020_Code) || !account.Parent_x0020_Code;

            try {
                let relatedAccounts = [];
                let titleText = '';

                if (isGroup) {
                    // Show children
                    titleText = 'Child Accounts';
                    const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                        "$select=Title,Parent_x0020_Code,Customer_x0020_Name,Team," +
                        "Line_x0020_Manager/Title,Service_x0020_Manager/Title," +
                        "Secondary_x0020_Service_x0020_Ma/Title," +
                        "Account_x0020_Manager/Title,Account_x0020_Director/Title," +
                        "Service_x0020_Director/Title,Jan_x002d_26&" +
                        "$expand=Line_x0020_Manager,Service_x0020_Manager," +
                        "Secondary_x0020_Service_x0020_Ma,Account_x0020_Manager," +
                        "Account_x0020_Director,Service_x0020_Director&" +
                        "$filter=Parent_x0020_Code eq '" + account.Title + "' and Title ne '" + account.Title + "'&" +
                        "$top=500";

                    const res = await fetch(url, {
                        headers: {
                            'Accept': 'application/json;odata=verbose'
                        },
                        credentials: 'include'
                    });

                    if (res.ok) {
                        const data = await res.json();
                        relatedAccounts = data.d.results;
                    }
                } else {
                    // Show parent
                    titleText = 'Parent/Group Account';
                    const url = SP_URL + "/_api/web/lists/getbytitle('" + SP_LIST + "')/items?" +
                        "$select=Title,Parent_x0020_Code,Customer_x0020_Name,Team," +
                        "Line_x0020_Manager/Title,Service_x0020_Manager/Title," +
                        "Secondary_x0020_Service_x0020_Ma/Title," +
                        "Account_x0020_Manager/Title,Account_x0020_Director/Title," +
                        "Service_x0020_Director/Title,Jan_x002d_26&" +
                        "$expand=Line_x0020_Manager,Service_x0020_Manager," +
                        "Secondary_x0020_Service_x0020_Ma,Account_x0020_Manager," +
                        "Account_x0020_Director,Service_x0020_Director&" +
                        "$filter=Title eq '" + account.Parent_x0020_Code + "'&" +
                        "$top=1";

                    const res = await fetch(url, {
                        headers: {
                            'Accept': 'application/json;odata=verbose'
                        },
                        credentials: 'include'
                    });

                    if (res.ok) {
                        const data = await res.json();
                        relatedAccounts = data.d.results;
                    }
                }

                document.getElementById('relatedAccountsTitle').textContent = titleText;
                renderRelatedAccountsGrid(relatedAccounts, isGroup);
                document.getElementById('relatedAccountsSection').style.display = 'block';

            } catch (err) {
                console.error('Error loading related accounts:', err);
            }
        }

        function renderRelatedAccountsGrid(accounts, isGroup) {
            const gridDiv = document.getElementById('relatedAccountsGrid');

            const columnDefs = [{
                    field: 'Title',
                    headerName: 'Account Code',
                    filter: 'agTextColumnFilter',
                    width: 180,
                    pinned: 'left',
                    cellStyle: {
                        fontWeight: '700'
                    }
                },
                {
                    field: 'Parent_x0020_Code',
                    headerName: 'Parent Code',
                    filter: 'agTextColumnFilter',
                    width: 180,
                    cellRenderer: params => {
                        const isParentRow = params.value === params.data.Title;
                        return isParentRow ?
                            `<span style="color: var(--success); font-weight: 700;">${params.value}</span>` :
                            params.value;
                    }
                },
                {
                    field: 'accountType',
                    headerName: 'Account Type',
                    filter: 'agSetColumnFilter',
                    width: 140,
                    valueGetter: params => {
                        return params.data.Title === params.data.Parent_x0020_Code ? 'Group' : 'Child';
                    },
                    cellRenderer: params => {
                        const badge = params.value === 'Group' ? 'badge-success' : 'badge-warning';
                        return `<span class="status-badge ${badge}">${params.value}</span>`;
                    }
                },
                {
                    field: 'Customer_x0020_Name',
                    headerName: 'Customer',
                    filter: 'agTextColumnFilter',
                    width: 220
                },
                {
                    field: 'Team',
                    headerName: 'Team',
                    filter: 'agSetColumnFilter',
                    width: 120
                },
                {
                    field: 'Line_x0020_Manager.Title',
                    headerName: 'Line Manager',
                    filter: 'agTextColumnFilter',
                    width: 160,
                    valueGetter: params => params.data.Line_x0020_Manager?.Title || ''
                },
                {
                    field: 'Service_x0020_Manager.Title',
                    headerName: 'Service Manager',
                    filter: 'agTextColumnFilter',
                    width: 160,
                    valueGetter: params => params.data.Service_x0020_Manager?.Title || ''
                },
                {
                    field: 'Secondary_x0020_Service_x0020_Ma.Title',
                    headerName: 'Secondary SM',
                    filter: 'agTextColumnFilter',
                    width: 160,
                    valueGetter: params => params.data.Secondary_x0020_Service_x0020_Ma?.Title || 'N/A'
                },
                {
                    field: 'Account_x0020_Manager.Title',
                    headerName: 'Account Manager',
                    filter: 'agTextColumnFilter',
                    width: 160,
                    valueGetter: params => params.data.Account_x0020_Manager?.Title || ''
                },
                {
                    field: 'Account_x0020_Director.Title',
                    headerName: 'Account Director',
                    filter: 'agTextColumnFilter',
                    width: 160,
                    valueGetter: params => params.data.Account_x0020_Director?.Title || ''
                },
                {
                    field: 'Service_x0020_Director.Title',
                    headerName: 'Service Director',
                    filter: 'agTextColumnFilter',
                    width: 160,
                    valueGetter: params => params.data.Service_x0020_Director?.Title || ''
                }
            ];

            const gridOptions = {
                columnDefs: columnDefs,
                rowData: accounts,
                defaultColDef: {
                    sortable: true,
                    filter: true,
                    resizable: true,
                    minWidth: 100
                },
                rowHeight: 50,
                headerHeight: 52,
                animateRows: true,
                enableCellTextSelection: true,
                pagination: true,
                paginationPageSize: 50,
                paginationPageSizeSelector: [25, 50, 100],
                onGridReady: params => {
                    relatedAccountsGridApi = params.api;
                },
                getRowStyle: params => {
                    // Alternate row colors with theme support
                    const isDark = document.body.getAttribute('data-theme') === 'dark' ||
                        document.body.getAttribute('data-theme') === 'duralux-dark';

                    if (params.node.rowIndex % 2 === 0) {
                        return {
                            background: isDark ? 'rgba(168, 85, 247, 0.08)' : 'rgba(168, 85, 247, 0.05)'
                        };
                    }
                    return null;
                }
            };

            if (relatedAccountsGridApi) {
                relatedAccountsGridApi.destroy();
            }

            gridDiv.innerHTML = '';
            agGrid.createGrid(gridDiv, gridOptions);
        }

        function searchRelatedAccounts(searchText) {
            if (!relatedAccountsGridApi) return;
            relatedAccountsGridApi.setGridOption('quickFilterText', searchText);
        }

        function injectAGGridThemeStyles() {
            const style = document.createElement('style');
            style.innerHTML = `
        /* Root wrapper */
        .ag-theme-alpine .ag-root-wrapper,
        .ag-theme-alpine .ag-root-wrapper-body,
        .ag-theme-alpine .ag-root {
            background: var(--bg-card) !important;
            border-color: var(--border-color) !important;
        }

        /* HEADER - most important fix */
        .ag-theme-alpine .ag-header {
            background: var(--bg-secondary, var(--bg-card)) !important;
            background-color: var(--bg-secondary, var(--bg-card)) !important;
            border-bottom: 2px solid var(--accent) !important;
        }

        .ag-theme-alpine .ag-header-row,
        .ag-theme-alpine .ag-header-viewport,
        .ag-theme-alpine .ag-pinned-left-header,
        .ag-theme-alpine .ag-pinned-right-header {
            background: var(--bg-secondary, var(--bg-card)) !important;
            background-color: var(--bg-secondary, var(--bg-card)) !important;
            border-bottom: none !important;
        }

        .ag-theme-alpine .ag-header-row.ag-header-row-column-group {
            border-bottom: 1px solid var(--border-color, var(--border)) !important;
        }

        .ag-theme-alpine .ag-header-cell.ag-header-span-height {
            border-bottom: none !important;
        }

        .ag-theme-alpine .ag-header-cell {
            background: var(--bg-secondary, var(--bg-card)) !important;
            background-color: var(--bg-secondary, var(--bg-card)) !important;
            color: var(--text-primary) !important;
            font-weight: 700 !important;
            border-right-color: var(--border-color) !important;
        }

        .ag-theme-alpine .ag-header-cell-text,
        .ag-theme-alpine .ag-header-cell-label {
            color: var(--text-primary) !important;
            font-weight: 700 !important;
        }

        .ag-theme-alpine .ag-icon {
            color: var(--text-secondary) !important;
        }

        /* ROWS */
        .ag-theme-alpine .ag-body-viewport,
        .ag-theme-alpine .ag-body,
        .ag-theme-alpine .ag-center-cols-viewport,
        .ag-theme-alpine .ag-pinned-left-cols-container,
        .ag-theme-alpine .ag-pinned-right-cols-container {
            background: var(--bg-card) !important;
        }

        .ag-theme-alpine .ag-row {
            background-color: var(--bg-card) !important;
            color: var(--text-primary) !important;
            border-bottom-color: var(--border-color) !important;
        }

        .ag-theme-alpine .ag-row-even {
            background-color: var(--bg-card) !important;
        }

        .ag-theme-alpine .ag-row-odd {
            background-color: var(--bg-secondary, rgba(168, 85, 247, 0.03)) !important;
        }

        .ag-theme-alpine .ag-row-hover,
        .ag-theme-alpine .ag-row-hover.ag-row-even,
        .ag-theme-alpine .ag-row-hover.ag-row-odd {
            background-color: rgba(168, 85, 247, 0.08) !important;
        }

        .ag-theme-alpine .ag-cell {
            color: var(--text-primary) !important;
            border-right-color: var(--border-color) !important;
        }

        /* PAGINATION */
        .ag-theme-alpine .ag-paging-panel {
            background: var(--bg-card) !important;
            color: var(--text-primary) !important;
            border-top-color: var(--border-color) !important;
        }

        .ag-theme-alpine .ag-paging-button {
            color: var(--text-primary) !important;
        }

        /* FILTER MENU */
        .ag-theme-alpine .ag-menu,
        .ag-theme-alpine .ag-filter {
            background: var(--bg-card) !important;
            color: var(--text-primary) !important;
            border-color: var(--border-color) !important;
        }

        .ag-theme-alpine .ag-filter-condition,
        .ag-theme-alpine .ag-filter-body-wrapper,
        .ag-theme-alpine input.ag-input-field-input {
            background: var(--bg-card) !important;
            color: var(--text-primary) !important;
            border-color: var(--border-color) !important;
        }

        .sm-ag-set-filter { padding: .5rem; min-width: 200px; max-width: 260px; }
        .sm-ag-set-search { width: 100%; box-sizing: border-box; margin-bottom: .45rem; padding: .35rem .5rem; border: 1px solid var(--border-color); border-radius: 8px; font-size: .75rem; background: var(--bg-card); color: var(--text-primary); }
        .sm-ag-set-list { max-height: 180px; overflow-y: auto; display: flex; flex-direction: column; gap: .2rem; }
        .sm-ag-set-option { display: flex; align-items: center; gap: .35rem; font-size: .75rem; cursor: pointer; padding: .15rem 0; color: var(--text-primary); }
        .sm-ag-set-actions { display: flex; gap: .35rem; margin-top: .45rem; }
        .sm-ag-set-actions button { flex: 1; padding: .25rem .4rem; font-size: .68rem; font-weight: 700; border: 1px solid var(--border-color); border-radius: 6px; background: var(--nab, rgba(168,85,247,.08)); color: var(--acc, #a855f7); cursor: pointer; }

        /* DURALUX OVERRIDES */
        [data-theme="duralux"] .ag-theme-alpine .ag-header,
        [data-theme="duralux-dark"] .ag-theme-alpine .ag-header {
            background: var(--bg-secondary, var(--bg-card)) !important;
            background-color: var(--bg-secondary, var(--bg-card)) !important;
            border-bottom-color: #4c6fff !important;
        }

        [data-theme="duralux"] .ag-theme-alpine .ag-header-row,
        [data-theme="duralux"] .ag-theme-alpine .ag-header-cell,
        [data-theme="duralux"] .ag-theme-alpine .ag-pinned-left-header,
        [data-theme="duralux"] .ag-theme-alpine .ag-pinned-right-header,
        [data-theme="duralux-dark"] .ag-theme-alpine .ag-header-row,
        [data-theme="duralux-dark"] .ag-theme-alpine .ag-header-cell,
        [data-theme="duralux-dark"] .ag-theme-alpine .ag-pinned-left-header,
        [data-theme="duralux-dark"] .ag-theme-alpine .ag-pinned-right-header {
            background: var(--bg-secondary, var(--bg-card)) !important;
            background-color: var(--bg-secondary, var(--bg-card)) !important;
            border-bottom: none !important;
        }

        [data-theme="duralux"] .ag-theme-alpine .ag-row-hover,
        [data-theme="duralux-dark"] .ag-theme-alpine .ag-row-hover {
            background-color: rgba(76, 111, 255, 0.08) !important;
        }
    `;
            document.head.appendChild(style);
        }
    </script>
    </div><!-- End SharePoint Compatibility Wrapper -->
    <!-- Modular view scripts -->
<script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/tsm-pending-escalation.js"></script>
<script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/pending.js"></script>
<script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/am-ad.js"></script>
<script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/transfer.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/customerservice.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/analytics.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/events-tt-activity.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/projectManagement.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/newsletter.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/contactus.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/suggestions.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/processdocs.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/qualityassurance.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/psd-assignment.js"></script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/rnps.js"></script>
  <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/semailbox.js"></script>
  <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/tsm-se.js?v=4"></script>
    <div class="dashboard" style="display:none;" id="executiveHubView"></div>
    <script>
        window.showExecutiveHub = function() {
            if (typeof analyticsTrackPage === 'function') analyticsTrackPage('Executive Operations Hub');
            ALL_VIEW_IDS.forEach(function(vid) {
                var el = document.getElementById(vid);
                if (el) {
                    el.style.display = 'none';
                    el.style.position = 'absolute';
                    el.style.zIndex = '-1';
                }
            });
            var view = document.getElementById('executiveHubView');
            if (view) {
                view.style.position = '';
                view.style.zIndex = '';
                view.style.display = 'block';
            }
            window.scrollTo(0, 0);
            if (typeof ehInit === 'function') ehInit();
            if (typeof lucide !== 'undefined') lucide.createIcons();
        };
        window.ehBackToDashboard = function() {
            var view = document.getElementById('executiveHubView');
            if (view) {
                view.style.display = 'none';
                view.style.position = 'absolute';
                view.style.zIndex = '-1';
            }
            var dc = document.getElementById('dashboardContent');
            if (dc) {
                dc.style.position = '';
                dc.style.zIndex = '';
                dc.style.display = 'block';
            }
            if (typeof switchDashboardSection === 'function') switchDashboardSection('dashboard-view');
            window.scrollTo(0, 0);
            if (typeof lucide !== 'undefined') lucide.createIcons();
        };
    </script>
    <script src="http://sharedspaces:8086/sites/SM/Shared%20Documents/executiveHub.js"></script>
    <div class="dashboard" style="display:none;" id="newsletterView">
        <div class="header">
            <div class="header-left">
                <h1><i data-lucide="mail" style="width:28px;height:28px;display:inline-block;vertical-align:middle;margin-right:10px;"></i>Newsletter</h1>
                <p>Service Management Platform</p>
            </div>
            <div class="header-actions">
                <button type="button" class="theme-btn" onclick="backFromNewsletter()" style="background:rgba(239,68,68,0.15);color:#ef4444;padding:10px 20px;border-radius:12px;border:none;cursor:pointer;font-weight:600;">
                    <i data-lucide="arrow-left" style="width:16px;height:16px;display:inline-block;vertical-align:middle;margin-right:6px;"></i>Back to Dashboard
                </button>
            </div>
        </div>
        <div style="max-width:1000px;margin:0 auto;padding:24px;">
            <!-- TABS -->
            <div style="display:flex;gap:8px;margin-bottom:1.5rem;">
                <button type="button" id="nlTabView" onclick="nlSetTab('view')" style="padding:10px 24px;border-radius:10px;font-size:13px;font-weight:700;cursor:pointer;border:none;background:var(--grad);color:#fff;display:inline-flex;align-items:center;gap:6px;">
                    <i data-lucide="newspaper" style="width:14px;height:14px;"></i>Newsletter
                </button>
                <button type="button" id="nlTabManage" onclick="nlSetTab('manage')" style="display:none;padding:10px 24px;border-radius:10px;font-size:13px;font-weight:700;cursor:pointer;border:1px solid var(--border);background:var(--bg-input);color:var(--t1);align-items:center;gap:6px;">
                    <i data-lucide="settings" style="width:14px;height:14px;"></i>Manage
                </button>
            </div>
            <!-- Loading -->
            <div id="nlLoading" style="text-align:center;padding:60px;color:var(--t2);">
                <i data-lucide="loader-2" style="width:40px;height:40px;animation:spin 1s linear infinite;color:var(--acc);display:inline-block;"></i>
                <div style="margin-top:12px;font-weight:600;">Loading Newsletter...</div>
            </div>
            <!-- Content -->
            <div id="nlContent" style="display:none;">
                <div id="nlViewContainer"></div>
            </div>
        </div>
    </div>
    <script>

    </script>
</body>

</html>
